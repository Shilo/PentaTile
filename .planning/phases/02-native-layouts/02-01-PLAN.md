---
phase: 2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/penta_tile/layouts/penta_tile_layout.gd
files_deleted: []
files_created:
  - .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md
requirements: [LAYER-05, LAYOUT-03, LAYOUT-06, PREVIEW-01]
autonomous: true

must_haves:
  truths:
    - "PentaTileLayout.bitmask_template replaces template_image (rename complete)."
    - "PentaTileLayout.fallback_tile_set @export is removed."
    - "PentaTileLayout.decoder_image is removed."
    - "PentaTileLayout has a get_fallback_tile_set() -> TileSet virtual STUB returning null (body filled in Wave 2)."
    - "Phase 1's 26 verification tests are migrated to a new spec referencing the new API surface (`layout: PentaTileLayout`, `PentaTileLayoutPenta(axis=...)`, `bitmask_template`, single-layer dispatch). New tests added for TWO/THREE/FIVE modes + AUTO_STRIP."
    - "Phase 1's 01-VERIFICATION.md is marked HISTORICAL with a heading note pointing at the migrated spec."
  artifacts:
    - path: "addons/penta_tile/layouts/penta_tile_layout.gd"
      provides: "Renamed/cleaned base layout class with stub virtual"
      contains: "bitmask_template"
    - path: ".planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md"
      provides: "Migrated verification spec covering new API + new modes"
  key_links:
    - from: "addons/penta_tile/layouts/penta_tile_layout.gd"
      to: "Wave 2 _synthesize_strip + _resolve_layout"
      via: "bitmask_template @export + get_fallback_tile_set() virtual stub"
      pattern: "@export var bitmask_template"
---

<objective>
Wave 1 (pre-work) lands the base-class API renames AND migrates the Phase 1 verification suite to the new API surface BEFORE any Wave 2 deletion lands. After this wave, `PentaTileLayout` exposes `bitmask_template` (renamed from `template_image`) and a `get_fallback_tile_set()` virtual stub; `fallback_tile_set` and `decoder_image` are gone. The migrated verification spec sits at `.planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md` and replaces the old 26-test spec for downstream wave verification.

Purpose: Wave 2 cannot execute the contract deletion + synthesis machinery + demo rebind atomically without (a) the renamed base class and the get_fallback_tile_set virtual stub being in place, (b) the verification suite already pointing at the new symbols (otherwise gap-closure cycles fail to find the API they reference). This wave is the load-bearing prep step.

Output: Modified `penta_tile_layout.gd` + new verification migration spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-native-layouts/02-CONTEXT.md
@.planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md

<interfaces>
<!-- Current state of penta_tile_layout.gd at HEAD (verified 2026-04-26) -->

```gdscript
@export var template_image: Texture2D                        # → RENAME to bitmask_template (LAYOUT-03 / PREVIEW-01)
@export var fallback_tile_set: TileSet                       # → REMOVE (LAYOUT-03)
@export_multiline var description: String = ""               # → KEEP unchanged
@export var decoder_image: Texture2D                         # → DELETE (LAYOUT-03)

func compute_mask(_coord: Vector2i, _sample_fn: Callable) -> int                 # KEEP unchanged
func mask_to_atlas(_mask: int) -> PentaTileAtlasSlot                              # KEEP unchanged
func is_dual_grid() -> bool                                                       # KEEP unchanged
func _pack_alternative(alt_id: int, transform_flags: int) -> int                  # KEEP unchanged (LAYOUT-05)
func _set_contract(contract: Resource) -> void                                    # → DELETE (contract going away in Wave 2)

var _contract: WeakRef = null                                                     # → DELETE (contract going away in Wave 2)
```

Subclasses currently extending this:
- `PentaTileLayoutPentaHorizontal` (Phase 1; deleted Wave 3) — references `template_image` indirectly via `.tres`
- `PentaTileLayoutPentaVertical` (Phase 1; deleted Wave 3) — extends Horizontal
</interfaces>
</context>

<threat_model>
Internal Godot addon refactor; no network/auth/PII/external-input attack surface. Sole threat surface: malicious source-atlas content in `bitmask_template` PNG could attempt path-traversal or oversized-image DoS — mitigated by Godot's stock Image loading (size limits + sandboxed file reads).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | bitmask_template PNG load path | accept | Godot's `Texture2D` loader is sandboxed; res:// path is project-internal |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1.1: Rename `template_image` → `bitmask_template` + remove `fallback_tile_set` @export + add `get_fallback_tile_set()` virtual STUB + delete `decoder_image` + delete `_contract` back-ref machinery</name>
  <files>addons/penta_tile/layouts/penta_tile_layout.gd</files>
  <read_first>
    - addons/penta_tile/layouts/penta_tile_layout.gd (CURRENT STATE, must see lines 15-18 + 51-58)
    - .planning/REQUIREMENTS.md (LAYOUT-03 / LAYOUT-06 / PREVIEW-01 — exact wording of each)
    - .planning/phases/02-native-layouts/02-CONTEXT.md (D-69 + D-70 indicate this stub will be filled in Wave 2; do NOT pre-fill the body here)
    - .planning/research/PITFALLS.md (§6 "Renaming Resource properties orphans saved scenes silently" — per CLAUDE.md HARD RULE, do NOT add `@export_storage` shadow; CHANGELOG documents the breakage in Phase 5)
    - CLAUDE.md (Breaking Changes Policy — no compat shims AND no forward-compat speculation)
  </read_first>
  <action>
    Edit `addons/penta_tile/layouts/penta_tile_layout.gd` to land the base-class API for Phase 2:

    1. Replace `@export var template_image: Texture2D` (line 15) with `@export var bitmask_template: Texture2D`. Update the trailing comment from `# PREVIEW-01: stock inspector preview` to `# PREVIEW-01 / LAYOUT-03: stock inspector preview AND fallback TileSet source pixels (single PNG, both roles)`.
    2. DELETE the line `@export var fallback_tile_set: TileSet` (line 16). Per LAYOUT-06, fallback is now codegen via `get_fallback_tile_set()`.
    3. KEEP `@export_multiline var description: String = ""` unchanged.
    4. DELETE the line `@export var decoder_image: Texture2D` (line 18). Was speculative; no consumer.
    5. DELETE the entire `_contract: WeakRef` field block (lines 20-25 — comment block + `var _contract`). The contract wrapper is going away in Wave 2; back-references are obsolete.
    6. DELETE the `_set_contract(contract: Resource)` method (lines 51-58, including the leading comment).
    7. ADD a new virtual STUB method below `_pack_alternative` (after line 48):
       ```gdscript
       # LAYOUT-06: virtual TileSet codegen path. Default returns null in Wave 1;
       # Wave 2 fills the body to construct a TileSet from `bitmask_template`.
       # Subclasses can override for custom logic.
       # Consumer (PentaTileMapLayer) calls this when tile_set == null (PREVIEW-03 wired in Phase 4).
       func get_fallback_tile_set() -> TileSet:
       	return null
       ```
       Use a TAB for indentation to match existing file conventions.
    8. Do NOT add a `version: int` field, `@export_storage` shadow, `__migrate__()` method, or any compat scaffold (per CLAUDE.md no-compat HARD RULE).
    9. Do NOT pre-fill the `get_fallback_tile_set()` body; that is Wave 2's job (it depends on synthesis machinery + bitmask_template parsing not yet built).
  </action>
  <verify>
    <automated>
    grep -c 'bitmask_template' addons/penta_tile/layouts/penta_tile_layout.gd | grep -q '^[1-9]' &&
    ! grep -q 'template_image' addons/penta_tile/layouts/penta_tile_layout.gd &&
    ! grep -q '@export var fallback_tile_set' addons/penta_tile/layouts/penta_tile_layout.gd &&
    ! grep -q 'decoder_image' addons/penta_tile/layouts/penta_tile_layout.gd &&
    ! grep -q '_contract' addons/penta_tile/layouts/penta_tile_layout.gd &&
    ! grep -q '_set_contract' addons/penta_tile/layouts/penta_tile_layout.gd &&
    grep -c 'func get_fallback_tile_set() -> TileSet' addons/penta_tile/layouts/penta_tile_layout.gd | grep -q '^1$' &&
    ! grep -qE '@export_storage|version: int|__migrate__' addons/penta_tile/layouts/penta_tile_layout.gd &&
    echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bitmask_template' addons/penta_tile/layouts/penta_tile_layout.gd` returns ≥ 1
    - `grep -c 'template_image' addons/penta_tile/layouts/penta_tile_layout.gd` returns 0
    - `grep -c '@export var fallback_tile_set' addons/penta_tile/layouts/penta_tile_layout.gd` returns 0
    - `grep -c 'decoder_image' addons/penta_tile/layouts/penta_tile_layout.gd` returns 0
    - `grep -c '_contract' addons/penta_tile/layouts/penta_tile_layout.gd` returns 0
    - `grep -c 'func get_fallback_tile_set() -> TileSet' addons/penta_tile/layouts/penta_tile_layout.gd` returns exactly 1
    - The `get_fallback_tile_set` body is `return null` (single-line stub) — verified by reading the file
    - No `@export_storage`, `version: int`, `__migrate__`, or any backwards-compat shim present anywhere in the file
    - File still has class declaration `class_name PentaTileLayout` and `extends Resource`
  </acceptance_criteria>
  <done>Base class renamed and pruned; virtual stub in place; ready for Wave 2 to fill the body and Wave 3 to consume.</done>
</task>

<task type="auto">
  <name>Task 1.2: Migrate Phase 1 verification suite to the new API surface + add new tests for TWO/THREE/FIVE + AUTO_STRIP</name>
  <files>
    .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md
    .planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md (the 26 tests being migrated; full file)
    - .planning/REQUIREMENTS.md (LAYER-05 — defines what migration covers)
    - .planning/phases/02-native-layouts/02-CONTEXT.md (the new API surface: `layout: PentaTileLayout`, `PentaTileLayoutPenta(axis=...)`, `bitmask_template`, single-layer dispatch, new slot ordering 0=IsolatedCell, 1=Fill, 2=Border, 3=InnerCorner, 4=OppositeCorners)
    - .planning/ROADMAP.md (Phase 2 success criteria 1-17 — tests must trace to these)
  </read_first>
  <action>
    Step A — Create `.planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md` with this concrete structure:

    ```markdown
    ---
    phase: 02-native-layouts
    supersedes: .planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md
    migrated: <ISO date>
    test_count: <see below>
    ---

    # Phase 2 Verification — Migrated from Phase 1 + New Mode Coverage

    Phase 1's 26 tests reference the deleted API surface (`atlas_contract`, `PentaTileLayoutPentaHorizontal/Vertical`, `template_image`, `_overlay_layer`). This file rewrites them against Phase 2's new surface AND adds coverage for the new modes (TWO/THREE/FIVE + AUTO_STRIP).

    ## Migration Map (Phase 1 → Phase 2)

    | Phase 1 Test | New API Reference | Notes |
    |---|---|---|
    | `atlas_contract = ExtResource(default_horizontal.tres)` → bit-identical to v0.1 | DEPRECATED — slot ordering changed; v0.1 bit-identity no longer holds. Replaced by FOUR-mode regression baseline (PENTA-SYNTH-12). | Wave 6 captures fresh baseline. |
    | `atlas_contract = null` → v0.1 lazy fallback | DELETED — no `_DEFAULT_LAYOUT` singleton in Phase 2; `layout == null` renders nothing (LAYER-02). | New test: `layer.layout = null` → no paint, no errors. |
    | `atlas_contract = same value` → 0 rebuilds (idempotence) | Migrated to `layer.layout = layer.layout` → 0 rebuilds. Setter at penta_tile_map_layer.gd `layout` property (Wave 2). | Same idempotence pattern (PITFALLS §5). |
    | Burst-of-10 emits → 10 rebuilds (no signal storm) | Migrated to `layer.layout.changed` × 10 → 10 rebuilds (1:1; deferred coalescing at `rebuild.call_deferred`). | Same pattern. |
    | `PentaTileLayout` subclassable, picker shows subclasses | Migrated unchanged (typed `@export var layout: PentaTileLayout`). | New subclasses Wave 3-4. |
    | `template_image` renders in inspector | `bitmask_template` renders in inspector (rename per LAYOUT-03 / PREVIEW-01). | Stock Texture2D preview; no behavior change. |
    | LOC checkpoint logged | LOC checkpoint at Wave 7 (~1230-1530 expected; ~1500 upper bound). | New target. |
    | 5 patterns × `default_horizontal.tres` pixel-diff=0 vs v0.1 | DELETED — slot ordering changed in Phase 2. Replaced by FOUR-mode baseline test (PENTA-SYNTH-12) at Wave 6. | Fresh capture, NOT v0.1 bit-equiv. |
    | 5 patterns × null contract pixel-diff=0 vs v0.1 | DELETED — null layout renders nothing in Phase 2. | LAYER-02 acceptance criterion. |
    | (D-19 silently-dropped `atlas_layout` enum) | Migrated as historical record only — `atlas_layout` and `atlas_contract` both gone. | No active test. |

    ## New Tests (Phase 2 modes not covered in Phase 1)

    Add these test specs to be exercised at Wave 6 (visual + behavioral) and Wave 7 (closeout):

    1. **PENTA-SYNTH-02 AUTO detection** — `PentaTileLayoutPenta(axis=HORIZONTAL, tile_count=AUTO)` on a 1×N atlas → mode resolves to ONE; on 4×N → FOUR; on 5×N → FIVE. Other axis sizes (0, 6+) → render disabled + warning.
    2. **PENTA-SYNTH-03 AUTO_STRIP detection** — `PentaTileLayoutPenta(tile_count=AUTO_STRIP)` on a 5×3 atlas with strip 0 = 5 tiles, strip 1 = 1 tile, strip 2 = 4 tiles → strips render with mode FIVE/ONE/FOUR respectively. Different strips can use different modes.
    3. **PENTA-SYNTH-08 warnings** — explicit `tile_count=FIVE` on a 4×N atlas → `update_configuration_warnings()` returns the explicit-mismatch warning. AUTO_STRIP gap (slot 2 populated, slot 1 empty) → AUTO_STRIP-gap warning.
    4. **TWO mode synthesis** — `PentaTileLayoutPenta(tile_count=TWO)` on a 2×1 atlas (slot 0 = IsolatedCell, slot 1 = Fill) → all 16 mask states paint; slot 1 used for mask 15; remaining masks synthesize from slot 0 sub-regions.
    5. **THREE mode synthesis** — 3×1 atlas (slot 0/1/2 = IsolatedCell/Fill/Border) → 16 mask states paint; Border (mask 3, 5, 10, 12) uses slot 2; InnerCorner + OppositeCorners synthesize from slot 0.
    6. **FIVE mode pure-authored** — 5×1 atlas with all five archetypes hand-drawn → 16 mask states paint; only OuterCorner derived from slot 0 (the implicit archetype across all modes).
    7. **PENTA-SYNTH-06 determinism** — synthesize twice with the same `(layout, axis, tile_count, source tile_set)`; hash both atlases via `Image.get_data().hash()`; assert equal. Bit-identical across 10 consecutive runs.
    8. **PENTA-SYNTH-07 overlay deletion** — `PentaTileMapLayer` instance has exactly ONE child visual layer named `_PentaTileVisual`; no `_PentaTileDiagonalOverlay` child exists. Verified via `find_child("_PentaTileDiagonalOverlay")` returns null.
    9. **LAYER-04 demo rebind** — opening `addons/penta_tile/demo/penta_tile_demo.tscn` in Godot 4.6 editor produces zero "missing dependency" / "missing script" errors after Wave 2 lands; PentaTileMapLayer node has `layout` property bound (not `atlas_contract`).
    10. **NATIVE-01..03 + MIN3x3-01** — each native layout's 16 mask states paint without seams on a 5×5 painted region (visual regression).

    ## Test Count

    Migrated from Phase 1 (still applicable): ~6 tests (idempotence, signal-storm, subclassable picker, bitmask_template inspector preview, LOC checkpoint, no-anti-patterns scan).

    Removed (Phase 1-only; v0.1 bit-equivalence no longer holds): 20 tests (10 visual-regression rows + 10 wiring rows that referenced now-deleted `atlas_contract` / `_DEFAULT_LAYOUT` / `_overlay_layer`).

    NEW (Phase 2 modes + architectural sweep): 10 tests above.

    Final test_count = ~16 — refined as new layouts ship in Waves 3-4. Wave 7 closeout updates the final number.
    ```

    Step B — Edit `.planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md`: prepend a new heading section AT THE VERY TOP (before the existing `---` frontmatter) saying:

    ```markdown
    > **HISTORICAL — superseded by `.planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md` (Phase 2 Wave 1, 2026-04-26).** The verification described below targets the deleted API surface (`atlas_contract`, `PentaTileLayoutPentaHorizontal/Vertical`, `template_image`, `_overlay_layer`). Slot ordering changed in Phase 2 (slot 3 was OuterCorner, is now InnerCorner; slot 0 was Fill, is now IsolatedCell), so v0.1 bit-equivalence claims below no longer hold. This file is preserved as a historical record of Phase 1's close but is no longer authoritative. See LAYER-05 + the migration map above.

    ---

    ```

    Then leave the rest of the file untouched.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md &&
    grep -c 'AUTO_STRIP' .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md | grep -q '^[1-9]' &&
    grep -c 'PENTA-SYNTH-12' .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md | grep -q '^[1-9]' &&
    grep -c 'HISTORICAL' .planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md | grep -q '^[1-9]' &&
    grep -c '02-01-VERIFICATION-MIGRATION' .planning/phases/01-contract-skeleton-penta-layouts/01-VERIFICATION.md | grep -q '^[1-9]' &&
    echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md` succeeds
    - The migration file contains a "Migration Map (Phase 1 → Phase 2)" table with at least 5 rows
    - The migration file contains a "New Tests (Phase 2 modes not covered in Phase 1)" section listing AT LEAST 10 numbered tests
    - The migration file references PENTA-SYNTH-02, PENTA-SYNTH-03, PENTA-SYNTH-06, PENTA-SYNTH-07, PENTA-SYNTH-08, PENTA-SYNTH-12, LAYER-04, NATIVE-01..03, MIN3x3-01 by name (each at least once) — verified by grep
    - Phase 1's `01-VERIFICATION.md` first non-blank line is the HISTORICAL banner and points at the new migration file
    - The original Phase 1 verification content (the 26-test tables) is intact below the banner
  </acceptance_criteria>
  <done>Wave 1 verification migration complete; downstream waves can reference 02-01-VERIFICATION-MIGRATION.md as the authoritative test spec and Phase 1's 01-VERIFICATION.md is clearly marked historical.</done>
</task>

</tasks>

<verification>
**Wave 1 verification (manual checklist for executor):**
1. `addons/penta_tile/layouts/penta_tile_layout.gd` opens in Godot 4.6 editor without parse errors. (`get_fallback_tile_set` is referenced in subclasses Wave 3-4 but inheriting from this stub is valid.)
2. `bitmask_template` appears in inspector when a `PentaTileLayout` resource is selected (stock `Texture2D` preview).
3. The Phase 1 `.tres` files (`default_horizontal.tres`, `penta_horizontal_default.tres`, etc.) WILL emit warnings about missing `template_image` on first load — this is expected (per CLAUDE.md HARD RULE no-compat-shims). Wave 2 deletes these `.tres` files atomically with the rest of the contract demolition; Wave 1 leaves them in place.
4. Migration file is reachable via `cat .planning/phases/02-native-layouts/02-01-VERIFICATION-MIGRATION.md`.
</verification>

<success_criteria>
- `bitmask_template` is the new property name on `PentaTileLayout`.
- `fallback_tile_set` and `decoder_image` are gone from the base class.
- `get_fallback_tile_set()` virtual exists with `return null` body (stub).
- The 26-test Phase 1 verification spec is marked HISTORICAL and superseded by `02-01-VERIFICATION-MIGRATION.md`.
- The migration spec covers all 10 new test scenarios listed under "New Tests."
- Total LOC delta in `penta_tile_layout.gd`: +20 to +30 (rename + add stub + delete contract back-ref).
</success_criteria>

<output>
After completion, create `.planning/phases/02-native-layouts/02-01-SUMMARY.md` recording: (a) exact LOC delta in `penta_tile_layout.gd`, (b) final test count in the migration spec, (c) confirmation that no compat shims were added.
</output>
