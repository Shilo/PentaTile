---
phase: 01
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - addons/tetra_tile/tetra_tile_map_layer.gd
  - .planning/phases/01-contract-skeleton-tetra-layouts/baselines/
  - .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - CONTRACT-04
  - CONTRACT-05
  - TETRA-01
  - TETRA-02
  - TETRA-03
threats:
  - T-01-03  # Resource.changed signal storm DoS — _rebuild_count instrumentation enables verification
tags:
  - phase-1
  - wave-0
  - baseline
  - instrumentation
  - tetra-tile
  - godot

must_haves:
  truths:
    - "10 v0.1 baseline screenshots exist (5 patterns × 2 atlas_layout enum values) — captured BEFORE any Phase 1 code lands"
    - "Pre-Phase-1 LOC snapshot saved to loc-baseline.txt"
    - "_rebuild_count instrumentation field exists on TetraTileMapLayer, gated by OS.is_debug_build(), incremented in _queue_rebuild"
    - "ROADMAP.md and REQUIREMENTS.md reflect D-27 expansion (Phase 3.5 + 3 new layouts) before any Phase 1 implementation begins"
  artifacts:
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-isolated.png"
      provides: "v0.1 horizontal baseline — single isolated tile pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-rectangle.png"
      provides: "v0.1 horizontal baseline — 2×3 rectangle pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-lshape.png"
      provides: "v0.1 horizontal baseline — L-shape pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-strip.png"
      provides: "v0.1 horizontal baseline — single-row strip pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-checkerboard.png"
      provides: "v0.1 horizontal baseline — 4×4 checkerboard pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-vertical-isolated.png"
      provides: "v0.1 vertical baseline — single isolated tile pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-vertical-rectangle.png"
      provides: "v0.1 vertical baseline — 2×3 rectangle pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-vertical-lshape.png"
      provides: "v0.1 vertical baseline — L-shape pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-vertical-strip.png"
      provides: "v0.1 vertical baseline — single-row strip pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-vertical-checkerboard.png"
      provides: "v0.1 vertical baseline — 4×4 checkerboard pattern"
    - path: ".planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt"
      provides: "Pre-Phase-1 LOC snapshot for end-of-phase comparison"
      contains: "tetra_tile_map_layer.gd"
  key_links:
    - from: "addons/tetra_tile/tetra_tile_map_layer.gd"
      to: "_rebuild_count counter"
      via: "_queue_rebuild() incrementer (debug-build gated)"
      pattern: "_rebuild_count.*\\+= 1"
---

<objective>
Wave 0 setup. Capture every prerequisite the rest of Phase 1 needs BEFORE any architectural code lands. Without this plan, the visual-regression bar (CONTRACT-04, TETRA-01/02/03) is unverifiable, the LOC checkpoint (success criterion 6) has no reference, and CONTRACT-05's idempotence verification has no instrumentation hook.

Purpose: Lock the verification preconditions. Once any task in Plan 02-05 modifies `tetra_tile_map_layer.gd`, the v0.1 baseline is destroyed. This plan is the "snapshot before surgery" gate.

Output: 10 baseline PNGs + 1 LOC snapshot text file + 1 source-file modification adding `_rebuild_count` debug instrumentation + ROADMAP/REQUIREMENTS expansion per D-27.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-skeleton-tetra-layouts/01-CONTEXT.md
@.planning/phases/01-contract-skeleton-tetra-layouts/01-VALIDATION.md
@addons/tetra_tile/tetra_tile_map_layer.gd
@addons/tetra_tile/demo/tetra_tile_demo.tscn

<interfaces>
<!-- v0.1 source: the file Task 0.3 modifies. Excerpts that matter for this plan: -->

From `addons/tetra_tile/tetra_tile_map_layer.gd:258-260` (the `_queue_rebuild` deferred coalescer — instrumentation hook):
```gdscript
func _queue_rebuild() -> void:
    if is_inside_tree():
        rebuild.call_deferred()
```

From `addons/tetra_tile/tetra_tile_map_layer.gd:6` (the enum that drives v0.1 horizontal vs vertical capture):
```gdscript
enum AtlasLayout { HORIZONTAL, VERTICAL }
```

From `addons/tetra_tile/tetra_tile_map_layer.gd:31-34` (the export that switches modes for baseline capture):
```gdscript
@export var atlas_layout: AtlasLayout = AtlasLayout.HORIZONTAL:
    set(value):
        atlas_layout = value
        _queue_rebuild()
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0.1: Capture v0.1 baseline screenshots (10 PNGs)</name>
  <read_first>
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-VALIDATION.md` (Wave 0 Requirements section, line 64; Manual-Only Verifications table)
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-CONTEXT.md` (D-19: enum is hard-removed; D-07: null-fallback path; success criteria 1-2)
    - `addons/tetra_tile/demo/tetra_tile_demo.tscn` (the demo scene to be opened)
    - `addons/tetra_tile/tetra_tile_map_layer.gd:6` and `:31-34` (the enum + export — the user toggles this between captures)
  </read_first>
  <what-built>This task is human-only. Claude has already created the `baselines/` directory in the prior task scaffolding step. The user opens the Godot editor, loads the demo scene, paints 5 reference patterns at each of the 2 atlas_layout enum values, and exports 10 screenshots.</what-built>
  <how-to-verify>
    Step 1: Open `C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe` and load the project at `C:\Programming_Files\Shilocity\TetraTile`.
    Step 2: Open `addons/tetra_tile/demo/tetra_tile_demo.tscn` in the editor.
    Step 3: Select the `TetraTileMapLayer` node. Confirm `atlas_layout = HORIZONTAL` in inspector (the v0.1 default).
    Step 4: Run the scene (F6). Use right-click drag-paint (existing `demo_runtime_painter.gd`) to clear the existing tilemap then paint these 5 patterns IN SEPARATE FRESH RUNS — each pattern starts from an empty tilemap:
      a) Single isolated tile at logic (5, 5).
      b) 2×3 rectangle at logic cells (4..5, 4..6).
      c) L-shape: 3 cells horizontal at (4..6, 5) + 2 cells vertical at (4, 6..7).
      d) Single-row strip: 5 cells at (3..7, 5).
      e) 4×4 checkerboard at (3..6, 3..6) where every other cell is filled.
    Step 5: For EACH pattern, while the scene is running, take a viewport screenshot (Project → Tools → Screenshot, or PrintScrn + crop, or use Godot's editor Save Screenshot). Save as PNG to `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-{isolated,rectangle,lshape,strip,checkerboard}.png`. Crop tightly to the visible tile area or save the full viewport — consistency matters more than crop tightness.
    Step 6: Stop the scene. In the inspector, change `atlas_layout` to `VERTICAL`. Repeat steps 4-5, saving as `v0.1-vertical-{isolated,rectangle,lshape,strip,checkerboard}.png`.
    Step 7: Confirm 10 PNGs total exist:
      `ls .planning/phases/01-contract-skeleton-tetra-layouts/baselines/`
      MUST show 10 files matching `v0.1-{horizontal,vertical}-{isolated,rectangle,lshape,strip,checkerboard}.png`.
    Step 8: After capture, RESET `atlas_layout` to `HORIZONTAL` (the v0.1 default) and DO NOT save the scene with VERTICAL set. The scene's saved state must remain HORIZONTAL.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-isolated.png` exists and is a non-zero-byte PNG
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-rectangle.png` exists and is a non-zero-byte PNG
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-lshape.png` exists and is a non-zero-byte PNG
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-strip.png` exists and is a non-zero-byte PNG
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/baselines/v0.1-horizontal-checkerboard.png` exists and is a non-zero-byte PNG
    - Same 5 PNGs exist for `v0.1-vertical-*.png`
    - Total of exactly 10 baseline PNGs in the `baselines/` directory
    - Demo scene `tetra_tile_demo.tscn` left with `atlas_layout = HORIZONTAL` (the v0.1 default — verified by `grep -c 'atlas_layout = ' addons/tetra_tile/demo/tetra_tile_demo.tscn` returning 0 since HORIZONTAL is the default value and isn't serialized)
  </acceptance_criteria>
  <resume-signal>Type "approved" when 10 baseline PNGs exist, or describe issues encountered (e.g., "screenshot tool produces different sizes in horizontal vs vertical captures — what should I use?")</resume-signal>

  <action>See `<what-built>` and `<how-to-verify>` blocks above — this is a checkpoint:human-verify task. The user performs the verification steps; Claude paused and waits for the resume-signal.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — automated verification not applicable; user follows steps in <how-to-verify> and types resume-signal"</automated>
  </verify>
  <done>See `<acceptance_criteria>` block above. The task is done when the user types "approved" per the `<resume-signal>` block.</done>
</task>

<task type="auto">
  <name>Task 0.2: Capture pre-Phase-1 LOC baseline</name>
  <read_first>
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-VALIDATION.md` (Wave 0 Requirements section)
    - `.planning/ROADMAP.md` (Identity Guardrails section — "End of Phase 1: LOC checkpoint")
    - `addons/tetra_tile/tetra_tile_map_layer.gd` (the only existing addon-proper .gd file; demo .gd files are excluded from the addon LOC budget)
  </read_first>
  <action>
    Create the file `.planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt` with the verbatim output of `wc -l` over the addon's `.gd` files (excluding demo). Use bash:

    ```bash
    wc -l addons/tetra_tile/tetra_tile_map_layer.gd > .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
    echo "" >> .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
    echo "Captured: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
    echo "Pre-Phase-1 baseline. End-of-Phase-1 (Plan 05) compares against this." >> .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
    echo "Demo scripts (demo/*.gd) are NOT counted — they are not part of the addon's surface area." >> .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt
    ```

    Do NOT include `demo/demo_player.gd` or `demo/demo_runtime_painter.gd` — those are example code, not addon surface (per identity guardrail "TetraTile must remain visibly smaller and simpler than TileMapDual" which counts the addon-proper code, not the demo). The end-of-Phase-1 checkpoint in Plan 05 will measure the same set: addon-proper `.gd` files only.
  </action>
  <verify>
    <automated>test -s .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt && grep -c 'tetra_tile_map_layer.gd' .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt` exists and is non-empty
    - File contains the line matching `\d+ addons/tetra_tile/tetra_tile_map_layer.gd` (live wc -l output)
    - File contains the literal string `Pre-Phase-1 baseline`
    - File contains a timestamp matching ISO-8601 UTC pattern
    - File does NOT contain references to `demo/demo_player.gd` or `demo/demo_runtime_painter.gd`
  </acceptance_criteria>
  <done>loc-baseline.txt is committed and demonstrably captures pre-Phase-1 LOC of the addon-proper .gd files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.3: Add _rebuild_count debug instrumentation</name>
  <read_first>
    - `addons/tetra_tile/tetra_tile_map_layer.gd:258-260` (the existing `_queue_rebuild` body — the exact insertion point)
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-VALIDATION.md` (Wave 0 Requirements item 2 — `_rebuild_count: int = 0` field, gated by OS.is_debug_build())
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-CONTEXT.md` (D-08 setter discipline — verification target for the counter; CONTRACT-05 success criterion)
    - `.planning/research/PITFALLS.md` (§5 setter loops + Resource.changed storms — the pitfall this counter detects)
  </read_first>
  <behavior>
    - The instrumentation MUST NOT execute outside debug builds (gate with `OS.is_debug_build()`)
    - In a debug build, every call to `_queue_rebuild()` increments `_rebuild_count` by exactly 1
    - The counter is readable from outside the script (declared without leading underscore would normally be public, but per CONVENTIONS.md and the v0.1 internal-naming policy this is `_rebuild_count` and tests read it via `obj._rebuild_count` — that's how Plan 05's verification reads it)
    - The counter is initialized to 0 at script load (default value of `int` field)
  </behavior>
  <action>
    Modify `addons/tetra_tile/tetra_tile_map_layer.gd` with two surgical edits:

    Edit 1 — Add the field declaration. Find the existing var declarations near line 56-57:
    ```gdscript
    var _primary_layer: TileMapLayer
    var _overlay_layer: TileMapLayer
    ```
    Add immediately AFTER `_overlay_layer`:
    ```gdscript
    var _overlay_layer: TileMapLayer

    # Debug-build instrumentation (Phase 1 Wave 0 — verifies CONTRACT-05 idempotence).
    # Counts every _queue_rebuild() call in debug builds. Read by verification recipes
    # (Plan 05 idempotence + signal-storm checks). Excluded from release builds via
    # OS.is_debug_build() gate inside _queue_rebuild.
    var _rebuild_count: int = 0
    ```

    Edit 2 — Increment in `_queue_rebuild`. Find the existing function at lines 258-260:
    ```gdscript
    func _queue_rebuild() -> void:
        if is_inside_tree():
            rebuild.call_deferred()
    ```
    Replace with:
    ```gdscript
    func _queue_rebuild() -> void:
        if OS.is_debug_build():
            _rebuild_count += 1
        if is_inside_tree():
            rebuild.call_deferred()
    ```

    Do NOT touch any other code in the file. Do NOT use `@export` on `_rebuild_count` — it is internal instrumentation, not a user-facing knob. Do NOT add a getter — Plan 05's verification reads it directly via `obj._rebuild_count` (GDScript allows external access to underscore-prefixed members; the underscore is a convention, not access control).

    After the edit, run a parse check:
    ```bash
    "C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe" --headless --check-only --path . addons/tetra_tile/tetra_tile_map_layer.gd
    ```
    The exit code MUST be 0.
  </action>
  <verify>
    <automated>"C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe" --headless --check-only --path . addons/tetra_tile/tetra_tile_map_layer.gd && grep -c 'var _rebuild_count: int = 0' addons/tetra_tile/tetra_tile_map_layer.gd && grep -c 'OS.is_debug_build()' addons/tetra_tile/tetra_tile_map_layer.gd</automated>
  </verify>
  <acceptance_criteria>
    - `addons/tetra_tile/tetra_tile_map_layer.gd` contains the literal line `var _rebuild_count: int = 0` (exactly once)
    - `addons/tetra_tile/tetra_tile_map_layer.gd` contains the literal substring `OS.is_debug_build()` (exactly once — only inside `_queue_rebuild`)
    - `addons/tetra_tile/tetra_tile_map_layer.gd` contains the literal substring `_rebuild_count += 1` (exactly once — only inside `_queue_rebuild`)
    - Godot --check-only exits 0 on the modified file
    - The pattern `func _queue_rebuild() -> void:` still appears exactly once in the file
    - The line `rebuild.call_deferred()` is still present (the counter does not replace the existing behavior)
    - File line count is approximately 261 + 5 = ~266 (3 lines added for field + comment + 2 lines added for OS check) — verify with `wc -l`; expect 264-267
  </acceptance_criteria>
  <done>The `_rebuild_count` field exists and is incremented in debug builds inside `_queue_rebuild`, with no behavioral change in non-debug builds. Godot parses the file without error.</done>
</task>

<task type="auto">
  <name>Task 0.4: Update ROADMAP.md and REQUIREMENTS.md per D-27</name>
  <read_first>
    - `.planning/ROADMAP.md` (sections to update: Phases list at line 23-27, Phase Details, Coverage table at line 137-145)
    - `.planning/REQUIREMENTS.md` (sections to update: add new requirement IDs near appropriate sections, add Traceability rows)
    - `.planning/phases/01-contract-skeleton-tetra-layouts/01-CONTEXT.md` (D-24, D-25, D-26, D-27 — the locked scope expansion that drives this update)
    - `.planning/research/layouts/PIXELLAB.md` (PIXLAB-01..04 requirement basis — read briefly to confirm these are sound IDs)
  </read_first>
  <action>
    This task captures the locked v0.2 scope expansion (D-24, D-25, D-26) into the planning artifacts so downstream phases plan against the correct requirement IDs. Per CONTEXT.md D-27: "executing them is part of planning rather than Phase 1's implementation work."

    **Edit 1 — `.planning/ROADMAP.md`:**

    1.1) In the Phases bullet list (around line 23-27), insert a new bullet for Phase 3.5 between Phase 3 and Phase 4:
    ```
    - [ ] **Phase 3.5: PixelLab Layouts + Variation-Seed Wiring** — Ship `TetraTileLayoutPixelLabTopDown` and `TetraTileLayoutPixelLabSideScroller` (8×8 atlas, single-grid, 4-bit corner mask, variation-bank). Wire `variation_seed` deterministic-hash bucket-pick. Add `TetraTileLayoutMinimal3x3` if not already shipped in Phase 2.
    ```

    1.2) After the Phase 3 details block (ending around line 80, before Phase 4 begins), insert a new Phase 3.5 details block:
    ```
    ### Phase 3.5: PixelLab Layouts + Variation-Seed Wiring

    **Goal**: Ship `TetraTileLayoutPixelLabTopDown` and `TetraTileLayoutPixelLabSideScroller` subclasses. Both consume PixelLab Aseprite plugin native 8×8 atlas output with variation banks. Both share the locked role-to-mask bijection `[4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` (corner mask). Wire `variation_seed` deterministic-hash bucket-pick: `mask → cells[]; pick = cells[hash(coord, variation_seed) % cells.size()]`.

    **Depends on**: Phase 1 (architecture), Phase 2 or Phase 3 (single-grid pipeline first consumed by Wang2Corner in Phase 2).

    **Requirements**: PIXLAB-01, PIXLAB-02, PIXLAB-03, PIXLAB-04, VAR-PIXEL-01.

    **Success Criteria** (what must be TRUE):
    1. `TetraTileLayoutPixelLabTopDown.compute_mask` and `mask_to_atlas` consume the locked role-to-mask mapping; visual regression on a PixelLab 8×8 sample matches the Aseprite plugin output.
    2. `TetraTileLayoutPixelLabSideScroller` shares the role-to-mask mapping; cell-to-role differs (the side-scroller variant). Visual regression on a side-scroller PixelLab 8×8 sample passes.
    3. Variation-bank: when a mask has multiple cells (PixelLab variations), `mask_to_atlas` returns a deterministic pick keyed on `(coord, variation_seed)`. Same `(coord, seed)` always returns the same cell across `rebuild()` invocations (no shimmering).
    4. Setting `variation_seed = N` produces a different deterministic pick than `variation_seed = N+1`, verified visually on a uniform painted region.

    **Plans**: TBD
    ```

    1.3) In the Phase 2 details block, ADD `TetraTileLayoutMinimal3x3` to the Goal description (per D-24): "Three TetraTile-native layout subclasses (DualGrid16, Wang2Edge, Wang2Corner) **plus Minimal3x3 (3×3 atlas, 9 tiles, single-grid, 4-bit edge mask — covers PixelLab Tileset 3×3 export + RPG Maker A2 + legacy Godot 3.x)**" and add `MIN3x3-01` to the Requirements line of Phase 2.

    1.4) Update the Coverage table at line 132-145. Add rows:
    ```
    | 2. Native Layouts | NATIVE-01..03, MIN3x3-01, PREVIEW-02 (partial), TEMPLATE-04 (partial) (6) |
    | 3.5. PixelLab Layouts + Variation-Seed Wiring | PIXLAB-01..04, VAR-PIXEL-01 (5) |
    ```
    Update the Total row: `**Total** | **39 + 6 = 45 / 45**` (was 39; added MIN3x3-01 + PIXLAB-01..04 + VAR-PIXEL-01 = 6).

    1.5) Update the Phases-Plans table at line 124-129 to include Phase 3.5:
    ```
    | 3.5. PixelLab Layouts + Variation-Seed Wiring | 0/TBD | Not started | - |
    ```

    1.6) Update the Identity Guardrails section to mention Phase 3.5 LOC contribution (~80-120 LOC for two layouts + variation wiring).

    **Edit 2 — `.planning/REQUIREMENTS.md`:**

    2.1) Add a new requirement section after `### Native Layouts (NATIVE)` (before `### TileBitTools-Decoded Layouts (TBT)`):
    ```
    ### Minimal 3×3 Layout (MIN3x3)

    Per D-24 — added during Phase 1 discuss session. Covers PixelLab Tileset 3×3 export + RPG Maker A2 + legacy Godot 3.x atlases.

    - [ ] **MIN3x3-01**: `TetraTileLayoutMinimal3x3` subclass — 3×3 atlas, 9 unique tiles, single-grid, 4-bit edge mask (T=1/E=2/B=4/W=8). Lands in Phase 2 alongside Wang2Edge.
    ```

    2.2) Add a new requirement section after `### TileBitTools-Decoded Layouts (TBT)` (before `### Preview & Fallback (PREVIEW)`):
    ```
    ### PixelLab Layouts (PIXLAB)

    Per D-25 — added during Phase 1 discuss session. Aseprite plugin native 8×8 atlas with variation banks; locked role-to-mask bijection from spike 003.

    - [ ] **PIXLAB-01**: `TetraTileLayoutPixelLabTopDown` subclass — 8×8 atlas, single-grid, 4-bit corner mask. Cell-to-role layout from `tileset_transform.lua` `tileset_output`. Role-to-mask = `[4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]`.
    - [ ] **PIXLAB-02**: `TetraTileLayoutPixelLabSideScroller` subclass — 8×8 atlas, single-grid, 4-bit corner mask. Cell-to-role layout from `tileset_transform.lua` `tileset_output_side`. Same role-to-mask bijection as PIXLAB-01.
    - [ ] **PIXLAB-03**: Both PixelLab layouts handle variation banks: when multiple cells map to the same mask, `mask_to_atlas` deterministically picks one keyed on `(coord, variation_seed)`. Differentiator from PixelLab's official exporter (which discards duplicates).
    - [ ] **PIXLAB-04**: Visual regression on a PixelLab Aseprite sample (8×8 PNG output) matches the Aseprite plugin's own canvas output for both top-down and side-scroller variants.
    ```

    2.3) Add a new requirement section after `### PixelLab Layouts (PIXLAB)`:
    ```
    ### Variation-Seed Wiring (VAR-PIXEL)

    Per D-26 — added during Phase 1 discuss session. Phase 3.5 prerequisite; Phase 1 declares `variation_seed: int = 0` on `TetraTileAtlasContract` (CONTRACT-02) and `alternative_tile: int = 0` on `AtlasSlot` (LAYOUT-04) so this can wire up cleanly.

    - [ ] **VAR-PIXEL-01**: `mask_to_atlas` (or a sibling helper) accepts variation context (per the Phase 1 back-reference from layout to contract; locked planner decision for variation-pick threading) and returns a deterministic cell from `mask → cells[]` keyed on `(coord, variation_seed)`. Hash function is `hash(Vector4i(coord.x, coord.y, atlas_coords.x, atlas_coords.y) + variation_seed)` per PITFALLS.md §2 (variation determinism).
    ```

    2.4) Append the new IDs to the Traceability table (around lines 169-209) — order them where they fit:
    ```
    | MIN3x3-01 | 2 | Pending |
    | PIXLAB-01 | 3.5 | Pending |
    | PIXLAB-02 | 3.5 | Pending |
    | PIXLAB-03 | 3.5 | Pending |
    | PIXLAB-04 | 3.5 | Pending |
    | VAR-PIXEL-01 | 3.5 | Pending |
    ```

    2.5) Update the Coverage block at lines 211-214:
    ```
    **Coverage:**
    - v1 requirements: 45 total (39 original + 6 added per Phase 1 discuss session)
    - Mapped to phases: 45 (after this update)
    - Unmapped: 0
    ```

    **Edit 3 — Map existing CONTRACT-01..05, LAYOUT-01..05, TETRA-01..03, PREVIEW-01 in the Traceability table to Phase 1.** They currently say `TBD` at lines 171-191; change those 14 entries' `Phase` column from `TBD` to `1`. This is a bookkeeping fix; the roadmap already lists them under Phase 1 (line 137).

    Do NOT touch other Traceability rows (Phase 2-5 IDs stay TBD until those phases plan). Do NOT change any v2 backlog items.
  </action>
  <verify>
    <automated>grep -c 'PIXLAB-01' .planning/REQUIREMENTS.md && grep -c 'MIN3x3-01' .planning/REQUIREMENTS.md && grep -c 'VAR-PIXEL-01' .planning/REQUIREMENTS.md && grep -c 'Phase 3.5' .planning/ROADMAP.md && grep -c '| CONTRACT-01 | 1 |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the literal string `MIN3x3-01` (at least once)
    - `.planning/REQUIREMENTS.md` contains the literal strings `PIXLAB-01`, `PIXLAB-02`, `PIXLAB-03`, `PIXLAB-04` (each at least once)
    - `.planning/REQUIREMENTS.md` contains the literal string `VAR-PIXEL-01`
    - `.planning/REQUIREMENTS.md` Traceability rows for `CONTRACT-01..05`, `LAYOUT-01..05`, `TETRA-01..03`, `PREVIEW-01` all show `Phase` column = `1` (no longer `TBD`); verified by `grep -c '| CONTRACT-01 | 1 |' .planning/REQUIREMENTS.md` returning 1
    - `.planning/REQUIREMENTS.md` Coverage block reads `v1 requirements: 45 total`
    - `.planning/ROADMAP.md` contains a Phase 3.5 details block with the literal heading `### Phase 3.5: PixelLab Layouts + Variation-Seed Wiring`
    - `.planning/ROADMAP.md` Phases bullet list contains the entry for Phase 3.5
    - `.planning/ROADMAP.md` Phases-Plans status table contains a row for Phase 3.5
    - `.planning/ROADMAP.md` Coverage table contains rows for `MIN3x3-01` and `PIXLAB-01..04` and `VAR-PIXEL-01`
  </acceptance_criteria>
  <done>ROADMAP.md and REQUIREMENTS.md reflect the locked D-24..D-27 scope expansion. Phase 1 implementation work (Plans 02-05) can now proceed without further roadmap drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → addon code | User can edit `.tres` resources at edit time; the addon's `Resource.changed` propagation must not amplify into a signal storm |
| user filesystem → addon | Future-phase decoder consumes `template_image: Texture2D` — bounded sampling by `tile_size`-relative anchors. Not exercised in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | future template decoder | accept (Phase 1) | Decoder NOT exercised in Phase 1; bounded in Phase 2+ by `tile_size`-relative sampling that cannot read out-of-bounds (D-12 anchor formulas are tile-size-invariant). Tracked for Phase 2 threat model. |
| T-01-02 | Tampering | `.tres` resource files (Phase 1's 4 bundled .tres) | accept | Godot's resource loader handles cycles in `[ext_resource]` references; the `atlas_contract` setter idempotence guard (D-08, locked in Plan 04) prevents unbounded reassignment cascades. |
| T-01-03 | DoS | `Resource.changed` signal storm | mitigate | Idempotence guard (`if value == _atlas_contract: return`) + disconnect-before-reconnect locked in Plan 04's setter. `_queue_rebuild` deferred coalescer (existing v0.1 pattern, preserved). This Plan 01 adds `_rebuild_count` instrumentation in `_queue_rebuild` (Task 0.3) so Plan 05's verification can detect storms quantitatively. |

Block-on threshold: HIGH. None of these threats block.

**This is the Phase-level threat model.** Plans 02-05 inherit it by reference; they do not re-enumerate identical threats.
</threat_model>

<verification>
After all tasks complete:

1. Run: `ls .planning/phases/01-contract-skeleton-tetra-layouts/baselines/`
   Expected: 10 PNG files matching `v0.1-{horizontal,vertical}-{isolated,rectangle,lshape,strip,checkerboard}.png`.

2. Run: `cat .planning/phases/01-contract-skeleton-tetra-layouts/loc-baseline.txt`
   Expected: includes `wc -l` output for `addons/tetra_tile/tetra_tile_map_layer.gd` plus a timestamp and the literal string "Pre-Phase-1 baseline".

3. Run: `"C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe" --headless --check-only --path . addons/tetra_tile/tetra_tile_map_layer.gd; echo $?`
   Expected: exit code `0`.

4. Run: `grep -c 'var _rebuild_count: int = 0' addons/tetra_tile/tetra_tile_map_layer.gd`
   Expected: `1`.

5. Run: `grep -c 'PIXLAB-01' .planning/REQUIREMENTS.md`
   Expected: ≥ `2` (one in the requirements section, one in the Traceability table).

6. Run: `grep -c 'Phase 3.5' .planning/ROADMAP.md`
   Expected: ≥ `3` (Phases bullet list + details heading + status table + Coverage table).
</verification>

<success_criteria>
- [ ] 10 v0.1 baseline PNGs captured and committed
- [ ] LOC baseline saved with timestamp
- [ ] _rebuild_count instrumentation added; Godot parses without error
- [ ] ROADMAP.md and REQUIREMENTS.md reflect D-24..D-27 expansion (Phase 3.5 + 6 new requirement IDs)
- [ ] Demo scene `tetra_tile_demo.tscn` left in `atlas_layout = HORIZONTAL` state (the v0.1 default — not modified for capture artifacts)
- [ ] No code change to `tetra_tile_map_layer.gd` beyond the surgical Task 0.3 edits (verified by reviewing the diff: only the field declaration + the `if OS.is_debug_build(): _rebuild_count += 1` insertion appear in the diff vs the pre-Phase-1 commit)
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-skeleton-tetra-layouts/01-01-SUMMARY.md` documenting:
- Baseline PNGs captured (list of 10 files with byte sizes)
- LOC baseline value (the wc -l number for tetra_tile_map_layer.gd)
- Instrumentation diff (the 5 lines added to tetra_tile_map_layer.gd)
- ROADMAP/REQUIREMENTS expansion (which sections changed, the new IDs added)
- The v0.1 source file is now ~266 LOC (was 261); this delta is tracked but does not count toward Phase 1's architectural LOC growth (Plan 05 measures the delta against this Wave 0 post-instrumentation baseline)
</output>
