---
phase: 03.5-pixellab-layouts-variation-seed-wiring
plan: 03
subsystem: layout
tags: [godot, gdscript, pixellab, layout, single-grid, corner-mask, autotile]

# Dependency graph
requires:
  - phase: 03.5-pixellab-layouts-variation-seed-wiring
    provides: bundled greybox PNG `penta_tile_layout_pixel_lab_side_scroller.png` (Plan 01)
provides:
  - PentaTileLayoutPixelLabSideScroller subclass (PIXLAB-02)
  - Locked _CELL_TO_ROLE 64-entry side-scroller table verbatim from tileset_transform.lua:28-36
  - _first_cell_by_mask cache pattern reused (D-89 row-major first-occurrence pick)
  - Side-scroller mask=0 dispatches to (0, 0) per D-104 (role 12 first cell at table top-left)
affects: [03.5-04 pixellab_first_cell_test, 03.5-05 comprehensive_bitmask_test matrix extension, 03.5-06 closeout]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cached first-cell-by-mask dispatch (Array[Vector2i]) populated once at _init — twin of top-down"
    - "Per-subclass const duplication for _CELL_TO_ROLE / _ROLE_TO_MASK (D-98 GDScript 2 cross-class const limitation)"

key-files:
  created:
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd.uid
  modified: []

key-decisions:
  - "Side-scroller _CELL_TO_ROLE differs from top-down — first row 12,12,12,12,13,3,3,3 (D-95)"
  - "_ROLE_TO_MASK bijection identical to top-down per D-94; D-98 duplicates per-subclass anyway"
  - "Mask=0 → role 12 → (0, 0) for side-scroller (vs top-down (2, 2)) per D-104"
  - "transform_flags=0 always (D-90 no rotation reuse); single-grid (D-96 is_dual_grid=false)"
  - "No variation_seed; no pixellab_version field (D-90, D-92 no-forward-compat)"

patterns-established:
  - "PixelLab side-scroller layout structurally identical to top-down — only cell-to-role table content + class name + bundled PNG path differ"

requirements-completed: [PIXLAB-02]

# Metrics
duration: 5min
completed: 2026-04-29
---

# Phase 03.5 Plan 03: PentaTileLayoutPixelLabSideScroller Summary

**PixelLab side-scroller 8x8 single-grid layout shipped — twin of top-down with side-scroller cell-to-role table; mask=0 resolves to (0,0) per D-104 (role 12 first appears at side-scroller table origin).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-29T09:43:30Z (immediately after Plan 02 close)
- **Completed:** 2026-04-29T09:48:38Z
- **Tasks:** 1
- **Files created:** 2 (.gd + .gd.uid sidecar)

## Accomplishments

- `PentaTileLayoutPixelLabSideScroller` subclass exists, parses cleanly under Godot 4.6.2, and is class_name-resolvable.
- 64-entry `_CELL_TO_ROLE` table transcribed verbatim from PixelLab's `tileset_transform.lua:28-36 tileset_output_side` (side-scroller version). Distinguishing first row `12, 12, 12, 12, 13, 3, 3, 3` (NOT top-down's `6, 6, 6, 6, 6, 6, 6, 6`).
- 16-entry `_ROLE_TO_MASK` bijection locked per spike 003: `[4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` — identical to top-down per D-94, duplicated per-subclass per D-98.
- `_first_cell_by_mask: Array[Vector2i]` populated once in `_init` via O(64) row-major scan; sanity-checked all 16 entries match the PLAN.md expected dispatch table verbatim:

  | mask | atlas_coords | mask | atlas_coords |
  |------|--------------|------|--------------|
  | 0    | (0, 0)       | 8    | (4, 0)       |
  | 1    | (4, 2)       | 9    | (3, 7)       |
  | 2    | (1, 2)       | 10   | (6, 2)       |
  | 3    | (5, 1)       | 11   | (6, 1)       |
  | 4    | (0, 1)       | 12   | (5, 0)       |
  | 5    | (0, 2)       | 13   | (0, 4)       |
  | 6    | (4, 1)       | 14   | (4, 7)       |
  | 7    | (3, 5)       | 15   | (7, 1)       |

- `compute_mask` uses 4-bit corner sampling (TL=1/TR=2/BL=4/BR=8) — body identical to top-down/DualGrid16.
- `mask_to_atlas` returns a `PentaTileAtlasSlot` with cached coords + `transform_flags = 0`; no `if mask == 0: return null` short-circuit (D-104 / Pitfall #9). For side-scroller, mask=0 → (0, 0) which is the legitimate first-cell location of role 12 in the side-scroller table.
- `is_dual_grid()` returns `false`; `_fallback_atlas_grid_size()` returns `Vector2i(8, 8)`.
- `_default_bitmask_template_path()` points at `res://addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png` (Plan 01 bundled PNG).
- `.gd.uid` sidecar `uid://c430mc3lmufs2` auto-generated by Godot's `--import` pass.
- Functional LOC = 52 (under D-105's 70-LOC cap; the 27-line doc-comment header is exempt). Total file = 102 lines.

## Task Commits

1. **Task 3.1: Create PentaTileLayoutPixelLabSideScroller subclass file (twin of top-down)** — `2a20bf9` (feat)

## Files Created/Modified

- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd` — subclass body (102 lines: 27 doc + 52 functional + 23 blank)
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd.uid` — `uid://c430mc3lmufs2` (auto-generated)

## Decisions Made

None — followed plan as specified. All design decisions (D-88 two subclasses, D-89 first-cell pick, D-90 no variation_seed, D-92 no pixellab_version, D-93 corner mask, D-94 role-to-mask shared, D-95 side-scroller cell-to-role, D-96 single-grid, D-97 cache at _init, D-98 per-subclass const, D-99 fallback grid 8x8, D-103 co-located file path, D-104 mask=0 dispatch (0,0) for side-scroller, D-105 LOC budget) were locked in CONTEXT.md before plan-phase. Executor consumed the verbatim const tables and subclass body from PLAN.md without modification.

## Deviations from Plan

None — plan executed exactly as written. The PLAN.md contained the complete subclass body verbatim; executor wrote it 1:1 (matching top-down structure, swapping only class name + cell-to-role table content + bundled PNG path), ran the parse check (exit 0), ran `--import` to generate the UID sidecar, performed an inline sanity check confirming all 16 first-cell entries match the expected dispatch table, and committed both files atomically.

## Issues Encountered

- **Godot executable path nesting (recurring):** The Windows path `C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe\` is a directory containing the actual exe at the same name. Same workaround as Plan 02 — use the doubled path `Godot_v4.6.2-stable_win64.exe/Godot_v4.6.2-stable_win64.exe`. Plan 02's SUMMARY already documented this; noted again here for trace continuity.

## Verification Results

| Acceptance criterion (from PLAN.md) | Status |
|-------------------------------------|--------|
| File `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd` exists | PASS |
| Contains `class_name PentaTileLayoutPixelLabSideScroller` | PASS |
| Contains `extends PentaTileLayout` | PASS |
| Contains `const _ROLE_TO_MASK := [4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` | PASS |
| Contains `const _CELL_TO_ROLE := [` | PASS |
| Contains row 0 distinguisher `12, 12, 12, 12, 13,  3,  3,  3,` (side-scroller marker — distinct from top-down's `6, 6, 6, 6, 6, 6, 6, 6,`) | PASS |
| Does NOT contain `6, 6, 6, 6, 6, 6, 6, 6,` (would indicate copy-paste from top-down) | PASS |
| Contains `var _first_cell_by_mask: Array[Vector2i]` | PASS |
| Contains `func _init_cache() -> void:` | PASS |
| Contains `func compute_mask(coord: Vector2i, sample_fn: Callable) -> int:` | PASS |
| `is_dual_grid()` returns `false` | PASS |
| Contains `return Vector2i(8, 8)` | PASS |
| Contains `"res://addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png"` | PASS |
| Godot `--check-only --script <file>` exits 0 | PASS (exit code 0) |
| `.gd.uid` sidecar generated by `--import` pass | PASS (`uid://c430mc3lmufs2`) |
| `.uid` content matches `^uid://.+$` (single line) | PASS |
| Manual verification: all 16 cached first-cell entries match PLAN.md expected table (especially mask=0 → (0,0)) | PASS (inline sanity check ALL_PASS, then temp script removed) |
| Functional LOC ≤ 70 (D-105) | PASS (52 functional LOC) |

## Next Plan Readiness

- Plan 04 (`pixellab_first_cell_test`) now has BOTH PIXLAB layouts available for the verify-the-regression contract test. The test must assert per-subclass: top-down mask=0 → (2,2), side-scroller mask=0 → (0,0), plus all 32 first-cell entries.
- Plan 05 (`comprehensive_bitmask_test` + visual regression) gains both layouts for the 8×18=144 combo matrix and per-layout PixelLab Aseprite-sample fixture tests.
- Plan 06 (closeout) records the cumulative ~104 LOC delta for both PIXLAB layouts toward the Phase 5 final identity-audit ledger.

## Self-Check: PASSED

- File exists: `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd` — FOUND
- File exists: `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd.uid` — FOUND
- Commit `2a20bf9` exists in git log — FOUND

---
*Phase: 03.5-pixellab-layouts-variation-seed-wiring*
*Completed: 2026-04-29*
