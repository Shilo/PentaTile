---
phase: 03.5-pixellab-layouts-variation-seed-wiring
plan: 02
subsystem: layout
tags: [godot, gdscript, pixellab, layout, single-grid, corner-mask, autotile]

# Dependency graph
requires:
  - phase: 03.5-pixellab-layouts-variation-seed-wiring
    provides: bundled greybox PNG `penta_tile_layout_pixel_lab_top_down.png` (Plan 01)
provides:
  - PentaTileLayoutPixelLabTopDown subclass (PIXLAB-01)
  - Locked _CELL_TO_ROLE 64-entry top-down table verbatim from tileset_transform.lua
  - Locked _ROLE_TO_MASK 16-entry bijection per spike 003
  - _first_cell_by_mask cache pattern (D-89 row-major first-occurrence pick)
  - Single-grid mask=0 dispatch via cache (no special-case if mask==0: return null)
affects: [03.5-04 pixellab_first_cell_test, 03.5-05 comprehensive_bitmask_test matrix extension, 03.5-06 closeout]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cached first-cell-by-mask dispatch (Array[Vector2i]) populated once at _init"
    - "Per-subclass const duplication for _CELL_TO_ROLE / _ROLE_TO_MASK (D-98 GDScript 2 cross-class const limitation)"

key-files:
  created:
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd.uid
  modified: []

key-decisions:
  - "First-cell row-major pick over variation-bank pick (D-89; bank pick deferred to v2 VAR-PIXEL-01)"
  - "Cache _first_cell_by_mask once at _init via O(64) row-major scan (D-97)"
  - "No variation_seed property; no pixellab_version field (D-90, D-92 no-forward-compat)"
  - "Mask=0 dispatches to (2, 2) via cache, NOT a special-case erase short-circuit (D-104, Pitfall #9)"
  - "Per-subclass const duplication — top-down and side-scroller each carry their own _CELL_TO_ROLE + _ROLE_TO_MASK (D-98)"

patterns-established:
  - "PixelLab layout pattern: 16-entry _first_cell_by_mask Array[Vector2i] cache + transform_flags=0 (single-grid, fully-authored, no rotation reuse)"
  - "Single-grid mask=0 handled by cache lookup (Vector2i value), not by if mask==0: return null branch"

requirements-completed: [PIXLAB-01]

# Metrics
duration: 2min
completed: 2026-04-29
---

# Phase 03.5 Plan 02: PentaTileLayoutPixelLabTopDown Summary

**PixelLab top-down 8x8 single-grid layout shipped — 4-bit corner mask, locked role-to-mask bijection from spike 003, row-major first-cell dispatch via cached Array[Vector2i].**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-29T09:41:38Z
- **Completed:** 2026-04-29T09:43:26Z
- **Tasks:** 1
- **Files created:** 2 (.gd + .gd.uid sidecar)

## Accomplishments

- `PentaTileLayoutPixelLabTopDown` subclass exists, parses cleanly under Godot 4.6.2, and is class_name-resolvable.
- 64-entry `_CELL_TO_ROLE` table transcribed verbatim from PixelLab's `tileset_transform.lua:17-26 tileset_output` (top-down version).
- 16-entry `_ROLE_TO_MASK` bijection locked per spike 003: `[4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]`.
- `_first_cell_by_mask: Array[Vector2i]` populated once in `_init` via O(64) row-major scan; manually verified all 16 entries match the PLAN.md expected dispatch table (e.g. mask 0 → (2,2), mask 15 → (0,0), mask 7 → (1,1)).
- `compute_mask` uses 4-bit corner sampling (TL=1/TR=2/BL=4/BR=8) — body identical to DualGrid16 except for the mask=0 special case.
- `mask_to_atlas` returns a `PentaTileAtlasSlot` with cached coords + `transform_flags = 0`; NO `if mask == 0: return null` short-circuit (D-104 / Pitfall #9 — single-grid mask=0 is a valid render target, not erase).
- `is_dual_grid()` returns `false`; `_fallback_atlas_grid_size()` returns `Vector2i(8, 8)`.
- `_default_bitmask_template_path()` points at the bundled top-down PNG shipped in Plan 01.
- `.gd.uid` sidecar `uid://dve83mfavy5r5` auto-generated by Godot's `--import` pass.
- Functional LOC = 52 (well under D-105's 70-LOC cap; the 25-line doc-comment header is exempt).

## Task Commits

1. **Task 2.1: Create PentaTileLayoutPixelLabTopDown subclass file** — `18ef118` (feat)

## Files Created/Modified

- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd` — subclass body (105 lines total: ~25 doc + 52 functional + 28 blank)
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd.uid` — `uid://dve83mfavy5r5` (auto-generated)

## Decisions Made

None — followed plan as specified. All design decisions (D-89 first-cell pick, D-90 no variation_seed, D-92 no pixellab_version, D-93 corner mask, D-94 role-to-mask, D-95 cell-to-role, D-96 single-grid, D-97 cache at _init, D-98 per-subclass const, D-99 fallback grid 8x8, D-104 mask=0 dispatch, D-105 LOC budget) were locked in CONTEXT.md before plan-phase. Executor consumed verbatim const tables from PLAN.md without modification.

## Deviations from Plan

None — plan executed exactly as written. The PLAN.md contained the complete subclass body verbatim; executor wrote it 1:1, ran the parse check (exit 0), ran `--import` to generate the UID sidecar, and committed both files atomically.

## Issues Encountered

- **Godot executable path nesting:** The Windows path `C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe\` is a **directory** containing `Godot_v4.6.2-stable_win64.exe`. Initial bash invocation hit "Is a directory" error. Resolved by using the doubled path `Godot_v4.6.2-stable_win64.exe/Godot_v4.6.2-stable_win64.exe` — matches what the PLAN.md verbatim command also used.

## Verification Results

| Acceptance criterion (from PLAN.md) | Status |
|-------------------------------------|--------|
| File `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd` exists | PASS |
| Contains `class_name PentaTileLayoutPixelLabTopDown` | PASS |
| Contains `extends PentaTileLayout` | PASS |
| Contains `_ROLE_TO_MASK := [4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` | PASS |
| Contains `const _CELL_TO_ROLE := [` | PASS |
| Contains row 1 marker `6, 7, 9, 10, 7, 9, 10, 6,` (top-down distinguisher) | PASS |
| Contains `var _first_cell_by_mask: Array[Vector2i]` | PASS |
| Contains `func _init_cache() -> void:` | PASS |
| Contains `func compute_mask(coord: Vector2i, sample_fn: Callable) -> int:` | PASS |
| `is_dual_grid()` returns `false` | PASS |
| Contains `return Vector2i(8, 8)` | PASS |
| Contains `"res://addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png"` | PASS |
| Godot `--check-only --script <file>` exits 0 | PASS (exit code 0) |
| `.gd.uid` sidecar generated by `--import` pass | PASS (`uid://dve83mfavy5r5`) |
| `.uid` content matches `^uid://.+$` (single line) | PASS |
| Manual verification: 16 cached first-cell entries match PLAN.md expected table | PASS (all 16 match — derived inverse-mapping confirms mask 0→role 12→(2,2), mask 15→role 6→(0,0), mask 7→role 7→(1,1), etc.) |

## Next Plan Readiness

- Plan 03 (`PentaTileLayoutPixelLabSideScroller`) is independent (Wave 2 sibling); the only shared artifact is the locked `_ROLE_TO_MASK` const which D-98 duplicates per-subclass.
- Plan 04 (`pixellab_first_cell_test`) consumes this layout to lock the D-89 first-cell-pick contract via verify-the-regression cycle.
- Plan 05 (`comprehensive_bitmask_test` + visual regression) extends the matrix to 8x18=144 combos; this layout participates without modification.

## Self-Check: PASSED

- File exists: `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd` — FOUND
- File exists: `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd.uid` — FOUND
- Commit `18ef118` exists in git log — FOUND

---
*Phase: 03.5-pixellab-layouts-variation-seed-wiring*
*Completed: 2026-04-29*
