---
phase: 03.5-pixellab-layouts-variation-seed-wiring
plan: 01
subsystem: addon-generator
tags: [godot, gdscript, pixellab, generator, bundled-png, greybox]

# Dependency graph
requires:
  - phase: 03-tilebittools-sourced-layouts
    provides: gen_blob_47_godot pattern (per-layout generator function + const data + main() registration)
  - phase: 02-native-layouts
    provides: TILE=32 generator convention; new_atlas/draw_corner_mask/draw_slot_outline helpers
provides:
  - PIXELLAB_TOP_DOWN_CELL_TO_ROLE constant (8x8 = 64 ints, verbatim from tileset_transform.lua:17-26)
  - PIXELLAB_SIDE_SCROLLER_CELL_TO_ROLE constant (8x8 = 64 ints, verbatim from tileset_transform.lua:28-36)
  - PIXELLAB_ROLE_TO_MASK constant (16 ints, locked role-to-mask bijection from spike 003)
  - draw_pixel_lab_cell per-cell helper (reuses draw_corner_mask)
  - gen_pixel_lab_top_down + gen_pixel_lab_side_scroller (8x8 atlas generators)
  - 2 bundled greybox PNGs (256x256 RGBA) at addons/penta_tile/layouts/penta_tile_layout_pixel_lab_*.png
  - 2 .import sidecars (auto-generated by Godot --headless --import)
affects: [03.5-02-PLAN (top-down layout subclass), 03.5-03-PLAN (side-scroller layout subclass), 03.5-04-PLAN (matrix-test extension), 03.5-05-PLAN (visual regression test)]

# Tech tracking
tech-stack:
  added: []  # No new libraries; reuses existing Pillow + Godot 4.6 toolchain
  patterns:
    - "Per-cell role-coded silhouette (D-101 option A) — cells sharing a role get visually identical silhouettes via draw_corner_mask"
    - "Cell-to-role table verbatim duplication between Python generator and GDScript layout consts (D-95 source-of-truth alignment)"

key-files:
  created:
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png.import
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png
    - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png.import
  modified:
    - addons/penta_tile/_generate_bitmasks.py (+72 / -1 — 3 const tables + 1 helper + 2 gen functions + 2 save calls + print count 15->17)

key-decisions:
  - "D-99 honored: 8x8 atlas grid; output PNG dimensions 256x256"
  - "D-100 honored: 32-px tiles per generator convention (Phase 2 baseline)"
  - "D-101 honored: greybox-generated, mask-coded silhouettes via reused draw_corner_mask"
  - "D-103 honored: flat-sibling co-location under addons/penta_tile/layouts/ (no subfolder; PixelLab is single-variant per axis)"
  - "Cell-to-role tables locked verbatim from spike 003 / tileset_transform.lua per D-95 — no paraphrasing"
  - "Print count update 15 -> 17 reflects two new layouts in main()"

patterns-established:
  - "PixelLab generator pattern — separates const cell-to-role tables from a per-role silhouette helper that reuses draw_corner_mask. Plans 02/03 will mirror these consts in their layout subclasses (D-98 per-subclass duplication)."
  - "2-step asset bring-up — (1) Python generator produces PNG; (2) Godot --headless --path . --import generates .import sidecar with uid:// + ctex hash. Both must commit together (Pitfall #7)."

requirements-completed: []  # Plan 01 frontmatter declares no requirements; this plan unblocks PIXLAB-01..04 in Plans 02/03/05.

# Metrics
duration: 3min
completed: 2026-04-29
---

# Phase 03.5 Plan 01: PixelLab Generator + 2 Bundled Greybox PNGs Summary

**Extended `_generate_bitmasks.py` with PixelLab cell-to-role tables, a per-role silhouette helper, and two 8x8 atlas generators producing 256x256 RGBA greybox PNGs that unblock the PixelLab layout subclasses landing in Plans 02/03.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-29T09:34:40Z
- **Completed:** 2026-04-29T09:37:27Z
- **Tasks:** 2 / 2
- **Files modified:** 5 (1 generator + 2 PNGs + 2 .import sidecars)

## Accomplishments

- Extended `_generate_bitmasks.py` with `PIXELLAB_TOP_DOWN_CELL_TO_ROLE`, `PIXELLAB_SIDE_SCROLLER_CELL_TO_ROLE`, `PIXELLAB_ROLE_TO_MASK` constants verbatim from spike 003 / `tileset_transform.lua:17-36`.
- Added `draw_pixel_lab_cell(draw, col, row, role)` helper that reuses the existing `draw_corner_mask` (TL=1, TR=2, BL=4, BR=8) with the role->mask bijection — cells sharing a role render identical silhouettes (D-101 option A).
- Added `gen_pixel_lab_top_down()` and `gen_pixel_lab_side_scroller()` 8x8 atlas generators; each iterates 64 cells in row-major order, fills its silhouette, and emits a 256x256 PNG.
- Updated `main()` print count from "Generated 15 bitmask PNGs" to "Generated 17 bitmask PNGs."
- Generated `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png` and `..._side_scroller.png` (both 256x256 RGBA).
- Ran `Godot --headless --path . --import` to produce `.import` sidecars (`uid://crhotgtg4lij6` for top-down; `uid://dwtrd5kuxrhpl` for side-scroller).
- Verified both PNGs load as 256x256 `Texture2D` from headless Godot via a smoke-load script (deleted after verification per plan).
- Confirmed generator idempotency — re-running produced byte-identical output (zero `git status` deltas after rerun).

## Task Commits

Each task was committed atomically:

1. **Task 1.1: PixelLab const tables + helper + 2 gen functions** — `04df07b` (feat)
2. **Task 1.2: Godot --import to produce .import sidecars** — `4a7fbb3` (chore)

**Plan metadata:** to be added with the final plan-closeout commit (this SUMMARY + STATE.md + ROADMAP.md update).

## Files Created/Modified

- `addons/penta_tile/_generate_bitmasks.py` — Extended with 3 const tables (192 ints total) + 1 per-cell helper + 2 atlas-grid generators; `main()` registration + print count update.
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png` — 256x256 RGBA greybox PNG produced by `gen_pixel_lab_top_down()`.
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png.import` — Godot 4.6 import sidecar (texture importer; CompressedTexture2D; `uid://crhotgtg4lij6`).
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png` — 256x256 RGBA greybox PNG produced by `gen_pixel_lab_side_scroller()`.
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png.import` — Godot 4.6 import sidecar (`uid://dwtrd5kuxrhpl`).

## Decisions Made

- **Plan-content fidelity over LOC budget interpretation.** D-105 reads "≤ 60 LOC for the new generator code" but the verbatim plan-action content (3 const tables with formatted Python-list spacing + 2 docstrings + helper + 2 gen functions) lands at 72 git-counted insertions / 62 non-blank-line additions. The plan author explicitly estimated ~50 LOC and labeled this "well under budget." The actual count is slightly higher because the verbatim const tables include extra alignment whitespace and the docstring comments are richer than the estimate assumed. No content was added beyond what the plan asked for verbatim. Treated as informational only — not a Rule 4 deviation. Documented here so Phase 5 final LOC audit has the precise delta.
- **Smoke-load script lifecycle.** Per the plan's Windows fallback note, wrote a temporary smoke-load script to `addons/penta_tile/tests/_pixellab_load_smoke.gd`, ran it via `Godot --headless --script`, then deleted it. The plan's heredoc form was an alternative that requires Git Bash and a more permissive shell environment than the executor sandbox provides; the tempfile path is the documented fallback.

## Deviations from Plan

None — plan executed exactly as written. The verbatim const tables, helper, gen functions, save calls, and print-count update were inserted into `_generate_bitmasks.py` byte-for-byte from the plan's `<action>` block. No bug fixes (Rule 1), no auto-added missing functionality (Rule 2), no blocking-issue patches (Rule 3) needed. The plan's specified Godot exec path was used (CLAUDE.md confirms the same path). The temp smoke-load script was created and deleted per the plan's "write to tempfile, then delete it after verification" fallback instruction.

## Authentication Gates

None encountered. All work was local — no network, no credentials, no MCP servers.

## Verification Results

| Check | Command | Result |
|------|---------|--------|
| Generator stdout count | `python addons/penta_tile/_generate_bitmasks.py 2>&1 \| grep -q "Generated 17 bitmask PNGs"` | PASS |
| Top-down PNG 256x256 RGBA | `python -c "from PIL import Image; im = Image.open(...); assert im.size == (256, 256); assert im.mode == 'RGBA'"` | PASS |
| Side-scroller PNG 256x256 RGBA | (same shape) | PASS |
| Top-down .import exists | `test -f .../penta_tile_layout_pixel_lab_top_down.png.import` | PASS |
| Side-scroller .import exists | `test -f .../penta_tile_layout_pixel_lab_side_scroller.png.import` | PASS |
| Top-down .import has `importer="texture"` | grep | PASS |
| Side-scroller .import has `importer="texture"` | grep | PASS |
| Both .import contain `uid="uid://..."` | grep | PASS |
| Both PNGs load as 256x256 Texture2D | Godot --headless --script smoke load | `ALL PASS` |
| Generator idempotent | re-run + `git status --short` | empty (byte-identical) |

## Threat Flags

None — pure-local generator script + bundled PNG assets. No new network surface, no auth paths, no untrusted input. Reaffirmed against the plan's `<threat_model>` block: trust boundary still N/A.

## LOC Tracking

- Generator code delta: +72 / -1 (raw `git diff --stat`); 62 non-blank added lines
- D-105 budget for generator code: <= 60 LOC
- Verbatim plan content (planner-estimated ~50 LOC) lands slightly higher because of formatted const-table whitespace and docstring richness
- Cumulative addons/penta_tile/ runtime LOC unchanged (this plan adds 0 GDScript runtime LOC; the generator script and the bundled PNGs are tooling/assets, not runtime code)
- Identity guardrail status: AT RISK carry-forward to Phase 5 final audit (Phase 3 close baseline ~2455 runtime LOC; Phase 3.5 Plan 01 contributes 0 runtime LOC delta)

## Self-Check: PASSED

Verified before commit:

- `addons/penta_tile/_generate_bitmasks.py` — modified, contains all 6 acceptance-criteria literal strings (PIXELLAB_TOP_DOWN_CELL_TO_ROLE, PIXELLAB_SIDE_SCROLLER_CELL_TO_ROLE, PIXELLAB_ROLE_TO_MASK exact-array form, def gen_pixel_lab_top_down, def gen_pixel_lab_side_scroller, "Generated 17 bitmask PNGs at:")
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png` — exists, 256x256 RGBA
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png` — exists, 256x256 RGBA
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.png.import` — exists, valid Godot import sidecar (uid://crhotgtg4lij6)
- `addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.png.import` — exists, valid Godot import sidecar (uid://dwtrd5kuxrhpl)
- Commit `04df07b` (Task 1.1) — present in git log
- Commit `4a7fbb3` (Task 1.2) — present in git log
- Smoke load test (Godot --headless) — `ALL PASS`
- Idempotency re-run — zero byte changes
