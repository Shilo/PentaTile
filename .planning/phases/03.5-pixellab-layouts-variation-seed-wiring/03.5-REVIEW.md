---
phase: 3.5
status: clean
depth: standard
files_reviewed: 8
files_reviewed_list:
  - addons/penta_tile/_generate_bitmasks.py
  - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_top_down.gd
  - addons/penta_tile/layouts/penta_tile_layout_pixel_lab_side_scroller.gd
  - tests/pixellab_first_cell_test.gd
  - tests/comprehensive_bitmask_test.gd
  - tests/bitmask_bounds_test.gd
  - tests/pixellab_visual_regression_test.gd
  - tests/run_tests.ps1
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
---

# Phase 3.5: Code Review Report

**Reviewed:** 2026-04-29T00:00:00Z
**Depth:** standard
**Files Reviewed:** 8
**Status:** clean

## Summary

Reviewed all eight Phase 3.5 files (PIXLAB-01..04 deliverables) at standard depth against the locked decisions in 03.5-CONTEXT.md and the project guardrails in CLAUDE.md. Every locked-decision check passes; no bugs, security issues, or quality regressions were found.

**Highlights of what was verified:**

1. **Locked constants match the spike-003 source verbatim.**
   - `_ROLE_TO_MASK = [4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` is identical across `penta_tile_layout_pixel_lab_top_down.gd:52`, `penta_tile_layout_pixel_lab_side_scroller.gd:50`, and `_generate_bitmasks.py:321` (PIXELLAB_ROLE_TO_MASK). D-94 honored.
   - `_CELL_TO_ROLE` (top-down) at `penta_tile_layout_pixel_lab_top_down.gd:38-47` is byte-equivalent to PIXELLAB_TOP_DOWN_CELL_TO_ROLE in `_generate_bitmasks.py:300-309`. D-93/D-95 honored.
   - `_CELL_TO_ROLE` (side-scroller) at `penta_tile_layout_pixel_lab_side_scroller.gd:37-46` is byte-equivalent to PIXELLAB_SIDE_SCROLLER_CELL_TO_ROLE in `_generate_bitmasks.py:310-319`. D-95 honored.
   - D-98 (per-subclass const duplication for GDScript 2 cross-class const limitation) is correctly applied; both layouts duplicate `_ROLE_TO_MASK` with the same value.

2. **No prohibited fields or short-circuits.**
   - Grepped both layouts for `variation_seed`, `pixellab_version`, and `if mask == 0` — none present. D-90 (no variation_seed property), D-92 (no version field per CLAUDE.md no-forward-compat HARD RULE), and D-104 (mask=0 dispatches via cache, not short-circuits) all honored.
   - `mask_to_atlas` in both layouts (`penta_tile_layout_pixel_lab_top_down.gd:89-97`, `penta_tile_layout_pixel_lab_side_scroller.gd:87-94`) returns `_first_cell_by_mask[mask]` unconditionally — Pitfall #9 respected (single-grid mask=0 is NOT erase).

3. **D-89 first-cell row-major pick is correctly implemented and contract-tested.**
   - `_init_cache()` uses `for row in 8: for col in 8` row-major scan with first-write-wins semantics (`if _first_cell_by_mask[mask] == Vector2i(-1, -1)`). Both layouts (`penta_tile_layout_pixel_lab_top_down.gd:63-73`, `penta_tile_layout_pixel_lab_side_scroller.gd:61-71`).
   - `pixellab_first_cell_test.gd` hand-derives expected first-cell coords for all 16 masks per layout and asserts `mask_to_atlas(m).atlas_coords` matches. Coverage explicitly includes mask=0 (D-104) and asserts `transform_flags == 0` (D-90 no rotation reuse) and `alternative_tile == 0` for every mask. Test methodology #5 (verify-the-regression) is documented in the test header (lines 18-20).

4. **Test methodology compliance (CLAUDE.md Test Methodology rules).**
   - `pixellab_visual_regression_test.gd` composes the rendered canvas via `_compose_canvas` (lines 161-193) and asserts structural invariants (canvas size = expected painted-region pixel bounds; bbox finite; bit-stable across two rebuilds). Test Methodology #1 (compose canvas, don't trust dispatch alone) and #5 (verify-the-regression — documented at lines 14-15) honored.
   - `comprehensive_bitmask_test.gd` already integrates both PIXLAB layouts (lines 89-90 in the layouts array) into the existing 19-pattern × per-layout matrix. Test Methodology #2 (test pattern × layout matrix) honored.
   - `bitmask_bounds_test.gd` validates the bundled bitmask PNGs for both PIXLAB atlases against `_solid_silhouette` (lines 94-102) — the option-B fallback after Plan 04's UAT correction (the comment at lines 87-93 documents the design pivot). Solidity invariant defended.
   - `pixellab_visual_regression_test.gd` saves canvas PNGs to `user://pixellab_visual_regression_*.png` regardless of pass/fail (line 264) — Test Methodology #4 (save PNG for eyeball inspection) honored.

5. **Generator extension is safe.**
   - `_generate_bitmasks.py` is invoked manually by maintainers (not automated CI); the only filesystem writes are `OUT_LAYOUTS / "...png"` paths derived from `__file__`'s parent. No user-input paths, no shell escapes, no eval. The PIL/Pillow API surface used (`Image.new`, `ImageDraw.Draw.rectangle`, `Image.save`) has been stable since Pillow 8.x and the script does not declare a hard version constraint — acceptable because this is an internal authoring tool, not shipped runtime code.
   - `gen_pixel_lab_top_down` / `gen_pixel_lab_side_scroller` (lines 347-371) build 8×8 atlases via row-major scan over `PIXELLAB_*_CELL_TO_ROLE`; the row/col index math (`row * 8 + col`) is bounds-safe (8×8 = 64 entries, exactly matches the array length).
   - The generator's `draw_pixel_lab_cell` (lines 324-344) draws solid 32×32 silhouettes — design rationale documented inline (lines 325-342) explaining the option-A → option-B pivot from the UAT cycle.

6. **GDScript 2 idioms honored.**
   - Both layouts open with `@tool`, declare `class_name PentaTileLayout...`, `extends PentaTileLayout`, and use snake_case file names matching their PascalCase class names.
   - Constants follow `_UPPER_SNAKE_CASE` private convention (`_TL`, `_TR`, `_BL`, `_BR`, `_CELL_TO_ROLE`, `_ROLE_TO_MASK`).
   - `var _first_cell_by_mask: Array[Vector2i]` is typed per CLAUDE.md "typed `Array[Resource]`" guidance (here parameterized to Vector2i).
   - `func _init() -> void: super(); _init_cache()` correctly invokes the base PentaTileLayout `_init` to honor the bundled-PNG auto-fill seam (`_default_bitmask_template_path()`).

7. **Test runner registration.**
   - `run_tests.ps1:69-70` correctly appends both new tests to `$allTests` (16th and 17th entries). Order is reasonable (after `single_grid_8_moore_propagation_test`); diffs against earlier commits show this is the only Phase 3.5 change to the runner.

**Out-of-scope items intentionally not flagged:**

- Variation-bank wiring is deferred to v2 (VAR-PIXEL-01 per D-89/D-90/D-91); the test header at `pixellab_first_cell_test.gd:31-43` and the layout doc-comments at `penta_tile_layout_pixel_lab_top_down.gd:13-16` correctly note this. Per task context, "missing variation_seed wiring" is explicitly out of review scope.
- The PixelLab format design itself is locked upstream and not to be critiqued.
- The existing `run_tests.ps1` `-GodotExe` default at line 20 has a doubled path segment (`...win64.exe\Godot_v4.6.2-stable_win64.exe`) — this is a PRE-EXISTING line untouched by Phase 3.5 (Phase 3.5 only added two test entries at lines 69-70). Out of scope for this review; flagging here as informational context only since `run_tests.ps1` is in the file list.

All reviewed files meet quality standards. No critical, warning, or info-level issues found.

---

_Reviewed: 2026-04-29T00:00:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
