---
phase: 3.5
slug: pixellab-layouts-variation-seed-wiring
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 3.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Godot 4.6 headless `--script` runner (no GUT, no third-party) |
| **Config file** | `addons/penta_tile/tests/run_tests.ps1` (PowerShell launcher; loops over each `*_test.gd`) |
| **Quick run command** | `pwsh addons/penta_tile/tests/run_tests.ps1 -Test pixellab_first_cell_test` |
| **Full suite command** | `pwsh addons/penta_tile/tests/run_tests.ps1` |
| **Estimated runtime** | ~30s (full suite, currently 15 tests; ~35s after Phase 3.5 adds 2 new tests + matrix extension) |

---

## Sampling Rate

- **After every task commit:** Run the affected test (`pixellab_first_cell_test` for the cache contract; `pixellab_visual_regression_test` for fixture-driven render; `comprehensive_bitmask_test` after layouts join the matrix)
- **After every plan wave:** Run full suite (`run_tests.ps1`)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~5s per single test, ~35s for full suite

---

## Per-Task Verification Map

> Filled in by gsd-planner during planning. Each task in the PLAN.md gets a row mapping it to a Requirement (PIXLAB-01..04) and a verification command. VAR-PIXEL-01 is deferred and does NOT appear in this table.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.5-01-XX | 01 | 1 | PIXLAB-01..04 | — | N/A (no network/auth/untrusted input — see PLAN.md `<threat_model>`) | unit | `pwsh addons/penta_tile/tests/run_tests.ps1 -Test {test_name}` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `addons/penta_tile/tests/pixellab_first_cell_test.gd` — locks D-89 first-cell row-major pick contract for both PIXLAB layouts (16 mask states × 2 layouts = 32 assertions)
- [ ] `addons/penta_tile/tests/pixellab_visual_regression_test.gd` — composed-canvas test against checked-in PixelLab sample fixtures (PIXLAB-04)
- [ ] `addons/penta_tile/tests/comprehensive_bitmask_test.gd` — extend matrix from 6 layouts to 8 layouts (108 → 144 combos)
- [ ] `addons/penta_tile/tests/bitmask_bounds_test.gd` — extend per-slot expected-silhouette dictionary with PIXLAB top-down + side-scroller entries
- [ ] PixelLab sample fixtures copied from `.planning/spikes/003-pixellab-bit-mapping/templates/` to `addons/penta_tile/tests/` (flat — no `fixtures/` subdir per existing convention)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Inspector preview shows the bundled greybox PNG when a `PentaTileLayoutPixelLabTopDown.tres` / `…SideScroller.tres` is opened | PREVIEW-01 (carry-forward from Phase 2) | Godot inspector rendering can't be asserted from headless `--script` mode | Open the .tres in Godot 4.6.x editor; confirm the `bitmask_template` slot renders the 256×256 PNG inline (8×8 grid visible). Repeat for both layouts. |
| Greybox silhouettes look "clean" (no broken seams, consistent silhouettes for cells of the same role) | TEMPLATE-04 carry-forward | Visual aesthetic check | Open each PNG in any image viewer; spot-check that cells with the same role-ID share visual silhouettes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (planner fills this in)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (5 test/fixture deliverables listed above)
- [ ] No watch-mode flags (Godot `--script` mode is one-shot; suite is fully scripted)
- [ ] Feedback latency < 35s (full suite)
- [ ] `nyquist_compliant: true` set in frontmatter (set by planner once per-task verify map is complete)

**Approval:** pending — set to `approved 2026-04-29` once gsd-plan-checker validates the per-task map against PIXLAB-01..04 + PLAN.md task list.
