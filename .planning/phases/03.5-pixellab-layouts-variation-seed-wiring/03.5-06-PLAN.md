---
phase: 03.5
plan: 06
type: execute
wave: 5
depends_on: ["03.5-01", "03.5-02", "03.5-03", "03.5-04", "03.5-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [closeout, planning-docs, traceability, roadmap, state-loc]

must_haves:
  truths:
    - "REQUIREMENTS.md Traceability marks PIXLAB-01..04 as Complete with Phase 3.5 commit anchors"
    - "ROADMAP.md Phase 3.5 row flipped from [ ] to [x] with completion date"
    - "ROADMAP.md Phase 3.5 stale references to variation_seed wiring updated to reflect v2 deferral (mirrors Phase 3 D-86 precedent)"
    - "VAR-PIXEL-01 NOT claimed as Phase 3.5 coverage; remains in v2 backlog explicitly"
    - "STATE.md cumulative LOC delta recorded; Roadmap Evolution entry summarizes Phase 3.5 closure"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PIXLAB-01..04 Traceability rows updated to Complete with Phase 3.5 commit references"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3.5 entry: [x] with completion date, Plans list with [x] markers, Goal text de-staled"
    - path: ".planning/STATE.md"
      provides: "Roadmap Evolution entry for Phase 3.5 closeout; cumulative runtime LOC measurement; phase status updated"
  key_links:
    - from: "REQUIREMENTS.md PIXLAB-01..04 rows"
      to: "Phase 3.5 commits via git log"
      via: "Status: Complete (Plan 02/03/04/05 / commit hashes)"
      pattern: "PIXLAB-0[1-4].*Complete"
---

<objective>
Phase 3.5 closeout: update planning docs (REQUIREMENTS Traceability + ROADMAP completion markers + STATE.md cumulative LOC + Roadmap Evolution entry) to reflect that all four in-scope PIXLAB requirements shipped, while explicitly preserving VAR-PIXEL-01's v2 deferral. Mirrors the Phase 3 D-86 closeout precedent (where TBT-01/TBT-02 stayed deferred while TBT-03/TBT-04 closed).

Purpose: Close the planning record. Future phases (Phase 4 fallback routing) can reference Phase 3.5's PIXLAB layouts as known-shipped without re-checking commit history. The ROADMAP "+ Variation-Seed Wiring" stale text is the single largest provenance hazard — fix it now so Phase 5 release notes don't repeat the misnomer.

Output: 3 modified planning files; no source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-pixellab-layouts-variation-seed-wiring/03.5-CONTEXT.md
@.planning/phases/03-tilebittools-sourced-layouts/03-06-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 6.1: Update REQUIREMENTS.md Traceability table for PIXLAB-01..04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — Traceability table around lines 311-371; PIXLAB rows currently mark all four "Pending" at lines 348-351)
    - .planning/REQUIREMENTS.md (v2 Requirements section — verify VAR-PIXEL-01 entry at line 222 stays unchanged)
    - .planning/phases/03-tilebittools-sourced-layouts/03-06-PLAN.md (precedent for closeout traceability format — TBT-01..04 entries around the table tail)
    - Phase 3.5 commit hashes (run `git log --oneline --all -- 'addons/penta_tile/layouts/penta_tile_layout_pixel_lab*' 'tests/pixellab_*'` to gather actual commit refs once Plans 02/03/04/05 land)
  </read_first>
  <behavior>
    - PIXLAB-01..04 status flips from `Pending` to `Complete` with commit/plan references
    - VAR-PIXEL-01 row in v2 Requirements stays untouched (deferred per D-89/D-90)
    - Coverage counter updated if needed (58 v1 reqs unchanged — PIXLAB-01..04 were already counted; the change is status not membership)
  </behavior>
  <action>
1. Open `.planning/REQUIREMENTS.md`. Locate the Traceability table (around lines 311-371).

2. Update the four PIXLAB rows. Replace:

```
| PIXLAB-01 | 3.5 | Pending |
| PIXLAB-02 | 3.5 | Pending |
| PIXLAB-03 | 3.5 | Pending |
| PIXLAB-04 | 3.5 | Pending |
```

With (use actual commit hashes from `git log` after Plans 02/03/04/05 land — placeholders shown):

```
| PIXLAB-01 | 3.5 | Complete (Plan 02 / `<hash>`: PentaTileLayoutPixelLabTopDown ships with verbatim 64-int _CELL_TO_ROLE table from tileset_transform.lua:17-26 + locked role-to-mask bijection + cached _first_cell_by_mask first-cell row-major pick per D-89; D-104 mask=0 -> role 12 -> cell (2, 2); is_dual_grid=false; 8x8 fallback grid) |
| PIXLAB-02 | 3.5 | Complete (Plan 03 / `<hash>`: PentaTileLayoutPixelLabSideScroller ships with verbatim 64-int _CELL_TO_ROLE table from tileset_transform.lua:28-36 tileset_output_side + duplicated role-to-mask bijection per D-98; D-104 mask=0 -> role 12 -> cell (0, 0); same cache + dispatch shape as top-down) |
| PIXLAB-03 | 3.5 | Complete (Plan 04 / `<hash>`: pixellab_first_cell_test enumerates all 16 masks for both layouts and asserts hand-derived first-cell coords + transform_flags == 0 invariant per D-90; verify-the-regression cycle confirmed; matrix in comprehensive_bitmask_test extended to 8 layouts x 18 patterns = 144 combos; bitmask_bounds_test extended with 8x8 PIXLAB silhouette helpers) |
| PIXLAB-04 | 3.5 | Complete (Plan 05 / `<hash>`: pixellab_visual_regression_test composes rendered canvas via blit + transform helper against checked-in spike-003 PixelLab sample fixtures; asserts non-empty cells + canvas-size match + bit-stable rebuild; D-102 case 3 closed) |
```

3. Find the Coverage notes block (around lines 372-386). After the existing line "Mapped to phases: 58 (after this update)", append a new bullet:

```
- 2026-04-29 Phase 3.5 closeout (Plan 06): PIXLAB-01..04 flipped to Complete. VAR-PIXEL-01 explicitly stays in v2 Requirements per D-89/D-90/D-91 — bank-pick wiring deferred until variation work reopens (design-coupled with VAR-01 + MULTITERR-01). The phase title "+ Variation-Seed Wiring" was a misnomer in v0.2 — no `variation_seed` property is exposed; layouts ship with first-cell pick only. Coverage counter unchanged: 58 v1 reqs (PIXLAB-01..04 were already counted as v1; the change is Status not membership).
```

4. Verify VAR-PIXEL-01 in the v2 Requirements section (line ~222) reads exactly the existing entry — DO NOT modify it.

5. No other Traceability rows change.

LOC delta: ~10 lines added.
  </action>
  <verify>
    <automated>[ "$(grep -c '^| PIXLAB-0[1-4] | 3.5 | Complete' .planning/REQUIREMENTS.md)" = "4" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains exactly 4 lines matching the regex `^\| PIXLAB-0[1-4] \| 3.5 \| Complete`
    - `.planning/REQUIREMENTS.md` does NOT contain the literal `| PIXLAB-01 | 3.5 | Pending |`
    - `.planning/REQUIREMENTS.md` does NOT contain the literal `| PIXLAB-02 | 3.5 | Pending |`
    - `.planning/REQUIREMENTS.md` does NOT contain the literal `| PIXLAB-03 | 3.5 | Pending |`
    - `.planning/REQUIREMENTS.md` does NOT contain the literal `| PIXLAB-04 | 3.5 | Pending |`
    - VAR-PIXEL-01 entry in v2 Requirements section is unchanged (`**VAR-PIXEL-01**:` line still present with the existing description text "Variation-bank deterministic pick for PixelLab layouts")
    - The commit-hash placeholders are replaced with actual git short hashes from the Phase 3.5 layout/test commits
    - The 2026-04-29 Phase 3.5 closeout bullet is appended in the Coverage notes block
  </acceptance_criteria>
  <done>Traceability rows reflect Complete status; VAR-PIXEL-01 deferral preserved; coverage notes updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6.2: Flip ROADMAP.md Phase 3.5 entry to [x] and de-stale the variation_seed text</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 3.5 list entry around line 27, detail block around lines 145-158, progress table around line 207)
    - .planning/phases/03-tilebittools-sourced-layouts/03-06-PLAN.md (Phase 3 closeout precedent for ROADMAP retitle + [x] + completion-date format)
    - .planning/phases/03.5-pixellab-layouts-variation-seed-wiring/03.5-CONTEXT.md (D-91 — phase title interpretation; closeout is allowed to retitle)
  </read_first>
  <behavior>
    - Phase 3.5 ROADMAP list entry flips `[ ]` -> `[x]` with completion date
    - Phase 3.5 retitled to mirror Phase 3's "(... deferred ...)" annotation pattern
    - Goal/Success Criteria text de-staled: variation-bank pick recast as v2-deferred (D-91)
    - Plans list under detail block gets `[x]` markers for all 6 plans
    - Progress table row updates to Complete with completion date
  </behavior>
  <action>
1. Open `.planning/ROADMAP.md`.

2. **List entry** (around line 27). Replace the existing `- [ ] **Phase 3.5: PixelLab Layouts + Variation-Seed Wiring** — ...` line with:

```markdown
- [x] **Phase 3.5: PixelLab Layouts (variation-bank pick deferred to v2 per D-91)** — Shipped `PentaTileLayoutPixelLabTopDown` and `PentaTileLayoutPixelLabSideScroller` (8×8 atlas, single-grid, 4-bit corner mask). Both share the locked role-to-mask bijection from spike 003 and dispatch via cached `_first_cell_by_mask` (first-cell row-major pick per D-89). The "+ Variation-Seed Wiring" suffix from the original phase title was a v0.2 misnomer — `variation_seed` property NOT exposed; bank-pick wiring deferred to v2 backlog as VAR-PIXEL-01 (design-coupled with VAR-01 + MULTITERR-01). Min3x3 already shipped in Phase 2 — no Phase 3.5 work needed. (Closed 2026-04-29.)
```

3. **Detail block Goal line** (around line 147). Replace the existing Goal sentence with:

```markdown
**Goal**: Ship `PentaTileLayoutPixelLabTopDown` and `PentaTileLayoutPixelLabSideScroller` subclasses. Both consume PixelLab Aseprite plugin native 8×8 atlas output. Both share the locked role-to-mask bijection `[4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1]` (corner mask). **Variation handling for v0.2 is first-cell row-major pick only** — when multiple cells map to the same mask, `mask_to_atlas` returns the row-major-first cell (D-89). The `variation_seed` deterministic-hash bucket-pick is **deferred to v2 backlog as VAR-PIXEL-01** per D-91 — design-coupled with VAR-01 (Y-axis variation) and MULTITERR-01 (multi-terrain).
```

4. **Requirements line** in the same block. Replace `**Requirements**: PIXLAB-01, PIXLAB-02, PIXLAB-03, PIXLAB-04, VAR-PIXEL-01.` with:

```markdown
**Requirements**: PIXLAB-01, PIXLAB-02, PIXLAB-03, PIXLAB-04. (VAR-PIXEL-01 deferred to v2 backlog per D-91 — see REQUIREMENTS.md "v2 Requirements" section.)
```

5. **Success Criteria #3 + #4** in the same block. Replace #3 with:

```markdown
3. Variation handling: when a mask has multiple cells (PixelLab variations), `mask_to_atlas` returns the row-major-FIRST cell deterministically (D-89). Bank pick (`hash(coord, variation_seed) % cells.size()`) is deferred to v2 — `VAR-PIXEL-01` in REQUIREMENTS.md.
```

   Replace #4 with:

```markdown
4. ~~`variation_seed` produces different picks for different seed values~~ — **DEFERRED to v2 (VAR-PIXEL-01)** per D-91. v0.2 Phase 3.5 ships first-cell pick only.
```

6. **Plans line** below the success criteria. Replace `**Plans**: TBD` with:

```markdown
**Plans**: 6 plans complete (6/6) — closed 2026-04-29

Plans:
- [x] 03.5-01-PLAN.md — Generator extension: gen_pixel_lab_top_down + gen_pixel_lab_side_scroller in _generate_bitmasks.py + 2 bundled PNGs at 256x256 + .import sidecars (TEMPLATE-01 partial — PixelLab half)
- [x] 03.5-02-PLAN.md — PentaTileLayoutPixelLabTopDown shipping (PIXLAB-01)
- [x] 03.5-03-PLAN.md — PentaTileLayoutPixelLabSideScroller shipping (PIXLAB-02)
- [x] 03.5-04-PLAN.md — pixellab_first_cell_test (D-89 cache contract) + comprehensive_bitmask_test matrix extension to 8x18=144 + bitmask_bounds_test 8x8 PIXLAB extension + run_tests.ps1 registration (PIXLAB-03)
- [x] 03.5-05-PLAN.md — pixellab_visual_regression_test composed-canvas test + checked-in spike-003 PixelLab samples + run_tests.ps1 registration (PIXLAB-04)
- [x] 03.5-06-PLAN.md — Closeout: REQUIREMENTS Traceability + ROADMAP retitle + ROADMAP [x] + STATE.md cumulative LOC + Roadmap Evolution entry + VAR-PIXEL-01 deferral preservation
```

7. **Progress table row** (around line 207). Replace `| 3.5. PixelLab Layouts (variation-bank pick deferred to v2) | 0/TBD | Not started | - |` with:

```markdown
| 3.5. PixelLab Layouts (variation-bank pick deferred to v2) | 6/6 | **Complete.** Both PixelLab layouts (top-down + side-scroller) shipped with cached first-cell row-major dispatch. 2 new tests (pixellab_first_cell_test + pixellab_visual_regression_test) green; matrix grew to 8 layouts x 18 patterns = 144 combos; bounds extended with PIXLAB silhouettes. VAR-PIXEL-01 (variation-bank pick) preserved in v2 backlog per D-91. | 2026-04-29 |
```

LOC delta: ~30 lines.
  </action>
  <verify>
    <automated>grep -q '^- \[x\] \*\*Phase 3.5: PixelLab Layouts (variation-bank pick deferred to v2 per D-91)\*\*' .planning/ROADMAP.md && grep -q '\*\*Plans\*\*: 6 plans complete' .planning/ROADMAP.md && grep -q '\*\*DEFERRED to v2 (VAR-PIXEL-01)\*\*' .planning/ROADMAP.md && grep -q '| 3.5\. PixelLab Layouts.*| 6/6 |' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains the literal `- [x] **Phase 3.5: PixelLab Layouts (variation-bank pick deferred to v2 per D-91)**`
    - `.planning/ROADMAP.md` does NOT contain the literal `- [ ] **Phase 3.5: PixelLab Layouts + Variation-Seed Wiring**` (old stale heading)
    - `.planning/ROADMAP.md` contains the literal `**Plans**: 6 plans complete (6/6) — closed 2026-04-29`
    - `.planning/ROADMAP.md` contains six lines beginning with `- [x] 03.5-0` (one per plan)
    - `.planning/ROADMAP.md` contains the literal `**DEFERRED to v2 (VAR-PIXEL-01)**` (Success Criterion #4 retitled)
    - `.planning/ROADMAP.md` Progress table row for Phase 3.5 contains `6/6` and `2026-04-29`
    - `.planning/ROADMAP.md` does NOT claim VAR-PIXEL-01 as Phase 3.5 in the Requirements line (the line now reads "PIXLAB-01..04. (VAR-PIXEL-01 deferred to v2 backlog ...)")
  </acceptance_criteria>
  <done>ROADMAP Phase 3.5 entry, detail block, plans list, and progress table all reflect Complete; stale variation_seed text de-staled with D-91 anchor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6.3: Update STATE.md with Phase 3.5 closure entry + cumulative LOC measurement + status</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — Roadmap Evolution section around lines 76-95; "Current Position" block lines 26-37; "Session Continuity" footer lines 188-197)
    - .planning/phases/03-tilebittools-sourced-layouts/03-06-PLAN.md (Phase 3 closeout STATE.md update precedent — adds Roadmap Evolution entry + LOC measurement)
    - Phase 3.5 commits (run `git log --oneline --since='2026-04-29' -- addons/penta_tile/` to gather refs)
  </read_first>
  <behavior>
    - STATE.md gains a new Roadmap Evolution entry summarizing Phase 3.5 closure (mirrors the 2026-04-29 Phase 3 entry's structure)
    - Cumulative runtime GDScript LOC measured directly via `find addons/penta_tile -name '*.gd' -not -path '*/tests/*' -not -path '*/demo/*' | xargs wc -l` and recorded
    - Phase delta noted (Phase 3 closeout reported ~2455; Phase 3.5 contribution ~140 LOC for 2 layouts brings cumulative to ~2595, still AT RISK pending Phase 5 final audit)
    - "Current Position" updates from "Phase 06 / Not started" to "Phase 04 / Ready to plan" (Phase 4 is the next sequential phase per ROADMAP)
    - "Session Continuity" Completed Phase list gains Phase 3.5
  </behavior>
  <action>
1. Open `.planning/STATE.md`.

2. **Roadmap Evolution section** (after the existing 2026-04-29 Phase 3 closeout entry around line 93). Append a new bullet:

```markdown
- 2026-04-29 (Phase 3.5 closed): **PixelLab Layouts shipped (PIXLAB-01..04 Complete; VAR-PIXEL-01 stays deferred per D-91).** PentaTileLayoutPixelLabTopDown + PentaTileLayoutPixelLabSideScroller ship as twin single-grid 4-bit corner-mask subclasses (D-93/D-96) with cached first-cell row-major dispatch (D-89/D-97). Cell-to-role tables come verbatim from spike 003's transcription of tileset_transform.lua:17-26 (top-down) and :28-36 (side-scroller); role-to-mask bijection [4, 10, 13, 12, 9, 14, 15, 7, 2, 3, 11, 5, 0, 8, 6, 1] is shared and duplicated per-subclass per D-98 (GDScript 2 cross-class const limitation). D-104 mask=0 dispatch: top-down -> role 12 -> cell (2, 2); side-scroller -> role 12 -> cell (0, 0). 2 new bundled greybox PNGs (256x256, 8x8 atlas) generated by 2 new functions in _generate_bitmasks.py (gen_pixel_lab_top_down + gen_pixel_lab_side_scroller; print count 15 -> 17). 2 new tests added: pixellab_first_cell_test (D-89 cache contract, 32 assertions = 16 masks x 2 layouts) + pixellab_visual_regression_test (composed-canvas + bit-stable rebuild against real spike-003 PixelLab samples checked into tests/). comprehensive_bitmask_test matrix grew 6x18=108 -> 8x18=144 combos; bitmask_bounds_test extended with PIXLAB top-down + side-scroller silhouette helpers; run_tests.ps1 inventory grew 15 -> 17 tests. Cumulative runtime LOC measured: ~<MEASURED> (Phase 3 baseline ~2455 + ~140 for 2 PIXLAB layouts; methodology: `find addons/penta_tile -name '*.gd' -not -path '*/tests/*' -not -path '*/demo/*' | xargs wc -l`). Identity guardrail AT RISK carry-forward — formal gate is Phase 5 final audit. **VAR-PIXEL-01 deferral preserved**: bank-pick wiring stays in v2 backlog per D-91, mirroring Phase 3's TBT-01/TBT-02 deferral precedent. Phase title's "+ Variation-Seed Wiring" misnomer fixed in ROADMAP.md (retitled to "PixelLab Layouts (variation-bank pick deferred to v2 per D-91)"). No source code changes in Plan 06; planning-docs only.
```

   Replace `<MEASURED>` with the actual LOC from the command output (run after Plans 02/03 land).

3. **Current Position** block (lines 26-37). Update:
   - Change `Phase: 06` to `Phase: 04`
   - Change `Plan: Not started` to `Plan: Ready to plan`
   - Change `Status: Ready to plan` to `Status: Ready to plan`
   - Update `Last activity: 2026-04-29` (keep date as today)
   - Replace the blockquote at line 33 with a Phase 3.5 closure summary mirroring the existing Phase 3 close summary:

```markdown
> Phase 3.5 closed 2026-04-29 with full scope: PIXLAB-01..04 Complete. PentaTileLayoutPixelLabTopDown + PentaTileLayoutPixelLabSideScroller ship as single-grid corner-mask twin subclasses with cached first-cell dispatch per D-89; 2 new bundled greybox PNGs (8x8 atlas, 256x256) co-located under addons/penta_tile/layouts/; 2 new tests (pixellab_first_cell_test + pixellab_visual_regression_test) green; matrix grew to 8 layouts x 18 patterns = 144 combos; bitmask_bounds_test extended; run_tests.ps1 lists 17 tests. VAR-PIXEL-01 (variation-bank pick) preserved in v2 backlog per D-91; ROADMAP retitled to drop the "+ Variation-Seed Wiring" misnomer. Phase 4 (Fallback Routing) is the next planning step.
```

4. **Session Continuity** footer (lines 188-200). After the existing `**Planned Phase:**` line, append:

```markdown
**Completed Phase:** 03.5 (PixelLab Layouts) — 6/6 plans, 4/4 in-scope requirements (PIXLAB-01..04), 17 automated tests green, VAR-PIXEL-01 stays in v2 backlog per D-91 — 2026-04-29
```

   Update `**Next Phase:**` line to read `**Next Phase:** 04 (Fallback Routing) — wires PentaTileMapLayer to use layout.get_fallback_tile_set() when tile_set == null; visual regression across all 10 layouts (5 Phase 2 + 1 Phase 3 + 2 Phase 3.5 = 8 actually-shipped; 2 Phase 3 Tilesetter pair deferred per D-86 b)`.

5. The 5-line `progress:` block in the YAML frontmatter (lines 8-14) updates:
   - `total_plans` increments from 21 to 27 (21 + 6 Phase 3.5 plans)
   - `completed_plans` increments from 21 to 27
   - `percent` stays at 100 (since the proportion is the same — completed_plans / total_plans)
   - `stopped_at` updates to "Phase 3.5 closeout — Plan 06 complete; Phase 4 ready to plan"
   - `last_updated` updates to a current ISO-8601 timestamp (use `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` for the value)

Run final verification:
```
pwsh tests/run_tests.ps1 -NoPause
```
Confirm `ALL GREEN (17 tests)` so the closeout records what's actually green.

LOC delta: ~30 lines (one Roadmap Evolution bullet + Current Position block update + Session Continuity additions + frontmatter increment).
  </action>
  <verify>
    <automated>grep -q "Phase 3.5 closed 2026-04-29" .planning/STATE.md &amp;&amp; grep -q "Completed Phase:\*\* 03.5" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains the literal `2026-04-29 (Phase 3.5 closed):` (Roadmap Evolution bullet)
    - `.planning/STATE.md` Roadmap Evolution bullet contains the literal `PIXLAB-01..04 Complete`
    - `.planning/STATE.md` Roadmap Evolution bullet contains the literal `VAR-PIXEL-01 deferral preserved`
    - `.planning/STATE.md` Current Position block contains `Phase: 04` (was `Phase: 06`)
    - `.planning/STATE.md` Current Position block contains the literal `Phase 3.5 closed 2026-04-29`
    - `.planning/STATE.md` Session Continuity contains the literal `**Completed Phase:** 03.5 (PixelLab Layouts)`
    - `.planning/STATE.md` Session Continuity `**Next Phase:**` line names Phase 04 Fallback Routing
    - `.planning/STATE.md` frontmatter `total_plans: 27` (was 21)
    - `.planning/STATE.md` frontmatter `completed_plans: 27` (was 21)
    - The cumulative LOC `<MEASURED>` placeholder is replaced with an actual integer from the wc -l command
    - Final test suite run confirms `ALL GREEN (17 tests)` — recorded in the closeout entry
  </acceptance_criteria>
  <done>STATE.md Roadmap Evolution gains Phase 3.5 closure entry; Current Position advances to Phase 04; cumulative LOC recorded; frontmatter plan counters incremented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure planning-doc edits; no source code changes; no network, auth, or untrusted input |

## STRIDE Threat Register

No threat surface — phase ships GDScript Resource subclasses + tests + bundled PNG assets. No network, auth, or untrusted-input handling. ASVS L1 controls vacuously satisfied. Plan 06 specifically: only modifies markdown files in `.planning/`; no executable code paths affected.
</threat_model>

<verification>
- REQUIREMENTS.md PIXLAB-01..04 Traceability rows mark Complete with commit/plan refs
- VAR-PIXEL-01 v2 backlog entry intact
- ROADMAP.md Phase 3.5 list entry, detail Goal/Requirements/Success Criteria, Plans list, and Progress table all reflect closure
- ROADMAP.md drops the "+ Variation-Seed Wiring" misnomer with D-91 anchor
- STATE.md Roadmap Evolution gains Phase 3.5 closure entry
- STATE.md Current Position advances Phase 06 -> Phase 04 (next sequential phase)
- STATE.md frontmatter plan counters incremented (21 -> 27)
- Cumulative runtime GDScript LOC recorded (informational; formal gate is Phase 5)
- Full test suite green (17 tests) recorded in the closeout entry
</verification>

<success_criteria>
1. PIXLAB-01..04 Traceability marked Complete with Phase 3.5 commit anchors
2. VAR-PIXEL-01 stays deferred in v2 (mirrors Phase 3 D-86 = b precedent)
3. ROADMAP.md Phase 3.5 entry flipped to [x] with completion date
4. ROADMAP.md "+ Variation-Seed Wiring" misnomer fixed; Goal + Success Criteria + Plans line de-staled with D-91 anchor
5. STATE.md Roadmap Evolution gains Phase 3.5 closure bullet
6. STATE.md Current Position advances to Phase 04
7. STATE.md frontmatter plan counters incremented to 27/27
8. Cumulative runtime LOC measured directly and recorded
9. Full 17-test suite green at closeout time
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-pixellab-layouts-variation-seed-wiring/03.5-06-SUMMARY.md`.
</output>
