---
phase: 01.1-pentatile-rename-penta-codename-establishment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/tetra_tile/  (entire folder renamed → addons/penta_tile/)
  - project.godot
autonomous: true
requirements: []   # Phase 1.1 has no formal REQ-IDs; this is a rename phase
must_haves:
  truths:
    - "Godot opens project.godot without missing-resource or parse errors"
    - "Demo scene tetra_tile_demo.tscn (now penta_tile_demo.tscn) loads in the editor"
    - "Runtime drag-paint in the demo still produces dual-grid visuals"
    - "GDScript parses: every renamed .gd file compiles cleanly"
    - "No `TetraTile`, `tetra_tile`, or `class_name TetraTile*` strings remain in addons/penta_tile/"
  artifacts:
    - path: "addons/penta_tile/plugin.cfg"
      provides: "Plugin manifest under new name"
      contains: 'name="PentaTile"'
    - path: "addons/penta_tile/penta_tile_map_layer.gd"
      provides: "Renamed core class file"
      contains: "class_name PentaTileMapLayer"
    - path: "addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd"
      provides: "Renamed Tetra-family layout (doubled-token pattern)"
      contains: "class_name PentaTileLayoutPentaHorizontal"
    - path: "addons/penta_tile/demo/penta_tile_demo.tscn"
      provides: "Renamed demo scene with rebound ext_resource paths"
    - path: "project.godot"
      provides: "Engine config under new name"
      contains: 'config/name="PentaTile"'
  key_links:
    - from: "addons/penta_tile/demo/penta_tile_demo.tscn"
      to: "addons/penta_tile/penta_tile_map_layer.gd"
      via: "ext_resource Script path"
      pattern: 'path="res://addons/penta_tile/penta_tile_map_layer.gd"'
    - from: "addons/penta_tile/demo/demo_runtime_painter.gd"
      to: "PentaTileMapLayer node in demo scene"
      via: "NodePath lookup"
      pattern: 'NodePath\("PentaTileMapLayer"\)'
    - from: "addons/penta_tile/contracts/default_horizontal.tres"
      to: "addons/penta_tile/penta_tile_atlas_contract.gd"
      via: "script_class declaration + ext_resource path"
      pattern: 'script_class="PentaTileAtlasContract"'
    - from: "project.godot"
      to: "addons/penta_tile/demo/penta_tile_demo.tscn"
      via: "run/main_scene"
      pattern: 'run/main_scene="res://addons/penta_tile/demo/penta_tile_demo.tscn"'
---

<objective>
Rename the entire `addons/tetra_tile/` source tree (GDScript classes, file/folder names, plugin.cfg, saved Godot resources, .uid files, ext_resource paths, project.godot references) to `addons/penta_tile/` in a single atomic operation. After this plan lands, the Godot project must open cleanly and the demo must paint correctly under the new name.

Purpose: Source code and saved resources are tightly coupled — renaming one without the other breaks the project. By landing them together in a single atomic commit, the working tree is never half-renamed.

Output: A fully renamed `addons/penta_tile/` folder, an updated `project.godot`, and a Godot project that loads + paints under the new name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md
@.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md
@CLAUDE.md
</context>

<rename_token_map>
<!-- Apply uniformly across all renames in this plan. -->
<!-- DO NOT replace generic English "tetra" (none exist in scope) or git history. -->

| Find | Replace |
|------|---------|
| `TetraTile` | `PentaTile` |
| `tetra_tile` | `penta_tile` |
| `Tetra` (when followed by `Tile`/`Layout`) | `Penta` |
| `tetra` (lowercase, project context) | `penta` |
| `TetraTileLayoutTetraHorizontal` | `PentaTileLayoutPentaHorizontal` |
| `TetraTileLayoutTetraVertical` | `PentaTileLayoutPentaVertical` |
| `_TetraTileVisual` | `_PentaTileVisual` |
| `_TetraTileDiagonalOverlay` | `_PentaTileDiagonalOverlay` |

**What does NOT rename in this plan:**
- `unique_id=...` Godot stable node identifiers in `.tscn` files (untouched)
- `uid="uid://..."` strings inside scene/resource headers (content-addressed, survive rename)
- The 4-tile `_FILL`/`_INNER_CORNER`/`_BORDER`/`_OUTER_CORNER` constants (archetype names, not project name)
- The doc-comment phrase "around four atlas tiles" in plugin.cfg description (Phase 5 polish, not Phase 1.1)
- The `.tres` description prose `"TetraTile 4-tile horizontal layout..."` — wait, this DOES rename: `TetraTile` → `PentaTile`. The "4-tile" stays (Phase 5 polish).
</rename_token_map>

<tasks>

<task type="auto">
  <name>Task 1: Rename addons/tetra_tile/ folder + GDScript source files + plugin.cfg + project.godot</name>
  <files>
    addons/tetra_tile/ (folder rename to addons/penta_tile/),
    addons/tetra_tile/plugin.cfg,
    addons/tetra_tile/tetra_tile_map_layer.gd,
    addons/tetra_tile/tetra_tile_atlas_contract.gd,
    addons/tetra_tile/tetra_tile_atlas_slot.gd,
    addons/tetra_tile/layouts/tetra_tile_layout.gd,
    addons/tetra_tile/layouts/tetra_tile_layout_tetra_horizontal.gd,
    addons/tetra_tile/layouts/tetra_tile_layout_tetra_vertical.gd,
    addons/tetra_tile/demo/demo_runtime_painter.gd,
    addons/tetra_tile/demo/demo_player.gd,
    addons/tetra_tile/templates/README.md,
    addons/tetra_tile/templates/_generate_greybox_templates.py,
    project.godot
  </files>
  <read_first>
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G1 + G3 sections — exact line numbers and excerpts for every file),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md (Scope-in for source code),
    addons/tetra_tile/plugin.cfg (current state, 6 lines),
    addons/tetra_tile/tetra_tile_map_layer.gd (lines 1-18, 58, 193-198 — class header + _DEFAULT_LAYOUT singleton),
    addons/tetra_tile/tetra_tile_atlas_contract.gd (lines 1-18 — class header + doc comment),
    addons/tetra_tile/tetra_tile_atlas_slot.gd (lines 1-12 — class header),
    addons/tetra_tile/layouts/tetra_tile_layout.gd (lines 1-14),
    addons/tetra_tile/layouts/tetra_tile_layout_tetra_horizontal.gd (lines 1-20),
    addons/tetra_tile/layouts/tetra_tile_layout_tetra_vertical.gd (full file),
    addons/tetra_tile/demo/demo_runtime_painter.gd (lines 1-7, 41-54 — node lookup + tetra_map references),
    addons/tetra_tile/demo/demo_player.gd (full file — should have zero tetra references; verify),
    addons/tetra_tile/templates/README.md (16 occurrences — all project-name; rename),
    addons/tetra_tile/templates/_generate_greybox_templates.py (read for any `tetra_tile` path strings),
    project.godot (lines 13-14 — config/name + run/main_scene)
  </read_first>
  <action>
    **Step 1 — Folder rename via `git mv`:**
    Use `git mv addons/tetra_tile addons/penta_tile`. This preserves git history of every file inside.

    **Step 2 — File renames inside the new folder (use `git mv` for each):**
    ```
    git mv addons/penta_tile/tetra_tile_map_layer.gd          addons/penta_tile/penta_tile_map_layer.gd
    git mv addons/penta_tile/tetra_tile_map_layer.gd.uid      addons/penta_tile/penta_tile_map_layer.gd.uid
    git mv addons/penta_tile/tetra_tile_atlas_contract.gd     addons/penta_tile/penta_tile_atlas_contract.gd
    git mv addons/penta_tile/tetra_tile_atlas_contract.gd.uid addons/penta_tile/penta_tile_atlas_contract.gd.uid
    git mv addons/penta_tile/tetra_tile_atlas_slot.gd         addons/penta_tile/penta_tile_atlas_slot.gd
    git mv addons/penta_tile/tetra_tile_atlas_slot.gd.uid     addons/penta_tile/penta_tile_atlas_slot.gd.uid
    git mv addons/penta_tile/layouts/tetra_tile_layout.gd     addons/penta_tile/layouts/penta_tile_layout.gd
    git mv addons/penta_tile/layouts/tetra_tile_layout.gd.uid addons/penta_tile/layouts/penta_tile_layout.gd.uid
    git mv addons/penta_tile/layouts/tetra_tile_layout_tetra_horizontal.gd     addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd
    git mv addons/penta_tile/layouts/tetra_tile_layout_tetra_horizontal.gd.uid addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd.uid
    git mv addons/penta_tile/layouts/tetra_tile_layout_tetra_vertical.gd       addons/penta_tile/layouts/penta_tile_layout_penta_vertical.gd
    git mv addons/penta_tile/layouts/tetra_tile_layout_tetra_vertical.gd.uid   addons/penta_tile/layouts/penta_tile_layout_penta_vertical.gd.uid
    git mv addons/penta_tile/tetra_tile_template.png        addons/penta_tile/penta_tile_template.png
    git mv addons/penta_tile/tetra_tile_template.png.import addons/penta_tile/penta_tile_template.png.import
    ```
    NOTE: `demo_runtime_painter.gd` and `demo_player.gd` filenames do NOT change (they have no `tetra_tile_` prefix). Only their internal references rename.

    **Step 3 — Edit GDScript file contents** (apply token rename map from `<rename_token_map>`):

    `addons/penta_tile/plugin.cfg`:
    Change `name="TetraTile"` → `name="PentaTile"`. Leave `description` and `author` and `version` untouched. (The "around four atlas tiles" prose is Phase 5 polish, not Phase 1.1.)

    `addons/penta_tile/penta_tile_map_layer.gd`:
    - Line 3: `class_name TetraTileMapLayer` → `class_name PentaTileMapLayer`
    - Line 6: `const _PRIMARY_LAYER_NAME := "_TetraTileVisual"` → `"_PentaTileVisual"`
    - Line 7: `const _OVERLAY_LAYER_NAME := "_TetraTileDiagonalOverlay"` → `"_PentaTileDiagonalOverlay"`
    - Any `TetraTileLayout` typed reference → `PentaTileLayout`
    - Any `TetraTileLayoutTetraHorizontal` (line 197 in `_DEFAULT_LAYOUT` instantiation) → `PentaTileLayoutPentaHorizontal`
    - Any `atlas_contract: TetraTileAtlasContract` → `PentaTileAtlasContract`
    - Doc comments containing `TetraTile` → `PentaTile`

    `addons/penta_tile/penta_tile_atlas_contract.gd`:
    - `class_name TetraTileAtlasContract` → `PentaTileAtlasContract`
    - All `TetraTileLayout` typed references → `PentaTileLayout`
    - Doc comments: "TetraTile atlas contract" → "PentaTile atlas contract"

    `addons/penta_tile/penta_tile_atlas_slot.gd`:
    - `class_name TetraTileAtlasSlot` → `PentaTileAtlasSlot`
    - Doc comment refs to `TetraTileLayout` → `PentaTileLayout`
    - Doc comment refs to `TetraTileMapLayer` → `PentaTileMapLayer`
    - Doc comment "(mask 6/9 in tetra)" → "(mask 6/9 in penta)" (refers to the named layout family, which is also being renamed)

    `addons/penta_tile/layouts/penta_tile_layout.gd`:
    - `class_name TetraTileLayout` → `PentaTileLayout`
    - Doc comment "TetraTile layout topologies" → "PentaTile layout topologies"

    `addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd`:
    - `class_name TetraTileLayoutTetraHorizontal` → `PentaTileLayoutPentaHorizontal`
    - `extends TetraTileLayout` → `extends PentaTileLayout`
    - Doc comment "TetraTile horizontal layout" → "PentaTile horizontal layout"
    - Path reference `addons/tetra_tile/tetra_tile_map_layer.gd:116-152` → `addons/penta_tile/penta_tile_map_layer.gd:116-152`

    `addons/penta_tile/layouts/penta_tile_layout_penta_vertical.gd`:
    - `class_name TetraTileLayoutTetraVertical` → `PentaTileLayoutPentaVertical`
    - `extends TetraTileLayout` (or whatever it extends) → `extends PentaTileLayout` (and if it extends `TetraTileLayoutTetraHorizontal` per axis-swap subclass pattern → `PentaTileLayoutPentaHorizontal`)
    - Doc comments: same prose-rename pattern

    `addons/penta_tile/demo/demo_runtime_painter.gd`:
    - Line 3: `NodePath("TetraTileMapLayer")` → `NodePath("PentaTileMapLayer")`
    - Line 7 (and everywhere in file): `var tetra_map: TetraTileMapLayer = get_node(map_path)` → `var penta_map: PentaTileMapLayer = get_node(map_path)`
    - Lines 41/52/54 (or wherever they are): all 4 `tetra_map.` references → `penta_map.`

    `addons/penta_tile/demo/demo_player.gd`:
    - Verify zero `tetra` references (PATTERNS.md confirms zero). If any exist, rename per token map. If none, no edit.

    `addons/penta_tile/templates/README.md`:
    - All 16 `TetraTile` / `tetra_tile` occurrences → `PentaTile` / `penta_tile`
    - All `Tetra` family-name references → `Penta`

    `addons/penta_tile/templates/_generate_greybox_templates.py`:
    - Any `addons/tetra_tile/` path strings → `addons/penta_tile/`
    - Any `tetra_tile_` filename refs → `penta_tile_`
    - Comments referring to `TetraTile` → `PentaTile`

    **Step 4 — Update project.godot** (lines 13-14):
    - `config/name="TetraTile"` → `config/name="PentaTile"`
    - `run/main_scene="res://addons/tetra_tile/demo/tetra_tile_demo.tscn"` → `run/main_scene="res://addons/penta_tile/demo/penta_tile_demo.tscn"` (note the demo `.tscn` itself is renamed in Task 2 — this string in project.godot must match the new path)

    **Step 5 — DO NOT touch `.uid` file CONTENTS.** The `.uid` files were renamed in Step 2 (alongside their `.gd` files). The single-line UID string inside each `.uid` file is content-addressed and stable across renames; leave it as-is.

    **Step 6 — Verify GDScript parses.** Define `GODOT="C:/Programming_Files/Godot/Godot_v4.6.2-stable_win64.exe"` (per CLAUDE.md the binary is not on PATH; bash on Windows accepts forward-slash paths) and run `"$GODOT" --headless --check-only --path .`. If parse errors surface, fix them before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      bash -ec '
      set -e
      # Each gate stands alone with an explicit failure point.
      test "$(grep -rln "class_name TetraTile" addons/penta_tile/ 2>/dev/null | wc -l)" -eq 0
      test "$(grep -rln "TetraTileLayout" addons/penta_tile/ 2>/dev/null | wc -l)" -eq 0
      grep -qF "name=\"PentaTile\"" addons/penta_tile/plugin.cfg
      grep -qF "class_name PentaTileMapLayer" addons/penta_tile/penta_tile_map_layer.gd
      grep -qF "class_name PentaTileLayoutPentaHorizontal" addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd
      grep -qF "class_name PentaTileLayoutPentaVertical" addons/penta_tile/layouts/penta_tile_layout_penta_vertical.gd
      grep -qF "NodePath(\"PentaTileMapLayer\")" addons/penta_tile/demo/demo_runtime_painter.gd
      grep -qF "config/name=\"PentaTile\"" project.godot
      grep -qF "run/main_scene=\"res://addons/penta_tile/" project.godot
      ! test -d addons/tetra_tile
      # Bare Tetra / lowercase tetra in renamed source — defends against regression.
      # Char class [A-Za-z0-9-] catches Tetra5, Tetra-System, TetraBake (letter, digit, or hyphen).
      # PATTERNS.md confirms current source has zero such patterns; these are defensive gates.
      ! grep -rE "\bTetra[A-Za-z0-9-]" addons/penta_tile/ 2>/dev/null
      ! grep -rE "\btetra\b" addons/penta_tile/ 2>/dev/null
      '
    </automated>
  </verify>
  <done>
    addons/penta_tile/ folder exists with all renamed .gd files, plugin.cfg, and templates/README.md. addons/tetra_tile/ folder no longer exists. project.godot points at the new paths and config/name. Zero TetraTile* class_name occurrences remain in addons/penta_tile/. GDScript parses cleanly under `godot --headless --check-only`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebind saved Godot resources (.tscn / .tres / .png.import) to renamed paths and class_names</name>
  <files>
    addons/penta_tile/demo/tetra_tile_demo.tscn (rename to penta_tile_demo.tscn + edit ext_resource paths + rename node names),
    addons/penta_tile/demo/tetra_tile_ground.tres (rename to penta_tile_ground.tres + edit ext_resource path),
    addons/penta_tile/demo/tetra_tile_ground.png (rename to penta_tile_ground.png),
    addons/penta_tile/demo/tetra_tile_ground.png.import (rename + edit source_file),
    addons/penta_tile/demo/tetra_tile_ground.png.import (or whatever .import file matches; rename),
    addons/penta_tile/contracts/default_horizontal.tres (rebind paths + script_class),
    addons/penta_tile/contracts/default_vertical.tres (rebind paths + script_class),
    addons/penta_tile/contracts/tetra_horizontal_default.tres (rename + rebind),
    addons/penta_tile/contracts/tetra_vertical_default.tres (rename + rebind),
    addons/penta_tile/templates/tetra_horizontal.png (rename),
    addons/penta_tile/templates/tetra_horizontal.png.import (rename + edit source_file),
    addons/penta_tile/templates/tetra_vertical.png (rename),
    addons/penta_tile/templates/tetra_vertical.png.import (rename + edit source_file),
    addons/penta_tile/templates/dual_grid_16.png.import (edit source_file path only — basename unchanged),
    addons/penta_tile/templates/wang_2corner.png.import (edit source_file path only),
    addons/penta_tile/templates/wang_2edge.png.import (edit source_file path only),
    addons/penta_tile/penta_tile_template.png.import (edit source_file path only — already renamed in Task 1)
  </files>
  <read_first>
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G2 section — full ext_resource path examples + script_class examples),
    addons/penta_tile/demo/tetra_tile_demo.tscn (full file — read all ext_resource lines + node name lines + tile_set SubResource — do NOT touch tile_map_data PackedByteArray),
    addons/penta_tile/demo/tetra_tile_ground.tres (full file — single ext_resource path),
    addons/penta_tile/contracts/default_horizontal.tres (full file — script_class + 2 ext_resource paths),
    addons/penta_tile/contracts/default_vertical.tres (full file — script_class + 2 ext_resource paths),
    addons/penta_tile/contracts/tetra_horizontal_default.tres (full file — script_class + 2 ext_resource paths + description prose),
    addons/penta_tile/contracts/tetra_vertical_default.tres (full file — script_class + 2 ext_resource paths + description prose),
    addons/penta_tile/templates/tetra_horizontal.png.import (read source_file line),
    addons/penta_tile/templates/dual_grid_16.png.import (read source_file line — basename unchanged but parent path renames),
    addons/penta_tile/penta_tile_map_layer.gd (re-confirm `class_name PentaTileMapLayer` from Task 1 — `script_class` strings in .tres must match)
  </read_first>
  <action>
    **NOTE:** Task 1 already renamed the parent folder `addons/tetra_tile/` → `addons/penta_tile/`. So the working paths below are all under `addons/penta_tile/`. The `tetra_tile_*` filenames inside the demo and contracts subfolders still need renaming.

    **Step 1 — Rename demo files via `git mv`:**
    ```
    git mv addons/penta_tile/demo/tetra_tile_demo.tscn        addons/penta_tile/demo/penta_tile_demo.tscn
    git mv addons/penta_tile/demo/tetra_tile_ground.tres      addons/penta_tile/demo/penta_tile_ground.tres
    git mv addons/penta_tile/demo/tetra_tile_ground.png       addons/penta_tile/demo/penta_tile_ground.png
    git mv addons/penta_tile/demo/tetra_tile_ground.png.import addons/penta_tile/demo/penta_tile_ground.png.import
    ```

    **Step 2 — Rename contract files via `git mv`:**
    ```
    git mv addons/penta_tile/contracts/tetra_horizontal_default.tres addons/penta_tile/contracts/penta_horizontal_default.tres
    git mv addons/penta_tile/contracts/tetra_vertical_default.tres   addons/penta_tile/contracts/penta_vertical_default.tres
    ```
    NOTE: `default_horizontal.tres` and `default_vertical.tres` filenames do NOT change (no `tetra_` prefix). Only their internal contents rebind.

    **Step 3 — Rename template files via `git mv`:**
    ```
    git mv addons/penta_tile/templates/tetra_horizontal.png        addons/penta_tile/templates/penta_horizontal.png
    git mv addons/penta_tile/templates/tetra_horizontal.png.import addons/penta_tile/templates/penta_horizontal.png.import
    git mv addons/penta_tile/templates/tetra_vertical.png          addons/penta_tile/templates/penta_vertical.png
    git mv addons/penta_tile/templates/tetra_vertical.png.import   addons/penta_tile/templates/penta_vertical.png.import
    ```

    **Step 4 — Edit `addons/penta_tile/demo/penta_tile_demo.tscn`:**
    Update every `path="res://addons/tetra_tile/..."` ext_resource line to `addons/penta_tile/...`. Specifically (per PATTERNS.md G2 excerpt):
    - `path="res://addons/tetra_tile/demo/tetra_tile_ground.png"` → `path="res://addons/penta_tile/demo/penta_tile_ground.png"`
    - `path="res://addons/tetra_tile/tetra_tile_map_layer.gd"` → `path="res://addons/penta_tile/penta_tile_map_layer.gd"`
    - `path="res://addons/tetra_tile/demo/demo_player.gd"` → `path="res://addons/penta_tile/demo/demo_player.gd"`
    - `path="res://addons/tetra_tile/demo/demo_runtime_painter.gd"` → `path="res://addons/penta_tile/demo/demo_runtime_painter.gd"`
    - `path="res://addons/tetra_tile/contracts/default_horizontal.tres"` → `path="res://addons/penta_tile/contracts/default_horizontal.tres"`

    Update node names:
    - `[node name="TetraTileDemo"` → `[node name="PentaTileDemo"`
    - `[node name="TetraTileMapLayer"` → `[node name="PentaTileMapLayer"` (this matches the new `NodePath("PentaTileMapLayer")` in `demo_runtime_painter.gd` from Task 1)

    **Leave untouched:**
    - The `uid="uid://..."` strings (content-addressed, stable)
    - The `unique_id=...` integer node identifiers
    - The `tile_map_data = PackedByteArray(...)` (encoded cell data, no strings inside)
    - Sub-resource IDs like `SubResource("TileSet_62ywd")`

    **Step 5 — Edit `addons/penta_tile/demo/penta_tile_ground.tres`:**
    - `path="res://addons/tetra_tile/demo/tetra_tile_ground.png"` → `path="res://addons/penta_tile/demo/penta_tile_ground.png"`
    - PATTERNS.md confirms zero `custom_data_layer_<N>/name` lines exist in this file. So the "custom data layer key migration" scope item is a no-op for this `.tres` (the prose-only rename of `tetra_role` / `tetra_lock_rotation` happens in Plan 02 across the 5 .md files that mention them).

    **Step 6 — Edit `addons/penta_tile/contracts/default_horizontal.tres`:**
    - `script_class="TetraTileAtlasContract"` → `script_class="PentaTileAtlasContract"`
    - `path="res://addons/tetra_tile/tetra_tile_atlas_contract.gd"` → `path="res://addons/penta_tile/penta_tile_atlas_contract.gd"`
    - `path="res://addons/tetra_tile/contracts/tetra_horizontal_default.tres"` → `path="res://addons/penta_tile/contracts/penta_horizontal_default.tres"`

    **Step 7 — Edit `addons/penta_tile/contracts/default_vertical.tres`:**
    Same pattern as default_horizontal.tres but with the `vertical` filename.

    **Step 8 — Edit `addons/penta_tile/contracts/penta_horizontal_default.tres`:**
    - `script_class="TetraTileLayoutTetraHorizontal"` → `script_class="PentaTileLayoutPentaHorizontal"`
    - `path="res://addons/tetra_tile/layouts/tetra_tile_layout_tetra_horizontal.gd"` → `path="res://addons/penta_tile/layouts/penta_tile_layout_penta_horizontal.gd"`
    - `path="res://addons/tetra_tile/templates/tetra_horizontal.png"` → `path="res://addons/penta_tile/templates/penta_horizontal.png"`
    - `description = "TetraTile 4-tile horizontal layout..."` → `description = "PentaTile 4-tile horizontal layout..."` (the "4-tile" stays — Phase 5 polish)

    **Step 9 — Edit `addons/penta_tile/contracts/penta_vertical_default.tres`:**
    Same pattern as the horizontal contract `.tres` but with `vertical`.

    **Step 10 — Edit `.png.import` files** (update `source_file` lines):
    For each of the following, change `source_file="res://addons/tetra_tile/..."` to `source_file="res://addons/penta_tile/..."` AND update the basename if the underlying `.png` was renamed:
    - `penta_tile_template.png.import` → source_file: `res://addons/penta_tile/penta_tile_template.png`
    - `demo/penta_tile_ground.png.import` → source_file: `res://addons/penta_tile/demo/penta_tile_ground.png`
    - `templates/penta_horizontal.png.import` → source_file: `res://addons/penta_tile/templates/penta_horizontal.png`
    - `templates/penta_vertical.png.import` → source_file: `res://addons/penta_tile/templates/penta_vertical.png`
    - `templates/dual_grid_16.png.import` → source_file: `res://addons/penta_tile/templates/dual_grid_16.png` (basename unchanged, parent path renamed)
    - `templates/wang_2corner.png.import` → source_file: `res://addons/penta_tile/templates/wang_2corner.png`
    - `templates/wang_2edge.png.import` → source_file: `res://addons/penta_tile/templates/wang_2edge.png`

    **Leave untouched** in `.import` files: the `dest_files=["res://.godot/imported/<name>-<hash>.ctex"]` array — that's regenerated automatically on next Godot reimport. The hash will look stale but Godot will rewrite it.

    **Step 11 — Open Godot editor to trigger reimport:**
    Define `GODOT="C:/Programming_Files/Godot/Godot_v4.6.2-stable_win64.exe"` and run `"$GODOT" --headless --quit --path .` (one-shot project load) to force the engine to reimport the renamed PNGs and refresh `.godot/imported/` cache. Watch stderr for any "missing resource" errors. If the engine reports stale UIDs or unresolvable ext_resources, those are the load-bearing breakages — fix them before declaring the task done.
  </action>
  <verify>
    <automated>
      ! test -f addons/penta_tile/demo/tetra_tile_demo.tscn &&
      test -f addons/penta_tile/demo/penta_tile_demo.tscn &&
      test -f addons/penta_tile/demo/penta_tile_ground.tres &&
      test -f addons/penta_tile/contracts/penta_horizontal_default.tres &&
      test -f addons/penta_tile/contracts/penta_vertical_default.tres &&
      test -f addons/penta_tile/templates/penta_horizontal.png &&
      grep -F 'script_class="PentaTileAtlasContract"' addons/penta_tile/contracts/default_horizontal.tres &&
      grep -F 'script_class="PentaTileLayoutPentaHorizontal"' addons/penta_tile/contracts/penta_horizontal_default.tres &&
      grep -F 'script_class="PentaTileLayoutPentaVertical"' addons/penta_tile/contracts/penta_vertical_default.tres &&
      grep -F '[node name="PentaTileMapLayer"' addons/penta_tile/demo/penta_tile_demo.tscn &&
      grep -F '[node name="PentaTileDemo"' addons/penta_tile/demo/penta_tile_demo.tscn &&
      ! grep -rln 'res://addons/tetra_tile/' addons/penta_tile/ &&
      ! grep -rln 'script_class="TetraTile' addons/penta_tile/
    </automated>
  </verify>
  <done>
    All `.tscn` / `.tres` / `.import` files inside addons/penta_tile/ point exclusively at `res://addons/penta_tile/...` paths and reference exclusively `PentaTile*` script_class names. Demo scene + ground TileSet + both default contracts + both penta-named contract layouts all rebound. Zero `res://addons/tetra_tile/` strings remain in the addon tree. Zero `TetraTile*` script_class strings remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies Godot opens, demo loads, and runtime drag-paint still produces dual-grid visuals</name>
  <what-built>
    Tasks 1-2 renamed the entire `addons/tetra_tile/` folder to `addons/penta_tile/`, renamed all `Tetra*` classes to `Penta*`, rebound every `.tscn` / `.tres` / `.import` ext_resource path, and updated `project.godot`. The project should open cleanly under the new name and the demo should paint exactly as before.
  </what-built>
  <how-to-verify>
    1. Open the Godot editor: launch `C:\Programming_Files\Godot\Godot_v4.6.2-stable_win64.exe` and open `c:\Programming_Files\Shilocity\TetraTile\project.godot`.
    2. Confirm window title shows "PentaTile" (not "TetraTile").
    3. Look at the "Output" panel at the bottom of the editor. Expected: zero "missing resource" errors, zero "parse error" lines, zero "UID not found" warnings related to the `addons/penta_tile/` files.
    4. Open `res://addons/penta_tile/demo/penta_tile_demo.tscn` in the 2D editor.
    5. Verify the scene tree shows `PentaTileDemo` (root) → `PentaTileMapLayer` (child).
    6. Inspect the `PentaTileMapLayer` node — confirm its `script` property points at `res://addons/penta_tile/penta_tile_map_layer.gd` and the `atlas_contract` property points at `res://addons/penta_tile/contracts/default_horizontal.tres`.
    7. Press F5 to run the demo.
    8. Left-click on the canvas to paint a tile. Right-click to erase. Drag-paint across multiple cells.
    9. Confirm the dual-grid visuals appear correctly: filled regions show fills, edges show borders, corners show inner/outer corners — exactly the same behavior as before the rename. The diagonal masks (6/9) still produce the overlay-composed visuals.
    10. Stop the demo (close the run window).
    11. **If anything is broken** — missing resources, the demo doesn't paint, the editor reports parse errors — describe the issue verbatim from the editor's Output panel. Tasks 1-2 must be revised before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" if Godot opens cleanly + demo paints correctly + no Output-panel errors. Otherwise, paste the editor's error messages so Tasks 1-2 can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → Godot editor | Renamed file paths and script_class strings cross here; mismatches surface as "missing resource" / "UID not found" errors at editor open time |
| `.tres` script_class field → loaded `.gd` `class_name` | If `script_class="PentaTileAtlasContract"` in a .tres but the .gd still says `class_name TetraTileAtlasContract`, Godot fails to instantiate the resource silently (resource appears as `[Resource]` placeholder in inspector) |
| `.png.import` source_file → actual .png filesystem path | If `source_file` points at `tetra_tile_ground.png` but only `penta_tile_ground.png` exists on disk, Godot regenerates a broken `.ctex` and the texture renders as solid magenta/checkerboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering (data integrity) | `.tres` files with mismatched script_class | mitigate | Task 2 grep-asserts both directions: zero `script_class="TetraTile*"` AND presence of `script_class="PentaTile*"` strings. Task 3 visually confirms inspector resource preview. |
| T-01.1-02 | Tampering (path integrity) | `.tscn` ext_resource paths broken after parent-folder rename | mitigate | Task 2 grep-asserts zero `res://addons/tetra_tile/` paths remain. Task 3's editor-load check surfaces any broken refs in the Output panel. |
| T-01.1-03 | Information disclosure (loss of UID) | Godot UID system regenerates UIDs after raw filesystem rename | mitigate | Use `git mv` (preserves file content, including .uid file body which carries the UID). Task 3 confirms scenes still load — proves UIDs survived the rename. |
| T-01.1-04 | Denial of service (project unloadable) | A botched rename leaves the project unable to open | mitigate | Atomic single-commit discipline: Tasks 1-2 land together. Task 3 is a hard gate before merging. |
| T-01.1-05 | Denial of service (`.import` cache corruption) | Stale `.ctex` files in `.godot/imported/` after PNG rename | accept | Godot regenerates `.import` caches on first project open (Task 2 Step 11 triggers this via `godot --headless --quit`). Self-healing; no mitigation needed beyond running the engine once. |
</threat_model>

<verification>
After all three tasks complete:

1. `addons/penta_tile/` exists; `addons/tetra_tile/` does not exist.
2. `grep -rln "TetraTile\|tetra_tile" addons/penta_tile/` returns 0 lines.
3. `grep -rln "tetra" addons/penta_tile/` returns 0 lines (no lowercase project-name leftovers).
4. `grep -F 'config/name="PentaTile"' project.godot` succeeds.
5. `grep -F 'run/main_scene="res://addons/penta_tile/demo/penta_tile_demo.tscn"' project.godot` succeeds.
6. Godot opens project.godot without errors (Task 3 human check).
7. Demo runtime drag-paint produces dual-grid visuals (Task 3 human check).
8. `git status` shows the rename as a single staged-changes set; commit message will read "refactor(01.1): rename addons/tetra_tile/ → addons/penta_tile/ + rebind saved resources".
</verification>

<success_criteria>
- Project opens in Godot 4.6 without missing-resource errors
- Demo scene loads and runtime drag-paint produces correct dual-grid visuals
- All `.gd` files use `class_name PentaTile*` (zero `TetraTile*` class_name strings remain)
- All `.tres` files reference `script_class="PentaTile*"` and `path="res://addons/penta_tile/..."` (zero old refs)
- `project.godot` config/name and run/main_scene point at PentaTile / `penta_tile/`
- Single atomic commit: source rename + saved-resource rebinding land together (no half-renamed working tree window)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-01-SUMMARY.md` with:
- Files renamed (count + paths)
- Files edited (count + paths)
- Key decisions during execution (e.g., "demo_player.gd had zero tetra refs — confirmed and skipped")
- Any breakage encountered and how it was fixed (esp. UID-related editor warnings)
- Demo verification result from Task 3
- Carryover notes for Plan 02 (planning docs sweep) and Plan 03 (repo + git + memory rename)
</output>
</content>
</invoke>