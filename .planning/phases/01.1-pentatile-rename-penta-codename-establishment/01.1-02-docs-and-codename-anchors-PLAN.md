---
phase: 01.1-pentatile-rename-penta-codename-establishment
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - .planning/**/*.md (all planning docs except archived 01-* phase + spikes 001..003)
  - .planning/REQUIREMENTS.md (TETRA-* and TETRA-SYNTH-* IDs → PENTA-* / PENTA-SYNTH-*)
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - CLAUDE.md (rename + new "Coined-Term Discipline" section)
  - README.md (rename + new "What is a Penta tileset?" section)
  - IMPLEMENTATION_PLAN.md
  - RESEARCH.md
  - CHANGELOG.md (created if absent)
autonomous: true
requirements: []
must_haves:
  truths:
    - "All non-archived project docs reference PentaTile / penta_tile / Penta consistently"
    - "Archived Phase 1 plan docs and spikes 001-003 retain TetraTile references (historical record)"
    - "The README has a canonical 'What is a Penta tileset?' section with a labeled archetype diagram"
    - "CLAUDE.md has a Coined-Term Discipline section establishing Penta as the 5-archetype codename"
    - "Requirement IDs in REQUIREMENTS.md and ROADMAP.md use PENTA-* / PENTA-SYNTH-* (renamed from TETRA-* / TETRA-SYNTH-*)"
    - "CHANGELOG.md documents the rename as a breaking change for v0.2"
    - "The 5 .md files that mention `tetra_role` / `tetra_lock_rotation` use `penta_role` / `penta_lock_rotation`"
  artifacts:
    - path: "README.md"
      provides: "Canonical Penta tileset definition with labeled archetype diagram"
      contains: "## 🍀 What is a Penta tileset?"
    - path: "CLAUDE.md"
      provides: "Coined-Term Discipline section as project invariant"
      contains: "## Coined-Term Discipline"
    - path: "CHANGELOG.md"
      provides: "v0.2 breaking-change record for the rename"
      contains: "TetraTile → PentaTile"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Renamed TETRA-* requirement IDs"
      pattern: "PENTA-01|PENTA-SYNTH-01"
  key_links:
    - from: "README.md `## What is a Penta tileset?` section"
      to: "addons/penta_tile/templates/penta_horizontal.png"
      via: "<img src=...> tag"
      pattern: 'src="addons/penta_tile/templates/penta_horizontal.png"'
    - from: "CLAUDE.md `## Coined-Term Discipline`"
      to: "README.md `## What is a Penta tileset?`"
      via: "Closing-line reference"
      pattern: 'Canonical "What is a Penta tileset\?" definition lives in `README.md`'
    - from: ".planning/ROADMAP.md requirement IDs"
      to: ".planning/REQUIREMENTS.md requirement IDs"
      via: "Cross-reference (must rename in lockstep)"
      pattern: "PENTA-SYNTH-"
---

<objective>
Sweep every project documentation file (.planning/**, root .md files, CHANGELOG) for `TetraTile`/`tetra_tile`/`Tetra`/`tetra` and rename to `PentaTile`/`penta_tile`/`Penta`/`penta`. Add the two net-new sections that anchor the codename: README "What is a Penta tileset?" + CLAUDE.md "Coined-Term Discipline." Create CHANGELOG.md if absent.

Purpose: The rename is invisible without docs that propagate the new name. The codename anchors (README definition + CLAUDE.md discipline section) are load-bearing for the codename to spread — without them, "Penta" stays a rename rather than becoming a coined term.

Output: A fully renamed documentation surface, two new canonical sections, a CHANGELOG entry, and an explicit boundary preserving historical record (archived Phase 1 plan docs + spikes 001-003 retain `TetraTile` as immutable history).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md
@.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md
@CLAUDE.md
@README.md
@.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-01-SUMMARY.md
</context>

<rename_token_map>
<!-- Same map as Plan 01, applied across docs. -->

| Find | Replace |
|------|---------|
| `TetraTile` | `PentaTile` |
| `tetra_tile` | `penta_tile` |
| `TETRA-` (in requirement-ID context, e.g., `TETRA-01`, `TETRA-SYNTH-`) | `PENTA-` |
| `Tetra` (when followed by `Tile`/`Layout`/`-System`) | `Penta` |
| `tetra` (lowercase, project-context noun like "the tetra layout") | `penta` |
| `tetra_role` | `penta_role` |
| `tetra_lock_rotation` | `penta_lock_rotation` |
| Folder paths `addons/tetra_tile/` (in docs) | `addons/penta_tile/` |
| Folder paths `tetra_tile_layout_tetra/` (referenced in Phase 2 design docs) | `penta_tile_layout_penta/` |

**Protected scope (DO NOT rename):**
- `.planning/phases/01-contract-skeleton-tetra-layouts/**` — entire folder is the historical record of completed Phase 1 work. The directory NAME stays as-is (it's referenced in git history and Phase 1 SUMMARY commits). The .md files INSIDE are read-only history; do not edit them.
- `.planning/spikes/001-template-decoder-feasibility/`, `.planning/spikes/002-blob47-decoder-generalization/`, `.planning/spikes/003-pixellab-bit-mapping/` — spike outputs are historical decision records.
- `.planning/spikes/CONVENTIONS.md` and `.planning/spikes/MANIFEST.md` — spike-system docs; review individually but default is rename if all refs are project-name.
- **`.planning/phases/01.1-pentatile-rename-penta-codename-establishment/` — this phase's own working docs.** `01.1-CONTEXT.md`, `01.1-PATTERNS.md`, the three `01.1-*-PLAN.md` files, and any future `01.1-*-SUMMARY.md` MUST retain side-by-side `Tetra`/`Penta` literals: rename tables, the verbatim `mv c--Programming-Files-Shilocity-TetraTile c--Programming-Files-Shilocity-PentaTile` command from CONTEXT.md lines 56-60, example old GitHub URLs in CHECKPOINT contexts, and the rename token map itself. These docs ARE the source-of-truth for the rename and would become incoherent if their literals were swept. Treat as a protected scope sub-region analogous to the archived `01-` directory.
- Git commit messages and history (immutable).
- Generic English "tetra" — none exist; flag for human review if any surface during the sweep.

**Reconciliation note for archived Phase 1 dir name:** The folder `01-contract-skeleton-tetra-layouts/` contains the slug "tetra-layouts." Per CONTEXT.md "Out of scope: Git history rewriting" + the historical-record principle, **the folder name stays.** Future Phase 2 docs already reference paths like `.planning/phases/01-contract-skeleton-tetra-layouts/01-VERIFICATION.md` — those references are correct as-is.
</rename_token_map>

<tasks>

<task type="auto">
  <name>Task 1: Sweep .planning/ docs (excluding archived 01-* and spikes 001-003) for token rename</name>
  <files>
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/REQUIREMENTS.md,
    .planning/codebase/ARCHITECTURE.md,
    .planning/codebase/CONCERNS.md,
    .planning/codebase/CONVENTIONS.md,
    .planning/codebase/INTEGRATIONS.md,
    .planning/codebase/STACK.md,
    .planning/codebase/STRUCTURE.md,
    .planning/codebase/TESTING.md,
    .planning/research/SUMMARY.md,
    .planning/research/STACK.md,
    .planning/research/FEATURES.md,
    .planning/research/ARCHITECTURE.md,
    .planning/research/PITFALLS.md,
    .planning/research/layouts/MASK_UNIFICATION.md,
    .planning/research/layouts/TILEBITTOOLS.md,
    .planning/research/layouts/RPG_MAKER.md (per PATTERNS.md: ALL refs are project-name; rename in full),
    .planning/research/layouts/TEMPLATE_CONVENTIONS.md (if exists; check via Grep),
    .planning/phases/02-native-layouts/*.md (Phase 2 docs — heavy Tetra* refs)
    <!-- DO NOT sweep the phase's own working docs:
         01.1-CONTEXT.md, 01.1-PATTERNS.md, 01.1-01-PLAN.md, 01.1-02-PLAN.md, 01.1-03-PLAN.md,
         and any future 01.1-*-SUMMARY.md. Those files legitimately contain side-by-side
         "Tetra → Penta" rename-table literals, the verbatim mv command from CONTEXT.md
         lines 56-60, and example old GitHub URLs in CHECKPOINT contexts. They are
         historical record / source-of-truth and must retain the literals to remain
         coherent after the rename lands. Treated as a protected scope sub-region
         analogous to .planning/phases/01-contract-skeleton-tetra-layouts/ and
         .planning/spikes/001..003/. -->
  </files>
  <read_first>
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G5 section — top-occurrence file table + concrete excerpts + protected-scope rules),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md (Scope-out and Out-of-scope clarifications — RPG_MAKER.md note in particular),
    .planning/REQUIREMENTS.md (read first — confirm requirement-ID layout: TETRA-01..03 + TETRA-SYNTH-01..12),
    .planning/ROADMAP.md (read full — find every TETRA-/TETRA-SYNTH- cross-reference for lockstep rename),
    .planning/research/layouts/RPG_MAKER.md (read full — PATTERNS.md confirmed all 11 refs are project-name; verify before sweeping)
  </read_first>
  <action>
    **Step 1 — Identify scope.** Use Grep to locate every `.md` file under `.planning/` that contains `TetraTile|tetra_tile|TETRA-|Tetra `:
    ```
    Grep pattern: "TetraTile|tetra_tile|TETRA-|Tetra "
    Path: .planning/
    output_mode: files_with_matches
    ```
    Then **subtract the protected scope** (do NOT edit these):
    - `.planning/phases/01-contract-skeleton-tetra-layouts/**` (entire folder — Phase 1 historical record)
    - `.planning/spikes/001-template-decoder-feasibility/**`
    - `.planning/spikes/002-blob47-decoder-generalization/**`
    - `.planning/spikes/003-pixellab-bit-mapping/**`
    - **`.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md`, `01.1-PATTERNS.md`, `01.1-01-source-and-resources-PLAN.md`, `01.1-02-docs-and-codename-anchors-PLAN.md`, `01.1-03-repo-git-memory-and-verify-PLAN.md`** — and any future `01.1-*-SUMMARY.md` files. The phase's own working docs are source-of-truth for the rename and legitimately contain side-by-side `Tetra`/`Penta` literals (rename tables, the verbatim `mv` command, old GitHub URL examples, the rename token map itself). They describe the rename, not the renamed thing.

    **Step 2 — For each non-protected file, apply the token rename map** from `<rename_token_map>` above. Examples of the load-bearing renames:

    `.planning/REQUIREMENTS.md` — Requirement IDs:
    - `TETRA-01` → `PENTA-01`
    - `TETRA-02` → `PENTA-02`
    - `TETRA-03` → `PENTA-03`
    - `TETRA-SYNTH-01` → `PENTA-SYNTH-01` (... through `PENTA-SYNTH-12`)
    - All prose refs: `TetraTileLayoutTetraHorizontal` → `PentaTileLayoutPentaHorizontal`

    `.planning/ROADMAP.md` — Cross-references must follow:
    - Coverage table: `TETRA-01..03, TETRA-SYNTH-01..12` → `PENTA-01..03, PENTA-SYNTH-01..12`
    - Phase 2 success criteria mentioning `TetraTileLayoutTetra` → `PentaTileLayoutPenta`
    - Phase 1 entry: leave the historical Phase 1 description intact for completed-phase fidelity, but rename forward-looking `Tetra*` class references where they appear.

    `.planning/STATE.md` — All `tetra_role` / `tetra_lock_rotation` refs → `penta_role` / `penta_lock_rotation`.

    `.planning/research/layouts/RPG_MAKER.md` — All 11 `TetraTile` references rename per PATTERNS.md verification: "all refs are project-name."

    `.planning/research/PITFALLS.md` — 16 occurrences, all project-name (PATTERNS.md verified). Rename all.

    `.planning/research/SUMMARY.md`, `.planning/research/FEATURES.md`, `.planning/research/STACK.md`, `.planning/research/ARCHITECTURE.md` — same token rename.

    `.planning/codebase/*.md` (7 files) — same token rename. Pay special attention to `.planning/codebase/STRUCTURE.md` if it lists folder paths (`addons/tetra_tile/`).

    `.planning/phases/02-native-layouts/02-CONTEXT.md` (97 occurrences) and `02-DISCUSSION-LOG.md` (115 occurrences) — heavy lift. Apply rename uniformly. The Phase 2 design refers to `TetraTileLayoutTetra` (the unified merged class) as the central design output; this becomes `PentaTileLayoutPenta` per the CONTEXT.md decision "Native flexible-count layout class is `PentaTileLayoutPenta`."

    `.planning/phases/01.1-.../01.1-CONTEXT.md`, `01.1-PATTERNS.md`, and the three `01.1-*-PLAN.md` files — **DO NOT TOUCH.** These are the phase's own working docs and form a protected scope sub-region (per the rename token map's Protected scope list above). They legitimately contain side-by-side `Tetra`/`Penta` literals — rename tables, the verbatim `mv c--Programming-Files-Shilocity-TetraTile c--Programming-Files-Shilocity-PentaTile` command from CONTEXT.md lines 56-60, example old GitHub URLs in CHECKPOINT contexts, and the rename token map itself. Sweeping these would make the docs incoherent.

    **Step 3 — Special-case `tetra_role` / `tetra_lock_rotation` (G4 prose-only):**
    Per PATTERNS.md G4, these custom-data-layer keys exist ONLY in 5 .md files (zero `.tres` yet). The 5 files are:
    - `CLAUDE.md` (line 20) — handled in Task 4 below (CLAUDE.md is multi-touch this plan)
    - `.planning/STATE.md`
    - `.planning/research/SUMMARY.md`
    - `.planning/research/FEATURES.md`
    - `.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md`

    For the 4 .planning/-side files (excluding CLAUDE.md), rename `tetra_role` → `penta_role` and `tetra_lock_rotation` → `penta_lock_rotation` as part of the general sweep.

    **Step 4 — Verify protected scope was NOT touched.** After the sweep, run Grep against ALL protected paths to confirm `TetraTile` references are still present (proving the sweep didn't over-replace):
    ```
    Grep pattern: "TetraTile"
    Path: .planning/phases/01-contract-skeleton-tetra-layouts/
    Expected: > 0 matches (historical record preserved)

    Grep pattern: "TetraTile"
    Path: .planning/spikes/
    Expected: > 0 matches (spike history preserved)

    Grep pattern: "TetraTile"
    Path: .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md
    Expected: > 0 matches (own-phase working docs preserved — rename tables and verbatim mv command must retain side-by-side literals)
    ```
  </action>
  <verify>
    <automated>
      bash -ec '
      set -e
      # === Negative gates: zero stale Tetra refs in non-protected scope ===
      # The 01.1 phase dir contains only protected own-docs (CONTEXT, PATTERNS,
      # three PLANs); excluded entirely from negative gates. Positive gates below
      # assert these files RETAIN TetraTile literals.
      SWEEP_PATHS=".planning/codebase/ .planning/research/ .planning/phases/02-native-layouts/ .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md"
      test -z "$(grep -rln "TetraTile" $SWEEP_PATHS 2>/dev/null)"
      test -z "$(grep -rln "tetra_tile" $SWEEP_PATHS 2>/dev/null)"
      # Bare Tetra — character class includes letter, digit, or hyphen; catches
      # Tetra5, Tetra-System, TetraBake (the original [A-Za-z]-only form missed
      # digits and hyphens, which are the actual real-world cases).
      test -z "$(grep -rlnE "\bTetra[A-Za-z0-9-]" $SWEEP_PATHS 2>/dev/null)"
      # Lowercase bare "tetra" as a standalone word; underscore is a word char in
      # POSIX/GNU grep -E so this does NOT match tetra_tile (verified semantics).
      test -z "$(grep -rlnE "\btetra\b" $SWEEP_PATHS 2>/dev/null)"
      # Requirement-ID rename — both the literal TETRA- prefix AND the broader
      # TETRA-with-trailing-digit/underscore/hyphen form (catches TETRA5-FOO,
      # TETRA_SYNTH, etc. — defends against any historical ID variant).
      test -z "$(grep -F "TETRA-" .planning/REQUIREMENTS.md .planning/ROADMAP.md 2>/dev/null)"
      test -z "$(grep -lE "\bTETRA[0-9_-]" .planning/REQUIREMENTS.md .planning/ROADMAP.md 2>/dev/null)"
      grep -qF "PENTA-01" .planning/REQUIREMENTS.md
      grep -qF "PENTA-SYNTH-01" .planning/REQUIREMENTS.md
      grep -qF "PENTA-SYNTH-12" .planning/REQUIREMENTS.md
      # === Positive gates: protected scope retains TetraTile literals ===
      test -n "$(grep -rln "TetraTile" .planning/phases/01-contract-skeleton-tetra-layouts/ 2>/dev/null)"
      test -n "$(grep -rln "TetraTile" .planning/spikes/ 2>/dev/null)"
      # Own-phase working docs MUST retain side-by-side literals (proves the
      # sweep did NOT over-reach into them):
      test -n "$(grep -l "TetraTile" .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md 2>/dev/null)"
      test -n "$(grep -l "TetraTile" .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md 2>/dev/null)"
      '
    </automated>
  </verify>
  <done>
    All non-archived .planning/ .md files use PentaTile/penta_tile/Penta/PENTA- consistently. REQUIREMENTS.md and ROADMAP.md cross-references match (TETRA-* IDs renamed in lockstep). Archived Phase 1 plan docs (`.planning/phases/01-contract-skeleton-tetra-layouts/`) and spikes 001-003 retain their original `TetraTile` references unchanged. `tetra_role`/`tetra_lock_rotation` renamed in the 4 .planning/-side .md files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep root-level docs (IMPLEMENTATION_PLAN.md, RESEARCH.md, README.md token rename)</name>
  <files>
    IMPLEMENTATION_PLAN.md,
    RESEARCH.md,
    README.md (token rename only — new section in Task 3)
  </files>
  <read_first>
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G5 section — root-file occurrence counts: README 36, IMPLEMENTATION_PLAN 2, RESEARCH 10),
    IMPLEMENTATION_PLAN.md (full — only 2 occurrences expected),
    RESEARCH.md (full — 10 occurrences expected),
    README.md (full — 36 occurrences expected; preserve all anchor links and structural formatting; the new section is added in Task 3 — only do the token rename here)
  </read_first>
  <action>
    **Step 1 — `IMPLEMENTATION_PLAN.md`:**
    Apply the token rename map. PATTERNS.md says only 2 occurrences — find and rename each.

    **Step 2 — `RESEARCH.md`:**
    Apply the token rename map. PATTERNS.md says 10 occurrences. Watch for refs to `tetra_role` / `tetra_lock_rotation` that should become `penta_role` / `penta_lock_rotation`.

    **Step 3 — `README.md` token rename pass (new section is Task 3, kept separate to avoid edit conflicts):**
    Apply the token rename map across all 36 existing occurrences. Specific load-bearing edits:

    - Line 1 heading: `# 🍀 TetraTile` → `# 🍀 PentaTile`
    - Line 3: `Just paint your tiles. Intuitive Godot autotiling addon...` — keep the prose. Change `[4-tile **Tetra**](#-the-tetra-system-template)` → `[5-archetype **Penta**](#-what-is-a-penta-tileset)`. The new anchor target lands in Task 3. The intro deliberately says **5-archetype** (not 4-tile): the codename "Penta" means 5 archetypes by definition, Phase 2 makes it true at runtime, and Phase 1.1 establishes the meaning. Reconciliation directive per CONTEXT.md line 40 — the README intro and the "What is a Penta tileset?" section MUST agree on the 5-archetype framing.

    - Line 5 `<img src="addons/tetra_tile/templates/tetra_horizontal.png">` → `<img src="addons/penta_tile/templates/penta_horizontal.png">` (path renamed by Plan 01 Task 2)
    - Table of Contents (lines 7-19): rename anchor texts. The new anchor `#-what-is-a-penta-tileset` goes in Task 3; don't add it here, but DO rename `[Why TetraTile?]` → `[Why PentaTile?]`, `[The Tetra-System Template]` → `[The Penta-System Template]`, `[Comparison: TetraTile vs. TileMapDual]` → `[Comparison: PentaTile vs. TileMapDual]`. Anchor hashes follow GitHub's slug convention (lowercase, dashes for spaces, emoji stripped) so `#-why-pentatile`, `#-the-penta-system-template`, etc.
    - Section heading line 21: `## 🚀 Why TetraTile?` → `## 🚀 Why PentaTile?`
    - Section heading line 41: `## 🎨 The Tetra-System Template` → `## 🎨 The Penta-System Template`
    - Section heading line 54: `## ⚔️ TetraTile vs. TileMapDual API` → `## ⚔️ PentaTile vs. TileMapDual API`
    - Comparison table cell line 60: `TetraTileMapLayer` → `PentaTileMapLayer`
    - Code block line 88-97 (Addon Layout): `addons/tetra_tile/` → `addons/penta_tile/`, `tetra_tile_map_layer.gd` → `penta_tile_map_layer.gd`, `tetra_tile_template.png` → `penta_tile_template.png`, `tetra_tile_demo.tscn` → `penta_tile_demo.tscn`, `tetra_tile_ground.png/.tres` → `penta_tile_ground.png/.tres`
    - Line 99 prose: `tetra_tile_template.png` → `penta_tile_template.png`, `demo/tetra_tile_ground.png` → `demo/penta_tile_ground.png`
    - Section heading line 101: `## 🔌 Current API` — rename `TetraTileMapLayer` in line 103 → `PentaTileMapLayer`.
    - Section heading line 130: `## 🧪 Demo` — line 132 `res://addons/tetra_tile/demo/tetra_tile_demo.tscn` → `res://addons/penta_tile/demo/penta_tile_demo.tscn`. Line 136 `TetraTileMapLayer` → `PentaTileMapLayer`.
    - Section heading line 158: `## 🗺️ Roadmap` — line 161 `TetraBake` → `PentaBake` (project-name-coupled tooling concept). Other items unaffected.
    - External Resources line 177: prose mentions `TetraTile's <code>Tetra5</code> "Opposite Corners"` — rename to `PentaTile's <code>Penta5</code> "Opposite Corners"`. **Caveat:** the `Tetra5` here is internal terminology that's been deprecated by the 5-mode design (`tile_count: TileCountMode FIVE` — see ROADMAP). For accuracy, change to `PentaTile's "Opposite Corners" archetype name.` and drop the obsolete `<code>Tetra5</code>` reference. (Phase 5 will polish this prose; for now, the rename + obsolete-term removal is sufficient.)
    - Anywhere else the literal strings `TetraTile` / `tetra_tile` / `Tetra-System` / `Tetra` (project-name-context) appear — rename uniformly.

    **Do NOT touch in README.md (this task):**
    - The new "What is a Penta tileset?" section content — that's Task 3.
    - External-link href values pointing at non-project resources (e.g., TileMapDual, Excalibur.js, RPG Maker tutorial) — those URLs are unrelated to the rename.
    - The `🍀` and other emojis in headings (preserved).
  </action>
  <verify>
    <automated>
      bash -ec '
      set -e
      ROOT_DOCS="IMPLEMENTATION_PLAN.md RESEARCH.md README.md"
      test -z "$(grep -lF "TetraTile" $ROOT_DOCS 2>/dev/null)"
      test -z "$(grep -lF "tetra_tile" $ROOT_DOCS 2>/dev/null)"
      # Bare Tetra — character class includes letter, digit, or hyphen; catches
      # Tetra5, Tetra-System, TetraBake. The original [A-Za-z]-only form missed
      # digits and hyphens (the actual real-world cases).
      test -z "$(grep -lE "\bTetra[A-Za-z0-9-]" $ROOT_DOCS 2>/dev/null)"
      # Lowercase bare "tetra" — underscore is a word char so tetra_tile does not match
      test -z "$(grep -lE "\btetra\b" $ROOT_DOCS 2>/dev/null)"
      grep -qF "PentaTile" README.md
      grep -qF "addons/penta_tile/templates/penta_horizontal.png" README.md
      grep -qF "## 🎨 The Penta-System Template" README.md
      grep -qF "## ⚔️ PentaTile vs. TileMapDual API" README.md
      '
    </automated>
  </verify>
  <done>
    Root-level IMPLEMENTATION_PLAN.md, RESEARCH.md, and README.md (token rename pass only) use PentaTile/penta_tile consistently. README structure preserved (table of contents, sections, comparison table). The new "What is a Penta tileset?" section is NOT yet added (Task 3 handles that).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add README "What is a Penta tileset?" section with labeled archetype diagram</name>
  <files>
    README.md (insert one new section)
  </files>
  <read_first>
    README.md (current state after Task 2 — confirm token rename completed),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G6 section — concrete analog excerpt + section structure recipe),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md (Codename discipline — what the section MUST include),
    .planning/ROADMAP.md (Phase 2 success criteria 3 + 5 — the actual slot ordering: 0=IsolatedCell, 1=Fill, 2=Border, 3=InnerCorner, 4=OppositeCorners with OuterCorner implicit)
  </read_first>
  <action>
    **Step 1 — Determine insertion point.**
    Insert the new section IMMEDIATELY AFTER `## 🚀 Why PentaTile?` (was `## 🚀 Why TetraTile?`) and BEFORE `## 🧩 Supported Layouts`. This places the codename definition near the top, where new readers encounter it before the layout list.

    Update the Table of Contents accordingly: insert `2. [What is a Penta tileset?](#-what-is-a-penta-tileset)` between `1. [Why PentaTile?]` and the existing `2. [Supported Layouts]` (renumbering the rest of the TOC).

    **Step 2 — Write the new section verbatim** (insert this block):

    ```markdown
    ## 🍀 What is a Penta tileset?

    <img src="addons/penta_tile/templates/penta_horizontal.png" width="256" alt="Penta archetype reference: IsolatedCell (slot 0, source of synthesized OuterCorner) + Fill / Border / InnerCorner / OppositeCorners (slots 1-4)">

    A **Penta tileset** is a 5-archetype autotile format. The five archetypes, **listed in canonical slot order**:

    1. **IsolatedCell** (slot 0) — a tile with all four edges and all four corners exposed; serves as the source for synthesizing OuterCorner.
    2. **Fill** (slot 1) — a tile with all four edges adjacent to the same terrain; the most common interior tile.
    3. **Border** (slot 2) — a tile on a straight terrain edge (one side adjacent to "different terrain").
    4. **InnerCorner** (slot 3) — a tile at the inside of an L-bend (two adjacent sides adjacent to "different terrain").
    5. **OppositeCorners** (slot 4) — a tile with two diagonally-opposite different-terrain corners.

    **OuterCorner** is implicit — synthesized from the corners of slot 0 (IsolatedCell) at load time. It does not occupy a dedicated slot.

    **Synthesis rule:** PentaTile supports a progressive 5-mode authoring scale (ONE through FIVE). Modes ONE through FOUR synthesize the missing archetypes from slot 0; mode FIVE provides all five archetypes hand-authored. Either way, every connectivity state at runtime resolves to one of the five archetypes above.

    **How "Penta" relates to other tileset codenames:**

    | Format | Tiles authored | Slot count | Year coined |
    |--------|---------------|------------|-------------|
    | Wang   | 16 / 64 / 256 | varies     | ~1986       |
    | Blob   | 47            | 47         | ~2010       |
    | Penta  | 1–5           | 5          | 2026        |

    "Penta" is reserved for the 5-archetype format only — never for unrelated 5-tile arrangements. This rule is encoded as a project invariant in `CLAUDE.md` § Coined-Term Discipline.
    ```

    **Step 3 — Update the TOC (lines 7-19 of README).**
    The current TOC is:
    ```
    1. [Why TetraTile?](#-why-tetratile)
    2. [Supported Layouts](#-supported-layouts)
    3. [The Tetra-System Template](#-the-tetra-system-template)
    ...
    ```
    After Task 2's token rename it should already say `Why PentaTile?`/`The Penta-System Template`. Insert the new entry as item 2 and renumber:
    ```
    1. [Why PentaTile?](#-why-pentatile)
    2. [What is a Penta tileset?](#-what-is-a-penta-tileset)
    3. [Supported Layouts](#-supported-layouts)
    4. [The Penta-System Template](#-the-penta-system-template)
    5. [Comparison: PentaTile vs. TileMapDual](#-pentatile-vs-tilemapdual-api)
    ... (continue renumbering through item 12)
    ```

    **Step 4 — Reconcile with the existing "Penta-System Template" section.**
    The section at line 41 (renamed `## 🎨 The Penta-System Template` after Task 2) describes the v0.1 4-tile model. The new "What is a Penta tileset?" section describes the v0.2 5-archetype model. These are not contradictory — v0.1 is the as-shipped state; v0.2 is the active milestone. Leave the existing template section unchanged in its current position; the two sections complement each other (intro definition vs. how-to-use). Phase 5 release polish will reconcile / merge if redundancy emerges.
  </action>
  <verify>
    <automated>
      bash -ec '
      set -e
      grep -qF "## 🍀 What is a Penta tileset?" README.md
      grep -qF "addons/penta_tile/templates/penta_horizontal.png" README.md
      # Canonical slot order — verify each numbered archetype line is present
      # (rewritten as a numbered list per Blocker-4 reconciliation; the inline
      # "0=IsolatedCell, 1=Fill, ..." comma-form was replaced).
      grep -qF "**IsolatedCell** (slot 0)" README.md
      grep -qF "**Fill** (slot 1)" README.md
      grep -qF "**Border** (slot 2)" README.md
      grep -qF "**InnerCorner** (slot 3)" README.md
      grep -qF "**OppositeCorners** (slot 4)" README.md
      grep -qF "**OuterCorner** is implicit" README.md
      grep -qF "[What is a Penta tileset?](#-what-is-a-penta-tileset)" README.md
      # Comparison table — verify the three codenames + 5-archetype framing
      grep -qF "| Penta " README.md
      grep -qF "| Wang " README.md
      grep -qF "| Blob " README.md
      grep -qF "Coined-Term Discipline" README.md
      '
    </automated>
  </verify>
  <done>
    README.md has a new `## 🍀 What is a Penta tileset?` section between Why PentaTile and Supported Layouts. The section names all 5 archetypes, states the canonical slot order, explains OuterCorner synthesis, includes the labeled diagram (img), and has the comparison table vs. Wang and Blob. The TOC includes the new entry and is renumbered. The closing line cross-references CLAUDE.md § Coined-Term Discipline (added by Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 4: Rename CLAUDE.md + add "Coined-Term Discipline" section</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md (full file — 137 lines, 15 occurrences),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-PATTERNS.md (G7 section — analog from "Identity Guardrails" + "Critical Pitfalls" + recommended insertion point),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md (Codename discipline scope — three invariant bullets the section MUST include)
  </read_first>
  <action>
    **Step 1 — Apply token rename across CLAUDE.md.**
    Find every `TetraTile` / `tetra_tile` / `tetra_role` / `tetra_lock_rotation` and replace per the rename map.

    Specific load-bearing edits:
    - Line 1: `# TetraTile — Claude Code Project Guide` → `# PentaTile — Claude Code Project Guide`
    - Line 5: prose mentions `TetraTile is a lightweight dual-grid autotiling addon... TetraTileMapLayer, that subclasses TileMapLayer.` → rename both to `PentaTile`/`PentaTileMapLayer`.
    - Line 7: `(Tetra, DualGrid16, Wang2Edge, Wang2Corner, Min3x3, TBT-decoded Blob/Wang, PixelLab, plus a Single-Tile prototyping layout)` — note `Tetra` here refers to the layout family name. Per CONTEXT.md decision "Native flexible-count layout class is `PentaTileLayoutPenta`," rename `Tetra` → `Penta` in this layout list (the layout is the codename anchor — first item in the list becomes the family name).
    - Line 20: `for per-tile flags like \`tetra_role\` and \`tetra_lock_rotation\`` → `for per-tile flags like \`penta_role\` and \`penta_lock_rotation\``
    - Lines 24-33 (Layout codeblock): `addons/tetra_tile/` → `addons/penta_tile/`; `tetra_tile_map_layer.gd` → `penta_tile_map_layer.gd`; `tetra_tile_template.png` → `penta_tile_template.png`; `tetra_tile_demo.tscn` → `penta_tile_demo.tscn`; `tetra_tile_ground.png/.tres` → `penta_tile_ground.png/.tres`.
    - Line 48: `Contract Skeleton + Tetra Layouts` — this is a Phase 1 description preserved as historical record (it shipped under the old name). Per the CONTEXT.md "no Git history rewriting" principle and the matching `01-contract-skeleton-tetra-layouts/` archived directory, **leave this Phase 1 description as `Tetra Layouts`** (historical accuracy). Phase 2's description on line 49 mentions `Tetra layouts gain load-time synthesis` — rename `Tetra` → `Penta` here (it's forward-looking).
    - Line 70: `The PROJECT.md identity constraint is **"TetraTile must remain visibly smaller and simpler than TileMapDual."**` → `**"PentaTile must remain visibly smaller and simpler than TileMapDual."**`
    - Line 126: `Class names: PascalCase (\`TetraTileMapLayer\`, \`TetraTileAtlasContract\`)` → `Class names: PascalCase (\`PentaTileMapLayer\`, \`PentaTileAtlasContract\`)`
    - Line 132: `File names: snake_case matching class name (\`tetra_tile_map_layer.gd\` → \`TetraTileMapLayer\`)` → `File names: snake_case matching class name (\`penta_tile_map_layer.gd\` → \`PentaTileMapLayer\`)`

    **Step 2 — Insert the new "Coined-Term Discipline" section.**
    Insertion point per PATTERNS.md G7 recommendation: between the "Critical Pitfalls" section (currently lines 110-122) and the "Coding Conventions" section (currently line 124). This places it as a project invariant alongside the other invariants.

    Insert verbatim (use `##` heading to match sibling sections):

    ```markdown
    ## Coined-Term Discipline

    **"Penta" is reserved exclusively for the 5-archetype tileset format used by PentaTile.** This is a project invariant — never use "Penta" for anything else, or the term loses descriptive meaning.

    - Use **PentaTile** for the project name, public class prefixes (`PentaTileMapLayer`, `PentaTileAtlasContract`, `PentaTileLayout*`), file/folder names (`addons/penta_tile/`, `penta_tile_map_layer.gd`), the plugin id, and demo scene names. The project happens to share its name with its native layout family — that coincidence is load-bearing for the codename to land.
    - Use **Penta** as a generic codename in phrases like "a Penta tileset," "the Penta layout family," "Penta archetypes," "Penta slot order." Never coin "Penta" prefixes for unrelated subsystems (e.g., do NOT introduce `PentaCache`, `PentaDecoder`, `PentaToolkit` for things that aren't the 5-archetype format).
    - The labeled archetype diagram in `README.md` § What is a Penta tileset? is **load-bearing** for codename propagation — without the picture-with-named-archetypes, the codename cannot spread (the Boris-the-Brave precedent for "Blob": classification stuck because the diagram was canonical).
    - When introducing the term to a new reader (docs, CHANGELOG, release notes), link or excerpt from the canonical README definition rather than re-explaining inline. Single source of truth keeps the term stable.

    Canonical "What is a Penta tileset?" definition lives in `README.md`.
    ```

    **Step 3 — Update the "Next Step" section (line 134 area).**
    The line `Run /gsd-progress to see current position, or jump into Phase 1 with /gsd-discuss-phase 1 (recommended) or /gsd-plan-phase 1.` is now outdated — Phase 1 is complete and Phase 1.1 is in progress. Update to: `Run /gsd-progress to see current position. Phase 1.1 (this rename) is in progress; Phase 2 is the next major work.`

    **Step 4 — Verify CLAUDE.md `##` heading depth uniformity** (PATTERNS.md G7 risk note). The new section MUST use `##` not `###`. Confirm by counting: pre-existing `##` sections are Project / Stack / Layout / GSD Workflow / Identity Guardrails / Quality Bar / Breaking Changes Policy / Critical Pitfalls / Coding Conventions / Next Step → 10 sections. After insert: 11 sections, all `##`.
  </action>
  <verify>
    <automated>
      bash -ec '
      set -e
      # === Negative gates: zero stale refs ===
      ! grep -qF "TetraTile" CLAUDE.md
      ! grep -qF "tetra_tile" CLAUDE.md
      ! grep -qF "tetra_role" CLAUDE.md
      ! grep -qF "tetra_lock_rotation" CLAUDE.md
      # Bare Tetra — character class includes letter, digit, or hyphen; catches
      # Tetra5, Tetra-System, TetraBake. NOTE: line 48 deliberately retains
      # "Tetra Layouts" as historical record — a space follows Tetra so
      # [A-Za-z0-9-] does NOT match (preservation is safe).
      ! grep -qE "\bTetra[A-Za-z0-9-]" CLAUDE.md
      # Lowercase bare "tetra" — underscore is a word char, so tetra_tile does
      # not match. Verify CLAUDE.md does not contain a standalone lowercase
      # "tetra" word after rename (the historical "Tetra Layouts" line uses a
      # capital T and is not affected).
      ! grep -qE "\btetra\b" CLAUDE.md
      # === Positive gates: new content present ===
      grep -qF "# PentaTile — Claude Code Project Guide" CLAUDE.md
      grep -qF "## Coined-Term Discipline" CLAUDE.md
      grep -qF "Penta\" is reserved exclusively for the 5-archetype tileset format" CLAUDE.md
      grep -qF "Boris-the-Brave precedent" CLAUDE.md
      grep -qF "Canonical \"What is a Penta tileset?\" definition lives in \`README.md\`" CLAUDE.md
      grep -qF "PentaTileMapLayer" CLAUDE.md
      # Informational: expected ≥ 11 top-level ## sections
      test "$(grep -cE "^## " CLAUDE.md)" -ge 11
      '
    </automated>
  </verify>
  <done>
    CLAUDE.md is renamed throughout (zero TetraTile/tetra_tile/tetra_role/tetra_lock_rotation refs remain). New `## Coined-Term Discipline` section sits between Critical Pitfalls and Coding Conventions, contains the three Penta-reserve invariants, references the Boris-the-Brave precedent, and closes with the canonical README pointer.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create CHANGELOG.md (or append entry) documenting v0.2 rename as breaking change</name>
  <files>
    CHANGELOG.md (create if absent; otherwise append v0.2 entry at top)
  </files>
  <read_first>
    CHANGELOG.md (read if exists; if absent, the file will be created),
    CLAUDE.md (Breaking Changes Policy section — sets the tone: CHANGELOG is the only acceptable compat work),
    .planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-CONTEXT.md (Scope (in) — full rename scope to enumerate)
  </read_first>
  <action>
    **Step 1 — Check whether CHANGELOG.md already exists.**
    Use Read on `CHANGELOG.md`. If it does not exist, create it. If it exists, prepend the v0.2 entry above any existing entries (Keep a Changelog convention: most recent at top).

    **Step 2 — Write the CHANGELOG entry.**
    Use Keep-a-Changelog format. The CHANGELOG starts with a header and then the unreleased / latest version's changes.

    Insert verbatim (creating the file if absent):

    ```markdown
    # Changelog

    All notable changes to PentaTile (formerly TetraTile) are documented in this file.
    Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). Pre-1.0 releases are allowed to ship breaking changes per the project's [Breaking Changes Policy](CLAUDE.md#breaking-changes-policy-hard-rule).

    ## [Unreleased] — v0.2 in progress

    ### BREAKING — Project rename: TetraTile → PentaTile

    **All TetraTile* identifiers and tetra_tile* paths have been renamed to PentaTile* / penta_tile*.** No backwards-compatibility shims are provided. Any project depending on v0.1.x's API must rename references in lockstep.

    Renamed surface (per `.planning/phases/01.1-pentatile-rename-penta-codename-establishment/`):

    - **GDScript classes:** `TetraTileMapLayer` → `PentaTileMapLayer`, `TetraTileAtlasContract` → `PentaTileAtlasContract`, `TetraTileAtlasSlot` → `PentaTileAtlasSlot`, `TetraTileLayout` → `PentaTileLayout`, `TetraTileLayoutTetraHorizontal` → `PentaTileLayoutPentaHorizontal`, `TetraTileLayoutTetraVertical` → `PentaTileLayoutPentaVertical`.
    - **Folder + files:** `addons/tetra_tile/` → `addons/penta_tile/`. All filenames inside (`tetra_tile_map_layer.gd`, `tetra_tile_demo.tscn`, etc.) renamed to `penta_tile_*` equivalents.
    - **Saved Godot resources:** all `.tscn` / `.tres` ext_resource paths and `script_class` strings rebound. `default_horizontal.tres` and `default_vertical.tres` filenames stayed; `tetra_horizontal_default.tres` / `tetra_vertical_default.tres` renamed to `penta_horizontal_default.tres` / `penta_vertical_default.tres`.
    - **Engine config:** `project.godot` `config/name="PentaTile"`. `run/main_scene` points at the renamed demo path.
    - **Custom data layer keys:** `tetra_role` → `penta_role`, `tetra_lock_rotation` → `penta_lock_rotation` (referenced in docs only at this milestone — no `.tres` content yet uses these keys).
    - **Plugin manifest:** `addons/penta_tile/plugin.cfg` `name="PentaTile"`.
    - **GitHub repo + local clone + Claude memory directory:** renamed in lockstep (see Phase 1.1 SUMMARY).

    ### Added — Penta codename anchors

    - `README.md` § What is a Penta tileset? — canonical 5-archetype definition with labeled diagram and comparison vs. Wang and Blob.
    - `CLAUDE.md` § Coined-Term Discipline — project invariant reserving "Penta" exclusively for the 5-archetype tileset format.
    - This `CHANGELOG.md` itself.

    ### Migration notes for v0.1.x consumers

    Per the project's no-backwards-compat policy, no migration shims are shipped. Consumers must:

    1. Rename every `TetraTile*` reference in their project to `PentaTile*` (class names, NodePath strings, file paths).
    2. Rebind any saved `.tscn` / `.tres` files that referenced `addons/tetra_tile/` paths or `TetraTile*` script_class names.
    3. Update any `git remote` URLs pointing at the old GitHub repo (GitHub redirects are best-effort; explicit `git remote set-url` is recommended).

    The Phase 1.1 SUMMARY documents the exact set of changes and the verification procedure.
    ```

    **Step 3 — If CHANGELOG.md already exists with prior content,** prepend the new entry above the existing first version-section while preserving the existing header. Do not lose any prior entries.
  </action>
  <verify>
    <automated>
      test -f CHANGELOG.md &&
      grep -F "## [Unreleased] — v0.2 in progress" CHANGELOG.md &&
      grep -F "BREAKING — Project rename: TetraTile → PentaTile" CHANGELOG.md &&
      grep -F "PentaTileMapLayer" CHANGELOG.md &&
      grep -F "addons/penta_tile/" CHANGELOG.md &&
      grep -F "Penta codename anchors" CHANGELOG.md &&
      grep -F "no-backwards-compat policy" CHANGELOG.md
    </automated>
  </verify>
  <done>
    CHANGELOG.md exists at repo root with a v0.2 in-progress entry that explicitly documents the TetraTile → PentaTile rename as a breaking change, enumerates the renamed surface (classes, files, paths, custom data layer keys, plugin manifest, repo/clone/memory), and provides migration notes pointing at the no-backwards-compat policy. The entry references the CLAUDE.md Breaking Changes Policy section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation surface → human reader (codename adoption) | If the docs are inconsistent, the codename fails to propagate; "Penta" stays a name rather than becoming a coined term |
| REQUIREMENTS.md ↔ ROADMAP.md cross-references | If TETRA-* IDs rename in REQUIREMENTS.md but not in ROADMAP.md (or vice versa), traceability breaks; future verification phases can't map plans to requirements |
| README definition section ↔ archetype diagram image | If the README references `addons/penta_tile/templates/penta_horizontal.png` but Plan 01 didn't rename that file (or vice versa), the image is broken — codename diagram is "load-bearing per CONTEXT.md," so a 404 image breaks codename propagation |
| Archived Phase 1 docs ↔ active Phase 2 docs | Over-replacement risk: if the sweep over-replaces into the archived dir, historical record is corrupted (commits referencing those files become inaccurate retrospectively) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06 | Tampering (historical-record corruption) | Archived `.planning/phases/01-*/` and `.planning/spikes/001..003/` directories over-swept | mitigate | Task 1 explicitly excludes these paths from the sweep AND has an automated post-sweep grep asserting `TetraTile` references are STILL present in those archived paths (proves the exclude worked). |
| T-01.1-07 | Tampering (cross-reference drift) | TETRA-* IDs renamed in REQUIREMENTS.md but stale in ROADMAP.md (or vice versa) | mitigate | Task 1 verify clause grep-asserts `! grep TETRA-` in BOTH files simultaneously, ensuring lockstep rename. |
| T-01.1-08 | Information disclosure (broken codename diagram) | README image src points at a path Plan 01 didn't rename | mitigate | Task 3 verify clause grep-asserts the literal string `addons/penta_tile/templates/penta_horizontal.png` exists in README. Plan 01 Task 2 separately renames the .png file. Cross-plan dependency captured in `depends_on: ["01.1-01"]`. |
| T-01.1-09 | Repudiation (Coined-Term Discipline lacks normative force) | If the discipline section is buried, contributors don't internalize it | accept | The `##`-level section between Critical Pitfalls and Coding Conventions is a structurally prominent slot. Future contributors reading CLAUDE.md top-to-bottom encounter it before they author code. Sufficient for solo-developer + Claude workflow. |
| T-01.1-10 | Tampering (CHANGELOG omission) | If the rename is unrecorded, the breakage is undocumented for v0.2 consumers | mitigate | Task 5 creates/appends to CHANGELOG.md with an explicit BREAKING entry. Task 5 verify asserts the file exists and contains the rename narrative. |
</threat_model>

<verification>
After all five tasks complete:

1. `! grep -rln "TetraTile\|tetra_tile" .planning/codebase/ .planning/research/ .planning/phases/02-native-layouts/ .planning/phases/01.1-*/ .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md README.md CLAUDE.md IMPLEMENTATION_PLAN.md RESEARCH.md` (all non-archived scope clean).
2. `grep -rln "TetraTile" .planning/phases/01-contract-skeleton-tetra-layouts/ .planning/spikes/` returns >0 (history preserved).
3. `! grep -F "TETRA-" .planning/REQUIREMENTS.md .planning/ROADMAP.md` (requirement IDs renamed).
4. `grep -F "## 🍀 What is a Penta tileset?" README.md` (codename section landed).
5. `grep -F "## Coined-Term Discipline" CLAUDE.md` (project invariant landed).
6. `test -f CHANGELOG.md && grep -F "BREAKING — Project rename" CHANGELOG.md` (CHANGELOG entry landed).
7. README still parses as valid Markdown (visual spot check via Read).
</verification>

<success_criteria>
- All non-archived project documentation uses PentaTile / penta_tile / Penta / PENTA- consistently
- Archived Phase 1 plan docs and spikes 001-003 retain TetraTile references unchanged (historical-record discipline)
- README.md has the canonical "What is a Penta tileset?" section with labeled archetype diagram
- CLAUDE.md has the "Coined-Term Discipline" section establishing Penta as the 5-archetype codename
- CHANGELOG.md documents the rename as a v0.2 breaking change with migration notes
- Requirement IDs (TETRA-* / TETRA-SYNTH-*) renamed in lockstep across REQUIREMENTS.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-pentatile-rename-penta-codename-establishment/01.1-02-SUMMARY.md` with:
- Files swept (count + paths) — break out by category (.planning/codebase, .planning/research, .planning/phases/02-*, root .md, CLAUDE.md, README.md)
- Token-rename summary (e.g., "TETRA-01..03 renamed to PENTA-01..03; TETRA-SYNTH-01..12 renamed to PENTA-SYNTH-01..12 in REQUIREMENTS.md and ROADMAP.md cross-refs")
- Confirmation that protected scope (archived Phase 1 + spikes) was untouched (grep verified)
- New section snippets (README "What is a Penta tileset?" + CLAUDE.md "Coined-Term Discipline") — quote first 5 lines of each as a record
- CHANGELOG entry confirmed
- Carryover notes for Plan 03 (repo + git + memory rename + final verification)
</output>
</content>
</invoke>