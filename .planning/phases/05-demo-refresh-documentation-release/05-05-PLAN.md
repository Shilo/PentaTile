---
phase: 05-demo-refresh-documentation-release
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-03, DOC-01, DOC-02, DOC-03, DOC-04, REL-01, REL-02, REL-03]  # Plan E asserts all 10 v1 reqs satisfied (DEMO/DOC by Plans 01/02; REL by Plan 04 workflow side-effects); Traceability flips happen here
must_haves:
  truths:
    - "GitHub Actions 'Release' workflow runs once successfully against main; produces a v<NEW_VERSION> Release with the addon zip attached"
    - "Release zip downloads + extracts to a fresh test directory; structure matches addons/penta_tile/<everything tracked at the tag>"
    - "Release body == CHANGELOG slice for the just-rewritten [<NEW_VERSION>] block (D-05-18)"
    - "REQUIREMENTS.md Traceability rows for DEMO-01..03, DOC-01..04, REL-01..03 flip to Complete with phase + commit references"
    - "ROADMAP.md Phase 5 row flips to [x] with completion date"
    - "STATE.md milestone line reads 'v0.2.0 SHIPPED' (or whatever the workflow auto-bumped to)"
    - "Identity guardrail AT RISK carry-forward note from Phase 4 is resolved per Plan C audit decision (recorded in STATE.md)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Final v0.2.0 milestone state"
      contains: "v0.2.0 SHIPPED"
  key_links:
    - from: ".planning/REQUIREMENTS.md Traceability"
      to: "GitHub Release page"
      via: "REL-01..03 status flips referencing the workflow run"
      pattern: "Complete \\(workflow run"
---

<objective>
Execute the v0.2.0 release workflow once, verify the published Release page is correct, then flip the planning artifacts (REQUIREMENTS Traceability + ROADMAP + STATE) to reflect Phase 5 + v0.2.0 milestone completion.

Purpose: Phase-close gate. The workflow run IS the formalization of `/gsd-verify-work` for REL-01..03 per D-05-13/D-05-14. Plans A, B, C, D shipped infrastructure; Plan E activates it.

Output: A published GitHub Release at `v<NEW_VERSION>` with the zip attached, plus three planning files updated. No new code; no new test files; no new doc sections. This is the milestone bow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-demo-refresh-documentation-release/05-CONTEXT.md
@.planning/phases/05-demo-refresh-documentation-release/05-VALIDATION.md
@.planning/phases/05-demo-refresh-documentation-release/05-01-SUMMARY.md
@.planning/phases/05-demo-refresh-documentation-release/05-02-SUMMARY.md
@.planning/phases/05-demo-refresh-documentation-release/05-03-SUMMARY.md
@.planning/phases/05-demo-refresh-documentation-release/05-04-SUMMARY.md
@.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md
@CHANGELOG.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger the release workflow via GitHub UI (or `gh workflow run release.yml`); confirm Release page exists with attached zip</name>
  <files>(none in this task - Plan E Task 1 is a workflow trigger, not a file edit; Tasks 2/3/4/5 produce file edits)</files>
  <action>This is a checkpoint:human-action task - see <what-built> / <how-to-verify> / <resume-signal> below for the verification surface. Executor pauses for the human to click 'Run workflow' in the GitHub UI (or invoke 'gh workflow run release.yml --ref main') and confirm the published Release page is correct before Tasks 2-5 flip the planning artifacts.</action>
  <verify>Verify the published GitHub Release per <how-to-verify> Step 5; verify zip extracts cleanly per Step 6; confirm plugin.cfg + tag match per Step 7. No automated CI check beyond the workflow's own internal stderr-grep failure detector.</verify>
  <done>Human responds via <resume-signal> with 'released - v<X.Y.Z>' to proceed to Task 2; or 'workflow failed at step <N>: <error>' to roll back this task.</done>
  <what-built>
    Plans A, B, C, D shipped:
    - Plan A: 8-instance demo scene + retired player/ground/4 orphan .tres + hover-target painter
    - Plan B: 4 README sections + accumulated CHANGELOG + 14 spec corrections
    - Plan C: identity audit (`05-LOC-AUDIT.md`) + README "Identity & Footprint" summary; D-05-11 ship/extract decision recorded
    - Plan D: `.github/workflows/release.yml` + `addons/penta_tile/tests/run_tests.sh`
    - All 18 tests still green throughout

    Plan E activates the workflow.
  </what-built>
  <how-to-verify>
    **PREREQUISITE: Plan C decision must be SHIP.**
    - Read `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` § "Decision per D-05-11"
    - If Outcome is **EXTRACT+OPTIMIZE** or **FIX-BEFORE-SHIP**: STOP. Do not run the workflow. Address the action items first (likely a Plan F).
    - If Outcome is **SHIP**: proceed.

    **Step 1 — Final pre-flight check (local Windows, ~2 min):**

    ```powershell
    # Run the full local test suite one last time:
    pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause -Test all
    ```

    Expected: `ALL GREEN (18 tests)`. If any test fails, STOP and fix before triggering the workflow — CI will fail at the same step and you'll have wasted a workflow run + a half-baked release commit.

    **Step 2 — Verify all Plan A/B/C/D commits are pushed to main:**

    ```bash
    git status
    git log --oneline origin/main..HEAD
    ```

    Expected: working tree clean (or only untracked files), `git log` shows zero commits ahead of origin/main. If there are unpushed commits, push them first (`git push origin main`) — the workflow runs against the remote main.

    **Step 3 — Trigger the workflow:**

    Two equivalent paths. Pick one:

    **Option A — GitHub UI (the user-experience the D-05-15 hard rule targets):**
    1. Open `https://github.com/<org>/PentaTile/actions/workflows/release.yml` in a browser
    2. Click "Run workflow" button (top-right)
    3. Confirm "Use workflow from: Branch: main" is selected
    4. Click "Run workflow" (green button)
    5. Wait for the workflow to start (refresh page if needed)

    **Option B — GitHub CLI from a terminal:**
    ```bash
    gh workflow run release.yml --ref main
    ```

    **Step 4 — Watch the workflow run (~5-7 minutes total):**

    ```bash
    # Get the most recent run id:
    gh run list --workflow=release.yml --limit 1
    # Watch live (press Ctrl+C to detach; the run continues server-side):
    gh run watch <run-id>
    ```

    Expected: the workflow proceeds through all 11 steps from D-05-17 — checkout, configure git, compute version (e.g., 0.1.0 → 0.2.0), download Godot, headless import, run 18-test suite, headless demo open, bump plugin.cfg, rewrite CHANGELOG header, commit/tag/push, build zip, extract release notes, publish Release. Total runtime: ~5-7 minutes (Godot binary download + 18-test suite are the long steps).

    **If the workflow fails at any step:**
    - **Step "Compute next version":** plugin.cfg version field malformed — verify locally with `grep -E '^version' addons/penta_tile/plugin.cfg`.
    - **Step "Headless project import":** an `ERROR:` or `SCRIPT ERROR:` line in stderr — likely an unimported asset or a script error. Reproduce locally: `<godot> --headless --path . --import --quit-after 2`.
    - **Step "Run 18-test suite":** any test failed — open the workflow log, find the FAIL line, reproduce locally with `run_tests.ps1 -Test <failing>`.
    - **Step "Headless-open demo scene":** the demo scene has an error on load — Plan A Task 4 should have caught this; verify the scene path and the new spatial-grid scene file structure.
    - **Step "Bump plugin.cfg":** sanity-check grep failed — the sed regex didn't preserve quotes. (Should not happen given Pitfall #7 mitigation; if it does, fix the YAML.)
    - **Step "Rewrite CHANGELOG header":** the `[Unreleased]` heading was missing OR the rewrite produced a malformed line — Plan B Task 3 should have left the heading verbatim; verify and re-trigger.
    - **Step "Commit, tag, push":** `permissions: contents: write` denied OR the remote moved. Check the workflow's Permissions section; check `git log origin/main` to see if someone pushed during the workflow run.
    - **Step "Publish GitHub Release":** `softprops/action-gh-release@v3` API failure. Re-run the workflow — usually transient.

    For ANY failure: read the failure message, fix root cause locally (don't patch the workflow YAML for a one-off), commit, push, retrigger the workflow.

    **Step 5 — Verify the published Release:**

    ```bash
    # Replace 0.2.0 below with whatever the workflow actually bumped to:
    gh release view v0.2.0
    ```

    Expected output:
    - Title: `PentaTile v0.2.0`
    - Tag: `v0.2.0`
    - Body: matches the CHANGELOG `[0.2.0] — <date>` slice (D-05-18)
    - Assets: `penta_tile-v0.2.0.zip` (one file)
    - Status: published (not draft)

    Then download and verify:

    ```bash
    mkdir /tmp/penta-release-verify && cd /tmp/penta-release-verify
    gh release download v0.2.0
    unzip -l penta_tile-v0.2.0.zip | head -30
    ```

    Expected:
    - Top-level prefix `penta_tile-v0.2.0/` (per `--prefix=` in the workflow)
    - Inside: `penta_tile-v0.2.0/addons/penta_tile/` with all addon files (plugin.cfg, *.gd, layouts/, demo/, tests/, ATTRIBUTION.md is NOT present per D-72/D-73)
    - NO `.godot/` directory (gitignored)
    - NO `.planning/` directory (excluded by pathspec)

    **Step 6 — Verify the bumped plugin.cfg matches the released tag:**

    ```bash
    git pull origin main
    grep -E '^version' addons/penta_tile/plugin.cfg
    git log -1 --format="%H %s"
    git tag -l "v$(grep -E '^version' addons/penta_tile/plugin.cfg | sed -E 's/^version\s*=\s*"?([^"]+)"?$/\1/')"
    ```

    Expected:
    - plugin.cfg version matches the released tag (e.g., `0.2.0` after a 0.1.0 → 0.2.0 bump)
    - Most recent commit message is `chore(release): v0.2.0`
    - The tag `v0.2.0` exists locally after `git pull`

    **Step 7 — Decide:**
    - **All checks pass:** respond "released — proceed to Task 2 traceability flip"
    - **Workflow ran but Release page is wrong:** describe what's wrong (missing asset? truncated body? wrong tag?) and how to fix
    - **Workflow failed:** describe which step + the error message
  </how-to-verify>
  <resume-signal>
    Type "released — v<X.Y.Z>" (replacing `<X.Y.Z>` with the actual auto-bumped version, e.g. "released — v0.2.0") to proceed to Task 2.
    Type "workflow failed at step <N>: <error>" to roll back this task and iterate on the offending plan.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip REQUIREMENTS.md Traceability rows for DEMO-01..03 + DOC-01..04 + REL-01..03 to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate the Traceability table; current rows for DEMO-01..03 / DOC-01..04 / REL-01..03 say "Pending")
    - .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md (Task 1 outputs the released-version metadata; no direct edit needed)
    - Recent git log (need the actual workflow-bumped version + release tag for the Status text)
    - .planning/phases/05-demo-refresh-documentation-release/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md, 05-04-SUMMARY.md (commit SHAs for each plan's atomic commit; cite in Status text)
  </read_first>
  <action>
    Determine the actual released version (from `git tag -l` or `gh release view --json tagName`) — call it `<RELEASED_VERSION>` (likely `0.2.0`). Determine the workflow's release commit SHA — call it `<RELEASE_SHA>`.

    Use the Edit tool to flip 10 rows in the Traceability table at the bottom of `.planning/REQUIREMENTS.md`:

    | Req ID | Old Status | New Status |
    |--------|-----------|-----------|
    | DEMO-01 | `Pending` | `Complete (Plan 05-01 / commit <SHA-A>: penta_tile_demo.tscn rewritten as 8-instance spatial-grid showcase; per-layout Label nodes; visual UAT approved)` |
    | DEMO-02 | `Pending` | `Complete (Plan 05-01 / commit <SHA-A>: every PentaTileMapLayer instance has tile_set = null; layout-bound bundled fallback engages get_fallback_tile_set() per Phase 4 PREVIEW-03 routing)` |
    | DEMO-03 | `Pending` | `Complete (Plan 05-01 / commit <SHA-A>: demo_runtime_painter.gd rewritten with hover-target detection across the 8-instance grid; manual UAT confirmed cross-instance painting + erasing)` |
    | DOC-01 | `Pending` | `Complete (Plan 05-02 / commit <SHA-B>: README "Layouts" section ships an 8-row table per SC-A reframe — 5 Phase 2 + 1 Phase 3 + 2 Phase 3.5; Tilesetter pair stays deferred to v0.3+ per D-86 b)` |
    | DOC-02 | `Pending` | `Complete (Plan 05-02 / commit <SHA-B>: README "Upgrading from 0.1.x" section enumerates the v0.1 → v0.2 surface migrations with cross-link to CHANGELOG)` |
    | DOC-03 | `Pending` | `Complete (Plan 05-02 / commit <SHA-B>: README "Authoring a Custom Layout" section ships with a minimal `@experimental`-marked subclass example per Phase 4 doc-comment sweep)` |
    | DOC-04 | `Pending` | `Complete (Plan 05-02 / commit <SHA-B>: CHANGELOG [Unreleased] block accumulated Phase 3 + 3.5 + 4 + 5 deltas; release workflow rewrote the header to `[<RELEASED_VERSION>] — <date>` per D-05-17 step 4 in commit <RELEASE_SHA>)` |
    | REL-01 | `Pending` | `Complete (Plan 05-04 + 05-05 / workflow run <run-id>: plugin.cfg version bumped 0.1.0 → <RELEASED_VERSION> by the release workflow per D-05-16 auto-increment rule; release commit <RELEASE_SHA>)` |
    | REL-02 | `Pending` | `Complete (Plan 05-04 + 05-05 / workflow run <run-id>: git tag v<RELEASED_VERSION> cut on release commit <RELEASE_SHA>)` |
    | REL-03 | `Pending` | `Complete (Plan 05-04 + 05-05 / workflow run <run-id>: penta_tile-v<RELEASED_VERSION>.zip published to the v<RELEASED_VERSION> Release; addons/penta_tile/ at archive root with bundled bitmask PNGs; per SC-B, NO ATTRIBUTION.md ships)` |

    Each Edit replaces a single Traceability table row. Anchor the `old_string` on the unique `| <REQ_ID> | <phase> | Pending |` form.

    Example for DEMO-01:

    `old_string`:
    ```
    | DEMO-01 | 5 | Pending |
    ```

    `new_string`:
    ```
    | DEMO-01 | 5 | Complete (Plan 05-01 / commit <SHA-A>: penta_tile_demo.tscn rewritten as 8-instance spatial-grid showcase; per-layout Label nodes; visual UAT approved) |
    ```

    Repeat for the other 9 rows. Use the actual SHAs and version from Task 1's verification.

    Also update the **Coverage** section at the bottom of REQUIREMENTS.md to reflect the new state. Find:

    `old_string` (the existing coverage line — currently mentions Phase 4 closeout):
    ```
    - 2026-04-29 Phase 3.5 closeout (Plan 06): PIXLAB-01..04 flipped to Complete. VAR-PIXEL-01 explicitly stays in v2 Requirements per D-89/D-90/D-91 — bank-pick wiring deferred until variation work reopens (design-coupled with VAR-01 + MULTITERR-01). The phase title "+ Variation-Seed Wiring" was a misnomer in v0.2 — no `variation_seed` property is exposed; layouts ship with first-cell pick only. Coverage counter unchanged: 58 v1 reqs (PIXLAB-01..04 were already counted as v1; the change is Status not membership).
    ```

    Append AFTER it (NOT replace):

    `new_string`:
    ```
    - 2026-04-29 Phase 3.5 closeout (Plan 06): PIXLAB-01..04 flipped to Complete. VAR-PIXEL-01 explicitly stays in v2 Requirements per D-89/D-90/D-91 — bank-pick wiring deferred until variation work reopens (design-coupled with VAR-01 + MULTITERR-01). The phase title "+ Variation-Seed Wiring" was a misnomer in v0.2 — no `variation_seed` property is exposed; layouts ship with first-cell pick only. Coverage counter unchanged: 58 v1 reqs (PIXLAB-01..04 were already counted as v1; the change is Status not membership).
    - <date>: Phase 5 closeout. DEMO-01..03 + DOC-01..04 + REL-01..03 flipped to Complete. v<RELEASED_VERSION> published via release workflow run <run-id>; commit <RELEASE_SHA>; git tag `v<RELEASED_VERSION>`. v0.2.0 milestone shipped end-to-end. Coverage: 58 / 58 v1 requirements satisfied (zero pending). Per SC-A reframe, demo + docs cover 8 actually-shipped layouts (Tilesetter pair stays deferred to v0.3+ per D-86 b). Per SC-B, no ATTRIBUTION.md ships (TileBitTools acknowledgment lives in README "External Resources" footnote per DOC-05). Per SC-C, identity is hot-path minimalism + anti-pattern absence (D-05-11), audit at `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` with summary in README "Identity & Footprint". Per SC-D, REL-01 ownership flipped from "manual commit" to "workflow side-effect" (D-05-16).
    ```

    The Coverage row "Mapped to phases: 58 / Unmapped: 0" should still be accurate.
  </action>
  <verify>
    <automated>
      # All 10 reqs flipped from Pending to Complete:
      [ "$(grep -cE '^\| (DEMO-0[123]|DOC-0[1234]|REL-0[123]) \| 5 \| Pending \|$' .planning/REQUIREMENTS.md)" -eq 0 ] &amp;&amp; \
      [ "$(grep -cE '^\| (DEMO-0[123]|DOC-0[1234]|REL-0[123]) \| 5 \| Complete' .planning/REQUIREMENTS.md)" -eq 10 ] &amp;&amp; \
      # Coverage section has the new closeout entry:
      grep -q "Phase 5 closeout" .planning/REQUIREMENTS.md &amp;&amp; \
      grep -q "milestone shipped end-to-end" .planning/REQUIREMENTS.md &amp;&amp; \
      # Old "v2 backlog" / Out-of-Scope sections untouched (no accidental edits):
      grep -q "## v2 Requirements" .planning/REQUIREMENTS.md &amp;&amp; \
      grep -q "## Out of Scope" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 0 rows in Traceability with `| <REQ> | 5 | Pending |` pattern for any of DEMO-01..03 / DOC-01..04 / REL-01..03
    - 10 rows in Traceability with `| <REQ> | 5 | Complete (...)` for those same IDs
    - Each Complete row references the appropriate Plan-N-M and a real commit SHA (placeholder `<SHA-A>` / `<SHA-B>` / `<RELEASE_SHA>` text replaced with real values from `git log` and `gh run view`)
    - Coverage section has a new entry dated <today> mentioning "Phase 5 closeout" and "milestone shipped end-to-end"
    - File preserves `## v2 Requirements` and `## Out of Scope` headings (greps return success)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md Traceability fully flipped. 58/58 v1 requirements satisfied. Coverage entry records the milestone ship.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip ROADMAP Phase 5 row to [x]; update Progress table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 5 entry around line 29 — currently `[ ]`; find Progress table at bottom)
    - Recent git log (for the actual workflow-bumped version + release commit SHA)
  </read_first>
  <action>
    Two grep-anchored edits.

    **Edit 1: Flip the Phase 5 row in the top-of-file Phases bullet list.**

    `old_string` (the actual current line — note that Plan B already updated this from "10 layouts" to "8 actually-shipped layouts"):
    ```
    - [ ] **Phase 5: Demo Refresh + Documentation + Release** — One updated demo scene showcasing all 8 actually-shipped layouts (5 Phase 2 + 1 Phase 3 + 2 Phase 3.5; Tilesetter pair stays deferred to v0.3+ per D-86 b), README sections (Layouts / Upgrading / Authoring a Custom Layout / Identity & Footprint), CHANGELOG, plugin.cfg bump, GitHub Release zip with `v0.2.0` tag.
    ```

    Note: Plan B's edit may not have touched this exact line (Plan B's SC-A edits anchored on different occurrences). Verify by grep first; if the existing text still says "10 layouts", flip after Plan B's edit.

    `new_string`:
    ```
    - [x] **Phase 5: Demo Refresh + Documentation + Release** — Closed <date>. v<RELEASED_VERSION> shipped via release workflow run <run-id>. 8-instance demo grid (every instance uses bundled fallback per DEMO-02); README extensions (Layouts / Upgrading / Authoring a Custom Layout / Identity & Footprint); accumulated CHANGELOG; auto-bumped plugin.cfg + git tag `v<RELEASED_VERSION>` + GitHub Release zip published. 58 / 58 v1 requirements satisfied. Identity audit per D-05-11: `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` outcome <SHIP / EXTRACT+OPTIMIZE-then-shipped / FIX-then-shipped>.
    ```

    **Edit 2: Update the Progress table row for Phase 5.**

    `old_string`:
    ```
    | 5. Demo Refresh + Documentation + Release | 0/TBD | Not started | - |
    ```

    `new_string`:
    ```
    | 5. Demo Refresh + Documentation + Release | 5/5 | **Complete.** v<RELEASED_VERSION> shipped end-to-end via the release workflow on <date>. 8-instance demo grid + 4 README sections + accumulated CHANGELOG + 14 spec corrections (SC-A..D) + identity audit (per D-05-11 — `<SHIP/EXTRACT/FIX>` outcome). 18 automated tests green; release workflow run <run-id> published `penta_tile-v<RELEASED_VERSION>.zip` to GitHub Releases. | <date> |
    ```

    Replace placeholders (`<date>`, `<RELEASED_VERSION>`, `<run-id>`, `<SHIP/EXTRACT/FIX>`) with actual values from Task 1's verification + Plan C's audit decision.

    Note about ROADMAP Phase 5 Plans subsection: Plan B did not touch the "**Plans**: TBD" line at the bottom of the Phase 5 description. Update that too:

    Find:
    ```
    **Plans**: TBD
    ```

    Replace with:
    ```
    **Plans**: 5 plans (5/5 complete)

    Plans:
    - [x] 05-01-PLAN.md — Demo refresh: 8-instance spatial-grid scene + hover-target painter + retired demo_player + authored ground + 4 legacy .tres orphans (DEMO-01..03)
    - [x] 05-02-PLAN.md — Documentation: 4 new README sections + accumulated CHANGELOG + 14 spec corrections (DOC-01..04 + SC-A..D)
    - [x] 05-03-PLAN.md — Identity audit: TileMapDual v5.0.2 comparison; 3-axis measurement; D-05-11 ship/extract decision; README "Identity & Footprint" summary (manual prerequisite per D-05-13)
    - [x] 05-04-PLAN.md — Release infrastructure: `.github/workflows/release.yml` + `addons/penta_tile/tests/run_tests.sh` (REL-01..03 enabled)
    - [x] 05-05-PLAN.md — Closeout: workflow run + Traceability flips + ROADMAP / STATE updates (REL-01..03 satisfied)
    ```

    Be careful with the (possibly multiple) "Plans: TBD" anchors — if RECTangler or PROJECT.md also contains the literal string "Plans: TBD", scope the Edit to ROADMAP.md only.
  </action>
  <verify>
    <automated>
      # Phase 5 top-of-file row flipped to [x]:
      grep -qE '^\- \[x\] \*\*Phase 5: Demo Refresh \+ Documentation \+ Release\*\*' .planning/ROADMAP.md &amp;&amp; \
      ! grep -qE '^\- \[ \] \*\*Phase 5: Demo Refresh' .planning/ROADMAP.md &amp;&amp; \
      # Progress table row flipped:
      grep -qE '\| 5\. Demo Refresh \+ Documentation \+ Release \| 5/5' .planning/ROADMAP.md &amp;&amp; \
      ! grep -qE '\| 5\. Demo Refresh \+ Documentation \+ Release \| 0/TBD \| Not started' .planning/ROADMAP.md &amp;&amp; \
      # Phase 5's "Plans:" subsection populated:
      grep -q "05-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      grep -q "05-05-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      ! grep -E '^\*\*Plans\*\*: TBD$' .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\- \[ \] \*\*Phase 5: Demo Refresh' .planning/ROADMAP.md == 0`
    - `grep -cE '^\- \[x\] \*\*Phase 5: Demo Refresh' .planning/ROADMAP.md == 1`
    - `grep -cE '\| 5\. Demo Refresh \+ Documentation \+ Release \| 5/5' .planning/ROADMAP.md == 1`
    - `grep -cE '\| 5\. Demo Refresh \+ Documentation \+ Release \| 0/TBD' .planning/ROADMAP.md == 0`
    - All 5 plan filenames appear in the Phase 5 Plans bullet list (05-01-PLAN.md through 05-05-PLAN.md)
    - Real values substituted for `<date>`, `<RELEASED_VERSION>`, `<run-id>`, `<SHIP/EXTRACT/FIX>` placeholders
  </acceptance_criteria>
  <done>
    ROADMAP shows Phase 5 closed; v0.2.0 ship date recorded.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update STATE.md — milestone status, Roadmap Evolution entry, Performance Metrics, Current Position advanced past v0.2.0</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (entire file — frontmatter at top, Current Position section, Performance Metrics, Roadmap Evolution, Decisions, Pending Todos, Blockers/Concerns, Deferred Items, Session Continuity)
    - .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md (for the cumulative-LOC-after-Phase-5 number — Plan A and B only modify demo / docs / spec files, so runtime LOC should be unchanged from the Phase 4 close baseline of 2884)
    - .planning/phases/05-demo-refresh-documentation-release/05-04-SUMMARY.md (for the workflow YAML LOC — workflow + run_tests.sh are NOT runtime LOC; record separately if the developer wants to track infrastructure LOC)
    - Recent git log + `gh release view` output (for ship-date, version, run-id, commit SHA)
  </read_first>
  <action>
    Five edits.

    **Edit 1: Frontmatter — flip status + milestone marker.**

    `old_string`:
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.2.0
    milestone_name: milestone
    status: planning
    stopped_at: Phase 5 context gathered
    last_updated: "2026-04-29T18:23:28.169Z"
    last_activity: 2026-04-29 -- Phase 4 closed
    progress:
      total_phases: 8
      completed_phases: 6
      total_plans: 32
      completed_plans: 32
      percent: 100
    ---
    ```

    `new_string` (substitute actual `<date>` and timestamps from Task 1):
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.2.0
    milestone_name: milestone
    status: shipped
    stopped_at: v0.2.0 SHIPPED
    last_updated: "<ISO-8601 timestamp from Task 1's gh release view>"
    last_activity: <date> -- Phase 5 closed; v0.2.0 published
    progress:
      total_phases: 8
      completed_phases: 7
      total_plans: 37
      completed_plans: 37
      percent: 100
    ---
    ```

    Note: `total_plans` / `completed_plans` increment by 5 (Plans 05-01 through 05-05).

    **Edit 2: "Current Position" section — flip from "Phase 5 / Not started" to "v0.2.0 / SHIPPED".**

    `old_string`:
    ```
    ## Current Position

    Phase: 5
    Plan: Not started
    Status: Ready to plan
    Last activity: 2026-04-29 -- Phase 4 closed
    ```

    `new_string`:
    ```
    ## Current Position

    Phase: 5
    Plan: 05-05 (closeout)
    Status: v0.2.0 SHIPPED
    Last activity: <date> -- Phase 5 closed; v<RELEASED_VERSION> published via release workflow run <run-id>
    ```

    **Edit 3: Add a new Roadmap Evolution entry at the END of the existing list (just before `### Decisions`).**

    Append after the last existing entry (the Phase 4 cross-AI review-fix loop closed entry):

    ```
    - <date>: **Phase 5 closed; v0.2.0 SHIPPED.** All 10 in-scope requirements flipped Complete (DEMO-01..03 / DOC-01..04 / REL-01..03). Plans:
      - **05-01:** Demo refresh — penta_tile_demo.tscn rewritten as 8-instance spatial-grid showcase (Penta FOUR / DualGrid16 / Wang2Edge / Wang2Corner / Min3x3 / Blob47Godot / PixelLabTopDown / PixelLabSideScroller); each instance uses bundled fallback (`tile_set = null`, `layout = <bundled>`); demo_runtime_painter.gd rewritten with hover-target detection. Retired (clean delete per breaking-changes policy): demo_player.gd + .uid, penta_tile_ground.{png,png.import,tres}, _regen_demo_ground.py, 4 legacy `penta_tile_*.tres` orphans. Pitfall #6 mitigation: Penta FOUR instance named literally "PentaTileMapLayer" so `_capture_baseline.gd` still works. (DEMO-01..03 satisfied)
      - **05-02:** Documentation — README ships 4 new sections (Layouts table 8 rows, Upgrading from 0.1.x, Authoring a Custom Layout with `@experimental` example, Identity & Footprint). CHANGELOG `[Unreleased]` block accumulated Phase 3 + 3.5 + 4 + 5 deltas before the workflow rewrote the header at release time. 14 spec corrections applied: SC-A (6 occurrences of "10 layouts" → "8 actually-shipped"), SC-B (3 ATTRIBUTION.md mentions deleted/reframed per D-72/D-73), SC-C (4 identity-constraint reframes per D-05-11), SC-D (REL-01 ownership flipped to workflow side-effect per D-05-16). (DOC-01..04 satisfied)
      - **05-03:** Identity audit (manual prerequisite per D-05-13, NOT a CI gate) — TileMapDual cloned at v5.0.2 (commit `<TMD-HASH>`); 3-axis measurement (LOC, public surface, hot-path); anti-pattern register check covering CLAUDE.md Identity Guardrails + PITFALLS.md AP entries; D-05-11 outcome `<SHIP / EXTRACT+OPTIMIZE-then-shipped / FIX-then-shipped>` recorded. README "Identity & Footprint" summary published. Full audit at `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md`.
      - **05-04:** Release infrastructure — `.github/workflows/release.yml` (single `workflow_dispatch` trigger, no inputs per D-05-15/D-05-16; auto-version-increment per D-05-16; 11-step pipeline per D-05-17; release notes = CHANGELOG slice per D-05-18; Pitfalls #1/#2/#3/#5/#7/#11 mitigated). `addons/penta_tile/tests/run_tests.sh` Linux/CI mirror of run_tests.ps1 with the same 18-test inventory.
      - **05-05:** Closeout — workflow ran once on main; v<RELEASED_VERSION> published with `penta_tile-v<RELEASED_VERSION>.zip`; CHANGELOG header rewritten to `## [<RELEASED_VERSION>] — <date>`; plugin.cfg bumped 0.1.0 → <RELEASED_VERSION>; git tag `v<RELEASED_VERSION>` cut on commit `<RELEASE_SHA>`. (REL-01..03 satisfied via workflow side-effects.)

      Identity guardrail: AT RISK carry-forward from Phase 4 RESOLVED per Plan C audit decision. Cumulative runtime LOC: 2884 (unchanged from Phase 4 close — Plan A modifies demo/, Plans B/C/D modify docs/spec/CI infrastructure; runtime code surface stayed flat through Phase 5). v0.3+ backlog persists: TBT-01-DEFERRED, TBT-02-DEFERRED, TEMPLATE-02-DEFERRED, VAR-PIXEL-01, VAR-01, TOP-01, MULTITERR-01..05, TERRAIN-01, RPGM-01..03, TOOL-01..04, IMPORT-01..02, DIST-01..02, PERF-01..02, NONROT-01.
    ```

    **Edit 4: Performance Metrics — add Phase 05 row to the by-phase table.**

    Find the existing table (around line 50-90):

    ```
    | Phase 04-fallback-routing P01 | 5min | 4 tasks | 4 files |
    | Phase 04-fallback-routing P02 | 50min | 3 tasks | 13 files |
    | Phase 04 P03 | — | 4 tasks | 5 files |
    | Phase 04 P04 | — | 3 tasks | 4 files |
    | Phase 04 P05 | — | 1 task  | 3 files |
    ```

    Append:
    ```
    | Phase 04 P05 | — | 1 task  | 3 files |
    | Phase 05 P01 | <est> | 4 tasks (3 auto + 1 checkpoint) | 12 files (10 deleted, 1 scene rewrite, 1 painter rewrite) |
    | Phase 05 P02 | <est> | 3 tasks | 6 files (REQ + ROADMAP + PROJECT + CLAUDE + README + CHANGELOG) |
    | Phase 05 P03 | <est> | 3 tasks (1 audit + 1 checkpoint + 1 README summary) | 2 files (LOC-AUDIT.md, README) |
    | Phase 05 P04 | <est> | 2 tasks | 2 files (workflow YAML, run_tests.sh) |
    | Phase 05 P05 | <est> | 4 tasks (1 checkpoint + 3 traceability flips) | 3 files (REQUIREMENTS, ROADMAP, STATE) |
    ```

    Replace `<est>` with actual durations if tracked; otherwise leave as `—`.

    Total plans: 5 new plans bring the cumulative count to 32 + 5 = 37.

    **Edit 5: Session Continuity — flip the In-progress / Next markers and add the Completed Phase 5 entry.**

    Find:
    ```
    **Next Phase:** 5 (Demo Refresh + Documentation + Release) — updated demo scene showcasing all 10 layouts (5 Phase 2 + 3 Phase 3 + 2 Phase 3.5; Tilesetter pair stays deferred per D-86 b — Phase 5 docs reference 8 actually-shipped + clear deferral note); README sections (Layouts / Upgrading / Authoring a Custom Layout); CHANGELOG; plugin.cfg bump 0.1.0 → 0.2.0; v0.2.0 git tag; GitHub Release zip
    ```

    Replace with:
    ```
    **Next Phase:** None for v0.2.0 (milestone shipped). Phase 6 (Editor Line/Rect/Bucket Tool Preview During Drag) deferred to far-future; revisit after the v0.3+ backlog priorities are set. Other v0.3+ backlog items: Tilesetter Wang 15 + Blob 47 (TBT-01-DEFERRED / TBT-02-DEFERRED), PixelLab variation-bank pick (VAR-PIXEL-01), Y-axis variation (VAR-01), top tiles (TOP-01), multi-terrain (MULTITERR-01..05), RPG Maker A2/A4 subtile compositor (RPGM-01..02).

    **Completed Phase:** 05 (Demo Refresh + Documentation + Release) — 5/5 plans, 10/10 in-scope requirements (DEMO-01..03 + DOC-01..04 + REL-01..03), 18 automated tests green throughout, identity audit per D-05-11 outcome `<SHIP/EXTRACT/FIX>`, release workflow ran successfully on <date> publishing v<RELEASED_VERSION> — <date>
    ```
  </action>
  <verify>
    <automated>
      # Frontmatter: status flipped from planning → shipped:
      grep -qE '^status: shipped' .planning/STATE.md &amp;&amp; \
      grep -qE '^stopped_at: v0\.2\.0 SHIPPED' .planning/STATE.md &amp;&amp; \
      # Current Position section reflects v0.2.0:
      grep -qE 'Status: v0\.2\.0 SHIPPED' .planning/STATE.md &amp;&amp; \
      # New Roadmap Evolution entry present (with the Phase 5 closed marker):
      grep -q "Phase 5 closed; v0\.2\.0 SHIPPED" .planning/STATE.md &amp;&amp; \
      # Performance Metrics — 5 new Phase 05 rows:
      [ "$(grep -c '^\| Phase 05 P0[12345]' .planning/STATE.md)" -eq 5 ] &amp;&amp; \
      # Session Continuity — Phase 5 marked Completed:
      grep -q '\*\*Completed Phase:\*\* 05 (Demo Refresh' .planning/STATE.md &amp;&amp; \
      # Next Phase no longer references Phase 5 as "next":
      ! grep -qE '\*\*Next Phase:\*\* 5 \(Demo Refresh' .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: shipped`
    - Frontmatter `stopped_at: v0.2.0 SHIPPED`
    - Frontmatter `progress.completed_phases: 7` (was 6) and `total_plans: 37` (was 32)
    - Current Position section says `Status: v0.2.0 SHIPPED`
    - One new Roadmap Evolution entry containing "Phase 5 closed; v0.2.0 SHIPPED" with all 5 sub-bullets (05-01 through 05-05)
    - Roadmap Evolution entry references the released version, run-id, and TileMapDual commit hash from Plan C
    - Identity guardrail "AT RISK carry-forward" note marked RESOLVED with reference to Plan C audit decision
    - Performance Metrics has 5 new "Phase 05 P0N" rows
    - Session Continuity "Next Phase" no longer points at Phase 5
    - Session Continuity adds a "Completed Phase: 05" entry with the actual ship date
    - Cumulative LOC line cites 2884 (or whatever Plan C measured) with the rationale "demo/docs/CI surface; runtime code unchanged"
  </acceptance_criteria>
  <done>
    STATE.md reflects v0.2.0 milestone closure. The state-of-the-project is captured for future-Claude / future-user sessions. Identity guardrail decision is recorded with traceability to Plan C.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Final commit + push (chore(05): Phase 5 closeout — REQUIREMENTS + ROADMAP + STATE)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - git status (verify only the 3 planning files are dirty after Tasks 2/3/4)
    - git log -1 (confirm the workflow's `chore(release): v<X.Y.Z>` commit is the most recent on main; this Plan-E closeout commit goes ON TOP of it)
  </read_first>
  <action>
    Commit the Tasks 2/3/4 edits as a single atomic commit and push to main.

    ```bash
    git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
    git commit -m "$(cat <<'EOF'
    chore(05): Phase 5 closeout — REQUIREMENTS + ROADMAP + STATE traceability flips

    Flipped 10 v1 requirements (DEMO-01..03 + DOC-01..04 + REL-01..03) from
    Pending to Complete in REQUIREMENTS.md Traceability. ROADMAP Phase 5 row
    now [x] with completion date; Progress table row updated to 5/5. STATE.md
    flipped status: planning → shipped, frontmatter completed_phases 6 → 7,
    total_plans 32 → 37; Roadmap Evolution gains the Phase 5 close entry +
    identity-guardrail-AT-RISK-RESOLVED note per Plan C audit decision.

    v0.2.0 milestone shipped end-to-end via the release workflow run.
    EOF
    )"

    git push origin main
    ```

    Verify the commit:

    ```bash
    git log -1 --format="%H %s"
    git log -1 --stat
    ```

    Verify only the 3 expected files in the commit (no stray adds):

    ```bash
    git diff HEAD~1 --name-only
    # Expected output (exactly 3 lines):
    # .planning/REQUIREMENTS.md
    # .planning/ROADMAP.md
    # .planning/STATE.md
    ```

    No `chore(release):` flavor — that was the workflow's commit. This is the closeout commit on top of it.
  </action>
  <verify>
    <automated>
      # Most recent commit is the Phase 5 closeout:
      git log -1 --format="%s" | grep -q "^chore(05): Phase 5 closeout" &amp;&amp; \
      # Exactly 3 files changed in the closeout commit:
      [ "$(git diff HEAD~1 --name-only | wc -l)" -eq 3 ] &amp;&amp; \
      # Each of the 3 expected files is in the diff:
      git diff HEAD~1 --name-only | grep -q "^\.planning/REQUIREMENTS\.md$" &amp;&amp; \
      git diff HEAD~1 --name-only | grep -q "^\.planning/ROADMAP\.md$" &amp;&amp; \
      git diff HEAD~1 --name-only | grep -q "^\.planning/STATE\.md$" &amp;&amp; \
      # Working tree clean:
      [ -z "$(git status --porcelain)" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - Most recent commit message starts with `chore(05): Phase 5 closeout`
    - The closeout commit changes exactly 3 files: REQUIREMENTS.md, ROADMAP.md, STATE.md
    - The release commit (`chore(release): v<X.Y.Z>`) created by the workflow is HEAD~1 (immediate predecessor)
    - `git status --porcelain` returns empty (clean working tree)
    - `git push origin main` succeeded (no `non-fast-forward` error)
  </acceptance_criteria>
  <done>
    Closeout commit pushed. main is now: workflow's chore(release) → planner's chore(05) closeout. v0.2.0 is live. Phase 5 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → GitHub Actions UI | User clicks "Run workflow"; the workflow's permissions are scoped at the job level (Pitfall #2). |
| GitHub workflow → public Release | The published Release is publicly visible. Body content (CHANGELOG slice) reviewed at Plan B Task 3 + workflow's awk extraction (verified at Plan E Task 1 visual check). |
| Workflow's `chore(release)` commit + tag → main | Pushed via `git push origin HEAD:main`. If main has diverged, push fails (Pitfall #4). |
| Plan E's `chore(05)` closeout commit → main | Hand-pushed AFTER the workflow's commit; race risk if multiple workflow triggers happen. Single-developer / single-active-branch scenario keeps this trivial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Tampering | Closeout commit on main | accept | Single-developer environment; no branch protection rules required at this scale. The closeout commit is reproducible from the planning artifacts. |
| T-05-28 | Information Disclosure | Released CHANGELOG content | mitigate | Released body = CHANGELOG slice paste. Plan B Task 3 reviewed the CHANGELOG content explicitly; Plan C reviewed the README "Identity & Footprint" summary. Public-facing docs reviewed before this point. |
| T-05-29 | Spoofing | Workflow vs human commits | mitigate | Workflow's commit author is `github-actions[bot]`; this closeout commit is the user's git config identity. The two are distinguishable in `git log`. |
| T-05-30 | Denial of Service | Recursive workflow re-trigger | accept | Plan E pushes a `chore(05)` commit. Plan D's workflow uses `workflow_dispatch` (not `on: push`), so the closeout push does NOT re-trigger the workflow. Pitfall #4 mitigation verified. |
| T-05-31 | Repudiation | Audit decision tied to ship | mitigate | Plan C's audit + Task 1's checkpoint resume-signal create a paper trail. Future readers can trace "v0.2.0 was shipped because Plan C audit returned SHIP". |
| T-05-32 | Elevation of Privilege | none | n/a | No new privilege boundary in closeout work. |
</threat_model>

<verification>
**Plan-level checks:**

1. Workflow run completed successfully (Task 1 checkpoint approved)
2. v<RELEASED_VERSION> Release page exists on GitHub with attached zip
3. Released zip extracts cleanly to a fresh test directory; structure is `penta_tile-v<X.Y.Z>/addons/penta_tile/...`
4. REQUIREMENTS.md Traceability shows 10 of 10 requirements as Complete (Task 2)
5. ROADMAP.md Phase 5 row marked [x] with completion date (Task 3)
6. STATE.md frontmatter `status: shipped`, Roadmap Evolution has the closeout entry, Performance Metrics has 5 new rows (Task 4)
7. Closeout commit pushed to main; working tree clean (Task 5)
8. The 18-test suite still passes (idle invariant)
</verification>

<success_criteria>
- v0.2.0 (or whatever the workflow auto-bumped to) is live on GitHub Releases
- All 58 v1 requirements satisfied (Pending count == 0)
- ROADMAP Phase 5 marked complete
- STATE.md `status: shipped`
- Identity guardrail AT RISK carry-forward from Phase 4 resolved per Plan C decision
- The 5 plans + closeout commit are all referenced in STATE.md and ROADMAP for future-Claude session continuity
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-refresh-documentation-release/05-05-SUMMARY.md` capturing:
- The released version, ship date, run-id, release commit SHA, closeout commit SHA
- The 10 flipped requirement IDs with their commit-SHA traceability
- A 1-paragraph "v0.2.0 shipped" milestone narrative suitable for the user to share / for the next milestone's STATE.md to inherit
- Any anomalies encountered during the workflow run (and how they were resolved) so future workflow runs benefit
- The link to the GitHub Release page for posterity
</output>
