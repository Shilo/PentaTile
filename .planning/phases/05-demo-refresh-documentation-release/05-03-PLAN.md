---
phase: 05-demo-refresh-documentation-release
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md
  - README.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "TileMapDual v5.0.2 is cloned to a temp directory, commit hash recorded"
    - "PentaTile cumulative runtime LOC measured via the canonical recipe (git ls-files | xargs wc -l, excluding tests/demo)"
    - "PentaTile public surface counted: @export properties + public methods + class_name'd classes"
    - "PentaTile hot-path traced: _update_cells → _paint_via_layout → layout.compute_mask → layout.mask_to_atlas → set_cell"
    - "TileMapDual equivalent measurements taken with the same recipe at v5.0.2"
    - "Anti-pattern register check executed against PITFALLS.md AP-1..AP-10 + CLAUDE.md Identity Guardrails reject list"
    - "Ship/extract decision recorded per D-05-11 framing (LOC large + clean hot path + zero anti-patterns → ship; LOC large + identifiable inefficiencies → extract+optimize first; any anti-pattern triggered → fix before ship)"
    - "README 'Identity & Footprint' placeholder text replaced with the audit summary + link"
    - "If extracts/optimizations are needed, the audit captures the patches as actionable TODOs (not implemented in Plan C — would spawn a Plan F if needed)"
  artifacts:
    - path: ".planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md"
      provides: "3-axis identity audit working artifact (LOC, public surface, hot-path) + anti-pattern register check + ship/extract decision"
      min_lines: 100
    - path: "README.md"
      provides: "Identity & Footprint section with audit summary"
      contains: "## 🔍 Identity &amp; Footprint"
  key_links:
    - from: "README.md § Identity & Footprint"
      to: ".planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md"
      via: "summary + link to full report"
      pattern: "05-LOC-AUDIT\\.md"
---

<objective>
Run the manual three-axis identity audit (LOC + public surface + hot-path complexity) per D-05-08..14, comparing against TileMapDual v5.0.2. Walk the anti-pattern register (PITFALLS.md AP-1..AP-10 + CLAUDE.md Identity Guardrails reject list). Make the ship/extract-and-optimize decision per D-05-11 framing. Replace Plan B's "Identity & Footprint" placeholder in README with the audit summary + link.

Purpose: Closes Phase 5's manual identity-audit prerequisite (D-05-13 — NOT a CI gate). Gives Plan E (release run) the green light to proceed. Produces the public-facing identity statement for v0.2.0.

Output: `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` (full working artifact) + an updated README "Identity & Footprint" section (summary + audit link). If the audit surfaces extracts/optimizations, captures them as actionable items — does NOT implement them in Plan C (would spawn a follow-up plan if needed; per D-05-11 cosmetic deletion is explicitly NOT allowed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-refresh-documentation-release/05-CONTEXT.md
@.planning/phases/05-demo-refresh-documentation-release/05-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/research/layouts/MASK_UNIFICATION.md
@CLAUDE.md
@addons/penta_tile/penta_tile_map_layer.gd
@addons/penta_tile/penta_tile_synthesis.gd
@addons/penta_tile/penta_tile_atlas_slot.gd
@addons/penta_tile/layouts/penta_tile_layout.gd
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clone TileMapDual v5.0.2, run identity audit recipe, produce 05-LOC-AUDIT.md</name>
  <files>.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md</files>
  <read_first>
    - .planning/phases/05-demo-refresh-documentation-release/05-RESEARCH.md (§ Code Examples → "Identity audit recipe — pinned to v5.0.2 (Plan C)" — copy the bash recipe verbatim)
    - .planning/phases/05-demo-refresh-documentation-release/05-CONTEXT.md (D-05-08 three axes; D-05-09 pinned tag; D-05-10 location; D-05-11 framing; D-05-13 not-a-CI-gate)
    - .planning/research/PITFALLS.md (AP-1..AP-10 anti-pattern register from Phase 3)
    - .planning/research/layouts/MASK_UNIFICATION.md (architectural anti-patterns reject list)
    - CLAUDE.md (Identity Guardrails reject list — terrain peering, watchers, coord cache, signal fanout, parallel paint API, EditorInspectorPlugin polish)
    - addons/penta_tile/penta_tile_map_layer.gd (for hot-path trace — read line ranges around `_update_cells` and `_paint_via_layout`; do NOT re-read the whole file twice)
    - addons/penta_tile/layouts/penta_tile_layout.gd (for hot-path trace continuation — the `compute_mask`, `mask_to_atlas`, `get_fallback_tile_set` virtuals)
    - addons/penta_tile/penta_tile_synthesis.gd (for the synthesis path complexity — referenced by the hot-path discussion but not part of the per-cell paint hot path; clarify that distinction)
  </read_first>
  <action>
    Run the audit in three phases. Capture all outputs verbatim into `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` as the working artifact.

    **Phase 1: Clone TileMapDual at the pinned tag.**

    ```bash
    rm -rf /tmp/tmd 2>/dev/null
    git clone --depth=1 --branch v5.0.2 https://github.com/pablogila/TileMapDual /tmp/tmd
    TMD_HASH=$(cd /tmp/tmd && git rev-parse HEAD)
    TMD_DATE=$(cd /tmp/tmd && git log -1 --format=%cd --date=short)
    echo "TileMapDual v5.0.2 commit: ${TMD_HASH} (${TMD_DATE})"
    ```

    Record the commit hash and date in the audit file's preamble.

    **Phase 2: Three-axis measurement.**

    Run the bash recipe verbatim from RESEARCH § Code Examples, capturing each section's output:

    ```bash
    # Axis 1: PentaTile cumulative runtime LOC
    cd <project root>
    git ls-files 'addons/penta_tile/*.gd' 'addons/penta_tile/layouts/*.gd' \
      | grep -v 'tests/' \
      | grep -v 'demo/' \
      | xargs wc -l > /tmp/penta_loc.txt
    cat /tmp/penta_loc.txt

    # Axis 2: PentaTile public surface
    PENTA_EXPORTS=$(grep -hcE '^@export' addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd | awk '{s+=$1} END {print s}')
    PENTA_PUBFNS=$(grep -hcE '^func [a-z]' addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd | awk '{s+=$1} END {print s}')
    PENTA_CLASSES=$(grep -hcE '^class_name ' addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd | awk '{s+=$1} END {print s}')
    echo "PentaTile @export: $PENTA_EXPORTS"
    echo "PentaTile public methods: $PENTA_PUBFNS"
    echo "PentaTile class_name'd: $PENTA_CLASSES"

    # Axis 3: TileMapDual comparison
    cd /tmp/tmd
    git ls-files '*.gd' \
      | grep -v 'test' \
      | grep -v 'demo' \
      | grep -v 'example' \
      | xargs wc -l > /tmp/tmd_loc.txt
    cat /tmp/tmd_loc.txt

    TMD_EXPORTS=$(find . -name "*.gd" -not -path '*/test*' -not -path '*/demo*' -not -path '*/example*' \
      | xargs grep -hcE '^@export' | awk '{s+=$1} END {print s}')
    TMD_PUBFNS=$(find . -name "*.gd" -not -path '*/test*' -not -path '*/demo*' -not -path '*/example*' \
      | xargs grep -hcE '^func [a-z]' | awk '{s+=$1} END {print s}')
    TMD_CLASSES=$(find . -name "*.gd" -not -path '*/test*' -not -path '*/demo*' -not -path '*/example*' \
      | xargs grep -hcE '^class_name ' | awk '{s+=$1} END {print s}')
    echo "TileMapDual @export: $TMD_EXPORTS"
    echo "TileMapDual public methods: $TMD_PUBFNS"
    echo "TileMapDual class_name'd: $TMD_CLASSES"
    ```

    Note: TileMapDual may use different file-organization conventions (e.g., its main code could live under `addons/tile_map_dual/` or directly at repo root). Inspect `/tmp/tmd` after clone and adapt the recipe — record the actual file glob used in the audit.

    **Phase 3: Hot-path trace (manual call-graph reading).**

    For PentaTile, read `addons/penta_tile/penta_tile_map_layer.gd` and trace the per-cell paint path from `_update_cells` to `set_cell`. Expected shape:

    ```
    _update_cells(coords)
      → _paint_via_layout(coords)
        → for each affected coord:
          → layout.compute_mask(coord, sample_fn)
          → layout.mask_to_atlas(mask)
          → set_cell(coord, source_id, atlas_coords, alternative)
    ```

    The synthesis pipeline (`PentaTileSynthesis._ensure_synthesized_tile_set`, etc.) runs once per layout-load — NOT per paint. Document this distinction explicitly: "load-time synthesis is O(slots × strips) once per layout assignment; per-cell paint is O(1) sample + O(1) dispatch".

    For TileMapDual, read `/tmp/tmd/addons/.../*.gd` (or wherever its core lives) and trace the equivalent per-cell paint path. Expected shape per CLAUDE.md "Identity Guardrails" notes: TileMapDual uses watchers + signals + cache lookups + terrain rule tries. Trace the actual call sequence; record verbatim function names and source file:line references.

    **Phase 4: Anti-pattern register check.**

    For each of the 6 CLAUDE.md "Identity Guardrails" reject items, verify by grep that PentaTile does NOT have it:

    | Anti-pattern | Grep query | Expected result |
    |--------------|-----------|-----------------|
    | Terrain peering metadata | `grep -rn "terrain_peering\|peering_bit\|TerrainSet" addons/penta_tile/` | 0 results |
    | Multi-terrain transitions | `grep -rn "transition_tile\|terrain_transition\|MULTITERR" addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd` | 0 results in source code (planning docs may mention MULTITERR-* deferrals; that's fine) |
    | Watcher / signal-fanout | `grep -rn "Watcher\|signal_fanout\|signal.*broadcast" addons/penta_tile/` | 0 results (the `Resource.changed` signal is BUILT-IN to Godot, not a watcher system) |
    | Persistent coordinate cache | `grep -rn "coord_cache\|persistent.*cache\|_cache_dict\|_coord_lookup" addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd` | 0 results |
    | Custom drawing API parallel to set_cell() | `grep -rn "func draw_cell\|func paint_cell\|custom_paint_api" addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd` | 0 results (verify nothing's been added since CLAUDE.md was written) |
    | EditorInspectorPlugin polish | `grep -rn "EditorInspectorPlugin\|EditorPlugin\|forward_canvas" addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd` | 0 results |

    Plus the 10 PITFALLS.md AP-1..AP-10 from Phase 3's TBT audit (read PITFALLS.md to get the actual list — may not be 1-10 exactly; the audit must cover whatever's there).

    Record EACH check's grep query, expected output, actual output, and PASS/FAIL status in the audit file.

    **Phase 5: Decision per D-05-11 framing.**

    Apply the D-05-11 decision tree:

    | Condition | Decision |
    |-----------|----------|
    | LOC large + clean hot path + zero anti-patterns | **SHIP** — LOC is data, not a verdict |
    | LOC large + identifiable inefficiencies / duplications | **EXTRACT + OPTIMIZE** before ship — captured as TODOs; spawn Plan F if work is significant |
    | Any anti-pattern from the register triggered | **FIX BEFORE SHIP** — same; capture and address |

    Record the decision verbatim with rationale citing the actual measurements.

    **Phase 6: Audit document structure.**

    Write `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` with the following structure:

    ```markdown
    # Phase 5 — Identity Audit

    **Performed:** <date YYYY-MM-DD>
    **PentaTile commit:** <git rev-parse HEAD>
    **TileMapDual reference:** v5.0.2 (commit `<TMD_HASH>`, dated `<TMD_DATE>`)
    **Decision (per D-05-11):** <SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP>

    ---

    ## Axis 1 — Cumulative Runtime LOC

    ### PentaTile

    Recipe: `git ls-files 'addons/penta_tile/*.gd' 'addons/penta_tile/layouts/*.gd' | grep -v 'tests/' | grep -v 'demo/' | xargs wc -l`

    ```
    <verbatim wc -l output>
    ```

    Total: **<N>** runtime LOC. (Methodology continuity: Phase 4 close was 2884.)

    ### TileMapDual v5.0.2

    Recipe: `git ls-files '*.gd' | grep -v 'test' | grep -v 'demo' | grep -v 'example' | xargs wc -l` (run from /tmp/tmd; adapted to actual repo layout — see notes below)

    ```
    <verbatim wc -l output>
    ```

    Total: **<M>** runtime LOC.

    ### Comparison

    | Repo | Runtime LOC | Notes |
    |------|-------------|-------|
    | PentaTile | <N> | <decision-relevant context> |
    | TileMapDual v5.0.2 | <M> | <repo-layout adaptation notes if any> |
    | Δ | <N − M> | <ratio if interesting> |

    Per D-05-11: **LOC is reported as signal, not as a fail criterion.**

    ## Axis 2 — Public Surface

    ### PentaTile

    | Surface element | Count |
    |-----------------|-------|
    | `@export` properties | <PENTA_EXPORTS> |
    | Public methods (`func [a-z]`) | <PENTA_PUBFNS> |
    | `class_name`'d classes | <PENTA_CLASSES> |

    Public method list (the user-facing API the layer + base layout expose):
    ```
    <grep -rE '^func [a-z]' addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd output>
    ```

    ### TileMapDual v5.0.2

    | Surface element | Count |
    |-----------------|-------|
    | `@export` properties | <TMD_EXPORTS> |
    | Public methods | <TMD_PUBFNS> |
    | `class_name`'d classes | <TMD_CLASSES> |

    ### Comparison

    PentaTile exposes <X> @exports + <Y> public methods + <Z> class_names; TileMapDual exposes <X'> + <Y'> + <Z'>. <Brief observation about which is broader / narrower.>

    ## Axis 3 — Hot-Path Complexity

    ### PentaTile per-cell paint path

    File: `addons/penta_tile/penta_tile_map_layer.gd`

    ```
    _update_cells(coords) [line N]
      → _paint_via_layout(coords) [line M]
        → layout.compute_mask(coord, sample_fn) [virtual; layout-specific]
        → layout.mask_to_atlas(mask) → AtlasSlot [virtual]
        → set_cell(coord, source_id, atlas_coords, alternative) [TileMapLayer parent class]
    ```

    Depth: <N> stack frames per cell. Synthesis (`PentaTileSynthesis._ensure_synthesized_tile_set`) runs ONCE at layout-bind time, NOT per paint.

    ### TileMapDual per-cell paint path

    File: `<actual TileMapDual file:line>`

    ```
    <traced call sequence — verbatim function names + line numbers from /tmp/tmd>
    ```

    Depth: <M> stack frames per cell. <List any caches / watchers / signal hops in the path.>

    ### Comparison

    PentaTile's per-cell hot path: <N> frames, no caches, no watcher dispatch.
    TileMapDual's per-cell hot path: <M> frames, includes <list of subsystems traversed>.

    Per D-05-11: **PentaTile prioritizes hot-path minimalism over LOC delta.** The hot path here is the load-bearing identity statement.

    ## Anti-Pattern Register Check

    ### CLAUDE.md Identity Guardrails

    | Anti-pattern | Grep query | Result | Status |
    |--------------|-----------|--------|--------|
    | Terrain peering metadata | `grep -rn "terrain_peering\|peering_bit\|TerrainSet" addons/penta_tile/` | <count> matches | ✓ ABSENT / ✗ PRESENT |
    | Multi-terrain transitions | `grep -rn "transition_tile\|terrain_transition" addons/penta_tile/*.gd addons/penta_tile/layouts/*.gd` | <count> matches | ✓ ABSENT / ✗ PRESENT |
    | Watcher / signal-fanout | <query> | <result> | ✓/✗ |
    | Persistent coordinate cache | <query> | <result> | ✓/✗ |
    | Parallel drawing API | <query> | <result> | ✓/✗ |
    | EditorInspectorPlugin polish | <query> | <result> | ✓/✗ |

    ### PITFALLS.md AP-1..AP-N (from Phase 3 TBT audit)

    | AP ID | Anti-pattern | Status | Notes |
    |-------|-------------|--------|-------|
    | AP-1 | <verbatim from PITFALLS.md> | ✓/✗ | <evidence> |
    | ...   | ...                          | ...   | ...        |

    ## Decision per D-05-11

    **Outcome:** <SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP>

    Rationale:
    - LOC: <N> vs TileMapDual's <M>. <Per D-05-11 this is signal, not verdict.>
    - Public surface: <observation>
    - Hot-path: <PentaTile depth> vs <TileMapDual depth>. <Anti-pattern absence statement.>
    - Anti-pattern register: <count> reject items checked, <count> ABSENT, <count> PRESENT. <If any present, list them and the action plan.>

    **Action items** (if any):
    - [ ] <e.g., "Extract _foo helper from _paint_via_layout — duplicated in 3 layouts; ~30 LOC reduction; spawn Plan F">
    - [ ] <only fill in if the audit surfaces concrete extracts or optimizations; otherwise this section says "None — ship as-is per D-05-11"

    ## README "Identity & Footprint" summary

    The README placeholder will be replaced with this paragraph (Task 2):

    > <2-3 sentence summary suitable for the README — describes hot-path minimalism, anti-pattern absence, and links back to this audit. Reads naturally as standalone prose.>
    ```

    Be honest in the audit. If anti-patterns are found, the decision flips to "FIX BEFORE SHIP" and Task 2 / Plan E hold until they're addressed (Plan F or a checkpoint with the user). Per D-05-11 cosmetic deletion is explicitly NOT allowed — if the audit recommends extracts, capture them as actionable TODOs but DO NOT implement them in this task. Implementation would be Plan F (added if needed). Ask the user via checkpoint whether to ship or trigger extract work.
  </action>
  <verify>
    <automated>
      # Audit file exists and is non-trivial:
      test -s .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      [ "$(wc -l &lt; .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md)" -ge 100 ] &amp;&amp; \
      # All 3 axes present:
      grep -q "## Axis 1" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      grep -q "## Axis 2" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      grep -q "## Axis 3" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      # TileMapDual pinned tag + commit hash recorded:
      grep -q "TileMapDual.*v5\.0\.2" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      # Anti-pattern register section present:
      grep -q "Anti-Pattern Register Check" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md &amp;&amp; \
      # Decision recorded:
      grep -qE "Decision per D-05-11.*Outcome" .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` exists and is at least 100 lines
    - File contains `TileMapDual v5.0.2` and a real commit hash (40-char hex string OR shortened hash format)
    - File contains exact heading `## Axis 1 — Cumulative Runtime LOC`
    - File contains exact heading `## Axis 2 — Public Surface`
    - File contains exact heading `## Axis 3 — Hot-Path Complexity`
    - File contains heading containing `Anti-Pattern Register Check`
    - File contains a `## Decision per D-05-11` section with an `Outcome:` line and one of: `SHIP`, `EXTRACT+OPTIMIZE`, `FIX-BEFORE-SHIP`
    - The 6 CLAUDE.md anti-patterns each have a row in the register table with grep query + result + status
    - The PITFALLS.md AP register is present (count of AP entries depends on what's in PITFALLS.md; record the count in the SUMMARY)
    - PentaTile LOC measured and recorded (single integer total at the end of `wc -l` output)
    - TileMapDual LOC measured and recorded (same)
    - Hot-path traces present for both repos with verbatim function names
  </acceptance_criteria>
  <done>
    The audit working artifact is written. The decision is recorded with rationale tied to the actual measurements. If any anti-patterns are PRESENT, Task 2 (README summary) records that gating state and Plan E is blocked until addressed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of audit findings + ship/extract decision approval</name>
  <files>(none - review of Task 1 audit artifact only)</files>
  <action>This is a checkpoint:human-verify task - see <what-built> / <how-to-verify> / <resume-signal> below for the verification surface. Executor pauses for the human to read the audit findings and decide SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP per D-05-11.</action>
  <verify>Human review of 05-LOC-AUDIT.md per the <how-to-verify> steps. No automated check.</verify>
  <done>Human responds via <resume-signal> with one of: 'approved', 'approved with summary edit: <text>', 'block - <reason>', or 'rework - <ask>'.</done>
  <what-built>
    `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` working artifact with:
    - 3-axis measurements (LOC, public surface, hot-path) for PentaTile + TileMapDual v5.0.2
    - Anti-pattern register check covering CLAUDE.md Identity Guardrails (6 items) + PITFALLS.md AP-1..AP-N
    - D-05-11 decision: SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP
    - Action items list (empty if SHIP; populated if EXTRACT/FIX)
    - 2-3 sentence summary paragraph for the README
  </what-built>
  <how-to-verify>
    1. **Read the audit file** at `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` end-to-end.

    2. **Sanity check each axis:**
       - **Axis 1 (LOC):** Does the PentaTile total match the expected ~2884 from STATE.md (or higher if Phase 5 itself added measurable LOC — Plans A and B touch demo/docs only, so runtime LOC should be unchanged from Phase 4 close at 2884)? Does the TileMapDual measurement look plausible (the upstream repo is small but multi-class)?
       - **Axis 2 (Public surface):** Do the @export / public-method counts feel right? PentaTile should have ~10-20 @exports across the layer + base + 8 layouts; TileMapDual is harder to predict.
       - **Axis 3 (Hot-path):** Trace the recorded PentaTile path against your mental model — `_update_cells → _paint_via_layout → compute_mask → mask_to_atlas → set_cell`. Verify the recorded TileMapDual path is the actual call sequence (the audit should have line refs like `tile_map_dual.gd:120`).

    3. **Anti-pattern register:**
       - Confirm zero CLAUDE.md anti-patterns are PRESENT. (If any are, the decision MUST be FIX-BEFORE-SHIP and we hold the release.)
       - Confirm the PITFALLS.md AP entries each have a status.

    4. **Decision sanity:**
       - **If outcome is SHIP:** confirm the rationale references actual numbers and the anti-pattern register is clean. This is the expected default outcome at v0.2.0.
       - **If outcome is EXTRACT+OPTIMIZE:** review the action items list. Each must be a concrete code-level extract (e.g., "function X duplicated across layouts Y and Z; consolidate into base class") — NOT a cosmetic deletion or LOC trim per D-05-11. Decide whether to spawn a Plan F now or defer to v0.3+.
       - **If outcome is FIX-BEFORE-SHIP:** review the anti-pattern that triggered the gate. Decide whether to fix in a hotfix plan or roll back the offending code.

    5. **README summary:**
       - Read the proposed 2-3 sentence summary paragraph.
       - Confirm it reads naturally, frames identity correctly per D-05-11 (hot-path minimalism + anti-pattern absence), and links back to the audit file.

    6. **Decide:**
       - **Approve** (audit passes, README summary is good): respond "approved — ship per D-05-11"
       - **Approve with edits** (good audit but tweak README summary wording): respond "approved with summary edit: <suggested wording>"
       - **Request rework** (audit missed something or measurements look wrong): respond with what to re-measure or re-trace
       - **Block on extract work** (audit recommends extracts that need to ship before v0.2.0): respond "block — spawn Plan F to do <list>"
  </how-to-verify>
  <resume-signal>
    Type "approved" or "approved with summary edit: <text>" to proceed to Task 3 (apply README summary).
    Type "block — <reason>" to stop Plan C here; planner will spawn a follow-up plan.
    Type "rework — <ask>" to iterate on the audit content.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace Plan B's README "Identity & Footprint" placeholder with the audit summary + audit-link</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md (Task 1 output — the "README 'Identity & Footprint' summary" section at the end has the proposed paragraph; user-approved at Task 2 checkpoint)
    - README.md (the placeholder section Plan B inserted — `## 🔍 Identity & Footprint` — find via grep and locate the placeholder text)
  </read_first>
  <action>
    Read the user-approved summary paragraph from `05-LOC-AUDIT.md` § "README 'Identity & Footprint' summary" (Task 2 may have modified it). Replace the placeholder text in README's `## 🔍 Identity & Footprint` section with:

    1. The user-approved summary paragraph (2-3 sentences from the audit)
    2. A 1-line link to the full audit: `[Full audit: \`.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md\`](.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md)`
    3. The keep-list of explicitly-rejected anti-patterns (the 6-bullet CLAUDE.md reject list)
    4. The hot-path code block (already in Plan B's placeholder; preserve unchanged)

    **Final shape of the section:**

    ```markdown
    ## 🔍 Identity & Footprint

    <2-3 sentence audit summary from 05-LOC-AUDIT.md, user-approved at Task 2>

    [Full audit: `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md`](.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md)

    PentaTile's identity is **hot-path minimalism + anti-pattern absence**, not raw LOC delta vs TileMapDual (per [D-05-11](.planning/phases/05-demo-refresh-documentation-release/05-CONTEXT.md)). The runtime path stays short:

    \`\`\`
    _update_cells(coords) → layout.compute_mask(coord, sample_fn) → layout.mask_to_atlas(mask) → set_cell(coord, source_id, atlas_coords)
    \`\`\`

    The addon explicitly does NOT include:

    - Terrain peering metadata or terrain-rule tries
    - Watcher / signal-fanout systems
    - Persistent coordinate caches
    - Parallel paint APIs alongside `set_cell()`
    - `EditorInspectorPlugin` polish

    ## 📝 Implementation Notes
    ```

    Use the Edit tool to replace the entire section block. The `old_string` should anchor on the placeholder text Plan B wrote ("Filled in by Plan C of Phase 5 (the manual identity audit). This placeholder will be replaced with..."); the `new_string` is the final shape above with the user-approved summary paragraph substituted in.

    Note: keep the link path RELATIVE (`.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md`) — works on GitHub web UI.
  </action>
  <verify>
    <automated>
      # Placeholder text gone:
      ! grep -q "Filled in by Plan C of Phase 5" README.md &amp;&amp; \
      ! grep -q "This placeholder will be replaced" README.md &amp;&amp; \
      # Audit link present:
      grep -q "05-LOC-AUDIT\.md" README.md &amp;&amp; \
      # Hot-path block preserved:
      grep -q "_update_cells(coords) → layout.compute_mask" README.md &amp;&amp; \
      # Section heading still exists:
      grep -q "^## 🔍 Identity &amp; Footprint$" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Filled in by Plan C" README.md == 0` (placeholder text removed)
    - `grep -c "This placeholder will be replaced" README.md == 0` (placeholder text removed)
    - `grep -c "05-LOC-AUDIT\.md" README.md >= 1` (link present)
    - `grep -c "^## 🔍 Identity & Footprint$" README.md == 1` (heading preserved)
    - `grep -c "_update_cells(coords) → layout.compute_mask" README.md == 1` (hot-path block preserved)
    - The replaced summary paragraph is at least 2 sentences (~30+ words; audit at least one period in the paragraph above the audit link)
  </acceptance_criteria>
  <done>
    README's Identity & Footprint section is the public-facing audit summary. Plan E can now ship — the developer-judgment prerequisite per D-05-13 is satisfied, the public surface speaks the truth, and the full audit is one click away in the linked file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → /tmp/tmd | TileMapDual v5.0.2 cloned via HTTPS. Trusted source (pablogila/TileMapDual is a public, established repo). The clone runs in /tmp; no permanent inclusion in PentaTile's repo. |
| Audit file → README | Audit summary paragraph crosses from internal working artifact to public README. Public-facing wording is reviewed at Task 2 checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | Public README audit summary | mitigate | Task 2 human checkpoint reviews the summary wording before it commits. The full audit lives in `.planning/` (still committed but private-audience). |
| T-05-14 | Tampering | TileMapDual clone | accept | `git clone --depth=1 --branch v5.0.2` retrieves the exact tagged commit; recorded hash in audit makes the comparison reproducible. Threat is upstream tag-moving; commit hash mitigates. |
| T-05-15 | Spoofing | TileMapDual identity claim | accept | Audit cites the actual commit hash; future readers can re-clone and verify. |
| T-05-16 | Repudiation | Audit decision rationale | mitigate | Decision per D-05-11 is recorded with verbatim measurements; review-trail is the audit file + Task 2 checkpoint resume-signal. |
| T-05-17 | Denial of Service | none | n/a | No runtime surface. |
| T-05-18 | Elevation of Privilege | none | n/a | No privilege boundary in audit work. |
</threat_model>

<verification>
**Plan-level checks:**

1. `05-LOC-AUDIT.md` exists with 3 axes + anti-pattern register + decision (Task 1 verify)
2. Human checkpoint approval recorded (Task 2 resume-signal)
3. README "Identity & Footprint" placeholder replaced with the user-approved summary (Task 3 verify)
4. The 18-test suite still passes (no test surface modified)
5. Per D-05-13: this audit is NOT a CI gate — Plan D's release workflow does not check for the audit's existence
</verification>

<success_criteria>
- D-05-08..14 manual identity audit complete with all three axes
- D-05-09 TileMapDual pinned at v5.0.2 with commit hash recorded
- D-05-10 audit lives in README + full report at `05-LOC-AUDIT.md`
- D-05-11 decision recorded (SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP) with rationale
- Anti-pattern register check covering CLAUDE.md Identity Guardrails + PITFALLS.md AP entries
- Plan B's placeholder section is replaced with real content
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-refresh-documentation-release/05-03-SUMMARY.md` capturing:
- The decision (SHIP / EXTRACT+OPTIMIZE / FIX-BEFORE-SHIP) and 1-line rationale
- The PentaTile vs TileMapDual numbers in a 3-row table (Axis 1 LOC, Axis 2 surface, Axis 3 hot-path depth)
- Anti-pattern check result (clean / list of triggers)
- Whether any action items were captured (and if so, what plan handles them)
- Confirmation Plan E is unblocked (or, if FIX-BEFORE-SHIP, what's blocking)
</output>
