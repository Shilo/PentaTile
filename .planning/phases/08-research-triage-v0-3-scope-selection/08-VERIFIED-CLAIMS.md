# Phase 8 Verified Claims

Evidence gate for Phase 8. Accepted rows use primary-source URLs and/or local repository artifacts. Rows marked `unverified` cannot drive v0.3 scope.

| Claim | Source Checked | Local Repo Check | Disposition | Notes |
| --- | --- | --- | --- | --- |
| TileMapDual is a broad dual-grid plugin covering square, isometric, and hex grids, not just orthogonal square grids. | Primary: https://github.com/pablogila/TileMapDual README, lines opened 2026-04-30 show square/isometric/hex positioning. | `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md` compares TileMapDual v5.0.2 public surface and hot path. | verified | Useful as competitor positioning; do not copy its grid-agnostic scope into PentaTile v0.3. |
| TileMapDual recommends multiple TileMapDual layers for more than two terrain types. | Primary: https://github.com/pablogila/TileMapDual README, "Multiple terrains" section. | `08-MULTI-TERRAIN-RESEARCH.md` records this as a constraint for one-public-layer PentaTile terrain work. | verified | Supports deferring broad multi-terrain blending complexity and starting with focused terrain metadata input. |
| TileMapDual owns watcher/cache/rule-trie territory in its runtime shape. | Local source audit in `.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md`; primary repo: https://github.com/pablogila/TileMapDual. | PentaTile audit shows 4-stack-frame path and no persistent caches, watcher systems, signal fanout, or terrain-rule tries. | verified | Identity firewall: PentaTile may borrow the idea of terrain signatures but not the heavy machinery. |
| TileBitTools is an archived Godot 4 editor plugin for autotile templates and terrain-bit editing. | Primary: https://github.com/dandeliondino/tile_bit_tools README; repository archived 2024-04-13; feature list includes templates and bulk terrain-bit editing. | Phase 3 decisions in `.planning/REQUIREMENTS.md` keep TBT as design inspiration only; no `addons/penta_tile/ATTRIBUTION.md`. | verified | Good evidence for authoring pain and Tilesetter-format demand; not a runtime model for PentaTile. |
| TileBitTools includes built-in Blob, Wang, Wang 3-terrain, 3x3, and 2x2 template families. | Primary: https://github.com/dandeliondino/tile_bit_tools README feature list. | `PentaTileLayoutBlob47Godot` exists locally; Tilesetter pair remains deferred as `TBT-01-DEFERRED` / `TBT-02-DEFERRED`. | verified | Supports keeping Tilesetter layouts as backlog, but tables still require primary source or user export sample. |
| TileBitTools does not support hex/isometric or alternative tiles. | Primary: https://github.com/dandeliondino/tile_bit_tools README limitations. | `08-MULTI-TERRAIN-RESEARCH.md` and REQUIREMENTS avoid using TBT as a source for alternative-tile terrain support. | verified | Reinforces that TBT cannot be the sole source for PentaTile multi-atlas/alternative behavior. |
| Better Terrain is a full replacement terrain system with a Terrains dock, categories, and custom matching behavior. | Primary: https://github.com/Portponky/better-terrain README. | AGENTS.md identity guardrails reject turning PentaTile into a general terrain framework. | verified | Useful inspiration for deterministic terrain/variation concepts; reject full-solver/editor-dock scope. |
| Better Terrain explicitly positions itself against Godot's built-in terrain behavior and runtime API. | Primary: https://github.com/Portponky/better-terrain README says Godot 4 terrain behavior is tricky, specific, slow, and hard to use at runtime. | `08-MULTI-TERRAIN-RESEARCH.md` rejects Godot terrain-solver delegation and Better Terrain-style full solver as baseline. | verified | Supports accepting "PentaTile solver over Godot solver" while staying smaller than Better Terrain. |
| Godot 4.6 `TileMapLayer.set_cell()` stores source ID, atlas coords, and alternative tile. | Primary: https://docs.godotengine.org/en/4.6/classes/class_tilemaplayer.html. | Current `PentaTileMapLayer` writes generated visuals through `set_cell()`; local audit documents this as the native API path. | verified | Confirms PentaTile can route to multiple atlas sources in principle without abandoning native painting. |
| Godot 4.6 terrain connect/path APIs are separate solver calls that may update neighbors. | Primary: https://docs.godotengine.org/en/4.6/classes/class_tilemaplayer.html (`set_cells_terrain_connect`, `set_cells_terrain_path`). | `MULTITERR-07` in `.planning/REQUIREMENTS.md` says PentaTile must not call them for generated output. | verified | Correct firewall: Godot terrain metadata may be input; Godot terrain solver is rejected for generated visuals. |
| Godot 4.6 `TileData` exposes terrain peering bits and custom data accessors. | Primary: https://docs.godotengine.org/en/4.6/classes/class_tiledata.html. | `MULTITERR-01` / `MULTITERR-02` already reference `TileData.terrain_set`, `terrain`, peering bits, custom data, and probability. | verified | Supports focused multi-terrain candidate indexing using metadata as authoring input. |
| Godot TileSet terrains can be authored as match-corners, match-sides, or combined modes. | Primary: https://docs.godotengine.org/en/4.6/tutorials/2d/using_tilesets.html. | `08-MULTI-TERRAIN-RESEARCH.md` maps this to single-grid-first terrain signatures and dual-grid corner signatures later. | verified | Valid source for terrain metadata shapes, not a mandate to use Godot's solver. |
| PentaTile v0.2.0 already has dual-grid support. | Local: `README.md` lists `PentaTileLayoutDualGrid16`; `addons/penta_tile/layouts/penta_tile_layout_dual_grid_16.gd` exists. | `README.md` "Layouts" table lists DualGrid 16; tests include fallback/composed-canvas coverage from Phase 4. | verified | Corrects stale research. PentaTile does not need to "transition to dual-grid" as a v0.3 prerequisite. |
| PentaTile v0.2.0 already has fallback routing/prototyping. | Local: `README.md` demo section and Phase 4 ROADMAP/SUMMARY note `tile_set = null` plus `layout.get_fallback_tile_set()`. | `penta_tile_layout.gd` exists with fallback virtual; Phase 4 `fallback_routing_test.gd` is referenced by ROADMAP. | verified | Corrects stale research that preview/fallback is absent. Remaining editor-preview debt is specifically line/rect/bucket drag preview, not fallback routing. |
| PentaTile v0.2.0 shipped release outputs. | Local: `addons/penta_tile/plugin.cfg` version is `0.2.0`; `README.md` and STATE record GitHub release `v0.2.0`. | `.planning/STATE.md` records release workflow run 25131034672 and tag `v0.2.0`. | verified | Corrects stale research. Phase 8 should not relitigate v0.2.0 shipping status. |
| PentaTile currently ships 8 built-in layouts, not 10. | Local: `README.md` "Layouts" table; `addons/penta_tile/layouts/` contains Penta, DualGrid16, Wang2Edge, Wang2Corner, Min3x3, Blob47Godot, PixelLab top-down, PixelLab side-scroller. | `.planning/ROADMAP.md` Phase 5 spec corrections changed "10 layouts" to "8 actually-shipped". | verified | Tilesetter Wang15/Blob47 are backlog, not shipped layouts. |
| PixelLab's Create Tileset tool produces top-down and side-scroller tilesets and exposes seed/options. | Primary: https://www.pixellab.ai/docs/tools/create-tileset. | `PentaTileLayoutPixelLabTopDown` and `PentaTileLayoutPixelLabSideScroller` exist locally; `VAR-PIXEL-01` remains deferred. | verified | Primary docs do not verify PentaTile's internal "variation bank" implementation; local code verifies current first-cell pick only. |
| PixelLab variation-bank deterministic pick is a needed PentaTile feature. | Local: `.planning/REQUIREMENTS.md` `VAR-PIXEL-01`; Phase 3.5 summary says first-cell row-major pick shipped and bank pick deferred. | PixelLab primary docs verify tile output family, not PentaTile's bank semantics. | verified-local | Accepted because it is a local backlog/design claim, not a PixelLab external claim. |
| Tilesetter Wang15 and Blob47 slot tables can be implemented from public primary sources. | Research source set did not locate a sufficient primary slot-table source during Phase 3; README only links Tilesetter docs as convention target. | `.planning/REQUIREMENTS.md` `TBT-01-DEFERRED`, `TBT-02-DEFERRED`, `TEMPLATE-02-DEFERRED` state primary source not located. | unverified | Do not promote until a Tilesetter export sample or authoritative table is available. |
| Editor line/rect/bucket preview is UX debt when a layout is bound. | Local: `.planning/research/editor-line-rect-preview.md` and STATE pending todo; Phase 6 exists as deferred. | No code change in Phase 8; AGENTS.md says Phase 6 is far-future/deferred. | verified-local | Keep as a scoped editor-preview issue; do not expand into EditorInspectorPlugin polish. |
| Performance needs optimization now because competitor tools are heavier. | Primary/local evidence does not show PentaTile exceeding demo-scale performance limits. | `.planning/REQUIREMENTS.md` `PERF-02` says benchmark before optimizing; `05-LOC-AUDIT.md` says hot path is clean. | unverified | Reject optimization-first claims. Accept benchmarks as the gate. |
| PentaTile must abandon native `set_cell()` to support advanced scope. | Godot `TileMapLayer.set_cell()` remains the native cell write API; competitor claims do not prove otherwise. | PentaTile identity and README require native `set_cell()`/editor painting; local hot-path audit confirms current use. | rejected | Stale/off-identity. Future work must preserve native painting unless identity is intentionally renegotiated. |
| Multi-terrain in one TileSet/one public layer is feasible if PentaTile reads Godot metadata but keeps its own solver. | Primary: Godot `TileData` and `TileMapLayer` docs; TileMapDual and Better Terrain repos as comparison. | `08-MULTI-TERRAIN-RESEARCH.md` and `MULTITERR-01..08` encode the staged design. | verified | Accepted as future design input only; it does not authorize Godot terrain-solver delegation. |
| Distribution/docs claims: Asset Library and formal GUT suite are not shipped in v0.2.0; MkDocs and LLM docs are shipped as repo/docs outputs. | Local: `07-SUMMARY.md`, `mkdocs.yml`, `docs/`, `.planning/REQUIREMENTS.md` `DIST-01` / `DIST-02`. | `README.md` says release zip packages only `addons/penta_tile/`; Phase 7 summary records MkDocs and llms artifact reversal. | verified | Asset Library remains backlog; docs discoverability improved in Phase 7. |
