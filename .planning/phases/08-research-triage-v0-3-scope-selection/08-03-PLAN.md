---
phase: 08-research-triage-v0-3-scope-selection
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-research-triage-v0-3-scope-selection/08-BACKLOG-CLEANUP.md
autonomous: true
requirements: [TRIAGE-05]
must_haves:
  truths:
    - "Backlog items include un-defer triggers, dependencies, and do-not-pursue notes discovered during triage."
    - "VAR-01, VAR-PIXEL-01, and MULTITERR-01..08 are refined as a coupled Terrain + Variation Authoring Research Spike, not standalone implementation work."
    - "Production terrain/variation refactors require spike findings plus user-side manual Godot testing outside this repo."
    - "Requirements preserve the no-backwards-compat and no-forward-compat policy."
    - "Rejected research ideas are represented as out-of-scope or do-not-pursue notes, not hidden."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical backlog refinements"
    - path: ".planning/phases/08-research-triage-v0-3-scope-selection/08-BACKLOG-CLEANUP.md"
      provides: "Traceable cleanup summary"
  key_links:
    - from: "08-CANDIDATE-MATRIX.md"
      to: ".planning/REQUIREMENTS.md"
      via: "candidate dependencies and triggers"
    - from: "08-SCOPE-FIREWALL.md"
      to: ".planning/REQUIREMENTS.md"
      via: "out-of-scope/do-not-pursue notes"
---

<objective>
Apply Phase 8 findings to the canonical backlog without changing source code or planning future implementation details prematurely.

Purpose: make future v0.3 planning inherit clear triggers and constraints instead of rediscovering the same triage decisions.
Output: refined REQUIREMENTS backlog and a cleanup summary artifact.
</objective>

<read_first>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-CONTEXT.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-DISPOSITION-MATRIX.md
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine backlog requirements with triggers and constraints</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Edit `.planning/REQUIREMENTS.md` using grep-anchored changes only. Refine existing backlog entries rather than creating duplicate IDs. Ensure these requirement families have explicit un-defer triggers and constraints: `VAR-01`, `VAR-PIXEL-01`, `TOP-01`, `TBT-01-DEFERRED`, `TBT-02-DEFERRED`, `TOOL-01`, `TOOL-02`, `PERF-02`, `DIST-01`, `MULTITERR-01..08`, Phase 6 editor preview, and Phase 7 docs follow-ups if any remain. Specifically update `VAR-01`, `VAR-PIXEL-01`, and the `MULTITERR` section so future planning knows variation, terrain identity, atlas/alternative strategy, `TileData.probability`, PixelLab banks, terrain banks, and multiple atlas sources must be brainstormed together before implementation. Add the hard gate: no production implementation or production refactor until the spike compares real Godot-authored TileSet strategies and the user completes manual outside-repo testing. Add do-not-pursue notes from `08-SCOPE-FIREWALL.md` to the Out of Scope table where they are not already present. Preserve all no-compat/no-forward-compat rules: do not add compatibility shims, deprecation aliases, version fields, schema markers, migration branches, or speculative extension points.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/REQUIREMENTS.md'; (Select-String -Path $p -Pattern 'un-defer|Re-trigger|trigger|Do not|do not' -Quiet) -and (Select-String -Path $p -Pattern 'VAR-01|VAR-PIXEL-01|TOP-01|TBT-01-DEFERRED|TBT-02-DEFERRED|PERF-02|DIST-01|MULTITERR-08' -Quiet) -and -not (Select-String -Path $p -Pattern 'add (a )?compatibility shim|introduce (a )?compatibility shim|add (a )?deprecation alias|introduce (a )?deprecation alias|add (a )?schema marker|introduce (a )?schema marker|add (a )?version field|introduce (a )?version field|migration branch' -Quiet)"</automated>
  </verify>
  <done>REQUIREMENTS.md contains refined triggers/constraints for the required backlog items and no banned compatibility/versioning machinery.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create backlog cleanup summary</name>
  <files>.planning/phases/08-research-triage-v0-3-scope-selection/08-BACKLOG-CLEANUP.md</files>
  <action>
    Create `08-BACKLOG-CLEANUP.md` with a traceability table: `| Requirement / Topic | Change Applied | Source Artifact | Trigger / Constraint |`. Include every canonical file edit made in Task 1, including any Out of Scope additions. Include a "Terrain + Variation Coupling" section explaining why variation should not be planned as a separate quick implementation until the spike resolves TileSet authoring layout. Add a short "No-Compat / No-Forward-Compat Check" section stating that no shims, aliases, version fields, schema markers, migration branches, or speculative extension points were added.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/08-research-triage-v0-3-scope-selection/08-BACKLOG-CLEANUP.md'; (Test-Path $p) -and (Select-String -Path $p -Pattern 'Requirement / Topic \\| Change Applied \\| Source Artifact \\| Trigger / Constraint') -and (Select-String -Path $p -Pattern 'No-Compat|No-Forward-Compat')"</automated>
  </verify>
  <done>`08-BACKLOG-CLEANUP.md` documents all REQUIREMENTS.md changes and the banned-machinery check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase artifacts -> canonical backlog | Temporary research conclusions become durable requirements text. |
| Canonical backlog -> future executors | Ambiguous wording can cause future phases to implement rejected systems. |
| Context decision -> requirements text | The user's hard external-testing gate can be lost if backlog entries only describe implementation mechanics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | `.planning/REQUIREMENTS.md` | mitigate | Use grep-anchored edits and record each change in `08-BACKLOG-CLEANUP.md`. |
| T-08-03-02 | Repudiation | Backlog cleanup | mitigate | Create a traceability table tying each edit to source artifacts. |
| T-08-03-03 | Elevation of Privilege | Future implementation scope | mitigate | Add do-not-pursue notes and preserve no-compat/no-forward-compat checks. |
| T-08-03-04 | Denial of Service | Future planning | accept | More precise backlog text may lengthen planning docs, but prevents repeated re-triage. |
| T-08-03-05 | Elevation of Privilege | Terrain/variation backlog | mitigate | Mark production refactors as blocked until spike findings plus user-side manual testing exist. |
</threat_model>

<verification>
Run both task checks and inspect `git diff -- .planning/REQUIREMENTS.md` to confirm the diff is requirements/backlog text only. Do not touch addon source code.
</verification>

<success_criteria>
- TRIAGE-05 is satisfied for REQUIREMENTS.md.
- All required backlog families have triggers, dependencies, or constraints where Phase 8 found them missing.
- Terrain and variation backlog entries preserve the approved research-first, external-testing-gated decision.
- The cleanup summary makes the canonical edits auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-research-triage-v0-3-scope-selection/08-03-SUMMARY.md`.
</output>
