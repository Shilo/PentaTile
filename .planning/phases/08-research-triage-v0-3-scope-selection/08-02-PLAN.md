---
phase: 08-research-triage-v0-3-scope-selection
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - .planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md
  - .planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md
autonomous: true
requirements: [TRIAGE-03, TRIAGE-04]
must_haves:
  truths:
    - "v0.3 candidates are ranked by user value, risk, dependency coupling, visual-test burden, external-testing burden, and identity fit."
    - "Terrain and automated variation are treated as one coupled TileSet-authoring research problem, not isolated implementation-ready work."
    - "Off-identity recommendations are rejected or quarantined explicitly."
    - "Godot terrain metadata is allowed as input only when PentaTile remains the solver."
    - "Completed 08-01 evidence remains valid and is consumed, not rewritten."
  artifacts:
    - path: ".planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md"
      provides: "Ranked v0.3 candidate matrix"
    - path: ".planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md"
      provides: "Scope firewall and do-not-pursue table"
  key_links:
    - from: "08-CONTEXT.md"
      to: "08-CANDIDATE-MATRIX.md"
      via: "approved Terrain + Variation Authoring Research Spike direction"
    - from: "08-DISPOSITION-MATRIX.md"
      to: "08-CANDIDATE-MATRIX.md"
      via: "only accept/defer items become candidate rows"
    - from: "08-MULTI-TERRAIN-RESEARCH.md"
      to: "08-SCOPE-FIREWALL.md"
      via: "metadata-input/solver-delegation distinction"
---

<objective>
Convert verified research dispositions and the approved discussion context into a ranked v0.3 candidate set and a hard scope firewall.

Purpose: make the next milestone choice from evidence, identity fit, and known uncertainty around terrain-plus-variation authoring, not from competitor feature breadth.
Output: candidate matrix and firewall artifacts consumed by backlog cleanup and final recommendation.
</objective>

<read_first>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-CONTEXT.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-VERIFIED-CLAIMS.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-DISPOSITION-MATRIX.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-MULTI-TERRAIN-RESEARCH.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-PATTERNS.md
@.planning/phases/05-demo-refresh-documentation-release/05-LOC-AUDIT.md
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rank v0.3 candidates</name>
  <files>.planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md</files>
  <action>
    Create `08-CANDIDATE-MATRIX.md` with a ranked table using these columns exactly: `| Candidate | Requirement IDs | User Value | Implementation Risk | Dependency Coupling | Visual-Test Burden | External-Test Burden | Identity Fit | Suggested Disposition | Notes |`. Cover at minimum: `Terrain + Variation Authoring Research Spike`, `VAR-01`, `VAR-PIXEL-01`, `TOP-01`, `TBT-01-DEFERRED`, `TBT-02-DEFERRED`, `TOOL-01`, `TOOL-02`, `PERF-02`, `DIST-01`, Phase 6 editor preview, Phase 7 follow-ups, and `MULTITERR-01..08`. Use `08-CONTEXT.md` and `08-DISPOSITION-MATRIX.md` as the source of candidate eligibility. State dependencies concretely, e.g. PixelLab bank pick depends on the same candidate/weight design as general deterministic variation; multi-terrain single-grid depends on real TileSet authoring strategy research and likely source-aware output; production terrain work depends on user-side manual testing outside this repo; performance optimization depends on PERF-02 benchmark results. Rank the approved Terrain + Variation Authoring Research Spike first unless a cited contradiction is discovered, but do not let this file be the final closeout recommendation; ranking and tradeoffs only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md'; (Test-Path $p) -and (Select-String -Path $p -Pattern 'Candidate \\| Requirement IDs \\| User Value') -and (Select-String -Path $p -Pattern 'Terrain \\+ Variation Authoring Research Spike') -and (Select-String -Path $p -Pattern 'VAR-01|VAR-PIXEL-01|TOP-01|TBT-01-DEFERRED|TBT-02-DEFERRED|TOOL-01|TOOL-02|PERF-02|DIST-01|MULTITERR')"</automated>
  </verify>
  <done>`08-CANDIDATE-MATRIX.md` ranks all required candidates and records dependency coupling and visual-test burden.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write scope firewall</name>
  <files>.planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md</files>
  <action>
    Create `08-SCOPE-FIREWALL.md` using the anti-pattern register shape from `05-LOC-AUDIT.md`, with columns exactly: `| Suggestion | Disposition | Identity Rationale | Allowed Narrow Form | Requirement / Note |`. Include these mandatory firewall rows: global solvers/backtracking, Godot terrain solver as PentaTile renderer, Terrains dock/editor wizard/bulk terrain-bit editor, persistent coordinate caches, custom paint APIs parallel to `set_cell()`, scriptable rule engines, metadata/entity-spawning systems, hex/isometric/grid-agnostic expansion, GPU/procedural world generation, compatibility shims, version fields/schema markers, speculative extension points, and production terrain/variation refactors before the spike passes. Include separate allowed rows for "Godot `TileData` terrain metadata as authoring/indexing input", "throwaway spike fixtures/prototypes", and "user-side manual Godot testing outside this repo"; link the terrain metadata row to `MULTITERR-01..08` and the testing row to `MULTITERR-08` plus `08-CONTEXT.md`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md'; (Test-Path $p) -and (Select-String -Path $p -Pattern 'allowed input only') -and (Select-String -Path $p -Pattern 'global solvers|Godot terrain solver|persistent coordinate caches|parallel to `set_cell\\(\\)`|version fields|schema markers')"</automated>
  </verify>
  <done>`08-SCOPE-FIREWALL.md` explicitly rejects or quarantines the required off-identity systems and preserves the terrain-metadata input exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disposition matrix -> candidate matrix | Rejected ideas must not be silently reintroduced as candidates. |
| Candidate ranking -> project identity | High-value competitor features can erode the small hot-path identity if ranked without firewall checks. |
| Discussion context -> candidate ranking | The approved terrain-plus-variation spike can be diluted if variation is treated as a separate easy implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | `08-CANDIDATE-MATRIX.md` | mitigate | Require every candidate to cite requirement IDs and dependency notes. |
| T-08-02-02 | Denial of Service | v0.3 scope | mitigate | Use dependency coupling and visual-test burden columns to prevent overpacked packages. |
| T-08-02-03 | Elevation of Privilege | Scope firewall | mitigate | Explicitly reject features that replace PentaTile's solver, native `set_cell()`, or demo-scale hot path. |
| T-08-02-04 | Repudiation | Candidate ranking | mitigate | Keep ranking separate from final recommendation so tradeoffs remain reviewable. |
| T-08-02-05 | Tampering | Variation scope | mitigate | Candidate matrix must state that variation and terrain authoring are coupled until the spike compares real TileSet layouts. |
</threat_model>

<verification>
Run both task checks. Then verify the candidate matrix contains no new requirement IDs for items that already exist in REQUIREMENTS.md; new labels may appear only as package names or notes, not as backlog IDs.
</verification>

<success_criteria>
- TRIAGE-03 is satisfied by a ranked matrix covering all required candidate families.
- TRIAGE-04 is satisfied by a firewall artifact that rejects the mandatory off-identity systems.
- The terrain rule is precise: metadata input allowed, solver delegation rejected.
- The approved research-first boundary is precise: throwaway spike work allowed, production refactor forbidden until external testing passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-research-triage-v0-3-scope-selection/08-02-SUMMARY.md`.
</output>
