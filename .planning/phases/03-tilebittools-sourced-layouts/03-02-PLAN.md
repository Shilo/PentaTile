---
phase: 03-tilebittools-sourced-layouts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md
autonomous: true
requirements: []
tags: [phase-3, audit, tbt, design-inspiration, no-code-lift]

must_haves:
  truths:
    - "A Wave 0b deliverable exists at `.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` that classifies every meaningful TBT pattern as ADOPT / PARTIAL / REJECT with reasoning grounded in PROJECT.md identity guardrails"
    - "Each ADOPT/PARTIAL pattern is cross-referenced against TileMapDual's equivalent surface (or ‘none’ when applicable)"
    - "Every PARTIAL or ADOPT-deferred pattern has a concrete backlog seed entry pointing to a `.planning/todos/pending/` filename + suggested phase"
    - "NO TBT code or data is reproduced — recommendations are written in PentaTile's own style as prose or pseudo-code"
  artifacts:
    - path: ".planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md"
      provides: "Per-pattern ADOPT/PARTIAL/REJECT verdicts + TileMapDual cross-reference + backlog seeds"
      min_lines: 350
      contains: "ADOPT"
  key_links:
    - from: ".planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md"
      to: "C:\\Programming_Files\\Godot\\tile_bit_tools-main\\addons\\tile_bit_tools\\"
      via: "file-path citations of TBT source for each audited pattern"
      pattern: "tile_bit_tools/.*\\.gd"
    - from: ".planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md"
      to: ".planning/research/layouts/TILEBITTOOLS.md"
      via: "extends prior informational audit with verdicts"
      pattern: "TILEBITTOOLS\\.md"
---

<objective>
Produce the Wave 0b research deliverable per D-84: a structured audit of TileBitTools (~3,825 LOC across `C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\`) classifying every meaningful TBT pattern as ADOPT / PARTIAL / REJECT, cross-referenced against TileMapDual's equivalent surface, with backlog seeds for PARTIAL items.

Per D-73 the audit reads TBT source but produces only ideas — NO code/data lift. Recommendations are written in PentaTile's own style.

Purpose: surface design patterns worth folding into PentaTile (now or future phases) and explicitly REJECT patterns that violate PROJECT.md's "smaller and simpler than TileMapDual" identity guardrail. Documents the reasoning so future plan-phases inherit the verdicts rather than re-litigating.
Output: `~600-1000 line` markdown deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tilebittools-sourced-layouts/03-CONTEXT.md
@.planning/phases/03-tilebittools-sourced-layouts/03-RESEARCH.md
@.planning/research/layouts/TILEBITTOOLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 03-TBT-DEEP-AUDIT.md per D-84 structure</name>
  <files>
    .planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/03-tilebittools-sourced-layouts/03-RESEARCH.md § 6 "TBT Source Tree Map (input for D-84 audit task)" — this provides a verified file inventory (30 GD files + 12 templates), per-file LOC counts, and a pre-audit ADOPT/PARTIAL/REJECT recommendation table per pattern; copy the file pointers + LOC counts verbatim and EITHER adopt the pre-audit recommendations or override them with explicit reasoning
    - .planning/research/layouts/TILEBITTOOLS.md — the prior informational audit; extend with explicit verdicts. Treat this as background, not as a verdict source.
    - .planning/PROJECT.md — identity guardrails ("smaller and simpler than TileMapDual"); the verdict reasoning column MUST cite which guardrail justifies the verdict
    - CLAUDE.md "Identity Guardrails" + "Coined-Term Discipline" + "Breaking Changes Policy (HARD RULE)" sections
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\plugin.gd (62 LOC) — the EditorPlugin entry; verify with file:line citations
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\inspector_plugin.gd (~353 LOC) — the EditorInspectorPlugin scene-tree-walking pattern (REJECT candidate)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\core\bit_data.gd, core\editor_bit_data.gd, core\template_bit_data.gd (BitData hierarchy — PARTIAL candidate)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\core\template_tag_data.gd + core\template_loader.gd (custom_tags vocabulary — PARTIAL ADOPT-DEFERRED candidate)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\core\globals.gd (Settings + Paul Tol palette — REJECT for v0.2)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\controls\tiles_preview\tiles_preview.gd (SubViewport overlay — REJECT)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\controls\tbt_plugin_control\theme_updater.gd (theme harmonization — REJECT)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\controls\tbt_plugin_control\popups\save_template_dialog.gd (REJECT)
    - C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\templates\ (12 .tres files) — list filenames + tile counts + custom_tags as a "curation pattern" (already-ADOPT)
  </read_first>
  <action>
    Create `.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` with the following sections in this order:

    **Header:**
    ```
    # Phase 3: TileBitTools Design-Inspiration Audit (Wave 0b deliverable, D-84)

    **Audited:** 2026-04-29
    **TBT version:** main branch as cloned at `C:\Programming_Files\Godot\tile_bit_tools-main\` (~3,825 LOC across 30 GDScript files + 12 .tres templates + 7 example PNGs)
    **Status:** Complete — all 9 audit categories classified
    **Policy:** D-73 (NO code copy, NO data lift). This audit produces IDEAS, not snippets. Every recommendation is written in PentaTile's own style as prose or pseudo-code; no TBT identifiers leak into PentaTile source.
    ```

    **Section 1 — TL;DR table.** A summary table with columns: `Pattern` | `Verdict` | `Action` | `Phase`. Verdict ∈ {ADOPT, PARTIAL, ADOPT-DEFERRED, REJECT}. Action is one short sentence. Phase is one of {3, 3.5, 4, 5, 0.3+, v2, never}.

    **Section 2 — Per-pattern verdict table.** Mandatory columns: `#` | `Pattern` | `TBT location (file:line LOC)` | `TileMapDual equivalent` | `Verdict` | `Reasoning (cite PROJECT.md / CLAUDE.md guardrail)` | `Action`.

    Cover all 9 of the patterns listed in `03-RESEARCH.md` § 6 "D-84 pattern locator table" PLUS:
      - The 12 bundled `.tres` files as a CURATION PATTERN (PentaTile already does an equivalent with bundled bitmask PNGs — verdict ADOPT-already-done)
      - `core/output.gd` verbosity channels (USER/INFO/DEBUG) — verdict REJECT for v0.2 (PentaTile has only `OS.is_debug_build()` `_rebuild_count` instrumentation; no multi-channel surface yet)
      - `controls/bit_data_draw/bit_data_draw.gd` peering-bit color overlay (~237 LOC) — verdict REJECT (PentaTile renders silhouettes, not bit colors)

    **Anti-pattern reminder for the table reasoning column:** when justifying a REJECT, cite the specific identity guardrail ("custom EditorPlugin polish" / "no editor UX" / "smaller and simpler than TileMapDual" / "no terrain peering"). Vague justifications fail the audit.

    **TBT class-name audit policy clarification (W-2):** TBT class names are PERMITTED in audit prose when citing TBT source (e.g., "TBT's `BitData` hierarchy lives at `core/bit_data.gd:1-N`"). The forbidden pattern is ADOPTING those names into PentaTile — i.e., recommendations of the form "PentaTile should add a `BitData` class" are forbidden; recommendations like "PentaTile could add a `PentaTileLayoutData` class with similar role" are permitted. The `class_name BitData` / `extends BitData` greps in the acceptance criteria catch the adoption pattern; descriptive citations are unaffected.

    **Section 3 — Cross-reference appendix.** For each pattern in Section 2, briefly describe what (if anything) TileMapDual does in the same conceptual space. TileMapDual's source is NOT vendored locally; if you don't have direct access, state "Not present in TileMapDual to my knowledge" and the audit still stands. Cite TileMapDual's GitHub repo URL if specific knowledge is invoked.

    **Section 4 — Backlog seeds (PARTIAL + ADOPT-DEFERRED only).** Each seed is a short subsection with:
      - **Title:** `Backlog seed: <pattern>`
      - **Suggested file:** `.planning/todos/pending/2026-04-29-<slug>.md` (the seed itself, NOT the audit file, declares this filename)
      - **Phase suggestion:** which roadmap phase / milestone
      - **Implementation sketch:** ≤10 lines of PROSE (NO code lift). Example: "Layout custom_tags vocabulary — add `tags : Array[StringName]` to PentaTileLayout base. Vocabulary: `[\"Public\", \"Tilesetter\", \"BorisTheBrave\", \"Empirical\"]`. Use Godot's `_get_property_list` to expose. Bounded to ~30 LOC; no editor UI."
      - **Why deferred:** what condition triggers the un-defer (layout count threshold, user request, milestone gate)

    Mandatory seeds based on RESEARCH § 6 pre-audit:
      - Layout `tags : Array[StringName]` vocabulary (PARTIAL ADOPT-DEFERRED → v0.3+ trigger: layout count ≥ 12)
      - `addons/penta_tile/output/show_debug_logs : bool` Project Settings key (PARTIAL ADOPT-DEFERRED → trigger: ≥2 verbosity surfaces)
      - Optional: TBT-style "save custom layout" dialog (REJECT for v0.2; capture as v2 backlog only if the audit surfaces a sound case)

    **Section 5 — Anti-pattern register (the REJECT reasoning crystallized).** A bullet list of ~6-10 patterns the audit explicitly REJECTS, with one-sentence "why this would harm PentaTile's identity" justifications. This section is the audit's load-bearing OUTPUT — future plan-phases reference it to bypass re-auditing.

    **Section 6 — Verification checklist (self-audit).** Before saving, confirm:
      - [ ] Every TBT file referenced in Section 2 has a verifiable `(file:line)` citation
      - [ ] Every PARTIAL has a corresponding Section 4 backlog seed
      - [ ] Every REJECT has a Section 5 register entry
      - [ ] No code blocks copy TBT source verbatim — all code blocks are PentaTile pseudo-code labelled as such
      - [ ] No identifier names from TBT (`BitData`, `EditorBitData`, `template_loader`, `tbt_plugin_control`) appear in any "Action" column or Section 4 implementation sketch — the audit names PentaTile's equivalents in PentaTile-style (`PentaTileLayoutData`, `_layout_loader`, etc.)

    **Section 7 — References.**
      - File paths under `C:\Programming_Files\Godot\tile_bit_tools-main\addons\tile_bit_tools\` (no GitHub URLs needed — local source)
      - `.planning/research/layouts/TILEBITTOOLS.md` (prior informational audit)
      - `.planning/PROJECT.md` Identity Guardrails section
      - `CLAUDE.md` Identity Guardrails / Coined-Term Discipline / Breaking Changes Policy

    **Length target: 600-1000 lines.** If the audit comes in shorter, expand the reasoning column in Section 2 (cite multiple guardrails per verdict where applicable). If longer, that's fine — the audit is research, not a tight artifact.

    **Anti-patterns:**
      - DO NOT copy/paste TBT GDScript code into the audit, even fenced as "this is what TBT does." Describe TBT behavior in PROSE; if pseudo-code is needed, write it in PentaTile's idiom. (D-73 enforcement.)
      - DO NOT reference `.tres` data layouts from `tile_bit_tools/templates/` in any verdict that informs Phase 3 layout slot tables. The audit may NOTE that TBT bundles 12 templates as a curation pattern, but it must NOT crib slot tables from those `.tres` files.
      - DO NOT use the audit to lobby for ADOPTING the EditorInspectorPlugin pattern. CLAUDE.md identity guardrails REJECT it; restating that REJECT in this audit is correct, but the audit is not the place to relitigate the project rejection.
      - DO NOT introduce Penta-prefixed names for TBT concepts that aren't the 5-archetype format (CLAUDE.md "Coined-Term Discipline"). Refer to PentaTile equivalents using the `PentaTile*` namespace prefix only.
  </action>
  <verify>
    <automated>powershell -Command "if ((Test-Path '.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md') -and ((Get-Content '.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md').Count -ge 350)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` exists and is at least 350 lines (target 600-1000)
    - File contains ALL of these literal heading substrings: `# Phase 3: TileBitTools Design-Inspiration Audit`, `Section 2 — Per-pattern verdict table`, `Section 4 — Backlog seeds`, `Section 5 — Anti-pattern register`
    - File contains ALL THREE verdict labels — `ADOPT`, `PARTIAL`, `REJECT` — at minimum once each
    - File contains AT LEAST 3 distinct file:line citations matching the regex pattern `core/[a-z_]+\.gd` or `inspector_plugin\.gd` or `controls/[a-z_/]+\.gd`
    - File contains ZERO occurrences of the GDScript keyword sequences `class_name BitData` or `class_name EditorBitData` or `class_name TemplateBitData` (anti-pattern: TBT identifiers MUST NOT appear in PentaTile-style recommendations)
    - File contains ZERO `extends BitData` references (anti-pattern: D-73 forbids leaking TBT class hierarchy into PentaTile source recommendations)
    - File `addons/penta_tile/ATTRIBUTION.md` does NOT exist after this task (anti-pattern guard — D-73)
    - File contains AT LEAST 1 occurrence of the literal substring `D-73` and AT LEAST 1 of `D-84` (proves the policy + decision references are in scope)
    - File contains AT LEAST 2 occurrences of `tile_bit_tools/` prefix in path citations (proves the audit referenced the verified TBT source-tree map)
    - File contains AT LEAST 1 backlog seed referencing `.planning/todos/pending/`
    - `git status` shows the new file as untracked OR as part of a clean commit
  </acceptance_criteria>
  <done>
    Audit deliverable shipped. Plan 04 (REQUIREMENTS/ROADMAP/STATE/README rewrites) can proceed in parallel with this. Any backlog seeds become real `.planning/todos/pending/` files in Phase 5 closeout (NOT this plan).
  </done>
</task>

</tasks>

<verification>
After Plan 02 completes:

1. The audit file exists at the documented path and is at minimum 350 lines.
2. `grep -c "ADOPT" .planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` returns >= 1; same for `PARTIAL` and `REJECT`.
3. `grep -c "class_name BitData\|extends BitData" .planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` returns 0 (no TBT identifier leakage).
4. `Test-Path addons/penta_tile/ATTRIBUTION.md` returns False (anti-pattern guard).

If any of these fail, the plan is INCOMPLETE.
</verification>

<success_criteria>
- [ ] `03-TBT-DEEP-AUDIT.md` exists at `.planning/phases/03-tilebittools-sourced-layouts/`
- [ ] All 9 RESEARCH §6 patterns + 2 supplementary patterns classified
- [ ] Cross-reference column populated for every pattern
- [ ] Backlog seeds exist for every PARTIAL / ADOPT-DEFERRED entry
- [ ] No code lift (no `class_name BitData` / `extends BitData` leakage)
- [ ] No `addons/penta_tile/ATTRIBUTION.md` created
- [ ] Length 600-1000 lines (350 minimum gate)
- [ ] Committed via `docs(03): add TBT design-inspiration audit (D-84 deliverable)`
</success_criteria>

<output>
After completion, create `.planning/phases/03-tilebittools-sourced-layouts/03-02-SUMMARY.md` per the GSD summary template. Required sections:
- Pattern count audited (target: ~11)
- Verdict distribution (count of ADOPT / PARTIAL / ADOPT-DEFERRED / REJECT)
- Backlog seed count + seed filenames suggested
- Anti-pattern verifications (no `BitData` identifier leakage; no ATTRIBUTION.md)
- Next: feeds Plan 04 (doc rewrites — Phase 3 README footnote acknowledges TBT as design inspiration per D-72/D-73)
</output>
