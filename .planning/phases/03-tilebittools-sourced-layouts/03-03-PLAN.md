---
phase: 03-tilebittools-sourced-layouts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - README.md
autonomous: true
requirements: [TBT-04, DOC-05]
tags: [phase-3, doc-rewrite, no-attribution-md, footnote, d-72, d-73]

must_haves:
  truths:
    - "Phase 3 entry in ROADMAP.md is retitled to ‘Public-Convention Layouts (Blob47 + Tilesetter)’ with the description matching D-72 (no ‘transcribed from TBT’ / no ‘Add ATTRIBUTION.md’ clauses)"
    - "REQUIREMENTS.md TBT-04 and DOC-05 are rewritten to reference a 1-line README footnote, NOT an `addons/penta_tile/ATTRIBUTION.md` file"
    - "README.md contains a 1-line footnote acknowledging TileBitTools as design inspiration with NO claim that code/data is reused"
    - "No `addons/penta_tile/ATTRIBUTION.md` file exists in the repository"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 entry retitled + description rewritten per D-72"
      contains: "Public-Convention Layouts"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TBT-04 + DOC-05 rewritten to README footnote"
      contains: "design inspiration"
    - path: "README.md"
      provides: "1-line footnote acknowledging TBT in External Resources section"
      contains: "tile_bit_tools"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "TBT-04 wording matches Phase 3 description in ROADMAP"
      pattern: "design inspiration"
    - from: ".planning/REQUIREMENTS.md"
      to: "README.md"
      via: "DOC-05 references the README footnote"
      pattern: "addons/penta_tile/README\\.md|README\\.md.*footnote"
---

<objective>
Land the D-72 + D-73 wording change across three docs and the README — TBT becomes "design inspiration only," `addons/penta_tile/ATTRIBUTION.md` is dropped from scope, replaced by a single-line README footnote.

This plan is parallel-runnable in Wave 1 alongside Plan 01 (pipeline patch + gate) and Plan 02 (TBT audit). It touches no source code and no other plans' files. The downstream Plans 04-05 (Blob47Godot + Tilesetter) can ship tests that grep for the README footnote AFTER this lands.

Purpose: ratify the user's policy decisions (D-72 retitle, D-73 no-attribution) into project artifacts so verifiers and future plan-phases inherit the policy without re-litigating.
Output: ~25 lines of markdown delta across 3 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tilebittools-sourced-layouts/03-CONTEXT.md
@.planning/phases/03-tilebittools-sourced-layouts/03-RESEARCH.md
@.planning/phases/03-tilebittools-sourced-layouts/03-PATTERNS.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ROADMAP.md Phase 3 entry per D-72</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (read full file — both the short Phase 3 line in the top "Phases" list AND the longer "### Phase 3:" detail block)
    - .planning/phases/03-tilebittools-sourced-layouts/03-CONTEXT.md "D-72" decision block (verbatim retitle wording)
  </read_first>
  <action>
    There are TWO occurrences of the Phase 3 description in `.planning/ROADMAP.md` to rewrite:

    **Edit 1 — the short "Phases" list line** (currently around line 26):

    Replace:
    ```
    - [ ] **Phase 3: TileBitTools-Sourced Layouts** — Transcribe slot tables from TBT's MIT-licensed `tilesetter_blob.tres`, `tilesetter_wang.tres`, and the matching Godot blob template `.tres`. Ship Blob47Godot, TilesetterWang15, TilesetterBlob47. Generate the 3 missing template PNGs from the slot tables. Add `ATTRIBUTION.md`.
    ```

    With:
    ```
    - [ ] **Phase 3: Public-Convention Layouts (Blob47 + Tilesetter)** — Ship `PentaTileLayoutBlob47Godot` (slot table from BorisTheBrave's 47-blob reference), `PentaTileLayoutTilesetterWang15`, and `PentaTileLayoutTilesetterBlob47` (slot tables sourced from each format's primary reference). Generate 3 bitmask PNGs co-located next to layout `.gd` files. Add a 1-line "design inspiration" footnote in `README.md` acknowledging TileBitTools — NO `addons/penta_tile/ATTRIBUTION.md` is created (D-73: no code or data is lifted from TBT).
    ```

    **Edit 2 — the long "### Phase 3:" detail block** (currently around line 120):

    Replace the heading line:
    ```
    ### Phase 3: TileBitTools-Sourced Layouts
    ```

    With:
    ```
    ### Phase 3: Public-Convention Layouts (Blob47 + Tilesetter)
    ```

    Then replace the **Goal** line:
    ```
    **Goal**: Three layouts whose slot tables are transcribed from TileBitTools' MIT-licensed `.tres` files (Tilesetter Wang 15, Tilesetter Blob 47, Godot Blob 47) ship with attribution. Greyboxed template PNGs are generated for these three layouts from the slot tables.
    ```

    With:
    ```
    **Goal**: Three layouts whose slot tables are sourced from each format's primary reference: `PentaTileLayoutBlob47Godot` from BorisTheBrave's 47-blob reference (`https://www.boristhebrave.com/permanent/24/06/cr31/stagecast/wang/blob.html`), `PentaTileLayoutTilesetterWang15` and `PentaTileLayoutTilesetterBlob47` from Tilesetter's manual + the user's own export sample (D-86 outcome). Co-located bitmask PNGs are generated for all three. A single-line README footnote acknowledges TileBitTools as design inspiration; per D-73 no code or data is lifted from TBT — the audit deliverable `03-TBT-DEEP-AUDIT.md` reads TBT source but produces only ideas/recommendations.
    ```

    Then UPDATE the **Success Criteria #4** entry:
    ```
    4. `addons/penta_tile/ATTRIBUTION.md` exists, credits TileBitTools by name with a link to https://github.com/dandeliondino/tile_bit_tools, copies the MIT license terms or links the upstream `LICENSE`, and identifies which TBT files were transcribed.
    ```

    Replace with:
    ```
    4. `README.md` "External Resources" section contains a 1-line footnote acknowledging TileBitTools (https://github.com/dandeliondino/tile_bit_tools) as design inspiration. Per D-73, NO `addons/penta_tile/ATTRIBUTION.md` is created — nothing is lifted from TBT, so nothing requires attribution. The audit deliverable `.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` reads TBT source for design analysis only.
    ```

    Update **Success Criteria #1, #2, #3** wording — replace each `transcribed from TBT's `tile_bit_tools/...`` phrase with `sourced from <FORMAT>'s primary reference (D-74 / D-75)`. Specifically:
      - SC #1: `... transcribed from TBT's tilesetter_wang.tres ...` → `... sourced from Tilesetter's primary reference (D-75 outcome) ...`
      - SC #2: `... transcribed from TBT's tilesetter_blob.tres ...` → `... sourced from Tilesetter's primary reference (D-75 outcome) ...`
      - SC #3: `... transcribed from the matching TBT template `.tres` ...` → `... sourced from BorisTheBrave's 47-blob reference (D-74) ...`

    Update **Success Criteria #5** wording — keep substantive content; just replace `_generate_greybox_templates.py` with `_generate_bitmasks.py` (the actual current filename per Phase 2 rename).

    **Anti-patterns:**
      - DO NOT delete or renumber the Coverage table at the bottom (Plan 04 / Plan 05 don't depend on table renumbering; preserving stability matters)
      - DO NOT rename the directory slug `03-tilebittools-sourced-layouts/` to `03-public-convention-layouts/` (per RESEARCH § 11 Q7 recommendation: keep the slug to avoid a sweeping rename across SUMMARYs and tooling references; D-72 makes this Claude's discretion and the directory rename overhead exceeds the benefit). Mention this NON-rename in the commit message so future readers understand the docs talk about "Public-Convention Layouts" but the directory still says `tilebittools`.
  </action>
  <verify>
    <automated>powershell -Command "$txt = Get-Content -Raw .planning/ROADMAP.md; if (($txt -match 'Public-Convention Layouts \(Blob47 \+ Tilesetter\)') -and ($txt -notmatch 'Add `ATTRIBUTION\.md`') -and ($txt -notmatch 'transcribed from TBT')) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains the literal substring `Public-Convention Layouts (Blob47 + Tilesetter)` exactly TWICE (once in the short Phases list, once in the detail-block heading) — confirm via `Select-String -Pattern 'Public-Convention Layouts \(Blob47 \+ Tilesetter\)' -Path .planning/ROADMAP.md` returning 2 matches
    - `.planning/ROADMAP.md` does NOT contain the literal substring `Add ATTRIBUTION.md` (D-73 anti-pattern; verify via `Select-String -SimpleMatch 'Add `ATTRIBUTION.md`'` returning 0 matches)
    - `.planning/ROADMAP.md` does NOT contain the literal substring `transcribed from TBT` (Phase 3 description rewrite; D-73 enforcement)
    - `.planning/ROADMAP.md` does NOT contain the literal substring `transcribed from TileBitTools` (D-73 enforcement; broader phrasing)
    - `.planning/ROADMAP.md` Success Criteria #4 references `1-line footnote` AND `https://github.com/dandeliondino/tile_bit_tools`
    - `.planning/ROADMAP.md` references the actual generator script filename `_generate_bitmasks.py` (NOT the obsolete `_generate_greybox_templates.py`)
    - `git status` reports the file as modified
  </acceptance_criteria>
  <done>ROADMAP.md Phase 3 entry retitled + Success Criteria #1-5 rewritten; no `Add ATTRIBUTION.md` clause remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md TBT-04 + DOC-05 + commit doc rewrites + README footnote</name>
  <files>
    .planning/REQUIREMENTS.md,
    README.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (read full file — locate the `### TileBitTools-Sourced Layouts (TBT)` section; TBT-04 entry; the `### Documentation (DOC)` section's DOC-05 entry; the Coverage Traceability table at the bottom)
    - .planning/phases/03-tilebittools-sourced-layouts/03-CONTEXT.md D-72 + D-73 wording
    - README.md lines 220-232 (the "External Resources" section — the natural spot for the design-inspiration footnote, after the existing TileMapDual / Excalibur references)
  </read_first>
  <action>
    Three edits — REQUIREMENTS.md (two entries), README.md (one footnote).

    **Edit 1 — `.planning/REQUIREMENTS.md` TBT-04.**

    Locate the entry in `### TileBitTools-Sourced Layouts (TBT)` section that currently reads:
    ```
    - [ ] **TBT-04**: `addons/penta_tile/ATTRIBUTION.md` credits TileBitTools (MIT, https://github.com/dandeliondino/tile_bit_tools) for the transcribed slot tables and links the upstream license file.
    ```

    Replace with:
    ```
    - [ ] **TBT-04**: `README.md` "External Resources" section contains a 1-line footnote acknowledging TileBitTools (https://github.com/dandeliondino/tile_bit_tools) as design inspiration. Per D-72 + D-73, NO `addons/penta_tile/ATTRIBUTION.md` is created — nothing is lifted from TBT, so nothing requires legal attribution. The Phase 3 audit deliverable `.planning/phases/03-tilebittools-sourced-layouts/03-TBT-DEEP-AUDIT.md` reads TBT source as design reference only.
    ```

    **ALSO update the section preamble** (immediately above TBT-01 in the same section). Currently reads:
    ```
    Layouts whose slot tables are transcribed (offline, by hand) from TileBitTools' MIT-licensed `.tres` files into hard-coded `mask_to_atlas` GDScript dispatch — no runtime decoding involved. Phase title was historically "Decoded Layouts" which was misleading; renamed 2026-04-28. Requirement ID prefix `TBT` is unchanged.
    ```

    Replace with:
    ```
    Layouts whose slot tables are sourced from each format's PRIMARY REFERENCE — `PentaTileLayoutBlob47Godot` from BorisTheBrave's published 47-blob reference (D-74), `PentaTileLayoutTilesetterWang15` and `PentaTileLayoutTilesetterBlob47` from Tilesetter's manual + the user-provided export sample (D-75, D-86 outcome). Phase title was historically "Decoded Layouts" then "TileBitTools-Sourced Layouts"; user pivot 2026-04-28 (D-72) renamed to "Public-Convention Layouts." Requirement ID prefix `TBT` is unchanged for traceability. Per D-73 no code or data is lifted from TBT — the audit deliverable `03-TBT-DEEP-AUDIT.md` reads TBT source but produces only ideas/recommendations.
    ```

    **Edit 2 — `.planning/REQUIREMENTS.md` DOC-05.**

    Locate (in `### Documentation (DOC)` section):
    ```
    - [ ] **DOC-05**: `addons/penta_tile/ATTRIBUTION.md` exists and credits TileBitTools (covered by TBT-04 but called out here as a doc deliverable).
    ```

    Replace with:
    ```
    - [ ] **DOC-05**: `README.md` "External Resources" section's TileBitTools design-inspiration footnote exists (covered by TBT-04 but called out here as a doc deliverable). Per D-72 + D-73, NO `addons/penta_tile/ATTRIBUTION.md` is created.
    ```

    **Edit 3 — `.planning/REQUIREMENTS.md` Out of Scope table.**

    Add a new row to the `## Out of Scope` table (NOT in v2 backlog — these are policy bans, not deferrals):
    ```
    | `addons/penta_tile/ATTRIBUTION.md` | D-72 / D-73: nothing is lifted from TBT, so nothing requires attribution. Replaced by a 1-line README footnote acknowledging TBT as design inspiration only. |
    | Code or data lift from TileBitTools | D-73: TBT is design-inspiration only. Layouts implemented from each format's primary reference. The audit `03-TBT-DEEP-AUDIT.md` reads TBT source for ideas, NOT for code/data extraction. |
    ```

    Insert these rows in alphabetical-ish order — they fit naturally near the bottom of the existing table.

    **Edit 4 — `README.md` design-inspiration footnote.**

    Locate the `## 🔗 External Resources` section (around line 223). Insert this NEW bullet AFTER the existing "Drawing Only 5 Tiles" bullet (around line 227, before the "Dual Tilemap Autotiling Technique (Excalibur.js)" bullet):

    ```
    - <a href="https://github.com/dandeliondino/tile_bit_tools" target="_blank" rel="noopener">TileBitTools (Godot 4 inspector plugin) ↗︎</a> - Design inspiration for PentaTile's layout-Resource architecture. PentaTile re-implements every layout from each format's primary reference (BorisTheBrave for 47-blob; Tilesetter manual for Tilesetter Wang/Blob); no code or data is copied from TBT.
    ```

    The wording is locked verbatim by this plan per RESEARCH § 11 Q6's recommended phrasing. Length: well under 250 chars including the link wrapper, fitting the "1-line footnote" requirement.

    **Anti-patterns:**
      - DO NOT create `addons/penta_tile/ATTRIBUTION.md` — D-73 explicitly forbids it. Verify with `Test-Path` after the commit.
      - DO NOT add a `## Attributions` heading or any section under that name in `addons/penta_tile/`. The footnote lives in the project README only.
      - DO NOT rephrase the footnote to claim "code derived from TBT" or "MIT-licensed slot tables" — D-73 makes those phrasings false. The footnote MUST say "design inspiration" and "no code or data is copied" (or equivalent).
      - DO NOT touch the existing "## 🙏 Attributions" section in README.md (line 233+) — that credits Kenney's Pico-8 Platformer + the Godot mascot; TBT does NOT belong there because nothing is lifted.

    **Commit message** (combine both rewrites with the README footnote in ONE atomic commit since they're a single policy ratification):
    ```
    docs(03): rewrite TBT-04/DOC-05 + ROADMAP Phase 3 + README footnote (D-72, D-73)

    - REQUIREMENTS.md: TBT-04 + DOC-05 rewritten — replace ATTRIBUTION.md
      with 1-line README footnote. TBT section preamble re-grounded on
      primary-source policy. Out-of-scope table grows by 2 rows.
    - ROADMAP.md: Phase 3 entry retitled "Public-Convention Layouts
      (Blob47 + Tilesetter)" in both Phases list and detail block.
      Success Criteria #1-5 rewritten — primary-reference sourcing
      replaces TBT data lift. Generator script reference fixed
      (_generate_bitmasks.py, not the obsolete _generate_greybox_templates.py).
    - README.md: 1-line footnote in External Resources acknowledging
      TBT as design inspiration. NO ATTRIBUTION.md (D-73).

    Directory slug `03-tilebittools-sourced-layouts/` intentionally
    NOT renamed — RESEARCH § 11 Q7 recommendation; rename overhead
    exceeds doc-hygiene benefit.
    ```
  </action>
  <verify>
    <automated>powershell -Command "$req = Get-Content -Raw .planning/REQUIREMENTS.md; $rm = Get-Content -Raw README.md; $att = Test-Path addons/penta_tile/ATTRIBUTION.md; if (($req -match 'TBT-04.*?design inspiration') -and ($req -match 'DOC-05.*?TileBitTools design-inspiration footnote') -and ($req -notmatch '`addons/penta_tile/ATTRIBUTION\.md` credits TileBitTools') -and ($rm -match 'TileBitTools.*?Design inspiration') -and (-not $att)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` TBT-04 entry contains the literal substring `1-line footnote` AND `design inspiration`
    - `.planning/REQUIREMENTS.md` TBT-04 entry does NOT contain the substring `addons/penta_tile/ATTRIBUTION.md` exists` (anti-pattern guard — that exact phrase came from the original wording)
    - `.planning/REQUIREMENTS.md` DOC-05 entry contains the literal substring `TileBitTools design-inspiration footnote` AND `NO `addons/penta_tile/ATTRIBUTION.md` is created`
    - `.planning/REQUIREMENTS.md` `## Out of Scope` section contains a row mentioning `addons/penta_tile/ATTRIBUTION.md` with rationale citing `D-72` AND `D-73`
    - `.planning/REQUIREMENTS.md` TBT section preamble references `BorisTheBrave's published 47-blob reference (D-74)` AND `Tilesetter's manual` AND `D-86 outcome`
    - `README.md` contains the literal substring `https://github.com/dandeliondino/tile_bit_tools` (proves the TBT link landed)
    - `README.md` contains the literal substring `Design inspiration for PentaTile's layout-Resource architecture`
    - `README.md` contains the literal substring `no code or data is copied`
    - `README.md` contains exactly ONE occurrence of `tile_bit_tools` link (no duplicate footnote)
    - The literal file `addons/penta_tile/ATTRIBUTION.md` does NOT exist (verify via `Test-Path` returning $False; also verify via `git ls-files` not listing it)
    - `git log -1 --format=%s` shows `docs(03): rewrite TBT-04/DOC-05 + ROADMAP Phase 3 + README footnote (D-72, D-73)`
    - `git status` reports a clean tree
  </acceptance_criteria>
  <done>
    Doc rewrites + README footnote ratified atomically. Plans 04-05 (Blob47Godot + Tilesetter implementation) can now reference TBT-04 / DOC-05 / Phase 3 ROADMAP entry as the FINAL form. Plan 06 (closeout) verifies these strings still match the implementation reality.
  </done>
</task>

</tasks>

<verification>
After Plan 03 completes:

1. `Select-String -Pattern 'Public-Convention Layouts \(Blob47 \+ Tilesetter\)' -Path .planning/ROADMAP.md` returns >= 2 matches.
2. `Select-String -SimpleMatch 'Add `ATTRIBUTION.md`' -Path .planning/ROADMAP.md` returns 0 matches.
3. `Select-String -SimpleMatch '1-line footnote' -Path .planning/REQUIREMENTS.md` returns >= 2 matches (TBT-04 + DOC-05).
4. `Select-String -SimpleMatch 'tile_bit_tools' -Path README.md` returns exactly 1 match.
5. `Test-Path addons/penta_tile/ATTRIBUTION.md` returns $False.
6. `git log -1 --format=%s` matches `docs(03):` prefix.
7. `git status` is clean.

If any of these fail the plan is INCOMPLETE.
</verification>

<success_criteria>
- [ ] ROADMAP.md Phase 3 retitled in both Phases list and detail block
- [ ] ROADMAP.md Success Criteria #1-5 reworded to drop "transcribed from TBT" wording
- [ ] REQUIREMENTS.md TBT-04 rewritten to README footnote
- [ ] REQUIREMENTS.md DOC-05 rewritten to match
- [ ] REQUIREMENTS.md Out of Scope table gains the ATTRIBUTION.md + code-lift bans
- [ ] README.md gains 1-line TBT design-inspiration footnote in External Resources
- [ ] No `addons/penta_tile/ATTRIBUTION.md` created
- [ ] Single atomic commit `docs(03):` covering all 3 files
- [ ] Directory slug intentionally NOT renamed (RESEARCH § 11 Q7)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tilebittools-sourced-layouts/03-03-SUMMARY.md` per the GSD summary template. Required sections:
- Files modified (3) + lines changed
- Anti-pattern verifications (no ATTRIBUTION.md; no `transcribed from TBT`; exactly 1 README footnote)
- D-72 / D-73 ratification notes
- Next: Plan 04 (Blob47Godot — fully unblocked, depends on Plan 01 pipeline patch only) and Plan 05 (Tilesetter — depends on Plan 01 D-86 gate outcome + Plan 04 PNG generator helper).
</output>
