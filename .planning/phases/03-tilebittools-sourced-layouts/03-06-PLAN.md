---
phase: 03-tilebittools-sourced-layouts
plan: 06
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified:
  - addons/penta_tile/tests/comprehensive_bitmask_test.gd
  - addons/penta_tile/tests/bitmask_bounds_test.gd
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TEMPLATE-02]
tags: [phase-3, closeout, matrix-integration, bounds-test, traceability]

must_haves:
  truths:
    - "All Phase-3 layouts that shipped (Blob47Godot always; Tilesetter pair conditional on D-86) appear in comprehensive_bitmask_test.gd's layouts array"
    - "All Phase-3 bundled PNGs that exist are validated by bitmask_bounds_test.gd"
    - "Phase 3 ROADMAP entry transitions from [ ] to [x] only after all 4 success criteria are confirmed (or scoped down per D-86 (b))"
    - "REQUIREMENTS.md Traceability table reflects each Phase-3 requirement's actual outcome (Complete / Deferred-to-v2)"
    - "STATE.md records Phase 3 closure + cumulative LOC delta"
    - "Full test suite green"
  artifacts:
    - path: "addons/penta_tile/tests/comprehensive_bitmask_test.gd"
      provides: "Pattern x layout matrix extended with Phase-3 layouts (1-3 entries)"
      contains: "Blob47Godot"
    - path: "addons/penta_tile/tests/bitmask_bounds_test.gd"
      provides: "PNG dimensions + slot-position checks extended with Phase-3 PNGs"
      contains: "blob_47_godot"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated with Phase-3 outcomes"
      contains: "TBT-03"
  key_links:
    - from: "addons/penta_tile/tests/comprehensive_bitmask_test.gd"
      to: "addons/penta_tile/layouts/penta_tile_layout_blob_47_godot.gd"
      via: "preload + layout matrix entry"
      pattern: "_Blob47GodotSc"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "TBT-01..04 + TEMPLATE-02 Coverage table updates match Phase 3 closure state"
      pattern: "TBT-0[1-4]"
---

<objective>
Phase 3 closeout — three responsibilities:

1. **Matrix integration** — extend `comprehensive_bitmask_test.gd` with Phase-3 layouts (Blob47Godot always; Tilesetter pair if D-86 = (a)/(c) and Plan 05 shipped them). Optionally extend the patterns array with 1-2 new patterns that exercise 8-Moore corner-with-both-edges configurations (RESEARCH section 8.1).

2. **Bounds-test extension** — extend `bitmask_bounds_test.gd` with each shipped Phase-3 PNG's expected dimensions + per-slot silhouette layout. Catches PNG/dict drift (Pitfall A: dict transcription error surfaces as a bounds-test failure).

3. **Doc reconciliation + Phase 3 closure** — update REQUIREMENTS.md Traceability table for TBT-01..04 + TEMPLATE-02 + DOC-05; if D-86 = (b), move TBT-01/02 + Tilesetter half of TEMPLATE-02 to v2 backlog with explicit deferred-to-v0.3+ markers; update ROADMAP.md Phase 3 row to `[x]` once all success criteria are confirmed; update STATE.md with cumulative LOC delta and Phase 3 close date.

Purpose: lock the Phase 3 outcome into project artifacts, run the full pattern-by-layout regression net, and signal Phase 4 (Fallback Routing) readiness.
Output: ~30 LOC GDScript test extensions, ~50 lines markdown delta across 3 doc files, full-suite verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tilebittools-sourced-layouts/03-CONTEXT.md
@.planning/phases/03-tilebittools-sourced-layouts/03-RESEARCH.md
@.planning/phases/03-tilebittools-sourced-layouts/03-PATTERNS.md
@.planning/phases/03-tilebittools-sourced-layouts/03-VALIDATION.md
@.planning/phases/03-tilebittools-sourced-layouts/03-01-SUMMARY.md
@.planning/phases/03-tilebittools-sourced-layouts/03-02-SUMMARY.md
@.planning/phases/03-tilebittools-sourced-layouts/03-03-SUMMARY.md
@.planning/phases/03-tilebittools-sourced-layouts/03-04-SUMMARY.md
@.planning/phases/03-tilebittools-sourced-layouts/03-05-SUMMARY.md
@CLAUDE.md
@addons/penta_tile/tests/comprehensive_bitmask_test.gd
@addons/penta_tile/tests/bitmask_bounds_test.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend matrix tests with Phase-3 layouts + 2 new 8-Moore patterns</name>
  <files>
    addons/penta_tile/tests/comprehensive_bitmask_test.gd,
    addons/penta_tile/tests/bitmask_bounds_test.gd
  </files>
  <read_first>
    - addons/penta_tile/tests/comprehensive_bitmask_test.gd (read full file — note `layouts` array near top with `name` / `script` / `is_dual_grid` fields per entry; note `patterns` array; note `_test_combo` body)
    - addons/penta_tile/tests/bitmask_bounds_test.gd (read full file — note the `_check_atlas` calls + helper functions per layout; note current per-layout silhouette helpers like `_corner_mask_silhouette` and `_solid_silhouette` if they exist)
    - .planning/phases/03-tilebittools-sourced-layouts/03-PATTERNS.md "addons/penta_tile/tests/comprehensive_bitmask_test.gd" + "bitmask_bounds_test.gd" sections
    - .planning/phases/03-tilebittools-sourced-layouts/03-05-SUMMARY.md (to determine which Phase-3 layouts shipped)
    - .planning/STATE.md `### Decisions` for the Plan 05 outcome record
  </read_first>
  <action>
    **Step 0 — Read STATE.md to determine which Phase-3 layouts shipped.**

    Grep STATE.md:
      - If `Plan 05 SKIPPED` appears → Tilesetter pair did NOT ship; integrate ONLY Blob47Godot.
      - Otherwise → all three Phase-3 layouts shipped; integrate all three.

    Set the variable `SHIPPED_LAYOUTS` accordingly: either `["Blob47Godot"]` or `["Blob47Godot", "TilesetterWang15", "TilesetterBlob47"]`.

    **Step 1 — Extend `addons/penta_tile/tests/comprehensive_bitmask_test.gd`.**

    Locate the preload-constants block near the top (after `extends SceneTree`). Add:
    ```
    const _Blob47GodotSc = preload("res://addons/penta_tile/layouts/penta_tile_layout_blob_47_godot.gd")
    ```

    If Tilesetter shipped, also add:
    ```
    const _TilesetterWang15Sc = preload("res://addons/penta_tile/layouts/penta_tile_layout_tilesetter_wang_15.gd")
    const _TilesetterBlob47Sc = preload("res://addons/penta_tile/layouts/penta_tile_layout_tilesetter_blob_47.gd")
    ```

    Locate the `var layouts: Array = [...]` definition (currently lists 5 entries: DualGrid16, Wang2Edge, Wang2Corner, Min3x3, Penta). Append:
    ```
        {"name": "Blob47Godot", "script": _Blob47GodotSc, "is_dual_grid": false},
    ```

    If Tilesetter shipped, also append:
    ```
        {"name": "TilesetterWang15", "script": _TilesetterWang15Sc, "is_dual_grid": false},
        {"name": "TilesetterBlob47", "script": _TilesetterBlob47Sc, "is_dual_grid": false},
    ```

    **Step 2 — Add 2 new 8-Moore-revealing patterns to the `patterns` array** (RESEARCH § 8.1). Insert after the existing `hollow_ring` pattern (or whatever the last pattern is in the current matrix):

    ```
        {"name": "plus_with_diagonals", "cells": [
            Vector2i(0, 0), Vector2i(1, 0), Vector2i(2, 0),
            Vector2i(0, 1), Vector2i(1, 1), Vector2i(2, 1),
            Vector2i(0, 2), Vector2i(1, 2), Vector2i(2, 2),
        ]},
        {"name": "diag_chain", "cells": [
            Vector2i(0, 0), Vector2i(1, 1), Vector2i(2, 2), Vector2i(3, 3),
        ]},
    ```

    **Why these patterns:** RESEARCH § 8.1 documents that the existing 16-pattern set adequately covers 4-cardinal layouts but doesn't specifically exercise 47-blob's "corner-with-both-edges" and "corner-collapses-to-zero" branches. `plus_with_diagonals` produces a center cell at mask=255 (all 4 edges + all 4 corners survive collapse). `diag_chain` produces cells whose only neighbors are diagonal (NE/SE/SW/NW) with NO edge bits set, exercising the corner-collapses-to-zero rule.

    **Step 3 — Extend `addons/penta_tile/tests/bitmask_bounds_test.gd`.**

    Locate the `_check_atlas` calls at the bottom of `_initialize` (currently 5 calls, one per Phase 2 layout). Append for each shipped Phase-3 layout. Use the existing solid-silhouette helper if `_check_atlas` already supports it, otherwise add a generic shared helper.

    For Blob47Godot:
    ```
    _check_atlas("Blob47Godot",
        "res://addons/penta_tile/layouts/penta_tile_layout_blob_47_godot.png",
        Vector2i(7, 7))
    ```

    For Wang15 (if shipped):
    ```
    _check_atlas("TilesetterWang15",
        "res://addons/penta_tile/layouts/penta_tile_layout_tilesetter_wang_15.png",
        Vector2i(6, 3))
    ```

    For TilesetterBlob47 (if shipped):
    ```
    _check_atlas("TilesetterBlob47",
        "res://addons/penta_tile/layouts/penta_tile_layout_tilesetter_blob_47.png",
        Vector2i(11, 5))
    ```

    Adjust the parameter list to MATCH the existing `_check_atlas` signature in the file. If the existing signature requires a per-slot silhouette helper as the 4th argument, pass either an existing `_solid_silhouette` helper (if defined) or `Callable()` to skip per-slot inspection (for Phase-3 silhouettes the layout's `mask_to_atlas` test already verifies dispatch correctness; PNG bounds verification only needs to confirm dimensions + atlas-occupancy alignment).

    **Edge case — atlas-occupancy gaps:** Phase-3 PNGs have intentional transparent cells:
      - `Blob47Godot`: cells (5, 6) and (6, 6) are gaps.
      - `TilesetterWang15`: cells (5, 1) and (5, 2) are gaps.
      - `TilesetterBlob47`: 8 gap cells per the documented occupancy table (col 10 rows 0-1; row 4 cols 0-3 + cols 9-10).

    If `_check_atlas` strictly asserts "every (col, row) in grid is opaque," extend it with an optional `gap_cells: Array[Vector2i] = []` parameter to whitelist transparent cells. Document in the helper's docstring.

    **Step 4 — Run the extended tests.**
      1. `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1 -Test comprehensive_bitmask_test`. Confirm exit 0 with `ALL PASS`. Pre-Phase-3: 5 layouts × ~16 patterns = 80 combos. Post-Phase-3: 6-8 layouts × 18 patterns = 108-144 combos. Expect ~30-60 second runtime (vs prior ~10s).
      2. `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1 -Test bitmask_bounds_test`. Confirm exit 0 with `ALL PASS`.
      3. `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1` (full suite). All tests green: 15 tests if D-86 = (b); 17 if D-86 = (a)/(c).

    **Anti-patterns:**
      - DO NOT remove existing layouts from the matrix to make room — the matrix grows, doesn't substitute.
      - DO NOT add the new patterns inside a `phase 3 only` branch — they exercise general behavior all layouts should handle correctly. The 4-cardinal layouts (Wang2Edge, Min3x3) just compute their own masks per pattern; the test asserts cells render, not specific mask values.
      - DO NOT skip the bitmask_bounds_test extension if D-86 = (b) — the Blob47Godot PNG still ships and needs bounds verification.
      - DO NOT use `randi()` in either test (Pitfall #2).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1 -Test comprehensive_bitmask_test</automated>
  </verify>
  <acceptance_criteria>
    - File `addons/penta_tile/tests/comprehensive_bitmask_test.gd` contains the literal substring `_Blob47GodotSc`
    - File `comprehensive_bitmask_test.gd` `layouts` array includes the literal `"Blob47Godot"`
    - If D-86 ≠ (b): file also contains `_TilesetterWang15Sc` AND `_TilesetterBlob47Sc` AND the strings `"TilesetterWang15"` AND `"TilesetterBlob47"`
    - File `comprehensive_bitmask_test.gd` `patterns` array includes both `"plus_with_diagonals"` AND `"diag_chain"` (verifiable via grep)
    - File `addons/penta_tile/tests/bitmask_bounds_test.gd` contains the literal substring `penta_tile_layout_blob_47_godot.png`
    - If D-86 ≠ (b): file also contains `penta_tile_layout_tilesetter_wang_15.png` AND `penta_tile_layout_tilesetter_blob_47.png`
    - `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1 -Test comprehensive_bitmask_test` exits 0 with `ALL PASS`
    - `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1 -Test bitmask_bounds_test` exits 0 with `ALL PASS`
    - `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1` (full suite) exits 0
    - Neither test file contains `randi(`
  </acceptance_criteria>
  <done>
    Phase-3 layouts are integrated into the canonical regression nets. Pattern matrix grows by 1-3 layouts × 2 new patterns. Bounds tests verify all shipped Phase-3 PNGs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS Traceability + ROADMAP Phase 3 status + STATE.md closure</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate the Traceability table at the bottom — the rows for TBT-01, TBT-02, TBT-03, TBT-04, TEMPLATE-02, DOC-05 currently show `Phase: 3, Status: Pending`)
    - .planning/ROADMAP.md (locate the Phase 3 row in the Progress table near the bottom — currently shows `[ ]` and `0/TBD`; locate the short Phase 3 line in the top Phases list — currently `[ ]`)
    - .planning/STATE.md (locate `## Current Position` + `## Accumulated Context > ### Roadmap Evolution`; the latter is where Phase 3 closure entries belong, dated)
    - .planning/phases/03-tilebittools-sourced-layouts/03-05-SUMMARY.md to determine if Tilesetter shipped
    - All earlier Phase-3 SUMMARY.md files (01..05) for cumulative LOC totals
  </read_first>
  <action>
    Three doc files updated atomically.

    **Step 1 — REQUIREMENTS.md Traceability table updates.**

    For each Phase-3 requirement, change the Status column based on what shipped:

    Always-Complete (regardless of D-86 outcome):
      - **TBT-03** Status: `Complete (Plan 04 / commit <hash>: PentaTileLayoutBlob47Godot ships with 7×7 atlas + collapse rule + 47-entry dict + collapse test green)`
      - **TBT-04** Status: `Complete (Plan 03: README "External Resources" footnote acknowledges TBT as design inspiration; ATTRIBUTION.md NOT created per D-73)`
      - **DOC-05** Status: `Complete (Plan 03: same as TBT-04 — 1-line README footnote)`
      - **TEMPLATE-02** Status (if D-86 ≠ (b)): `Complete (Plan 04 + Plan 05: 3 bundled PNGs at penta_tile_layout_blob_47_godot.png / _tilesetter_wang_15.png / _tilesetter_blob_47.png; bounds test green)`
      - **TEMPLATE-02** Status (if D-86 = (b)): `Partial — Blob47Godot PNG ships (Plan 04); Tilesetter pair deferred to v0.3+ per D-86 option (b) gate decision`

    D-86-conditional (TBT-01 + TBT-02):
      - If D-86 = (a) or (c): both Status: `Complete (Plan 05 / commit <hash>)`
      - If D-86 = (b): both Status: `Deferred to v0.3+ per D-86 (Plan 01 Task 1 gate; Plan 05 SKIPPED)`

    **Step 2 — REQUIREMENTS.md backlog if D-86 = (b).** Add to the v2 Requirements section a new subsection:

    ```
    ### TBT (Phase 3 deferred — Tilesetter only)

    - **TBT-01-DEFERRED**: PentaTileLayoutTilesetterWang15 (5×3 atlas + stray-fill slot) — primary source not located in 2026-04-28 plan-phase research (RESEARCH § 3); user opted to defer per D-86 option (b). Re-trigger when user has a sample Tilesetter export OR when v0.3+ planning revisits.
    - **TBT-02-DEFERRED**: PentaTileLayoutTilesetterBlob47 (11×5 atlas with sub-block gaps) — same reason as TBT-01-DEFERRED.
    ```

    Also update the v2 Out of Scope table with a row noting that Tilesetter primary-source absence is a real upstream barrier, not a PentaTile design choice.

    **Step 3 — ROADMAP.md Phase 3 row.**

    In the Progress table at the bottom, change the Phase 3 row:
      - From: `| 3. TileBitTools-Sourced Layouts | 0/TBD | Not started | - |`
      - To (if D-86 = (a)/(c)): `| 3. Public-Convention Layouts (Blob47 + Tilesetter) | 6/6 | Complete (3 layouts + audit + footnote + 8-Moore patch; <test count> automated tests green; cumulative <runtime LOC> runtime LOC) | <YYYY-MM-DD> |`
      - To (if D-86 = (b)): `| 3. Public-Convention Layouts (Blob47 only; Tilesetter deferred to v0.3+) | 6/6 | Complete with reduced scope per D-86 (b) — Blob47Godot ships; Tilesetter pair backlog. <test count> tests green. | <YYYY-MM-DD> |`

    Also flip the short Phase 3 line in the top "Phases" list from `[ ]` to `[x]`.

    **Step 4 — STATE.md closure entry.**

    Append to the `## Accumulated Context > ### Roadmap Evolution` section (matches the format of the existing dated bullets):

    ```
    - 2026-04-29: **Phase 3 closed.** Public-Convention Layouts shipped:
      <list>(a)/(c)</list>: Blob47Godot + TilesetterWang15 + TilesetterBlob47 — 3 layouts + 3 bundled PNGs + 4 new tests + 1 audit deliverable + 1 README footnote + 8-Moore pipeline patch.
      <list>(b)</list>: Blob47Godot only; Tilesetter pair deferred to v0.3+ per D-86 option (b) — 1 layout + 1 PNG + 2 new tests + 1 audit + 1 README footnote + 8-Moore patch.
      Cumulative runtime LOC: <Phase 2 baseline 1827 + Phase 3 delta XXX = total>. Identity guardrail: <STATUS — within budget if cumulative < 2500; AT RISK note if higher>.
      No ATTRIBUTION.md created (D-73). No TBT data lifted (D-73). 47-blob layout: BorisTheBrave primary source (D-74). Tilesetter slot tables: <D-86 outcome citation>. Test suite grew 12 (Phase 2 close) → <NEW total> (Phase 3 close).
    ```

    Use whichever paragraph matches the actual D-86 outcome. Compute cumulative LOC by:
    ```
    git ls-files addons/penta_tile -- '*.gd' | xargs wc -l | tail -1
    ```
    Report the total minus the test-file line count (tests aren't runtime LOC). If unsure, document the methodology inline.

    Also update `## Current Position` near the top of STATE.md:
    ```
    Phase: 03 (public-convention-layouts) — COMPLETE (D-86 outcome <a/b/c>; <X> plans executed; cumulative <YYY> runtime LOC; full test suite <Z> green)
    ```

    **Step 5 — Run the full suite ONE MORE TIME to confirm Phase 3 green.**

    ```
    powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1
    ```

    All tests must pass. The expected count:
      - D-86 = (a)/(c): 17 tests (12 Phase 2 + 5 Phase 3 — 8-Moore propagation + Blob47 collapse + Blob47 hollow + Wang15 dispatch + TilesetterBlob47 collapse)
      - D-86 = (b): 15 tests (12 Phase 2 + 3 Phase 3 — 8-Moore propagation + Blob47 collapse + Blob47 hollow)

    **Step 6 — Final commit.**

    ```
    docs(03): close Phase 3 — Traceability + ROADMAP + STATE updates (D-86 outcome <a/b/c>)

    - REQUIREMENTS.md Traceability: TBT-03/04 + TEMPLATE-02 + DOC-05 marked Complete.
      <if D-86=(a)/(c): TBT-01/02 marked Complete (Plan 05).>
      <if D-86=(b): TBT-01/02 + Tilesetter half of TEMPLATE-02 marked Deferred to v0.3+;
                   v2 Requirements section gains TBT-01-DEFERRED + TBT-02-DEFERRED.>
    - ROADMAP.md: Phase 3 row flipped to [x] with completion date and runtime LOC delta.
    - STATE.md: Roadmap Evolution closes Phase 3 with cumulative LOC + test count + D-86 outcome citation.

    Phase 4 (Fallback Routing) is now ready to plan via `/gsd-plan-phase 4`.
    ```

    **Anti-patterns:**
      - DO NOT mark TBT-01 / TBT-02 as Complete if D-86 = (b). The user explicitly deferred them; marking them Complete misrepresents the milestone.
      - DO NOT delete the original TBT-04 / DOC-05 wording from REQUIREMENTS.md history — Plan 03 already rewrote them; this task only updates the Traceability table Status column.
      - DO NOT add Phase-4-related entries to STATE.md (Phase 4 is the next planning step, not closed by this plan).
      - DO NOT renumber Phase 3 to remove the placeholder slug `03-tilebittools-sourced-layouts/`. The directory rename is OPTIONAL per RESEARCH § 11 Q7; we keep the slug to minimize cross-doc churn.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` Traceability table for `TBT-03` Status column contains `Complete`
    - `.planning/REQUIREMENTS.md` Traceability table for `TBT-04` Status column contains `Complete` AND references `README` footnote
    - `.planning/REQUIREMENTS.md` Traceability table for `DOC-05` Status column contains `Complete`
    - If D-86 = (a) or (c): Traceability rows for `TBT-01` AND `TBT-02` show `Complete`
    - If D-86 = (b): Traceability rows for `TBT-01` AND `TBT-02` show `Deferred` AND a v2 Requirements subsection contains `TBT-01-DEFERRED` AND `TBT-02-DEFERRED`
    - `.planning/ROADMAP.md` short Phase 3 line contains `[x]` (not `[ ]`)
    - `.planning/ROADMAP.md` Progress table Phase 3 row contains `Complete` AND a date matching the regex `\d{4}-\d{2}-\d{2}`
    - `.planning/STATE.md` `## Accumulated Context > ### Roadmap Evolution` contains a 2026-04-29 entry referencing `Phase 3 closed`
    - `.planning/STATE.md` `## Current Position` references `Phase 03` AND `COMPLETE`
    - `git log -1 --format=%s` matches `docs(03): close Phase 3` AND references `D-86`
    - `powershell -ExecutionPolicy Bypass -File ./addons/penta_tile/tests/run_tests.ps1` (full suite) exits 0
    - `git status` reports a clean tree
    - File `addons/penta_tile/ATTRIBUTION.md` does NOT exist (D-73 anti-pattern guard, final check)
  </acceptance_criteria>
  <done>
    Phase 3 is closed. Traceability is consistent across REQUIREMENTS / ROADMAP / STATE. Test suite is green. Phase 4 (Fallback Routing) can be planned next via `/gsd-plan-phase 4`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase artifacts → future plan-phases | REQUIREMENTS.md / ROADMAP.md / STATE.md are read by every subsequent plan-phase. Inaccurate Status fields lead future agents to mis-scope (e.g., re-shipping a deferred requirement). Mitigation: explicit acceptance criteria grep for the right Status keywords. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | Wrong Status field on a Traceability row | mitigate | Acceptance criteria explicitly require D-86-outcome-conditional grep results; mismatched Status caught at plan-check / verify time |
| T-03-06-02 | Denial of Service | Matrix test runtime growth | accept | Pattern × layout matrix grows from 80 to up to 144 combos. Demo-scale; runtime under 1 minute is acceptable. |
| T-03-06-03 | Tampering | Lossy LOC accounting | mitigate | LOC delta documented inline in STATE.md with the methodology; future audits can re-run `git ls-files | xargs wc -l` and reconcile |
</threat_model>

<verification>
After Plan 06 completes:

1. `Select-String -Pattern '\[x\] \*\*Phase 3' -Path .planning/ROADMAP.md` returns 1 match.
2. REQUIREMENTS.md Traceability for TBT-03 / TBT-04 / DOC-05 shows `Complete`; TBT-01/02 shows `Complete` if D-86 = (a)/(c) or `Deferred` if D-86 = (b).
3. STATE.md Roadmap Evolution has a 2026-04-29 Phase 3 close entry.
4. The full test suite runs green at the expected count.
5. `Test-Path addons/penta_tile/ATTRIBUTION.md` returns False.
6. `git status` is clean.

If any of these fail, Plan 06 is INCOMPLETE.
</verification>

<success_criteria>
- [ ] comprehensive_bitmask_test extended with Phase-3 layouts + 2 new patterns
- [ ] bitmask_bounds_test extended with Phase-3 PNG paths
- [ ] REQUIREMENTS.md Traceability table updated for all 6 Phase-3 requirements
- [ ] If D-86 = (b): v2 Requirements gains TBT-01-DEFERRED + TBT-02-DEFERRED
- [ ] ROADMAP.md Phase 3 row flipped to [x] with date
- [ ] STATE.md Phase 3 close entry + cumulative LOC reported
- [ ] No ATTRIBUTION.md created (D-73 final guard)
- [ ] Full suite green
- [ ] Atomic commit `docs(03): close Phase 3`
</success_criteria>

<output>
After completion, create `.planning/phases/03-tilebittools-sourced-layouts/03-06-SUMMARY.md` per the GSD summary template. Required sections:
- D-86 outcome (verbatim) — must align with all earlier Phase-3 SUMMARY records
- Test count growth (12 Phase 2 close → final Phase 3 close count)
- Cumulative runtime LOC (Phase 2 baseline 1827 + Phase 3 delta = total)
- Identity guardrail status (within budget / AT RISK)
- Phase 3 success criteria checklist (all 4 from ROADMAP.md confirmed met OR scoped down with rationale)
- Anti-pattern guards verified (no ATTRIBUTION.md; no `tile_bit_tools/` references in any new file across all Phase 3 plans)
- Phase 3 closure → Phase 4 (Fallback Routing) is the next planning step
</output>
