---
phase: 04-fallback-routing
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03-PLAN
files_modified:
  - .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md
  - .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md
  - .planning/phases/04-fallback-routing/04-CODEX-REVIEW-FIX.md
autonomous: false
requirements: []
tags:
  - cross-ai-review
  - codex
  - fix-loop
must_haves:
  truths:
    - "04-CODEX-PROMPT.md exists — the headless Codex CLI prompt mirroring 04-GEMINI-PROMPT.md (same review surface, same severity/theme schema, same disqualification list)"
    - "04-CODEX-REVIEW.md exists — raw Codex findings against the POST-Gemini-fix codebase per D-04-10 (strict order)"
    - "04-CODEX-REVIEW-FIX.md exists — disposition row per finding + atomic-fix commit SHAs"
    - "Every applied Codex finding has a corresponding `fix(04): CODEX-{SEVERITY-LETTER}-{NN} — ...` commit per D-04-15"
    - "Test suite stays ALL GREEN (18 tests) after every applied fix"
    - "No D-04-14 hard-trigger findings slip through (compat shim / forward-compat / Phase 5 / ATTRIBUTION.md / Coined-Term / locked-decision)"
    - "Codex pass runs AFTER Gemini fix-loop is complete per D-04-10 — codebase Codex reviews is the post-Gemini-fix state"
  artifacts:
    - path: ".planning/phases/04-fallback-routing/04-CODEX-PROMPT.md"
      provides: "Headless Codex CLI prompt"
      contains: "Severity: Critical | High | Medium | Low | Info"
    - path: ".planning/phases/04-fallback-routing/04-CODEX-REVIEW.md"
      provides: "Raw Codex findings"
      contains: "reviewer: codex"
    - path: ".planning/phases/04-fallback-routing/04-CODEX-REVIEW-FIX.md"
      provides: "Per-finding disposition log + atomic-fix commit SHAs"
      contains: "## Disposition Table"
  key_links:
    - from: ".planning/phases/04-fallback-routing/04-CODEX-REVIEW-FIX.md"
      to: "git log --oneline | grep 'fix(04): CODEX-'"
      via: "atomic-commit-per-finding linkage per D-04-15"
      pattern: "fix\\(04\\): CODEX-"
    - from: ".planning/phases/04-fallback-routing/04-CODEX-PROMPT.md"
      to: ".planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md"
      via: "shared template — same review surface + schema, only reviewer name and prior-pass note differ"
      pattern: "Codex Cross-AI Review"
---

<objective>
Run the SECOND cross-AI review pass — Codex headless — on the post-Gemini-fix codebase per D-04-10 (strict order). Apply valid findings via the same severity-tiered + disqualification-list workflow as Plan 03.

Purpose: Phase 4 success criterion #7 — `/gsd-review codex` (or `codex exec --skip-git-repo-check -` per RESEARCH § 3 / Phase 3.5 precedent) against the post-Gemini-fix codebase provides genuine "second-look" coverage. The two-reviewer pattern (D-04-09) substitutes Gemini for Cursor (subscription gap) + Codex for IDE-based options (Antigravity is IDE-only); both are headless CLI-driven.

Output: 3 markdown artifacts + N atomic fix-commits. After this plan completes, Plan 05 inspects all 4 closeout artifacts (FALLBACK-UAT, DOC-SWEEP, GEMINI-REVIEW-FIX, CODEX-REVIEW-FIX) and flips ROADMAP `[x]`.

Per D-04-13, this plan is `autonomous: false` because Medium-tier Codex findings require user approval before being committed; Low/Info findings require user choice between apply / defer.

This plan is structurally a TWIN of Plan 03 (Gemini), with: different reviewer name, different invocation CLI, different ID prefix (`CODEX-`), and the post-Gemini-fix codebase as input. Same prompt template + same disposition workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/04-fallback-routing/04-CONTEXT.md
@.planning/phases/04-fallback-routing/04-RESEARCH.md
@.planning/phases/04-fallback-routing/04-PATTERNS.md
@.planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md
@.planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md
@.planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md
@.planning/phases/04-fallback-routing/04-03-SUMMARY.md
@.planning/phases/02-native-layouts/02-REVIEW-FIX.md
@.planning/phases/03.5-pixellab-layouts-variation-seed-wiring/03.5-REVIEWS.md

<interfaces>
<!-- Codex CLI invocation (verified per RESEARCH § 3 — Codex CLI 0.124.0). -->

**Codex headless invocation** (per RESEARCH § 3 + Phase 3.5 precedent in `03.5-REVIEWS.md`):
```bash
cat .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md | codex exec --skip-git-repo-check - > .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md 2>&1
```

PowerShell:
```powershell
Get-Content .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md | codex exec --skip-git-repo-check - > .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md 2>&1
```

**Output post-check** (RESEARCH § 8 Pitfall #14): `[ -s 04-CODEX-REVIEW.md ]`. On empty: retry with `codex review` subcommand: `cat 04-CODEX-PROMPT.md | codex review - > 04-CODEX-REVIEW.md`.

**Atomic-commit-per-fix format** (D-04-15):
```
fix(04): CODEX-{SEVERITY-LETTER}-{NN} — {one-line description}
```
Example: `fix(04): CODEX-M-03 — clarify _make_slot axis-invariance contract in doc-comment`. Em-dash (`—`, U+2014).

**Severity ladder, disqualification triggers, severity-tiered policy** — all same as Plan 03 (sourced from D-04-13 / D-04-14 / RESEARCH § 4 / § 5 / § 7). See Plan 03 `<interfaces>` section for the full reference.

**Findings That Touch the Same Line as a Gemini Fix** (RESEARCH § 6 "Findings That Touch the Same Line"): apply sequentially, NOT via rebase. The Codex commit builds on the Gemini commit. The disposition table records both commits independently with their respective SHAs. If two findings genuinely conflict — apply judgment, pick one, log the rejected one as `rejected-other` with rationale citing the conflicting Gemini finding ID.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 04-CODEX-PROMPT.md (the headless review prompt)</name>
  <files>.planning/phases/04-fallback-routing/04-CODEX-PROMPT.md</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md (the Plan 03 prompt — copy structure VERBATIM, swap reviewer name + tooling references)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 3 "Codex CLI" + § 4 (output schema) + § 5 (disqualification) + § 8 Pitfall #11 (Coined-Term guard) + Pitfall #12 (TileMapDual identity)
    - .planning/phases/04-fallback-routing/04-PATTERNS.md "Deliverable 3 / 04-CODEX-REVIEW.md" subsection ("Identical schema to 04-GEMINI-REVIEW.md... reviewer: codex... CODEX-{...} IDs... post-Gemini-fix codebase")
    - .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md (so Codex sees the prior-pass disposition log — included as supporting evidence in the review surface so Codex doesn't re-propose what Gemini already proposed)
    - CLAUDE.md (Identity Guardrails + Coined-Term Discipline + Breaking Changes Policy + Critical Pitfalls)
    - .planning/PROJECT.md (identity constraint statement)
  </read_first>
  <action>
Create `.planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` as a STRUCTURAL TWIN of `04-GEMINI-PROMPT.md` (the Plan 03 prompt). Differences:

1. **Title:** `# Codex Cross-AI Review — PentaTile Phase 4` (was: Gemini).
2. **Pre-amble note:** add a 2-sentence note explaining this is the SECOND pass per D-04-10 strict order, and that Codex is reviewing the POST-Gemini-fix codebase. Quote: "Per CONTEXT.md D-04-10, this is the SECOND pass of a strict-order Gemini → fix → Codex → fix workflow. The first reviewer (Gemini) has already filed findings, the implementer dispositioned them, and atomic fix-commits landed. You are reviewing the POST-Gemini-fix codebase to provide genuine 'second-look' coverage. The Gemini disposition log (`04-GEMINI-REVIEW-FIX.md`) is included in your review surface — please AVOID re-filing findings already addressed there (whether `applied`, `rejected-disqualification`, or `deferred`)."
3. **Review surface bullet list:** ADD `04-GEMINI-REVIEW.md` and `04-GEMINI-REVIEW-FIX.md` to the list of in-scope `.planning/` files so Codex can see the prior-pass findings + their dispositions.
4. **Output frontmatter:** `reviewer: codex` (was: `reviewer: gemini`).
5. **Output finding IDs:** `CODEX-{C|H|M|L|I}-{NN}` (was: `GEMINI-...`).

**Everything else is identical to 04-GEMINI-PROMPT.md** — same Hard Constraints (CLAUDE.md Identity Guardrails verbatim + Breaking Changes Policy HARD RULE + Coined-Term Discipline), same 7 Disqualification Triggers, same Severity × Theme rubric, same per-finding REQUIRED schema, same anti-hallucination guard, same "output ONLY the markdown report" instruction.

The 7 hard triggers from RESEARCH § 5 land VERBATIM. Specifically the locked-decision list now includes one more constraint Codex must respect:

> 7a. **Already-dispositioned Gemini findings.** If you observe an issue that Gemini also flagged AND was dispositioned in `04-GEMINI-REVIEW-FIX.md` (whether applied / rejected / deferred), DO NOT re-file the finding unless your insight differs materially — in which case clearly state "Gemini also flagged this as GEMINI-XXX-NN; my finding differs because <X>."

This guard prevents redundant findings at the disposition layer.

**Constraints:**
- Per CLAUDE.md Coined-Term Discipline — don't introduce new "Penta" prefixes in the prompt prose.
- Per CLAUDE.md HARD RULE — no `version:` references / no compat-shim suggestions.
- Per RESEARCH § 8 Pitfall #11 — Coined-Term guard verbatim in prompt.
- Per RESEARCH § 8 Pitfall #12 — TileMapDual identity guardrails verbatim.
- Per D-04-04 — do NOT propose adding a doc-coverage lint test.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md && grep -c 'reviewer: codex\|CODEX-{' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md</automated>
  </verify>
  <acceptance_criteria>
    - **File exists and non-trivial:** `wc -l .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 80 lines.
    - **Title says "Codex":** `head -2 .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md | grep -c 'Codex Cross-AI Review'` returns 1.
    - **Strict-order note present:** `grep -c 'D-04-10\|second pass\|strict-order' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 1.
    - **Gemini prior-pass artifacts included in review surface:** `grep -c '04-GEMINI-REVIEW\|04-GEMINI-REVIEW-FIX' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 2.
    - **Output frontmatter says `reviewer: codex`:** `grep 'reviewer: codex' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 1.
    - **Finding IDs use `CODEX-` prefix:** `grep -c 'CODEX-{C|H|M|L|I}-{NN}\|CODEX-' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 2.
    - **CLAUDE.md identity guardrails embedded verbatim:** `grep -c 'visibly smaller and simpler than TileMapDual' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 1.
    - **Coined-Term Discipline embedded:** `grep -c 'Coined-Term Discipline\|"Penta" is reserved' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 1.
    - **All 7 disqualification triggers present:** `grep -cE '^[1-7]\.' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns ≥ 7.
    - **No `@deprecated` proposal text / lint-test proposal:** `grep -i 'add @deprecated\|add a doc.coverage lint' .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md` returns nothing.
  </acceptance_criteria>
  <done>04-CODEX-PROMPT.md committed; structural twin of GEMINI prompt with reviewer/CLI/ID-prefix swaps + post-Gemini-fix sequencing note.</done>
</task>

<task type="auto">
  <name>Task 2: Run Codex headless review → 04-CODEX-REVIEW.md</name>
  <files>.planning/phases/04-fallback-routing/04-CODEX-REVIEW.md</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md (just-created prompt — about to be piped to codex)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 3 "Codex CLI" (codex exec --skip-git-repo-check - precedent from Phase 3.5)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 8 Pitfall #14 (`codex exec` swallows shell errors — verify non-empty output, retry with `codex review` if empty)
    - .planning/phases/03.5-pixellab-layouts-variation-seed-wiring/03.5-REVIEWS.md (Phase 3.5 codex precedent — confirms this invocation works in this project)
  </read_first>
  <action>
Run the Codex CLI invocation per RESEARCH § 3:

PowerShell (Windows):
```powershell
Get-Content .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md | codex exec --skip-git-repo-check - > .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md 2>&1
```

bash (cross-platform fallback):
```bash
cat .planning/phases/04-fallback-routing/04-CODEX-PROMPT.md | codex exec --skip-git-repo-check - > .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md 2>&1
```

**Post-check (RESEARCH § 8 Pitfall #14):**
```bash
[ -s .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md ] || echo "FAIL: empty output, retry with codex review subcommand"
head -20 .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md  # confirm frontmatter
```

If output is empty / error-only:
- Retry with `codex review` subcommand: `cat 04-CODEX-PROMPT.md | codex review - > 04-CODEX-REVIEW.md 2>&1`.
- If still failing: surface to user (Codex CLI may be auth-broken or model-unavailable).

**If Codex output contains shell noise** (e.g. ANSI codes, log lines from the wrapper), strip the noise post-hoc to leave only the markdown report. The frontmatter MUST be the first content in the file.

**Commit:** `git add .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md && git commit -m "docs(04): capture Codex cross-AI review (raw findings)"`.

**HARD CONSTRAINTS:**
- Per CLAUDE.md Coined-Term Discipline — if Codex output COINS new "Penta" prefixes (e.g., suggests "PentaCache"), the prompt's hard-trigger #6 means those findings get rejected at Task 3 disposition time. Don't filter at the raw-output stage; capture as-is.
- Per CLAUDE.md HARD RULE — same for compat-shim or forward-compat suggestions. Capture raw, reject at disposition.
- Per RESEARCH § 8 Pitfall #11 / #12 — same.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md && grep -c 'reviewer: codex' .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - **Review file exists and non-trivial:** `wc -c .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md` returns ≥ 500 (non-trivial response).
    - **Frontmatter present at top:** `head -20 .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md | grep -c 'reviewer: codex'` returns 1.
    - **Totals counter present:** `grep -c '^\s*total:' .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md` returns ≥ 1.
    - **At least one severity section OR `status: clean`:** `grep -cE '^## (Critical|High|Medium|Low|Info)$|^status: clean$' .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md` returns ≥ 1.
    - **No raw error / shell-noise in lieu of markdown:** `head -5 .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md | grep -E '^(error|panic|stderr|Error:|Traceback)'` returns nothing.
    - **No empty file:** `wc -c .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md` returns > 0.
  </acceptance_criteria>
  <done>04-CODEX-REVIEW.md committed with valid frontmatter + sectioned-by-severity body (or `status: clean` if no findings).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply Codex findings per severity-tiered policy → 04-CODEX-REVIEW-FIX.md</name>
  <files>.planning/phases/04-fallback-routing/04-CODEX-REVIEW-FIX.md, addons/penta_tile/* (per-finding files)</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md (the raw findings just produced)
    - .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md (the prior-pass disposition log — Codex findings that overlap with already-dispositioned Gemini findings should be cross-referenced)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 4 (REVIEW-FIX schema) + § 5 (disqualification) + § 6 ("Findings That Touch the Same Line as a Gemini Fix") + § 7 (severity-tiered policy)
    - .planning/phases/04-fallback-routing/04-CONTEXT.md D-04-13 / D-04-14 / D-04-15
    - .planning/phases/02-native-layouts/02-REVIEW-FIX.md (precedent — atomic-commit-per-finding)
    - CLAUDE.md HARD RULE + Coined-Term Discipline (rejection criteria)
  </read_first>
  <what-built>
    Codex's raw findings now exist at `04-CODEX-REVIEW.md` (post-Gemini-fix codebase per D-04-10). Same severity/theme/finding/suggested-fix/rationale schema as Gemini. Same disposition workflow.

    Per D-04-13 severity-tiered policy (twin of Plan 03 Task 4):
    - **Critical/High → auto-apply** (no user prompt). Atomic commit per D-04-15. Disposition: `applied`.
    - **Medium → user-gated.** Surface 1-paragraph proposal; user approves → apply + commit. User rejects → `rejected-other`.
    - **Low/Info → logged + user picks.** Apply (Medium workflow) OR defer (CONTEXT.md `## Deferred Ideas`).

    Per D-04-14 disqualification — BEFORE applying any finding (regardless of severity), scan the 7 hard triggers in ≤ 30s. Plus: per RESEARCH § 6, if a Codex finding touches the SAME LINE as a Gemini-applied fix, apply SEQUENTIALLY (Codex commit on top of Gemini commit), don't rebase.
  </what-built>
  <how-to-verify>
    **Step 1 — Disqualification scan (per finding, ≤ 30s each):**
    Same 7 hard triggers as Plan 03 Task 4. Plus an 8th implicit guard: if the Codex finding REPEATS a Gemini-already-dispositioned finding without material new insight, disposition `rejected-other` with rationale "duplicates GEMINI-XXX-NN (already <applied|rejected|deferred>)."

    **Step 2 — Anti-hallucination check:** verify each cited file:line ACTUALLY contains the code Codex described. Hallucinated → `rejected-other`.

    **Step 3 — Conflict check (RESEARCH § 6):** if a Codex fix would touch a line ALREADY modified by a Gemini fix this phase, apply sequentially — the Codex commit lands on top of the Gemini commit. Both commits stay in the disposition log with separate SHAs. If the two fixes are mutually exclusive — pick one, log the rejected one as `rejected-other` with rationale "conflicts with GEMINI-XXX-NN; chose <other> because <reason>."

    **Step 4 — Apply by severity tier:**
    Same as Plan 03 Task 4:
    - Critical / High → auto-apply, atomic commit `fix(04): CODEX-{S}-{NN} — ...` (em-dash), test-suite re-run after each.
    - Medium → propose to user → on approval apply + commit; on reject `rejected-other`.
    - Low / Info → batch surface to user → user picks apply / defer.

    **Step 5 — Build `04-CODEX-REVIEW-FIX.md` per RESEARCH § 4 schema:**

    Frontmatter:
    ```yaml
    ---
    phase: 04-fallback-routing
    reviewer: codex
    fixed_at: <ISO timestamp>
    review_path: .planning/phases/04-fallback-routing/04-CODEX-REVIEW.md
    findings_total: NN
    applied: NN
    applied_partial: NN
    rejected_disqualification: NN
    rejected_other: NN
    deferred: NN
    status: all_dispositioned
    ---
    ```

    Body — same shape as `04-GEMINI-REVIEW-FIX.md`:
    ```markdown
    # Phase 4: Review-Fix Log (Codex)

    ## Disposition Table

    | ID | Severity | Theme | File | Disposition | Commit | Rationale |
    |----|----------|-------|------|-------------|--------|-----------|
    | <one row per finding>

    ## Applied Fixes (Detail)
    <one block per applied fix>

    ## Rejected Findings (Detail)
    <one block per rejected-disqualification or rejected-other>

    ## Deferred Findings (to v0.3+ or v2)
    <one block per deferred>
    ```

    **Step 6 — Sanity checks before returning:**
    - `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` exits 0 with `ALL GREEN (18 tests)`.
    - `git log --oneline | grep -c '^[a-f0-9]\+ fix(04): CODEX-'` matches the `applied` count in REVIEW-FIX.md frontmatter.
    - Every disposition row has either a 7-char commit SHA OR a disqualification trigger reference OR a deferral target.
    - NO uncommitted changes remain (`git status` is clean).

    **Resume signal:** Type `approved` if the fix-loop completed cleanly. If user vetoed Medium findings or chose to defer Low/Info findings, surface those decisions and proceed.
  </how-to-verify>
  <resume-signal>Type "approved" when all Codex findings are dispositioned (applied / applied-partial / rejected-disqualification / rejected-other / deferred), every applied fix has an atomic commit per D-04-15, and the test suite is green. If any Medium finding requires user decision, surface it now with the proposal paragraph; same for Low/Info findings as a batch.</resume-signal>
  <acceptance_criteria>
    - **REVIEW-FIX.md exists with valid frontmatter:** `grep '^findings_total:' .planning/phases/04-fallback-routing/04-CODEX-REVIEW-FIX.md` returns 1.
    - **Disposition counters sum to total:** `applied + applied_partial + rejected_disqualification + rejected_other + deferred == findings_total` (manual frontmatter check).
    - **Disposition table covers every finding:** count of `^| CODEX-` rows in the table equals `findings_total`.
    - **Atomic-commit-per-finding linkage:** `git log --oneline --since="1 day ago" | grep -c '^[a-f0-9]\+ fix(04): CODEX-'` matches the `applied` count exactly.
    - **Em-dash separator:** `git log --oneline --since="1 day ago" | grep 'fix(04): CODEX-' | grep -cv ' — '` returns 0.
    - **Test suite green:** `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` exits 0 with `ALL GREEN (18 tests)`.
    - **NO disqualified findings have commits:** every `disposition: rejected-disqualification` row has `Commit: —`.
    - **NO compat shims / forward-compat / Phase 5 work / ATTRIBUTION.md introduced this pass:** `git log --oneline --since="1 day ago" | grep 'fix(04): CODEX-'` results referenced together with `git diff <SHA>~..<SHA>` show no `version:` / no `legacy_*` aliases / no `ATTRIBUTION.md` files / no Phase 5 work.
    - **status: all_dispositioned** in frontmatter.
    - **`git status` is clean** at task completion.
  </acceptance_criteria>
  <action>
    See <how-to-verify> below — this checkpoint runs the human-verification flow described there.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — see <how-to-verify> for verification steps; resume signal in <resume-signal> gates progression"</automated>
  </verify>
  <done>Human approved per <resume-signal> AND all <acceptance_criteria> conditions met (verifiable via grep/test/file-existence checks listed there).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External LLM (Codex) → local fix-application | Reviewer output is untrusted text; mitigated by anti-hallucination check + Coined-Term/identity guardrails embedded in prompt + post-fix test re-run. |
| Codex sees Gemini's prior-pass output | Codex's review surface includes `04-GEMINI-REVIEW.md` and `04-GEMINI-REVIEW-FIX.md` so it can avoid duplicates. Mitigated by RESEARCH § 6 sequential-not-rebase rule. |
| User (Medium-tier approval) → commit | Same as Plan 03 — user signs off Medium-tier fixes before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | Codex hallucinates file/line citations | mitigate | Implementer verifies each finding's file:line code BEFORE applying; hallucinated findings → `rejected-other`. |
| T-04-04-02 | Tampering | Tampered Codex output / man-in-the-middle on CLI | mitigate | Atomic-commit-per-finding; post-fix `pwsh run_tests.ps1` after every commit catches behavior tampering. |
| T-04-04-03 | Repudiation | Applied Codex fix lacks audit trail | mitigate | REVIEW-FIX.md disposition table records every finding; nothing lands silently. |
| T-04-04-04 | Information Disclosure | Codex prompt includes Gemini disposition log (still-rejected findings reveal internal triggers) | accept | The disposition log is part of the planning workspace — public-domain when v0.2.0 ships; revealing trigger rationale to a 2nd reviewer is INTENDED (so Codex doesn't re-file). |
| T-04-04-05 | Denial of Service | Codex CLI hangs / model unavailable | mitigate | RESEARCH § 8 Pitfall #14 — empty-output check; retry with `codex review` subcommand; surface to user if still failing. |
| T-04-04-06 | Elevation of Privilege | Auto-applied High finding from Codex introduces a regression on top of Gemini's fixes | mitigate | RESEARCH § 6 sequential-apply rule + post-fix test-suite re-run; if Codex's fix breaks a Gemini fix, the test catches it. |
| T-04-04-07 | Tampering | Prompt-injection in Codex finding text smuggling extra instructions | mitigate | Implementer reads finding manually; finding application is mechanical (apply at cited file:line) — Codex can't inject "now also do X." |
</threat_model>

<verification>
- All 3 tasks complete and committed.
- `04-CODEX-PROMPT.md` exists with structural-twin shape + post-Gemini-fix sequencing note.
- `04-CODEX-REVIEW.md` exists with valid frontmatter + at least one severity section OR `status: clean`.
- `04-CODEX-REVIEW-FIX.md` exists with disposition table covering EVERY finding.
- Every applied finding has an atomic `fix(04): CODEX-{SEVERITY}-{NN} — ...` commit.
- Test suite ALL GREEN (18 tests) at every commit boundary.
- ZERO findings hitting D-04-14 hard triggers were applied.
- Codex's review was run AFTER Gemini's fix-loop (per D-04-10 strict order).
</verification>

<success_criteria>
Phase 4 success criterion #7 from ROADMAP satisfied: Codex cross-AI review pass complete + valid findings dispositioned + atomic-fix commits committed.

Plan 05 (phase close-out) consumes this plan's output by:
1. Confirming all 4 closeout artifacts exist (FALLBACK-UAT, DOC-SWEEP, GEMINI-REVIEW-FIX, CODEX-REVIEW-FIX).
2. Flipping ROADMAP Phase 4 row from `[ ]` to `[x]`.
3. Flipping REQUIREMENTS.md Traceability rows for PREVIEW-03 / PREVIEW-04 from `Pending` → `Complete`.
4. Updating STATE.md cumulative LOC + closing Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fallback-routing/04-04-SUMMARY.md` with:
- `## Artifacts created` listing the 3 file paths.
- `## Codex review outcome` — total findings, applied count, rejected-disqualification count, deferred count, conflicts-with-Gemini count.
- `## Atomic commits` — list of `fix(04): CODEX-...` commit SHAs.
- `## Cross-pass interaction` — for each Codex finding that touched a Gemini-modified line, note the sequential-apply outcome.
- `## Notes for Plan 05 consumer` — list the 4 closeout artifacts (paths) that the phase-close gate inspects.
</output>
