---
phase: 04-fallback-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN
  - 04-02-PLAN
files_modified:
  - .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md
  - .planning/phases/04-fallback-routing/04-DOC-SWEEP.md
  - .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md
  - .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md
  - .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md
autonomous: true
requirements:
  - PREVIEW-03
  - PREVIEW-04
tags:
  - uat
  - cross-ai-review
  - gemini
  - documentation-summary
must_haves:
  truths:
    - "Manual demo eyeball pass complete on all 8 actually-shipped layouts; all 9 rows in 04-FALLBACK-UAT.md flipped from `pending` to `pass` (or `partial`/`fail` with notes); each row's `Signed-off:` line carries a non-`pending` value (≥ 9 sign-offs grep-checkable)"
    - "04-DOC-SWEEP.md exists with per-file before/after coverage table for the 12 swept addon scripts"
    - "04-GEMINI-PROMPT.md exists — the headless Gemini CLI prompt embedding the review surface (codebase + planning docs + identity guardrails + severity/theme schema + disqualification list)"
    - "04-GEMINI-PROMPT.md disqualification triggers cover all 7 categories with content-aware checks (compat shim, forward-compat, deferred features, Phase 5 territory, ATTRIBUTION.md ban, Coined-Term Discipline, locked-decision contradictions); W5 fix from checker"
    - "04-GEMINI-REVIEW.md exists — raw Gemini findings categorized by Severity × Theme per D-04-11"
    - "04-GEMINI-REVIEW-FIX.md exists — disposition row per finding (applied / applied-partial / rejected-disqualification / rejected-other / deferred) per D-04-13/14"
    - "Every applied Gemini finding has a corresponding `fix(04): GEMINI-{SEVERITY-LETTER}-{NN} — ...` commit per D-04-15 (atomic-per-finding); commit-count check uses the anchor-SHA range from 04-PRE-PHASE-ANCHOR.txt (Plan 01 Task 0) per B4 fix"
    - "Every disposition row references either a commit SHA (applied / applied-partial) OR a § 5 disqualification trigger (rejected-disqualification) OR a deferral target (deferred)"
    - "Test suite stays ALL GREEN (18 tests) after every applied fix"
    - "No backwards-compat shims / forward-compat versioning / Phase 5 work / Coined-Term violations / locked-decision contradictions slip through (per D-04-14 hard triggers 1-7)"
  artifacts:
    - path: ".planning/phases/04-fallback-routing/04-FALLBACK-UAT.md"
      provides: "Filled-in UAT sign-off — all 9 rows have `result: pass | partial | fail` and `Signed-off:` non-pending; status flips from pending to complete"
      contains: "status: complete"
    - path: ".planning/phases/04-fallback-routing/04-DOC-SWEEP.md"
      provides: "Doc-sweep summary — per-file coverage table + style references + verification checklist + stats"
      contains: "## Per-File Coverage Table"
    - path: ".planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md"
      provides: "Headless Gemini CLI prompt — review surface + severity/theme schema + 7 disqualification triggers (compat / forward-compat / deferred / Phase 5 / ATTRIBUTION ban / Coined-Term / locked-decision) + Coined-Term Discipline guard"
      contains: "Severity: Critical | High | Medium | Low | Info"
    - path: ".planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md"
      provides: "Raw Gemini findings"
      contains: "reviewer: gemini"
    - path: ".planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md"
      provides: "Per-finding disposition log + atomic-fix commit SHAs"
      contains: "## Disposition Table"
  key_links:
    - from: ".planning/phases/04-fallback-routing/04-FALLBACK-UAT.md"
      to: "addons/penta_tile/tests/fallback_routing_test.gd"
      via: "Programmatic backing for the manual eyeball pass per D-04-06"
      pattern: "fallback_routing_test"
    - from: ".planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md"
      to: "git log --oneline ${ANCHOR}..HEAD | grep 'fix(04): GEMINI-'"
      via: "atomic-commit-per-finding linkage per D-04-15; anchor SHA from 04-PRE-PHASE-ANCHOR.txt (B4 fix)"
      pattern: "fix\\(04\\): GEMINI-"
    - from: ".planning/phases/04-fallback-routing/04-DOC-SWEEP.md"
      to: ".planning/phases/04-fallback-routing/04-02-SUMMARY.md"
      via: "doc-sweep coverage stats sourced from Plan 02's SUMMARY"
      pattern: "12 addon scripts"
---

<objective>
Run the human eyeball UAT on the demo (filling 04-FALLBACK-UAT.md), write the 04-DOC-SWEEP.md summary that consumes Plan 02's coverage stats, then run the FIRST cross-AI review pass (Gemini headless) and apply valid findings per the severity-tiered policy. Codex pass deferred to Plan 04 per D-04-10 (strict order).

Purpose: Satisfies the Plan 4 success criteria 1, 2, 5, 6 from ROADMAP. PREVIEW-03/04 manual eyeball half (pairs with Plan 01's programmatic half per D-04-06 belt+suspenders). Cross-AI review surfaces identity-guardrail violations qualitatively (D-04-08 — formal LOC + identity audit stays in Phase 5).

Output: 5 markdown artifacts + N atomic fix-commits where N = applied-finding count. The phase-close gate (Plan 05) inspects all 4 closeout artifacts (FALLBACK-UAT, DOC-SWEEP, GEMINI-REVIEW-FIX, CODEX-REVIEW-FIX) and flips the ROADMAP `[x]`.

**Mode (B2 fix from checker):** This plan is `autonomous: true`. The two formerly-checkpoint tasks (manual UAT eyeball pass and Gemini fix-loop) are now `type="auto"` with grep-checkable `<acceptance_criteria>` that prove the manual flow happened (UAT sign-off lines flipped; atomic fix commits landed on the anchor-bounded range). The user-gating that D-04-13 mandates for Medium/Low/Info findings happens during the fix-loop dialog with the human; `<acceptance_criteria>` measure the artifact state AFTER that dialog completes. The autonomous-true flag describes the plan's gate logic, not "no human interaction" — the human still drives the eyeball pass and disposition decisions, but the executor pins down completion via deterministic post-conditions instead of a free-text "approved" handshake.

W3 note (acceptable per checker): autonomous: true is valid here — D-04-13 user-gating governs review fix-loops as a workflow inside the task, not as a checkpoint task type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/04-fallback-routing/04-CONTEXT.md
@.planning/phases/04-fallback-routing/04-RESEARCH.md
@.planning/phases/04-fallback-routing/04-PATTERNS.md
@.planning/phases/04-fallback-routing/04-FALLBACK-UAT.md
@.planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt
@.planning/phases/04-fallback-routing/04-01-SUMMARY.md
@.planning/phases/04-fallback-routing/04-02-SUMMARY.md
@.planning/phases/02-native-layouts/02-HUMAN-UAT.md
@.planning/phases/02-native-layouts/02-REVIEW.md
@.planning/phases/02-native-layouts/02-REVIEW-FIX.md

<interfaces>
<!-- CLI invocations (verified per RESEARCH § 3 against installed tooling — Gemini CLI 0.38.2). -->

**Gemini headless invocation** (RESEARCH § 3 / Pitfall #13 — use STDIN to avoid Windows PowerShell quoting issues):
```bash
Get-Content .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md | gemini --model gemini-2.5-pro --include-directories ".planning,addons/penta_tile" -p - > .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md 2>&1
```

Or unix-shell equivalent:
```bash
cat .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md | gemini --model gemini-2.5-pro --include-directories ".planning,addons/penta_tile" -p - > .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md 2>&1
```

**Output post-check** (RESEARCH § 8 Pitfall #14): `[ -s 04-GEMINI-REVIEW.md ]`. On empty: retry with `--model gemini-2.5-flash` OR `--model gemini-2.5-pro` explicit override.

**Atomic-commit-per-fix format** (D-04-15):
```
fix(04): GEMINI-{SEVERITY-LETTER}-{NN} — {one-line description}
```
Example: `fix(04): GEMINI-H-01 — restore @experimental tag on PentaTileLayout class doc`. Em-dash (`—`, U+2014), NOT hyphen.

**Severity ladder** (D-04-13 + RESEARCH § 4):
- **Critical / High** → auto-apply (no user prompt). Atomic commit. Disposition: `applied`.
- **Medium** → propose to user (1 paragraph: what / why / commit message). Wait for approval. Disposition: `applied` or `rejected-other`.
- **Low / Info** → surface in REVIEW-FIX.md "Low/Info Findings" section. User picks `applied` (treated as Medium workflow) or `deferred` (logged to CONTEXT.md `## Deferred Ideas`).

**Anchor-based commit count** (B4 fix):
The pre-Phase-4 anchor SHA lives at `.planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt` (created by Plan 01 Task 0). Use it instead of the fragile `--since="1 day ago"` wall-clock filter:
```bash
ANCHOR=$(cat .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt)
git log --oneline ${ANCHOR}..HEAD | grep -c 'fix(04): GEMINI-'
```
This survives Medium-tier dispositions taking > 24h and gives content-bounded counts.

**Disqualification triggers** (D-04-14 + RESEARCH § 5 — implementer scans in ≤ 30s per finding):
1. Backwards-compat shim / deprecation alias / version-detection branch
2. Forward-compat versioning field / schema marker / speculative extension point
3. Feature deferred to v0.3+ or v2 (TBT-01/02-DEFERRED, VAR-01, VAR-PIXEL-01, TOP-01, NONROT-01, MULTITERR-01..05, TERRAIN-01, RPGM-01..03, IMPORT-01/02, TOOL-01..04, PERF-01/02, DIST-01/02)
4. Phase 5 territory (LOC trim, README sections, CHANGELOG, demo refresh, plugin.cfg bump, git tag, release zip)
5. `addons/penta_tile/ATTRIBUTION.md` proposal (banned per D-72/D-73)
6. Coined-Term Discipline violation (any "Penta" prefix on non-5-archetype subsystems)
7. Locked-decision contradiction (any D-XX in PROJECT.md, STATE.md, phase CONTEXT.md files)

**Demo eyeball steps** (RESEARCH § 2 "Two-Tier Verification"):
1. Open `addons/penta_tile/demo/penta_tile_demo.tscn` in Godot editor.
2. For each of 8 layouts: swap `layout` to that layout's class (instantiate via inspector picker OR use existing `.tres` if available), clear `tile_set` to `null`, drag-paint a small region in the running game, eyeball.
3. PREVIEW-04 contract: assign a custom `tile_set` directly → confirm overrides; clear back to `null` → confirm fallback re-engages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual demo eyeball UAT — fill 04-FALLBACK-UAT.md (PREVIEW-03 + PREVIEW-04)</name>
  <files>.planning/phases/04-fallback-routing/04-FALLBACK-UAT.md</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md (skeleton from Plan 01 — 9 rows pending, 9 `Signed-off: pending` lines)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 2 "Two-Tier Verification" (manual UAT steps)
    - .planning/phases/02-native-layouts/02-HUMAN-UAT.md (filled-in analog — shows the result-line idiom)
    - addons/penta_tile/demo/penta_tile_demo.tscn (the demo scene the user opens)
  </read_first>
  <action>
This task drives a HUMAN eyeball pass on the demo and pins down completion via grep-checkable `<acceptance_criteria>`. The executor coordinates the human through the steps below; per B2 fix, the gate is the artifact state at the end (no free-text "approved" handshake).

**Steps (executor coordinates the human):**

1. Run the full test suite first to confirm baseline: `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` — expect `ALL GREEN (18 tests)`. Specifically `fallback_routing_test` MUST report PASS.
2. Open Godot 4.6.2 and load `addons/penta_tile/demo/penta_tile_demo.tscn`.
3. For each of the 8 actually-shipped layouts (Penta, DualGrid16, Wang2Edge, Wang2Corner, Min3x3, Blob47Godot, PixelLabTopDown, PixelLabSideScroller):
   a. Select the `PentaTileMapLayer` node in the scene tree.
   b. In the inspector, clear `tile_set` to `null` (right-click → Clear) — this triggers the auto-fill chain in `penta_tile_map_layer.gd:54-70`. The `_tile_set_is_fallback` flag flips to true.
   c. Set `layout` to a fresh instance of the target layout class (use the inspector picker → New PentaTileLayout<X>). The setter again triggers the auto-fill chain.
   d. Run the demo (F5 or the play button). Drag-paint a small region (5-10 cells).
   e. Eyeball: confirm visible greybox tiles render correctly. NO empty cells, NO obviously-broken silhouettes, NO error messages in the Godot output console.
   f. Update row N in `04-FALLBACK-UAT.md`:
      - flip `result: pending` to `result: pass` (or `result: partial — <notes>` / `result: fail — <notes>` with screenshot path under `user://fallback_<layout>.png` if anything went wrong).
      - flip `Signed-off: pending` to `Signed-off: <initials> YYYY-MM-DD` (or any non-`pending` token).
4. Row #9 — PREVIEW-04 contract regression:
   a. With any layout bound and `tile_set` auto-filled, in the inspector assign a custom `TileSet` (drag from `addons/penta_tile/demo/penta_tile_ground.tres`). Confirm `_tile_set_is_fallback` flag becomes false (visible in inspector under "Storage" or via `print(node._tile_set_is_fallback)` in remote inspector).
   b. Clear `tile_set` to `null`. Set `layout` to a fresh layout instance — confirm `_tile_set_is_fallback` flips back to true and a fallback TileSet is auto-bound.
   c. Reassign a custom TileSet, then change layout — confirm tile_set is preserved (SC-4; complements `_test_preview_04_user_tileset_preserved` from Plan 01).
   d. Update row 9 result + Signed-off line.
5. Update the frontmatter:
   - `status:` → `complete` (if all 9 rows pass) OR `partial` (if any rows are partial/fail).
   - `updated:` → current ISO timestamp.
6. Update the `## Summary` block: increment `passed`, `partial`, etc. counters.
7. Fill the `## Closure Notes` section: cite the `fallback_routing_test.gd` PASS as the programmatic backing for the manual eyeball pass per D-04-06.
8. Commit: `git add .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md && git commit -m "docs(04): manual UAT sign-off for PREVIEW-03/04 fallback routing"`.

**HARD CONSTRAINT — auto-rejection conditions for the eyeball pass:**
- If any layout shows obvious empty cells under fallback: row is `partial` or `fail`. Document the failure mode (which mask state is broken / which cells are empty / screenshot path).
- If `_tile_set_is_fallback` doesn't behave per the contract in row #9: row #9 is `fail`. Phase 4 cannot close until this is fixed (the wiring shipped in Phase 2 is what's under test).
- Per CLAUDE.md HARD RULE: any failure in row #9 is a regression of PREVIEW-04 wiring → spawn a Wave 2.5 / Wave 3 fix; do NOT defer to v0.3+. The Phase 2 wiring is the source of truth; if a fix is needed it lands in this phase via the Gemini fix-loop or a separate atomic commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md && grep -c "Signed-off:" .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is committed.
    - All 9 numbered rows have `result: pass | partial | fail` (NO `result: pending` remaining): `grep -c '^result: pending$' .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` returns 0.
    - **B2 fix grep gate — UAT sign-off lines flipped:** the artifact carries ≥ 9 non-pending `Signed-off:` lines. Concretely:
      - `grep -c '^Signed-off:' .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` returns ≥ 9.
      - `grep -c '^Signed-off: pending$' .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` returns 0 (every sign-off line carries a non-pending value).
    - Frontmatter `status:` is `complete` or `partial` (NOT `pending`): `grep -E '^status: (complete|partial)$' .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` returns ≥ 1.
    - Summary counters consistent: `passed + partial + fail + skipped + blocked == 9` (manual sanity check).
    - Closure notes cite programmatic test: `grep 'fallback_routing_test' .planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` returns ≥ 1.
  </acceptance_criteria>
  <done>04-FALLBACK-UAT.md committed with all 9 rows result-flipped + Signed-off non-pending; status: complete or partial; closure notes cite the programmatic test.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-DOC-SWEEP.md summary (per-file coverage stats from Plan 02)</name>
  <files>.planning/phases/04-fallback-routing/04-DOC-SWEEP.md</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-02-SUMMARY.md (Plan 02's per-file coverage stats — primary data source)
    - .planning/phases/04-fallback-routing/04-CONTEXT.md (D-04-01 / D-04-02 / D-04-03 / D-04-04 — sweep scope and tag set)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 1 (Godot doc-comment format spec)
    - .planning/phases/04-fallback-routing/04-PATTERNS.md "Deliverable 4 / 04-DOC-SWEEP.md" subsection (proposed structure — novel artifact, no exact analog)
    - .planning/phases/02-native-layouts/02-07-SUMMARY.md (closest secondary structure — per-file table + audit-decision pattern)
    - The 12 swept addon scripts (Read each to confirm the "after" coverage stats are accurate — class-level block, public-method count, @export-property count)
  </read_first>
  <action>
Create `.planning/phases/04-fallback-routing/04-DOC-SWEEP.md` per the proposed structure in 04-PATTERNS.md "Deliverable 4."

**Frontmatter:**
```yaml
---
phase: 04-fallback-routing
swept_at: 2026-04-29T...
scripts_swept: 12
status: complete
---
```

**Body sections:**

```markdown
# Phase 4: Doc-Comment Sweep Summary

**Scope (D-04-01):** 12 addon scripts under `addons/penta_tile/` (excluding `tests/` + `demo/`).
**Format source-of-truth:** [Godot 4.x GDScript Documentation Comments](https://docs.godotengine.org/en/stable/tutorials/scripting/gdscript/gdscript_documentation_comments.html)
**Coverage depth (D-04-02):** class-level `##` block + every public method (no leading underscore) + every `@export` property. Private `_foo` methods documented only when WHY is non-obvious.
**Tags used (D-04-03):**
- Structural: `@tutorial(label)` (where applicable), `@experimental` (PentaTileLayout abstract base ONLY).
- BBCode inline: `[param x]`, `[code]...[/code]`, `[Class TileMapLayer]`, `[method foo]`, `[member bar]`.
- ZERO `@deprecated` tags (RESEARCH § 8 Pitfall #3 — Phase 4 doesn't use it).

## Per-File Coverage Table

| File | Class block | Public methods (count) | @export props (count) | `@experimental` | `@tutorial` count |
|------|-------------|------------------------|----------------------|-----------------|--------------------|
| `penta_tile_map_layer.gd`             | NEW       | 0/N → N/N | 0/M → M/M | no  | varies |
| `penta_tile_synthesis.gd`             | EXISTING  | partial → full | partial → full | no  | 0 |
| `penta_tile_atlas_slot.gd`            | EXISTING  | n/a | partial → full | no  | 0 |
| `layouts/penta_tile_layout.gd`        | EXISTING + `@experimental` | partial → full | partial → full | YES | 0 (Phase 4 didn't add) |
| `layouts/penta_tile_layout_penta.gd`  | EXISTING (preserved) | 0/N → N/N | 0/M → M/M | no  | 0 |
| `layouts/penta_tile_layout_dual_grid_16.gd` | EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_wang_2_edge.gd`  | EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_wang_2_corner.gd`| EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_minimal_3x3.gd`  | EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_blob_47_godot.gd`| EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_pixel_lab_top_down.gd`     | EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
| `layouts/penta_tile_layout_pixel_lab_side_scroller.gd`| EXISTING (preserved) | 0/N → N/N | n/a | no  | 0 |
```

(Replace `N/N` and `M/M` with the actual counts read from the post-sweep files. Source: `04-02-SUMMARY.md` plus a fresh `grep -c '^func [a-z]'` and `grep -c '@export ' <file>` per file.)

```markdown
## Style Reference

- **Class-level template:** `addons/penta_tile/penta_tile_synthesis.gd:1-19`
- **Field-doc template:** `addons/penta_tile/penta_tile_atlas_slot.gd:1-9` (per-property `##` blocks added in sweep).
- **Method-doc template:** `addons/penta_tile/layouts/penta_tile_layout.gd` virtual methods (added in sweep).

## Verification

- [x] No `#` (single-hash) interruptions inside `##` blocks (RESEARCH § 8 Pitfall #1).
- [x] All structural `@keyword:` tags have NO space before colon (RESEARCH § 1 critical rule).
- [x] All BBCode tags closed (RESEARCH § 8 Pitfall #6).
- [x] No `@deprecated` tags introduced (RESEARCH § 8 Pitfall #3).
- [x] `@experimental` lands ONLY on `PentaTileLayout` (D-04-03 + RESEARCH § 8 Pitfall #5).
- [x] Test suite ALL GREEN after sweep — sweep was annotation-only, zero behavior change (D-04-04 cross-AI review is the verification mechanism, not a lint test).
- [ ] Cross-AI review (Gemini + Codex) consumes this artifact as input — pass surfaces any missed `##` blocks / wrong tag usage as `Doc`-themed findings (per D-04-04).

## Stats

- Lines of `##` doc added (estimated): NN (sum of git-diff added lines starting with `## ` across 12 files).
- Public methods documented: NN / NN (post-sweep / total).
- @export properties documented: NN / NN.
- @experimental tags added: 1 (on `PentaTileLayout` only).
- @tutorial tags added: 0 (Phase 4 didn't introduce any new tutorial pointers; existing `## See:` paragraphs in PATTERNS-cited files preserve their research-doc cross-references via prose).
- @deprecated tags added: 0.

## Out-of-Scope (Phase 5 / v0.3+)

Per RESEARCH § 9 — items the sweep DID NOT touch:
- README "Layouts" / "Upgrading from 0.1.x" / "Authoring a Custom Layout" sections (DOC-01..03 / Phase 5).
- CHANGELOG.md v0.2.0 entry (DOC-04 / Phase 5).
- Doc-coverage lint test (D-04-04 — no lint test added; cross-AI review is the verification).
- Doc-sweep on `tests/` and `demo/` (D-04-01 — internal-use surfaces excluded).
```

**Constraints:**
- Per CLAUDE.md HARD RULE — no compat-shim discussion in the summary; no `version:` migration prose; no @deprecated suggestions.
- Per Coined-Term Discipline — only existing PentaTile* class names referenced.
- Per D-04-04 — do NOT propose adding a doc-coverage lint test.
- Per D-04-08 — do NOT propose a Phase 5 LOC pre-audit; defer to Phase 5.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-fallback-routing/04-DOC-SWEEP.md && grep -c '^| ' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/04-fallback-routing/04-DOC-SWEEP.md`.
    - Frontmatter `status: complete`: `grep '^status: complete$' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns 1.
    - 12-row coverage table present: count of file rows in the markdown table is 12.
    - All 12 file paths present: `grep -c 'penta_tile_map_layer.gd\|penta_tile_synthesis.gd\|penta_tile_atlas_slot.gd\|penta_tile_layout.gd\|penta_tile_layout_penta.gd\|penta_tile_layout_dual_grid_16.gd\|penta_tile_layout_wang_2_edge.gd\|penta_tile_layout_wang_2_corner.gd\|penta_tile_layout_minimal_3x3.gd\|penta_tile_layout_blob_47_godot.gd\|penta_tile_layout_pixel_lab_top_down.gd\|penta_tile_layout_pixel_lab_side_scroller.gd' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns ≥ 12.
    - `@experimental` row identifies PentaTileLayout: `grep 'penta_tile_layout.gd.*YES' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns ≥ 1.
    - Verification checklist + Stats sections present: `grep -c '^## Verification\|^## Stats\|^## Style Reference\|^## Per-File Coverage Table' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns ≥ 4.
    - **NO compat-shim / version: int / migration prose:** `grep -i 'compat shim\|backwards.compat\|version:\s*int\|migration' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns nothing OR only matches inside the "Out-of-Scope" / "Verification" sections referencing the HARD RULE.
    - **NO doc-coverage-lint-test proposal:** `grep -i 'lint test\|coverage test' .planning/phases/04-fallback-routing/04-DOC-SWEEP.md` returns matches only in the "Out-of-Scope" / "D-04-04" reference (no positive proposal).
  </acceptance_criteria>
  <done>04-DOC-SWEEP.md committed with 12-row coverage table + per-file stats sourced from Plan 02's SUMMARY; status: complete.</done>
</task>

<task type="auto">
  <name>Task 3: Write 04-GEMINI-PROMPT.md (the headless review prompt) and run Gemini</name>
  <files>.planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md, .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 3 "Gemini CLI" + § 4 "Cross-AI Review Output Schemas" (full schema for raw findings)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 5 "Standard Disqualification Checklist" (the prompt mentions these so Gemini can pre-filter where possible, though final disposition lives in REVIEW-FIX.md)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 8 Pitfall #11 (Coined-Term Discipline guard — must include in prompt verbatim) + Pitfall #12 (TileMapDual identity guardrails — include verbatim)
    - .planning/phases/04-fallback-routing/04-PATTERNS.md "Deliverable 3 / 04-GEMINI-REVIEW.md" subsection (frontmatter + per-finding schema)
    - .planning/phases/02-native-layouts/02-REVIEW.md (severity-classified finding format precedent)
    - CLAUDE.md "Identity Guardrails" + "Coined-Term Discipline" + "Breaking Changes Policy" + "Critical Pitfalls" (include verbatim in prompt)
    - .planning/PROJECT.md (identity constraint statement)
  </read_first>
  <action>
**Sub-task 3a — write `04-GEMINI-PROMPT.md`:**

Create the prompt file with the following structure:

```markdown
# Gemini Cross-AI Review — PentaTile Phase 4

You are reviewing the **PentaTile** Godot 4.6 dual-grid autotiling addon as
preparation for v0.2.0 release. Phase 4 of the GSD execution plan ("Fallback
Routing + Doc Sweep + Cross-AI Review") is closing — your output gates the
phase per CONTEXT.md D-04-09 / D-04-10 / D-04-16.

## Review Surface

You have the entire `addons/penta_tile/` source tree and `.planning/` workspace
attached via `--include-directories`. Specifically:
- `addons/penta_tile/*.gd` (3 core scripts: penta_tile_map_layer.gd, penta_tile_synthesis.gd, penta_tile_atlas_slot.gd)
- `addons/penta_tile/layouts/*.gd` (9 layout scripts: 1 base + 8 shipped subclasses)
- `addons/penta_tile/tests/*.gd` (18 test scripts — supporting evidence, NOT in scope for doc-comment sweep)
- `.planning/PROJECT.md` (vision + identity guardrails)
- `.planning/ROADMAP.md` (phase plan + success criteria)
- `.planning/REQUIREMENTS.md` (v1 requirements + v2 deferred)
- `.planning/STATE.md` (locked decisions D-XX)
- `.planning/phases/04-fallback-routing/04-CONTEXT.md` (Phase 4 locked decisions D-04-01..16)
- `.planning/phases/04-fallback-routing/04-FALLBACK-UAT.md` (manual UAT sign-off)
- `.planning/phases/04-fallback-routing/04-DOC-SWEEP.md` (doc sweep summary)
- `CLAUDE.md` (Identity Guardrails + Critical Pitfalls + Breaking Changes Policy)

## Hard Constraints (Read Before Reviewing)

### CLAUDE.md Identity Guardrails (verbatim)

> The PROJECT.md identity constraint is "PentaTile must remain visibly smaller
> and simpler than TileMapDual." When making implementation decisions, reject:
> - Terrain peering metadata or terrain rule tries (TileMapDual / Better Terrain territory)
> - Multi-terrain transitions (deferred to a future milestone)
> - Watcher / signal-fanout systems (TileMapDual's leaks/crashes are cited evidence)
> - Persistent coordinate caches (demo-scale doesn't need them)
> - Custom drawing API parallel to `set_cell()` (defeats the v0.1 native-API win)
> - `EditorInspectorPlugin` polish (typed `@export` + `@export_group` is enough)

### CLAUDE.md Breaking Changes Policy HARD RULE (both directions)

> **Breaking changes are always allowed. Always.** This project is pre-1.0...
> No backwards-compatibility shims, deprecation aliases, version-detection branches.
> No forward-compatibility / speculative versioning (`version: int` fields,
> schema markers, abstract slots, extension points "in case a future feature
> needs them"). YAGNI applies hardest to versioning machinery.

### CLAUDE.md Coined-Term Discipline

> "Penta" is reserved exclusively for the 5-archetype tileset format used by
> PentaTile. Never coin "Penta" prefixes for unrelated subsystems
> (e.g., do NOT introduce `PentaCache`, `PentaDecoder`, `PentaToolkit` for
> things that aren't the 5-archetype format).

### Disqualification Triggers — DO NOT propose findings hitting any of these (REQUIREMENTS.md / D-XX in STATE.md)

1. Backwards-compat shim / deprecation alias / version-detection branch / migration fallback.
2. Forward-compat versioning field / schema marker / speculative extension point.
3. Feature deferred to v0.3+ or v2: TBT-01-DEFERRED / TBT-02-DEFERRED / TEMPLATE-02-DEFERRED / VAR-01 / VAR-PIXEL-01 / TOP-01 / NONROT-01 / MULTITERR-01..05 / TERRAIN-01 / RPGM-01..03 / IMPORT-01/02 / TOOL-01..04 / PERF-01/02 / DIST-01/02.
4. Phase 5 territory: LOC trim, README sections, CHANGELOG, demo refresh, plugin.cfg bump, GitHub Release zip.
5. `addons/penta_tile/ATTRIBUTION.md` proposal (BANNED per D-72/D-73).
6. Coined-Term Discipline violation (any "Penta" prefix on non-5-archetype subsystems).
7. Locked-decision contradiction (any D-XX in PROJECT.md / STATE.md / phase CONTEXT.md files — D-04-01 through D-04-16, D-86, D-87, D-89..D-105, etc.).

(Findings hitting these will be rejected as `rejected-disqualification` in the disposition log; you save us time by not proposing them.)

## Review Themes

For each finding, categorize by **Severity × Theme**.

**Severity:**
- **Critical** — security / data-loss class bug; demo crashes on load; saved scene corruption; runtime exception in autotile hot path.
- **High** — correctness regression; broken locked contract (PREVIEW-03/04 wiring); identity-guardrail violation; locked-decision contradiction; coined-term violation; broken Godot doc-comment format (e.g., `##` block with `#` interruption per Pitfall #1).
- **Medium** — quality issue with debatable fix; doc-comment ambiguity that could mislead a custom-layout author.
- **Low** — cosmetic / style polish; minor naming inconsistency; debt-but-not-bug.
- **Info** — observation without action; future-phase hint.

**Theme:**
- **Bug** — code does the wrong thing.
- **Identity** — change moves PentaTile toward TileMapDual surface area.
- **Goal-misalignment** — out-of-scope creep; contradicts a project goal.
- **Doc** — doc-comment is missing/wrong/misleading/contradicts code.
- **Design** — debatable architecture / pattern; refactor opportunity (NOT a bug).

## Output Format

Output EXACTLY this markdown structure (verbatim — populated by your findings):

```markdown
---
phase: 04-fallback-routing
reviewer: gemini
reviewed_at: <ISO timestamp>
files_reviewed:
  - addons/penta_tile/penta_tile_map_layer.gd
  - addons/penta_tile/penta_tile_synthesis.gd
  - <... all 12 addon scripts ...>
findings:
  critical: <count>
  high: <count>
  medium: <count>
  low: <count>
  info: <count>
  total: <sum>
status: <clean | issues-found>
---

# Phase 4: Cross-AI Review Report (Gemini)

## Summary

<1-paragraph overall assessment>

## Critical
<### GEMINI-C-NN: ... blocks; OMIT if zero findings>

## High
<### GEMINI-H-NN: ... blocks; OMIT if zero findings>

## Medium
<### GEMINI-M-NN: ... blocks; OMIT if zero>

## Low
<### GEMINI-L-NN: ... blocks; OMIT if zero>

## Info
<### GEMINI-I-NN: ... blocks; OMIT if zero>
```

Per-finding template (REQUIRED schema):

```markdown
### GEMINI-{C|H|M|L|I}-{NN}: <one-line title>

**File:** `<path>:<line-range>` (REQUIRED — concrete; defends against hallucination per RESEARCH § 8 Pitfall #10)
**Severity:** <Critical | High | Medium | Low | Info>
**Theme:** <Bug | Identity | Goal-misalignment | Doc | Design>
**Finding:** <what's wrong, 1-3 sentences>
**Suggested fix:** <what to change, 1-3 sentences with code if useful>
**Rationale:** <why it matters — links to identity guardrail / pitfall / locked decision (D-XX) / requirement / Godot best practice>
```

Sort findings by severity (Critical → High → Medium → Low → Info), then ID-ascending within each tier.

## Anti-hallucination Guard

Every finding MUST cite a CONCRETE file path and line range that exists in the
attached source tree. We will reject any finding citing a path/line that
doesn't exist in the codebase as `rejected-other` (reviewer hallucination).

If you are uncertain about a file/line, omit the finding rather than fabricate
one.

## Begin Review

Review now. Output ONLY the markdown report described above — no preamble,
no postamble, no commentary outside the schema.
```

**Sub-task 3b — invoke Gemini headless:**

Run the Gemini CLI invocation per RESEARCH § 3:

PowerShell (Windows):
```powershell
Get-Content .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md | gemini --model gemini-2.5-pro --include-directories ".planning,addons/penta_tile" -p - > .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md 2>&1
```

bash (cross-platform fallback):
```bash
cat .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md | gemini --model gemini-2.5-pro --include-directories ".planning,addons/penta_tile" -p - > .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md 2>&1
```

**Post-check (RESEARCH § 8 Pitfall #14):** verify the output file is non-empty AND has valid frontmatter:
```bash
[ -s .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md ] || echo "FAIL: empty output, retry with --model gemini-2.5-flash or fall back to gemini-2.5-pro explicit"
head -20 .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md  # confirm frontmatter present
```

If the output is empty / contains only an error message: retry with `--model gemini-2.5-flash`. If retry also fails: surface the failure to the user (Gemini CLI may be auth-broken or network-unavailable).

**Commit:** `git add .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md && git commit -m "docs(04): capture Gemini cross-AI review (raw findings)"`.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md && test -s .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md && grep -c '^reviewer: gemini$' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - **Prompt file exists and is non-trivial:** `wc -l .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 80 lines.
    - **Prompt embeds CLAUDE.md identity guardrails verbatim:** `grep -c 'visibly smaller and simpler than TileMapDual' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
    - **Prompt embeds Coined-Term Discipline (W5 fix — content-aware check #6):** `grep -ci 'Coined-Term Discipline\|Penta. is reserved' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
    - **W5 fix — 7 content-aware disqualification trigger checks** (replacing the previous loose `^[1-7]\.` regex):
      - Trigger 1 (backwards-compat): `grep -ci 'backwards.compat\|deprecation alias\|version.detection' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 2 (forward-compat): `grep -ci 'forward.compat\|schema marker\|speculative extension' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 3 (deferred features): `grep -c 'TBT-01-DEFERRED\|TBT-02-DEFERRED\|VAR-01\|TOP-01\|MULTITERR' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 4 (Phase 5 territory): `grep -ci 'Phase 5 territory\|LOC trim\|README rewrite\|CHANGELOG\|demo refresh\|plugin.cfg\|release zip' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 5 (ATTRIBUTION ban): `grep -c 'ATTRIBUTION.md\|D-72\|D-73' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 6 (Coined-Term — already covered above): `grep -ci 'Coined-Term Discipline\|Penta. is reserved' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
      - Trigger 7 (locked decisions): `grep -cE 'locked.decision|D-[0-9]{2}-[0-9]{2}' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 1.
    - **Prompt specifies finding schema:** `grep -c 'Severity\|Theme\|File:\|Suggested fix' .planning/phases/04-fallback-routing/04-GEMINI-PROMPT.md` returns ≥ 4.
    - **Review output exists and has frontmatter:** `head -20 .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md | grep -c 'reviewer: gemini'` returns 1.
    - **Review output has totals counter:** `grep -c '^\s*total:' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md` returns ≥ 1.
    - **Review output is well-formed (sectioned by severity):** the file contains at least one of `^## Critical$ | ^## High$ | ^## Medium$ | ^## Low$ | ^## Info$` (depending on Gemini's output — at least ONE severity section must be present, OR `status: clean` with all-zero counts).
    - **No empty / error-only output:** `wc -c .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md` returns ≥ 500 (a non-trivial response).
  </acceptance_criteria>
  <done>04-GEMINI-PROMPT.md and 04-GEMINI-REVIEW.md committed; review captures Gemini's raw findings with valid frontmatter + sectioned-by-severity body; W5 fix's 7 content-aware disqualification checks all pass.</done>
</task>

<task type="auto">
  <name>Task 4: Apply Gemini findings per severity-tiered policy → 04-GEMINI-REVIEW-FIX.md</name>
  <files>.planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md, addons/penta_tile/* (per-finding files)</files>
  <read_first>
    - .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md (the raw findings just produced)
    - .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt (the anchor SHA for commit-count grep range — B4 fix)
    - .planning/phases/04-fallback-routing/04-RESEARCH.md § 4 "04-{TOOL}-REVIEW-FIX.md Schema (Disposition Log)" + § 5 "Standard Disqualification Checklist" + § 6 "Atomic-Commit-Per-Fix Pattern" + § 7 "Severity-Tiered Fix-Application Policy"
    - .planning/phases/04-fallback-routing/04-CONTEXT.md D-04-13 (severity-tiered) + D-04-14 (disqualification) + D-04-15 (atomic commits)
    - .planning/phases/04-fallback-routing/04-PATTERNS.md "Deliverable 3 / 04-GEMINI-REVIEW-FIX.md" subsection (disposition table + per-applied-fix detail blocks + rejected/deferred sections)
    - .planning/phases/02-native-layouts/02-REVIEW-FIX.md (precedent — Phase 2's WR-fix log; commits ea0ba23, ae5d787, 9ca342e, d74df0e, 2ca04e0, 720f017, 79af1e3 are the canonical atomic-fix commit history)
    - CLAUDE.md HARD RULE (no compat shims, no forward-compat) + Coined-Term Discipline (rejection criteria)
  </read_first>
  <action>
This task applies Gemini's findings via the severity-tiered policy. Per B2 fix, the gate is the artifact state (commit-count grep against the anchor range, disposition table fully populated, status: all_dispositioned), not a free-text "approved" handshake.

Per D-04-13 severity-tiered policy:
- **Critical/High → auto-apply** (no user prompt). Atomic commit per D-04-15. Disposition: `applied`.
- **Medium → user-gated.** Each Medium finding gets surfaced to the user with a 1-paragraph proposal (what / why / commit message). User says "approved" → apply + commit. User says "reject" → `rejected-other` with rationale.
- **Low/Info → logged + user picks.** Each Low/Info finding gets surfaced together at the end. User picks `applied` (treated as Medium) or `deferred` (logged to CONTEXT.md `## Deferred Ideas` AND row in `04-GEMINI-REVIEW-FIX.md` with `disposition: deferred`).

Per D-04-14 disqualification — BEFORE applying any finding (regardless of severity), scan the 7 hard triggers in ≤ 30 seconds. If a trigger fires → `rejected-disqualification` with the specific trigger ID cited in rationale.

**Step 1 — Disqualification scan (per finding, ≤ 30s each):**
Read every finding in `04-GEMINI-REVIEW.md` and scan against the 7 hard triggers from RESEARCH § 5:
1. Compat shim / deprecation alias / version-detection?
2. Forward-compat versioning / schema marker / extension point?
3. v0.3+ / v2 deferred feature (TBT-01/02-DEFERRED, VAR-*, TOP-01, MULTITERR-*, etc.)?
4. Phase 5 territory (LOC trim / README rewrite / CHANGELOG / demo refresh / plugin.cfg / release zip)?
5. `ATTRIBUTION.md` proposal?
6. Coined-Term violation ("Penta" prefix on non-5-archetype subsystem)?
7. Locked-decision contradiction (D-XX in PROJECT.md / STATE.md / CONTEXT.md)?

For findings where a trigger fires: row `disposition: rejected-disqualification` + rationale `D-04-14 trigger N — <specific reason>`. NO commit.

**Step 2 — Anti-hallucination check (per surviving finding):**
Per RESEARCH § 8 Pitfall #10, verify the cited file:line ACTUALLY contains the code Gemini described. If hallucinated → `rejected-other` with rationale "reviewer hallucination — line N contains different code than cited."

**Step 3 — Apply by severity tier:**

For each Critical / High finding:
- Implement the fix.
- Atomic commit: `git commit -m "fix(04): GEMINI-{SEVERITY-LETTER}-{NN} — <one-line description>"` per D-04-15 (em-dash separator).
- Run `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` after every commit. If green, continue. If red, the fix introduced a regression — investigate, additional commit OR `git reset` and re-think.
- Update disposition table in `04-GEMINI-REVIEW-FIX.md`: `disposition: applied`, commit SHA (short form, 7 chars), rationale.

For each Medium finding:
- Propose to user (1 paragraph: what / why / commit message). Ask: "approve / reject?"
- On approval: implement + commit (same atomic format) + update REVIEW-FIX.md `applied`.
- On reject: REVIEW-FIX.md `rejected-other` + user's rationale.

For each Low / Info finding:
- Surface ALL of them together to the user in one batch. For each, ask: "apply / defer?"
- Apply → Medium workflow.
- Defer → REVIEW-FIX.md `deferred` + log to `04-CONTEXT.md ## Deferred Ideas` with the finding ID.

**Step 4 — Build `04-GEMINI-REVIEW-FIX.md` per RESEARCH § 4 schema:**

Frontmatter:
```yaml
---
phase: 04-fallback-routing
reviewer: gemini
fixed_at: <ISO timestamp>
review_path: .planning/phases/04-fallback-routing/04-GEMINI-REVIEW.md
findings_total: NN
applied: NN
applied_partial: NN
rejected_disqualification: NN
rejected_other: NN
deferred: NN
status: all_dispositioned
---
```

Body:
```markdown
# Phase 4: Review-Fix Log (Gemini)

## Disposition Table

| ID | Severity | Theme | File | Disposition | Commit | Rationale |
|----|----------|-------|------|-------------|--------|-----------|
| GEMINI-H-01 | High | Doc | layouts/penta_tile_layout.gd:1 | applied | abc1234 | Auto-applied per D-04-13 (High = auto). |
| <... one row per finding ...>

## Applied Fixes (Detail)

### GEMINI-H-01 — Commit `abc1234`

**Files modified:** <files>
**Applied fix:** <1-3 sentence description of the fix as actually committed>
**Requires human verification:** YES | NO — <if YES: what to verify>

<... one block per applied fix ...>

## Rejected Findings (Detail)

### GEMINI-M-02 — Disqualified (D-04-14 trigger 2: forward-compat)
<1-3 sentence description of why this hits the trigger>

## Deferred Findings (to v0.3+ or v2)

### GEMINI-L-01 — Deferred to CONTEXT.md `## Deferred Ideas`
<paste deferral rationale + finding summary>
```

**Step 5 — Sanity checks before returning:**
- `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` exits 0 with `ALL GREEN (18 tests)`.
- **Anchor-based commit count (B4 fix):**
  ```bash
  ANCHOR=$(cat .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt)
  git log --oneline ${ANCHOR}..HEAD | grep -c 'fix(04): GEMINI-'
  ```
  matches the `applied` count in REVIEW-FIX.md frontmatter.
- Every disposition row has either a 7-char commit SHA OR a disqualification trigger reference OR a deferral target.
- NO uncommitted changes remain (`git status` is clean).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md && grep -c '^status: all_dispositioned$' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md</automated>
  </verify>
  <acceptance_criteria>
    - **REVIEW-FIX.md exists with valid frontmatter:** `grep '^findings_total:' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md` returns 1.
    - **Disposition counters sum to total:** `applied + applied_partial + rejected_disqualification + rejected_other + deferred == findings_total` (manual check from frontmatter).
    - **Disposition table covers every finding:** count of `^| GEMINI-` rows in the table equals `findings_total`. Concretely: `grep -c '^| GEMINI-' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md` returns the value of `findings_total`.
    - **B4 fix — atomic-commit-per-finding linkage via anchor range:** `ANCHOR=$(cat .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt) && git log --oneline ${ANCHOR}..HEAD | grep -c 'fix(04): GEMINI-' returns ≥ {applied_count_from_REVIEW-FIX.md frontmatter}`.
    - **B2 fix grep gate — applied dispositions correlate with commits:** `grep -c '^| GEMINI-.*| applied |' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md` returns the value of `applied` in frontmatter (or matches the anchor-bounded commit count above).
    - **Commit message format correct (em-dash separator):** `ANCHOR=$(cat .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt) && git log --oneline ${ANCHOR}..HEAD | grep 'fix(04): GEMINI-' | grep -cv ' — '` returns 0 (zero commits with hyphen separator).
    - **Test suite still green:** `pwsh -File addons/penta_tile/tests/run_tests.ps1 -NoPause` exits 0 with `ALL GREEN (18 tests)`.
    - **NO disqualified findings have commits:** every row with `disposition: rejected-disqualification` has `Commit: —` (literal em-dash placeholder, no SHA).
    - **NO compat shims / forward-compat / Phase 5 work / ATTRIBUTION.md introduced:** `ANCHOR=$(cat .planning/phases/04-fallback-routing/04-PRE-PHASE-ANCHOR.txt) && git diff ${ANCHOR}..HEAD -- 'addons/penta_tile/**/*.gd' | grep -E 'version:\s*int|legacy_template_image|ATTRIBUTION\.md'` returns nothing.
    - **status: all_dispositioned** in frontmatter: `grep '^status: all_dispositioned$' .planning/phases/04-fallback-routing/04-GEMINI-REVIEW-FIX.md` returns 1.
    - **`git status` is clean** at task completion (no uncommitted changes).
  </acceptance_criteria>
  <done>04-GEMINI-REVIEW-FIX.md committed with disposition table covering all findings; commit-count check passes against anchor range (B4 fix); test suite green; status: all_dispositioned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External LLM (Gemini) → local fix-application | Reviewer output is untrusted text; mitigated by RESEARCH § 8 Pitfall #10 (anti-hallucination check before applying) + Pitfall #11 (Coined-Term guard in prompt) + Pitfall #12 (Identity guardrails verbatim in prompt). |
| External LLM → committed fix code | Critical/High findings auto-apply; mitigated by D-04-14 disqualification scan (≤30s per finding) + post-fix test-suite re-run. |
| User (Medium-tier approval) → commit | User signs off Medium-tier fixes before commit; protects against debatable changes slipping in. |
| Demo eyeball UAT → trust signal | Eyeball pass is human-judgment; row-by-row tracking in `04-FALLBACK-UAT.md` provides audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | Gemini reviewer hallucinates file/line citations | mitigate | RESEARCH § 8 Pitfall #10 — implementer verifies each finding's file:line code BEFORE applying; hallucinated findings → `rejected-other` (no commit). |
| T-04-03-02 | Tampering | Tampered Gemini output / man-in-the-middle on CLI invocation | mitigate | Atomic-commit-per-finding (D-04-15) means each fix is a separate, revertable git operation; test-suite green check after EACH commit catches tampering that changes behavior. |
| T-04-03-03 | Repudiation | Applied fix lacks audit trail | mitigate | REVIEW-FIX.md disposition table records EVERY finding with disposition + commit SHA OR rejection rationale; nothing lands silently. |
| T-04-03-04 | Information Disclosure | Reviewer prompt leaks unintended internal state | accept | Prompt content is the planning workspace + addon source — already public-domain when v0.2.0 ships; no PII / secrets. |
| T-04-03-05 | Denial of Service | Gemini CLI hangs / network unavailable | mitigate | RESEARCH § 8 Pitfall #14 — empty-output check after invocation; retry with fallback model; surface to user if still failing. |
| T-04-03-06 | Elevation of Privilege | Auto-applied High finding introduces a regression | mitigate | RESEARCH § 5 hard triggers reject identity-guardrail / locked-decision violations BEFORE apply; post-fix `pwsh run_tests.ps1` re-run catches behavior regressions; D-04-13 boundary defines "auto-apply" only for unambiguous severity tiers. |
| T-04-03-07 | Tampering | Reviewer-supplied prompt-injection in finding text smuggling extra instructions | mitigate | Implementer reads finding manually; prompt-injection patterns are surfaced visually; finding application is mechanical (apply the suggested code change at the cited file:line) — reviewer can't inject "now also do X." |
</threat_model>

<verification>
- All 4 tasks complete and committed.
- `04-FALLBACK-UAT.md` has all 9 rows with `result: pass | partial | fail` (no `pending` remaining); status: complete (or partial with documented blockers); ≥ 9 `Signed-off:` lines all non-pending.
- `04-DOC-SWEEP.md` 12-row coverage table committed.
- `04-GEMINI-PROMPT.md` exists with hard constraints embedded verbatim AND all 7 content-aware disqualification trigger checks pass (W5 fix).
- `04-GEMINI-REVIEW.md` exists with valid frontmatter + at least one severity section OR `status: clean`.
- `04-GEMINI-REVIEW-FIX.md` exists with disposition table covering EVERY finding; counters sum to total.
- Every applied finding has an atomic `fix(04): GEMINI-{SEVERITY}-{NN} — ...` commit, anchor-bounded count matches `applied` (B4 fix).
- Test suite ALL GREEN (18 tests) at every commit boundary.
- ZERO findings hitting D-04-14 hard triggers were applied (compat shims / forward-compat / Phase 5 work / ATTRIBUTION.md / Coined-Term violations / locked-decision contradictions).
</verification>

<success_criteria>
Phase 4 success criteria 1, 2, 5, 6 from ROADMAP satisfied:
- (1, 2) PREVIEW-03/04 manual eyeball pass complete — pairs with Plan 01's programmatic test for D-04-06 belt+suspenders.
- (5) Doc-comment sweep summary committed.
- (6) Gemini cross-AI review pass complete + valid findings dispositioned + fixes committed.

Plan 04 (Codex review pass) consumes this plan's output by:
1. Running on the post-Gemini-fix codebase per D-04-10 (strict order).
2. Following the same prompt template + disposition workflow.
3. Reading the same anchor SHA file for commit-count gate (B4 fix).
</success_criteria>

<output>
After completion, create `.planning/phases/04-fallback-routing/04-03-SUMMARY.md` with:
- `## Artifacts created` listing the 5 file paths.
- `## UAT outcome` summarizing the 9-row sign-off (X passes, Y partials, Z fails).
- `## Doc-sweep summary stats` (per-file counts from 04-DOC-SWEEP.md).
- `## Gemini review outcome` — total findings, applied count, rejected-disqualification count, deferred count.
- `## Atomic commits` — list of `fix(04): GEMINI-...` commit SHAs (computed via anchor-bounded `git log` range per B4 fix).
- `## Notes for Plan 04 consumer` — Codex sees the post-Gemini-fix codebase; same prompt template; same disposition workflow; same anchor SHA file for commit-count gate.
</output>
